# 핸드오프 — 2026-07-30 (이력 문서)

> ⚠ **재개 정본은 `docs/HANDOFF-2026-07-31.md` 다**(2026-07-31 강등). §3 남은 작업의
> 미해소분은 그 문서 §3 이 참조로 승계하며, 내용은 여기가 정본으로 남는다.
> 본 문서 §4 는 함정 ㊹~(56) 의 정본이다.

> **(강등 전 기술)** `docs/HANDOFF-2026-07-29-B.md` 는 §3 남은 작업의
> **1-부수(복원 검증 자동화)·1-c(관리자 페이지)·2(P3-④ logrotate)·5(P1-3·P4)가 완료**되어
> 이력 문서로 강등됐다.
> **함정 목록 정본**: `-27-B` §5(①~⑬) · `-28` §4(⑭~⑲) · `-29` §4(⑳~㉗) · `-29-B` §4(㉘~㊸) ·
> **본 문서 §4(㊹~(53))**.
>
> ## ★ 이번 세션에 권한 개념이 생겼다
>
> `require_member` 하나뿐이던 권한 계층 위에 **운영자**가 생겼다(SP-AUTH-19). 그 결과
> `DECIDED_BY_ID` 가 세션에서 자동 주입되어 **감사 기록이 자율신고를 벗어났다.**
> 콘솔은 **SSH 터널에서만** 보인다 — 인터넷에서는 404다(실측).

---

## 0. 한 눈에 보기

| 구분 | 상태 |
|---|---|
| 브랜치 | `main` = `origin/main` = **`81f8042`**, 작업트리 클린·푸시 완료 |
| 서비스 | mysql · nginx · loupit-api · loupit-beta-api · loupit-backup.timer · loupit-ops-digest.timer · **loupit-restore-drill.timer** 전부 active |
| M9(로그인) | ✅ prod ON · beta ON |
| 회사 | 102개 · 복지 1,465행 · 도메인 100/102(98%) |
| **운영 콘솔** | ✅ **활성** — `ssh -L 8000:127.0.0.1:8000` 후 `http://127.0.0.1:8000/api/v1/console` |
| **백업** | 1차 `/var/backups/loupit`(14일) + 2차 `/db/backups/loupit`(7일) + **주간 복원 훈련** |
| **메일 폭탄** | ✅ 하루 상한 1,440통 → **약 33통**(SP-AUTH-18 배달주소 백오프) |
| **디스크** | 루트 **62%**(19G 여유. 74%에서) · journald 1G 상한 · nginx logrotate 200M 상한 |
| 참여 데이터 | TMEMBER 1행(실회원 = 운영자) · 큐 3종 전부 0건 |
| 테스트 | 백엔드 **540** · sc14 3 · 생성기 **230** · 프론트 **635** |
| **배지** | ✅ 출처 계보 3종 — 공식 / 공식·재직자 수정 / 재직자 등록(§4-b) |
| **파비콘** | ✅ svg + ico + apple-touch(§5-a) |
| 링크 | ✅ 내부 링크·sitemap·앵커 전수 정상(`infra/verify/link-audit.py`) |
| 미결 | **백업 서버 밖 자동화**(유일한 비가역 위험의 잔여분) · DMARC 승급 · 그룹 공통 복지 태깅 · 404 페이지 `noindex` · 호스트 방화벽 미가동(§5-b) |

---

## 1. 이번 세션에 한 일 (커밋 순)

| 커밋 | 내용 |
|---|---|
| `43a8361` | **로그 저장소 상한** — journald 1G/60d 드롭인 + nginx logrotate `maxsize 200M`, 루트 74→62% |
| `9b9dce4` | **P1-3 해소** — 배달주소 기준 지수 백오프(SP-AUTH-18). 하루 1,440 → 약 33통 |
| `d64d765` | **주간 복원 훈련** — `loupit-restore-drill.timer` + 결과를 일일 요약 메일에 편입 |
| `2840317` | **SSH 터널 전용 운영 콘솔**(SP-AUTH-19) — 없던 권한 개념 신설 + 감사 자동 주입 |
| `cff6efc` | docs: 재개 정본 갱신(함정 ㊹~(53)) |
| `8399217` | **콘솔 로그인 단계 신설** — 관문만 만들고 입구를 안 만들었다(터널 안엔 로그인 화면이 없었다) |
| `63cb6af` | docs: 방화벽 미가동 사실 + 모바일 접근 보류 근거 |
| `8cfd1c2` | **편집이력 '회사 정보 보기' 404 수정** — 숫자 COMP_ID 로 주소를 만들고 있었다(한 번도 동작한 적 없음) |
| `96cafd7` | **출처 아웃링크 화면에서 제거** — 12건 중 3건이 죽어 있었고, 404 로 가는 출처는 없느니만 못하다 |
| `f90ee0f` | **배지 = 출처 계보** — 공식 / 공식·재직자 수정 / 재직자 등록(§4-b) |
| `81f8042` | **파비콘 추가** — 페이지마다 하나씩 나던 404 해소 |

**정본은 SPEC 이다**: `SPEC/13` **SP-AUTH-18**(발송 백오프) · **SP-AUTH-19**(운영 콘솔) ·
SP-AUTH-8 개정분(웹 관리 패널 금지 → 인터넷 노출 금지) / `docs/OPS-backup.md` §2-1(복원 훈련).

---

## 2. 🚨 이번 세션의 가장 큰 교훈 — 방어를 넣을 때 **반대 방향 사고**를 먼저 세어라

핸드오프의 계획은 "배달주소 기준 일일 **상한**"이었다. 그대로 만들면 이렇게 된다:

> 제3자가 피해자 주소로 하루치를 태우면 **피해자가 그날 로그인을 못 한다.**

폭탄을 잠금으로 바꾼 것이지 없앤 것이 아니다. 그리고 이 코드베이스는 **같은 위험을 이미 한 번
인식하고 있었다** — `_DOT_INSENSITIVE_DOMAINS` 주석의 "무관한 제3자의 코드 발송이 막히는
반대 방향 사고". 함정 ㉘("커버리지를 넓히면 실패 경로도 넓어진다")과 같은 뿌리다.

그래서 **끊는 대신 늦췄다**: 누적 발송량에 따라 쿨다운이 2배씩 늘고 1시간에서 멈춘다. 하루
상한은 44배 줄었지만 **완전 차단은 없다.** 방어를 설계할 때 "무엇을 막나"만 세지 말고
**"막다가 무엇을 부수나"**를 같이 세라.

### 부수 교훈 — 내가 주석으로 경계한 함정을 같은 줄에서 밟았다

복원 훈련의 완결성 검사를 "서빙에 있는 테이블이 전부 복원됐는가"로 짜면서, 바로 그 자리에
*"모드에 따라 뜻이 달라지는 값을 재지 않는다(함정 ㊵)"*는 주석을 달았다. 그런데 서빙 스키마는
배포와 함께 나아가고 백업은 그 시점에 고정돼 있다 — **시점이 다른 두 값**이었고, 첫 실행에서
바로 빨개졌다. 함정을 아는 것과 안 밟는 것은 다르다. 기준을 **산출물 자신**(덤프가 주장하는
테이블 목록)으로 바꾸자 표류가 사라졌다.

---

## 3. 남은 작업 (우선순위)

### 1. 백업 서버 밖 **자동화** — 유일하게 남은 비가역 위험

수동 1부는 2026-07-29 확보됐고(그 시점 고정), 이번 세션에 **복원 훈련이 자동화**됐다.
남은 것은 **서버 밖 사본이 계속 갱신되게 하는 자동화**뿐이다.

- 이 서버엔 `gh`·`rclone`·`oci`·`aws` CLI 가 **전부 미설치**(2026-07-29 확인).
- rclone + 클라우드 드라이브가 균형이 좋으나 **OAuth 승인은 사용자가 브라우저로 직접** 해야
  한다 → 작업 중간에 반드시 멈춘다.
- ⚠ Oracle Object Storage 는 자동화가 쉽지만 **같은 클라우드 계정**이라 계정 사고엔 함께 당한다.

### 2. DMARC `p=none` → `quarantine` → `reject`

Postmark 주간 요약을 몇 주 본 뒤 판단. ⚠ 데이터 없이 올리면 로그인 메일이 막힌다.
⚠ `aspf=r` 유지 / `ruf=` 금지.

### 3. 도메인 커버리지 잔여 (`-29-B` §3-3 그대로)

HPSP·덕산네오룩스 2사 보류(게시 이메일 0건) · LIG 구 도메인 `lignex1.com` 보류 ·
컴투스홀딩스를 추가하면 `com2us.com` 을 `IN (...)` 으로 합쳐야 한다(SED-5 가 강제).
남은 1사짜리 그룹 도메인: `lg.com`·`hyosung.com`·`doosan.com`·`lotte.net`.

### 4. 보류 — 그룹 공통 복지 태깅 (`-29-B` §3-6b 그대로)

재벌 계열사 전반에서 "복지 항목 목록으로 회사를 비교한다"는 설계가 무너진다(함정 ㊸).
스키마 차원(복지 행에 "적용 범위" 축)에서 다뤄야 한다. 관련: 메모리 `loupit-content-thinness`.

### 5. 선택 — 처리방침 연락처를 도메인 주소로 (`-29` §3-6 그대로)

### 4-b. 배지 = 출처 계보 (2026-07-31 신설)

배지가 "어떻게 수집했나"가 아니라 **"누가 마지막으로 손댔나"**를 말한다. 우선순위는
**만료 → 재직자 등록 → 공식·재직자 수정 → 공식 → 추정** 이고, 세 렌더러(정적 페이지·비교
리포트·디렉터리)가 **같은 순서**를 지켜야 한다.

- **파생이지 컬럼이 아니다.** 근거는 `TBENEFIT_EDIT_LOG`(append-only 감사 원장) 하나다 —
  컬럼을 따로 두면 원장과 두 갈래가 되고 어긋나는 날 어느 쪽이 참인지 알 수 없다.
  `services/reference.py` 가 파생하고, 생성기·API 가 그 함수를 공유하므로 한 곳만 고치면 된다.
- **'재직자'는 수사가 아니다** — 복지 편집은 `require_employment` 게이트 뒤라 그 회사 재직
  인증자만 쓸 수 있다(2026-07-31 문구 결정: '사용자'보다 정확하고 신뢰도도 정직하게 전달).
- 라벨 정리: `공식 확인` → **`공식`**. 세 렌더러가 서로 다른 문구를 쓰고 있었고(디렉터리는
  이미 '공식') 이 변경이 셋을 일치시킨다.
- ⚠ 이력 조회 실패는 **익명 열람을 죽이지 않는다**(전부 '공식'으로 보고 계속 + 경고 로그).
  이 함수는 회사·복지 전체의 유일한 공급원이라 M9 테이블 하나로 사이트가 비면 안 된다.
- 현재 편집 이력 0행 → 1,465행 전부 `공식`. 재직자가 편집을 시작하면 자동으로 갈라진다.

### 5-a. 링크 전수 감사 결과 — 남은 것 둘 (2026-07-30)

`infra/verify/link-audit.py`(신설, 수동 실행)가 내부 링크 131·sitemap 111·앵커 28을 전수
조회한다. **대조군(정상 200 / 부재 404)이 먼저 맞아야** 결과를 신뢰한다 — 봇 방어 403·리밋
429 로 전부 빨개지는 것과 링크 결함을 구분하지 못하면 그 감사는 무의미하다.

현재 내부 링크는 전부 정상이고(1건 404 는 오류 페이지의 자기 canonical), 남은 것은 둘이다.

- ~~`/favicon.ico` → 404~~ ✅ **해소(2026-07-31)**. `favicon.svg`(주) + `favicon.ico`(루트
  프로브용) + `apple-touch-icon.png`. 이 호스트엔 Pillow·ImageMagick·rsvg 가 **하나도 없어**
  `infra/tools/make_favicon.py` 가 표준 라이브러리만으로 PNG-in-ICO 를 직접 그린다.
  ⚠ **SVG 와 그 스크립트가 같은 도형을 두 번 정의한다** — 한쪽만 고치면 SVG 를 쓰는
  브라우저와 `.ico` 를 쓰는 브라우저가 다른 그림을 본다. 양쪽 주석에 못박아 뒀다.
- **404 페이지에 `noindex` 가 없고 자기 canonical 이 있다.** 색인 대상이 될 수 있다(경미, 미해소).

### 5-b. 호스트 방화벽이 꺼져 있다 (2026-07-30 확인)

`ufw status` = **inactive**. 리포에 `infra/deploy/firewall.sh` 가 있으나 이 호스트엔 적용된
적이 없다. 지금 실제 노출이 없는 이유는 **앱이 `127.0.0.1` 에만 바인딩돼 있고**(`ss -lntp`
확인: 8000·8001 전부 루프백) OCI 보안 목록이 앞을 막기 때문이지, **호스트 방화벽이 막아서가
아니다.**

> 지금은 무해하다. 위험한 것은 **다음에 포트를 하나라도 넓힐 때** 이 사실이 조용한 전제로
> 깔린다는 것이다(예: VPN·모니터링·두 번째 uvicorn). 바인딩을 넓히는 작업을 하려거든
> **방화벽부터 켜라.**

### 6. 콘솔 후속 (선택 — 지금 필요는 없다)

- **모바일 접근은 검토 후 보류**(2026-07-30 사용자 결정: 스킵). 콘솔은 앱 포트 직결에서만
  열리므로 폰에서는 원리적으로 안 된다. 선택지는 넷이었다 — Tailscale(공개 노출 0·앱 설치
  필요·**방화벽 선행**) / 폰 SSH 터널(변경 0·모바일 OS 가 백그라운드를 죽이면 끊김) /
  mTLS 클라이언트 인증서(어디서나 되지만 **구조적 보증을 nginx 설정 의존으로 되돌린다** —
  함정 ⑭) / 모바일에선 CLI(감사가 자율신고로 되돌아간다). 다시 물으면 이 표를 재사용하라.

- 큐가 커지면 페이징·필터. 지금은 전부 0건이라 불필요.
- `TOPERATOR` 테이블 이전은 **운영자가 늘 때**. 지금은 `.env` 화이트리스트가 최적이다.
- 되돌릴 수 없는 조작(복지 하드 삭제·인증 폐기)은 **의도적으로 CLI 에만** 남겼다 —
  옮기려면 그 결정을 먼저 개정하라(TS-1·CO-10 의 정확 집합이 부재를 지킨다).

---

## 4. 함정 — 이번 세션에 추가된 것 (㊹~)

**①~⑬ `-27-B` §5 · ⑭~⑲ `-28` §4 · ⑳~㉗ `-29` §4 · ㉘~㊸ `-29-B` §4 가 정본이다.** 아래는 그 뒤.
ⓘ 원문자는 ㊿(50)에서 끝난다 — 51번부터는 `(51)` 표기를 쓴다.

- ㊹ **`lsof` 는 아무것도 못 찾아도 종료코드 1이다.** `set -o pipefail` 과 함께 쓰면 가드가
  **조용히 스크립트를 죽인다**(함정 ㊱의 짝). 종료코드를 삼켜야 한다면, 삼킨 대가로
  **양성 대조군**을 필수로 걸어라 — 고장난 `lsof` 도 "0건"을 돌려주므로 그 0건은 고아의
  증거가 아니라 **무증거**다(함정 ㉙의 도구판).
- ㊺ **글롭으로 읽히는 설정 디렉터리 안에 백업을 두지 마라.** `/etc/logrotate.d/nginx.
  bak-20260730` 을 만들자 logrotate 가 그것까지 읽어 `duplicate log entry` 로 **전체 파싱이
  깨졌다.** 기본 taboo 는 `.bak` 같은 고정 확장자뿐이라 날짜 접미는 안 걸린다.
  백업은 `/var/backups/loupit-config/` 처럼 **글롭 밖**에 둬라.
- ㊻ 🚨 **폭탄을 막으려다 잠금을 만들지 마라.** 하드 상한은 제3자가 피해자의 예산을 태워
  피해자를 하루 종일 잠그는 **반대 방향 사고**가 된다. 끊는 대신 **늦춰라**(지수 백오프) —
  총량은 눌리고 정당한 사용자는 언젠가 반드시 통과한다. 상한값은 공격자에게 물리는 벌이자
  **정당한 사용자가 겪을 최대 대기**다. 한쪽만 보고 올리지 마라.
- ㊼ **퍼지되는 행 위에 세운 판정은 그 행의 TTL 보다 긴 시간 규모를 표현할 수 없다.**
  재전송 쿨다운은 `TAUTH_CODE` 행을 보는데 그 행은 5분 뒤 만료·퍼지된다 → 쿨다운을 5분보다
  길게 잡으면 창은 남았는데 행이 사라져 **조용히 무력해진다.** 하루 규모를 다루려면 퍼지에
  살아남는 별도 상태가 필요하다. 그리고 그 상태의 **보존은 창보다 길어야** 한다 — 짧으면
  퍼지 자체가 우회 수단이 된다(`TAUTH_CODE` 와 방향이 정반대다).
- ㊽ 🚨 **시점이 다른 두 값을 비교하지 마라**(함정 ㊵ 재발 — 그 함정을 주석으로 경계해 놓고
  같은 줄에서 밟았다). 복원 훈련의 완결성을 "**서빙**에 다 있는가"로 재면, 스키마를 추가한
  날엔 다음 백업 전까지 반드시 빨개진다. 기준을 **산출물 자신**(덤프가 주장하는 목록)으로
  옮겨라. 표류하는 쪽을 기준으로 삼으면 경보가 늑대소년이 된다.
- ㊾ 🚨 **뒷정리 트랩을 락 획득 **전에** 걸지 마라.** 락 경합으로 건너뛰는 경로에서 그 트랩이
  돌면, 지금 **다른 프로세스가 쓰고 있는 대상을 뒷정리가 파괴한다** — 락으로 막으려던 사고를
  뒷정리가 저지르는 꼴이다. 락 전에는 비밀 파일 삭제 같은 최소 트랩만 걸어라.
  그리고 **"락을 못 여는 것"과 "락이 잠긴 것"은 다른 사건이다.** 한 갈래로 묶어 "건너뜀"으로
  처리하면 권한 문제인 호스트에서 **영원히 조용히 안 돈다**(그 침묵은 성공과 구분되지 않는다).
- ㊿ 🚨 **`/var/lock` 은 크로스 유저 락 파일에 쓸 수 없다.** 이 커널은
  `fs.protected_regular = 2` 라 **끈적(sticky)·전체쓰기 디렉터리에서 남이 소유한 정규 파일을
  열지 못한다 — root 도 마찬가지다.** 모드 0666 을 줘도 소용없다(걸리는 것은 모드가 아니라
  **소유자 불일치**). `/run/loupit/` 같은 전용 디렉터리를 tmpfiles 로 선언해 써라.
- (51) 🚨 **대역이 모르는 SQL 을 만나 예외를 던지는데 호출부가 fail-open 이면 거짓 초록이다.**
  배달주소 백오프를 넣자 기존 메일 폭탄 테스트 8건이 **새 경로를 한 줄도 실행하지 않은 채
  전부 초록**이었다 — 백오프의 fail-open 이 대역의 `AssertionError` 를 "상태 조회 실패"로
  삼켰다. 대역을 확장하고 **대역이 실제로 호출됐는지**(`calls > 0`)를 함께 재라.
- (52) **`INSERT IGNORE` 는 중복키뿐 아니라 모든 오류를 경고로 낮춘다.** 데이터 절단·타입
  오류까지 조용히 삼키고, 중복마다 MySQL 경고를 찍어 정상 경로가 매번 경고를 낸다.
  "있으면 넘어가라"는 `ON DUPLICATE KEY UPDATE <pk> = <pk>` 로 표현하라.
- (56) **Jinja 템플릿의 `<!-- -->` 는 생성물에 그대로 실린다.** 파비콘 선언에 개발자용 설명을
  HTML 주석으로 달았더니 **111개 페이지 전부에 그 설명이 나갔다**. 렌더되지 않는 설명은
  `{# #}`(Jinja 주석)를 써라. 재생성 대조에서 "설명되지 않는 diff" 로 잡혔다.
- (55) 🚨 **Pydantic 응답 모델은 모르는 필드를 조용히 떨어뜨린다.** 번들 빌더에 `edit_origin`
  을 넣고 `ReferenceBundle` 에 선언하지 않았더니, **정적 페이지(빌더 직접 소비)는 새 배지가
  나오는데 비교 리포트·디렉터리(API 소비)는 영영 '공식'만** 보였다. 서버도 클라이언트도 아무
  오류를 내지 않는다 — 실배포에서 응답을 직접 열어 보고서야 알았다. → 특정 필드가 아니라
  **"빌더가 만든 필드는 모두 모델을 통과한다"**는 일반 계약을 테스트로 걸었다
  (`test_response_model_keeps_every_builder_field`). 번들에 필드를 더할 땐 모델도 함께 고쳐라.
- (54) 🚨 **테스트가 결함을 고정하고 있을 수 있다 — 목업이 실제 응답과 다르면 특히 그렇다.**
  편집 이력의 '회사 정보 보기' 링크가 **숫자 `COMP_ID`** 로 주소를 만들어(`/company/40`)
  **항상 404** 였다(정적 회사 페이지는 `comp_eng_nm` 슬러그 경로다). 그런데 회귀 테스트는
  `assert href === '/company/40'` 으로 **그 잘못된 출력을 정답으로 박아** 두고 있었다.
  뿌리는 목업이다 — 실제 API 는 `comp_eng_nm` 을 주는데 목업은 `{comp_id, comp_nm}` 만
  줬다. **응답에 실제로 있는 필드를 목업에서 빼면, 그 필드를 안 쓰는 구현이 정답이 된다.**
  → 목업은 실 응답을 보고 맞추고, 어서션은 **문자열 모양이 아니라 계약**(그 주소가 실제로
  존재하는가)을 재라. "숫자 경로를 만들지 않는다"를 별도 어서션으로 못박았다.
- (53) 🚨 **메일 경로를 메일을 태워서 검증하지 마라.** 배선 확인이랍시고 prod
  `/members/login-code` 에 `@example.invalid` 주소를 던졌다(CSRF 403 이 우연히 막아 줬다).
  prod 는 `MAILER_MODE=smtp` 라 성공했으면 **하드 바운스로 발신 도메인 평판이 깎였다.**
  서비스 함수만 직접 호출해 SQL 경로를 재고 탐침 행을 지워라.

---

## 5. 도구·명령

```bash
# 테스트
DB_NAME=loupit_test python3 -m pytest server/tests -q -m "not sc14"   # 535
DB_NAME=loupit_test python3 -m pytest server/tests -q -m sc14         # 3
python3 -m pytest generator/tests -q                                   # 225
node --test 'web/**/*.test.js'                                         # 626

# ── 운영 콘솔 (SP-AUTH-19) ──────────────────────────────────────────────
# 본인 PC 에서:
ssh -L 8000:127.0.0.1:8000 <서버>
#   그다음 브라우저로 http://127.0.0.1:8000/api/v1/console
#   ⚠ 세션 쿠키는 도메인별이라 **이 주소에서 다시 로그인**해야 한다(jobcho.wiki 쿠키는 안 온다).
#   권한: server/.env 의 OPERATOR_EMAILS. 비우면 라우터가 아예 등록되지 않는다.
#   인터넷에서는 404 다 — nginx 를 거친 요청(프록시 헤더 보유)을 앱이 끊는다.

# 운영 CLI (되돌릴 수 없는 조작은 여기에만 있다)
python3 -m server.ops digest                          # 큐 3종 + 복원 훈련 상태
python3 -m server.ops list-pending                    # 재직 수동 승인 큐
python3 -m server.ops list-company-requests           # 회사 등록 요청 큐
python3 -m server.ops list-suppressed                 # 메일 발송 억제 목록
python3 -m server.ops delete-benefit <id>             # ⚠ 콘솔에 없음(비가역)
python3 -m server.ops revoke-verification <mbr> <comp>  # ⚠ 콘솔에 없음(비가역)

# ── 백업·복원 훈련 ──────────────────────────────────────────────────────
sudo systemctl start loupit-backup.service && journalctl -u loupit-backup -n 3 --no-pager
sudo systemctl start loupit-restore-drill.service     # 즉시 1회(주 1회 일 03:30 UTC)
cat /var/backups/loupit/restore-drill.json            # 마지막 결과(일일 요약이 읽는다)
journalctl -u loupit-restore-drill -n 30
#   ⚠ 훈련은 loupit_test 를 통째로 갈아엎는다 — pytest 와 /run/loupit/testdb.lock 으로 상호 배제.

# 시드를 prod 에 멱등 적용 — ⚠ release.sh 금지(테스트 게이트가 서빙 스키마 DROP/CREATE)
python3 db/seed/load.py

# 정적 사이트 재생성 — 회사를 추가하면 **반드시** 해야 한다
set -a; . server/.env; set +a
python3 -m generator.build --out /tmp/<scratch>/dist-new    # 함정 ⑳ — 먼저 대조
python3 -m generator.build --out web/dist                   # 실반영(원자적 스왑)
sudo systemctl restart loupit-api   # REFERENCE_CACHE_TTL=3600 캐시 비우기

# 스키마를 하나 추가할 때(prod) — release.sh 없이 DDL 한 문장만
#   db/schema.sql 에서 해당 CREATE TABLE 을 추출해 적용하고, 전후 행수를 확인하라.
#   그다음 conftest MAIL_OPS_CREATE_ORDER + run_tests.sh PART_TABLES 에 **반드시** 편입
#   (안 하면 릴리스마다 그 테이블이 소실된다 — SI-1·test_runner_backup 이 강제한다).

# 메일 설정 검증 — 호스트별 기대값이 다르다
python3 infra/deploy/verify_mail.py                   # prod: smtp 필수
sudo python3 infra/deploy/verify_mail.py --host beta  # beta: console 필수

# nginx — ⚠ release.sh 는 conf 를 배포하지 않는다(함정 ⑭). 항상 손으로 배치
sudo install -m644 infra/nginx/loupit.conf /etc/nginx/sites-available/ && sudo nginx -t && sudo systemctl reload nginx
```

---

## 6. 참조

- **함정 정본**: `-27-B` §5(①~⑬) · `-28` §4(⑭~⑲) · `-29` §4(⑳~㉗) · `-29-B` §4(㉘~㊸) ·
  본 문서 §4(㊹~(53))
- **SPEC 정본**: `SPEC/13` SP-AUTH-7.4(도메인 증거)·SP-AUTH-8/8.1(CLI·요약)·SP-AUTH-16(웹훅)·
  SP-AUTH-17(회사 요청)·**SP-AUTH-18**(발송 백오프)·**SP-AUTH-19**(운영 콘솔)
- **운영**: `docs/OPS-backup.md` §1-1b(미러) · **§2-1(복원 훈련)**
- **이력**: `docs/HANDOFF-2026-07-29-B.md`(CJ 계열·도메인 커버리지) · `-29`(M9 활성화) ·
  `-28`(P3·P5·웹훅) · `-27-B`(리밋·메일)
- **감사**: `docs/AUDIT-2026-07-17.md`
