# ISC ((주)아이에스시 · KOSDAQ 095340) — 복지 수집 가능성 실사 프로브

**판정: 가능** — 회사 공식 도메인 `www.isc21.kr` 의 채용 페이지 「i-Gle LIFE」에 `법적 외 복리후생 제도` 섹션이 있고, 12개 블록 38개 라벨이 **서버렌더 HTML 텍스트**로 있다. robots.txt 가 두 호스트 모두 404(부재)라 `*`·AI 봇 전부 허용이고, AI 봇 명시 차단은 없다.

- 정본 URL: `https://www.isc21.kr/careers/i-gle-life` (GNB 「i-Gle LIFE 채용 정보」)
- 항목 수: **12블록 / 38라벨**(단일 카테고리 `법적 외 복리후생 제도`). 이 중 **법정 약 10라벨**(4대 보험 4·연차/산전후/육아/생리 4·퇴직연금 2)과 **슬로건 블록 1개**(8번 「좋은 동료들」)를 빼면 등록 후보 약 27
- 렌더: Next.js App Router **서버렌더 HTML**. `<section class="benefitSection">` → `<ul class="benefitList">` → `<li class="benefitItem">` 12개. RSC 플라이트(`__next_f`)에는 복지 문자열이 **없다** → 헤드리스 렌더 불필요, HTML 직접 파싱

⚠ 사명이 약어라 **동명 도메인 3곳이 전부 주차(판매용) 도메인**이었다 — `isc.co.kr`·`isc.kr`·`isc21.com` 은 이 회사가 아니다(① 참조). 도메인 소유 근거는 ①-3 에 적었다.

- 회사: (주)아이에스시 / ISC INC. — 반도체 테스트 소켓(실리콘 러버 소켓·포고 소켓). 사이트 표기 본사/기술연구소 `(13453) 경기도 성남시 수정구 금토로40번길 26`, 대표자명 `김정렬`, TEL `031-777-7675`
- 프로브 일시: 2026-09-19 · **HTTP 요청 27회**(한도 40. 이 중 3회는 TLS 연결 타임아웃). 전부 일반 브라우저 UA · GET · 요청 간 ≥1초. HEAD 미사용. 헤드리스 렌더 미사용
- 리드 제공 hm_url 없음 — DART 미조회. 도메인은 DNS + 자체 표기로 찾았다

---

## ① 공식 채용/복지 페이지 URL

### 1. 동명 오염 — 「ISC」 이름의 도메인 3곳은 전부 이 회사가 아니다

| 호스트 | 실측 | 결론 |
|---|---|---|
| `www.isc.co.kr` / `isc.co.kr` | **443 연결 타임아웃**(curl exit 28, 15초). 80 은 살아 있다: `/` **200 · 545B**, 본문이 `<frameset>` 한 장으로 `http://idomain.cafe24.com/domain.html` 을 띄운다(`<title>` 도 그 URL). `/robots.txt` 는 **403 JSON** `{"status":403,"error":"Forbidden","message":"Access Denied"}`(Spring, JSESSIONID 발급) | **도메인 판매 중개 페이지.** 회사 자산 아님 |
| `www.isc.kr` / `isc.kr` | `robots.txt` **200 · 66B**(`User-agent: *` / `Allow: /` / `LLM-Policy: /llms.txt` / `Sitemap: /sitemap.xml`). `/` **200 · 114B** = `window.location.href="/lander"` 스텁. 인증서 `CN=www.isc.kr`, GoDaddy **DV**, IP 13.248.169.48·76.223.54.146 | **GoDaddy 주차 도메인.** 회사 자산 아님 |
| `www.isc21.com` / `isc21.com` | 443 타임아웃. 80: `robots.txt` **200 · 26B**(`User-agent: *` / `Disallow:` = 전면 허용, `Server: Kestrel`), `/` **302 → `https://www.hugedomains.com/domain_profile.cfm?d=isc21.com`** | **HugeDomains 매물.** 회사 자산 아님 |
| `isc21.co.kr`·`www.isc21.co.kr`·`iscnet.co.kr`·`isc-socket.co.kr`·`iscsocket.co.kr`·`iscsemicon.com` 등 | **NXDOMAIN** | — |
| `isctech.co.kr`·`isc-tech.com`·`isctec.com`·`isctest.com` | DNS 응답만 확인(각각 203.245.24.6 / 89.248.96.227 / 107.172.128.166 / 154.82.37.29). **HTTP 는 요청하지 않았다** | 정체 **미확인**. 이 회사 것이라는 근거도, 아니라는 근거도 이번 세션에 없다 |

⚠ `www.isctech.co.kr` 은 A 레코드가 **15.164.72.152** 로 아래 공식 도메인과 같은 IP 다. 같은 nginx 호스트에 얹힌 별개 vhost 인지 동일 주체인지 **확인하지 않았다**(HTTP 미요청). 수집 때 이 호스트를 출처로 쓰지 말 것.

### 2. 정본 = 공식 도메인 `www.isc21.kr`

| 용도 | URL |
|---|---|
| **복리후생 (정본)** | `https://www.isc21.kr/careers/i-gle-life` |
| 회사소개(귀속 근거) | `https://www.isc21.kr/company/isc-intro` |
| 홈(GNB·IR Center) | `https://www.isc21.kr/` |

GNB 에서 복지로 가는 경로: 홈 GNB **Careers → 「i-Gle LIFE 채용 정보」** → `/careers/i-gle-life`. 홈 원본 HTML 의 내부 링크는 10개뿐이고(`/company/isc-intro` `/company/history` `/product/test-socket/iSC` `/careers/i-gle-life` `/pr/press-release` `/ir/electronic-notice` `/ir/financial-notice/financial-statements` `/ir/financial-notice/external-auditor` `/esg/governance/ethics` `/contact-us` `/privacy-policy`), **채용 아래는 `i-gle-life` 한 장뿐**이다. `/careers` 자체는 404, `sitemap.xml` 도 404 라 다른 복지 페이지가 숨어 있을 여지는 확인된 범위에서 없다.

### 3. 도메인 소유(사업자 동일성) 근거 — 「이 사이트가 KOSDAQ 095340 의 것인가」

1. **종목코드를 사이트가 스스로 건다.** 홈 `IR Center` 카드 「주가정보」의 링크가 `https://finance.naver.com/item/main.nhn?code=095340` 다(원본 HTML `<a target="_blank" href=...>`, alt="주가정보"). 사이트 운영 주체가 자기 주가 페이지로 **095340** 을 지목한다 — 이 프로브에서 가장 강한 동일성 근거다.
2. **법인 표기가 전 페이지 푸터에 고정**: `(주)아이에스시` · `대표자명 : 김정렬` · `TEL : 031-777-7675` · `FAX : 031-777-7699` · `주소 : (13453) 경기도 성남시 수정구 금토로40번길 26` · `Copyright 2025 ISC INC. All Right Reserved.`
3. **회사소개 페이지가 이 도메인을 자기 주소로 적는다**: `/company/isc-intro` 의 「ISC 본사/기술연구소」 블록에 주소·TEL·FAX 와 함께 `Link : www.isc21.kr`.
4. 회사 전용 IR 호스트 `https://isc21.irpage.co.kr/` 를 홈에서 링크한다(회사명 서브도메인).
5. 푸터가 `https://ethics.sk.co.kr/Main/Index`(「SK 윤리경영」)를 링크하고, 복지 6번 블록에 `SK 패밀리 카드` 가 있다 → **SK 계열 편입 정황이 원문에 있다.** 다만 지분 관계·편입 시점은 이번 세션에서 **확인하지 않았다**.
6. 사업 설명이 종목과 일치: `/company/isc-intro` 「반도체 IC Chip 을 Test 하기 위해 필요한 Test Socket 을 개발 및 생산」, 제품군 `Test Socket / Burn in Socket / Pogo Socket / Thermal Control Unit / Tester / EFEM / Chemical / PCB`, 「세계 최초로 ISC 가 상용화한 Silicone Rubber Socket」.

**인증서로는 법인을 확인할 수 없다**: `CN=isc21.kr`(SAN 에 `www.isc21.kr`), **Let's Encrypt DV**, 발급 2026-09-18 · 만료 2026-12-17. DV 라 `O=` 법인 필드가 없다 — 소유 근거는 위 1~3 의 본문 표기다.

**사업자등록번호는 사이트에서 찾지 못했다.** `/privacy-policy` 는 200(9,235자)이지만 본문이 얇고 사업자번호·법인등록번호 표기가 없다. 리드가 법인 동일성을 한 겹 더 원하면 DART corp_code 대조가 필요하다.

## ② 복지 항목 명시 여부 — 명시됨. 12블록 / 38라벨, 전부 HTML 텍스트

섹션 제목은 `법적 외 복리후생 제도` 하나뿐이다(카테고리 분류 없음). 블록 제목은 카테고리가 아니라 **구어체 슬로건**이고, 실제 항목은 `desc` 한 줄에 쉼표로 나열된다.

| # | 블록 제목(슬로건) | 라벨 수 | 원문 desc |
|---|---|---|---|
| 1 | 4대 보험 | 4 | 국민연금, 건강보험, 고용보험, 산재보험 |
| 2 | 휴가는 눈치보지 말아요 | 9 | 자유로운 연차사용, 반차제도, 산전/산후휴가, 육아휴직, 생리휴가, 경조휴가, 생일휴가, 권장휴가, \*자율휴가 |
| 3 | 성과는 모두 함께 나눠요 | 3 | 인센티브, 직책수당, 포상제도 |
| 4 | 우리 굶지 말아요 | 3 | 아침, 점심, 저녁 식사 제공 |
| 5 | 퇴직연금 | 2 | 확정급여형 D.B, 확정 기여형 D.C |
| 6 | 구성원의 일상도 챙깁니다 | 2 | 복지포인트, SK 패밀리 카드 제공 |
| 7 | 구성원의 온전한 쉼을 지원해요 | 1 | 전국 34개 리조트 회원권 지원 등 다양한 휴식 권장 복지 제공 |
| 8 | 좋은 동료들 | 1 | 최고의 복지는 같이 일하고 싶은 동료들이라고 생각해요 |
| 9 | 구성원의 성장을 위해 지원해요 | 5 | OJT, 온보딩, 교육비, 온라인 그룹교육 플랫폼, 어학교육 사외 직무 교육 지원 |
| 10 | 우리는 팀이에요 | 3 | 팀빌딩, 조직운영비, 워크샵 |
| 11 | 집 걱정, 회사가 함께 덜어드려요 | 1 | 주택대출이자 지원 |
| 12 | 슬픔과 기쁨은 함께 나눠요 | 4 | 정기행사, 경조사, 동호회비, 자녀 대학학자금 지원 |
| **계** | **12블록** | **38** | |

**예시 2개**: `복지포인트`(6번), `전국 34개 리조트 회원권 지원`(7번).

- **금액 명시: 0건.** 섹션 안에 `원` 단위 표기가 하나도 없다(정규식 실측 0). 정량 표기는 **`추가 휴일 5일`**(자율휴가)과 **`전국 34개 리조트`** 둘뿐이다.
- **CSS 로 숨긴 텍스트가 1개 있다(가져가야 하는 쪽).** 2번 블록 제목 옆 물음표 아이콘의 툴팁 `<p class="guideText">` 에 `*자율휴가 / 법정 연차 외에, 구성원들의 워라벨과 휴식을 지원하기 위해 마련된 추가 휴일 5일` 이 들어 있고, 스타일이 `display:none`(호버 시 노출)이다. **가시 텍스트만 긁으면 자율휴가의 유일한 정량 근거가 빠진다.** 원본 HTML 에는 있다.
- **법정 제도가 섞여 있다(제목과 반대로).** 섹션 제목이 「법적 외」인데 1번 블록이 통째로 **4대 보험**이고, 2번에 `산전/산후휴가`·`육아휴직`·`생리휴가`·`자유로운 연차사용`, 5번이 `퇴직연금(DB/DC)` 이다. 법정 라벨 약 **10개**. SP-LEGAL 기준으로 배지 처리·집계 제외 대상이고, 「법적 외」라는 회사 표기를 그대로 믿으면 안 된다.
- **8번 「좋은 동료들」은 제도가 아니다**(「최고의 복지는 같이 일하고 싶은 동료들이라고 생각해요」). 마크업 구조가 나머지 11개와 완전히 같아서 기계 파싱하면 그대로 한 행이 된다 — 로보티즈 `what is NEXT...?` 카드와 같은 유형.
- **복합 라벨 주의**: 4번 `아침, 점심, 저녁 식사 제공` 은 쉼표로 3개로 갈라지지만 제도는 「3식 제공」 하나다. 9번 `어학교육 사외 직무 교육 지원` 은 쉼표가 없어 한 라벨이지만 실제로는 두 제도일 수 있다(원문에 구분자 없음). 2번 `산전/산후휴가` 도 슬래시 복합이다.
- **항목이 이미지 안에 있는 경우는 없다.** 복지 섹션에 `<img>` 0개.
- **항목 아닌 섹션 4개**(같은 페이지, 복지로 오인 금지): `인재상`(4E 1P — 격려/결단력/에너지/실행력/열정, 각 아이콘 PNG), `직급 및 승진 체계`(임원·팀장·파트장·매니저), `보상체계`, `평가제도`.
  - `보상체계`·`평가제도` 본문은 **이미지 안에만 있다**: `/_next/static/media/img_compensation.320815ed.png` (**3456×540 · 83,717B · 200**) = 「정직한 보상 / 투명한 기준」 → `Base Salary`(동종업계 최고 수준 지향) · `Incentive Bonus`(회사 실적 및 개인·팀 성과를 반영한 보상) · `Recognition & Awards`(성공 사례 또는 우수 구성원에 대한 각종 포상). `/_next/static/media/img_evaluation.cc0697d5.png` (**2217×749 · 549,447B · 200**) = 인사평가 = 역량평가 + 성과평가 → 승진, 포상, 연봉, 성과급.
  - 둘 다 **판독 가능한 해상도**다(1500px 폭으로 줄여도 글자가 선명). 다만 내용은 보상 **철학**이지 항목이 아니고, 실제 제도명(인센티브·포상제도)은 이미 3번 블록에 텍스트로 있다 → **이미지 판독은 불필요**.
- 공고 본문 출처는 없다: 사이트에 채용공고 게시판·지원하기 링크가 **없다**(복지 페이지 마크업의 내부 링크는 `/`·`/contact-us`·`/privacy-policy` 뿐, 외부 ATS 링크 0). 페이지 전용 JS 청크(`949-…js`)도 prop-types 라이브러리라 숨은 링크 없음.
- **유령 항목 점검 통과**: 복지 페이지 HTML 주석은 2개이고 둘 다 공백(`' '`)이다. 복지 섹션 안에 `hidden`·`visibility:hidden` 속성 0. 캐러셀·중복 슬러그 없음. ATS 샘플 템플릿이 아니라 자체 제작 페이지다(styled-components 클래스, 회사 고유 조어 `i-Gle`).

## ③ robots — 두 호스트 모두 **404(부재) = 전면 허용**, AI 봇 명시 차단 없음

| 호스트 | robots.txt | 판정 |
|---|---|---|
| **`www.isc21.kr`** | **404**, 6,609B, `text/html; charset=utf-8`. Next.js not-found 페이지가 돌아온다(선두 `<!DOCTYPE html>`, **BOM 없음**). `User-agent`·`Disallow`·`Allow`·`Sitemap` 지시 **0줄** | 규칙 파일 부재 → `*` · ClaudeBot · anthropic-ai · GPTBot · CCBot · Google-Extended **전부 `/careers/i-gle-life` 허용** |
| `isc21.kr` (apex) | **404**, 6,609B, 같은 not-found 페이지 | 같음 |
| (참고) `isc21.irpage.co.kr` | **404**, 181B, JSON `{"result":{"code":"404","msg":"요청하신 페이지 또는 API 경로를 찾을 수 없습니다."}}` | 부재 = 허용. 이번 수집에는 쓰지 않는다 |

- 계약 §4 「404 = 허용」에 그대로 해당한다. 네패스식 **AI 봇 선별 차단 없음**, 신한식 **다중 UA 한 레코드** 문제도 해당 없음(규칙 자체가 없다).
- **계약 §「웨이브 4 추가」대로 robots 를 호스트마다 단독으로 먼저 받고**, 404 를 확인한 뒤에야 본문을 요청했다. `www.isc21.kr` robots(요청 #10) → 홈(#11) → 복지(#12) 순서.
- ⚠ 404 본문이 HTML 이라 **robots 파서에 그대로 물리면 HTML 을 규칙으로 읽는다**(한국금융지주 `recruit.truefriend.com` 과 같은 함정, 거기선 200). 상태코드로 먼저 끊을 것.
- ⚠ not-found 페이지에는 `<meta name="robots" content="noindex">` 가 붙어 있다. **404 페이지 한정**이고 정본 페이지에는 없다(복지 페이지 `<meta name="robots">` 0건).
- `sitemap.xml` 도 404 라 그룹 sitemap 누수(이수페타시스 유형) 여지가 없다.

## ④ 접근성 — SSR, 차단 없음, TLS 정상, UTF-8

| 항목 | 결과 |
|---|---|
| 응답 | `/careers/i-gle-life` GET **200 · 44,143B · 0.09s** / 홈 200 · 36,839B · 0.07s / `/company/isc-intro` 200 · 0.07s. 전부 `nginx/1.30.3`, `X-Powered-By: Next.js`, IP 15.164.72.152(AWS 서울) |
| 캐시 | 홈은 프리렌더(`x-nextjs-cache: HIT`, `x-nextjs-prerender: 1`, `s-maxage=31536000`), **복지 페이지는 `Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate`** — 매 요청 서버 생성. 그래도 HTML 에 항목이 다 들어온다 |
| 렌더링 | **서버 컴포넌트 렌더 결과가 HTML 마크업에 그대로** 있다. `<script>` 를 전부 제거한 뒤 태그만 걷어내도 12블록 38라벨이 나온다. **RSC 플라이트(`__next_f`, 6,973자)에는 복지 문자열이 없다**(`복지포인트`·`자율휴가`·`주택대출이자`·`benefitItem` 전부 미포함) → JSON 경로 없음, **헤드리스 렌더 불필요**(그래서 쓰지 않았다) |
| 봇 차단 | 없음. 일반 브라우저 UA 로 전부 200. 캡차·429·WAF·쿠키 게이트 미관측. 2KB 미만 200 본문·「접근 차단/Access Denied/비정상 접근」 안내문 없음(주차 도메인 쪽 403 은 회사 자산이 아니다) |
| HEAD/GET | GET 만 사용 |
| TLS | `ssl_verify_result=0`, curl exit 0, **TLSv1.3**. `CN=isc21.kr`, SAN 에 `www.isc21.kr`, **Let's Encrypt(YE1) DV**, **2026-09-18 ~ 2026-12-17**(90일 자동갱신형, 하루 전 갱신됨) |
| 인코딩 | 전 구간 **UTF-8**(`text/html; charset=utf-8`, `<meta charSet="utf-8"/>`). EUC-KR 구간 없음 |
| www/apex | `isc21.kr`·`www.isc21.kr` 둘 다 15.164.72.152, 같은 앱. **www 를 정본으로 쓴다**(메타/og 는 apex `https://isc21.kr/meta/...` 를 가리켜 혼재) |
| 다국어 | 헤더 `KOR`/`ENG` 토글이 **`<button>`(href 없음)** 이고 `/en/careers/i-gle-life` 는 **404**. ENG 전환이 실제로 작동하는지, 영문 복지 문안이 따로 있는지는 **확인하지 못했다** |
| 빌드 식별자 | `buildId = X9W3texYqERu4yjqhuC8L`(홈·복지 동일). 사이트 개편 감지에 쓸 수 있다 |

## ⑤ 재무·직원 3축 (리드 확인용)

- DART **미조회**(이번 세션에 DART API·DB 를 건드리지 않았다). 설립일·사업보고서 이력·empSttus 가용 여부는 **확인하지 못했다**.
- 사이트 근거로 신설 법인 신호는 없다: 홈 IR Center 가 `전자공고`·`재무제표`·`외부감사인 선임` 메뉴를 갖추고 있고, 자체 링크가 네이버 금융 **095340** 을 가리킨다. 다만 `/ir/electronic-notice` 는 CSR(「게시글 불러오는 중」)이라 **게시 이력 연수는 확인하지 못했다**.
- 회사 전용 IR 호스트 `isc21.irpage.co.kr` 이 별도로 있다(robots 부재). 사업보고서·IR 자료가 여기 있을 수 있으나 **본문을 열지 않았다**.
- 리포 대조: `db/seed/corp_code_map.csv`(138사)와 `db/seed/benefit/sql/`(138파일)에 **ISC·아이에스시 없음** → 신규 등록 대상이 맞고 중복 아님. SK 계열 기등록 형제는 **SK하이닉스·SK텔레콤·SK이노베이션·SK바이오팜 4사**다.

**그룹 공통 복지 규칙(계약 §1·웨이브 4 표) 해당 없음**: 정본이 그룹 공통 채용사이트가 아니라 **ISC 자기 도메인의 자기 채용 페이지**이고, 「계열사별 상이」 류 면책 문구도 없다. 항목도 ISC 고유 문안(`i-Gle`, 자율휴가 5일, 전국 34개 리조트)이다. 그룹 색이 있는 라벨은 6번 `SK 패밀리 카드` 하나뿐이고, 이것도 ISC 가 자기 구성원 복지로 적은 것이라 귀속에 문제 없다.

---

## 수집 설계 시 유의 (수집·SQL 은 이번 범위 밖)

1. **파싱은 `section.benefitSection > ul.benefitList > li.benefitItem` 로 고정하고, 가시 텍스트가 아니라 원본 HTML 을 읽어라.** `p.number`/`div.title`/`p.desc` 3원소 구조라 안정적이다. 2번 블록의 `p.guideText` 는 `display:none`(호버 툴팁)이라 **렌더 후 가시 텍스트로는 사라진다** — 자율휴가 「추가 휴일 5일」이 여기에만 있다. RSC JSON 에는 복지가 없으니 HTML 이 유일한 경로다.
2. **「법적 외 복리후생 제도」라는 제목을 믿지 마라.** 1번 블록 전체가 4대 보험이고 2번의 연차·산전후휴가·육아휴직·생리휴가, 5번 퇴직연금(DB/DC)도 법정이다. 법정 약 10라벨은 SP-LEGAL 배지·집계 제외로 가고, 8번 「좋은 동료들」은 제도가 아니라 **행을 만들지 않는다**(구조가 나머지와 동일해 기계 파싱하면 그대로 들어온다). 등록 후보는 약 27라벨.
3. **쉼표 분할을 그대로 행으로 만들지 마라.** 4번 `아침, 점심, 저녁 식사 제공` 은 3행이 아니라 「3식 제공」 1행이고, 9번 `어학교육 사외 직무 교육 지원` 은 구분자가 없어 1라벨로 보이지만 두 제도일 수 있다. 2번 `산전/산후휴가` 는 슬래시 복합, 자율휴가 라벨에는 별표(`*자율휴가`)가 붙어 있으니 정규화할 것.
4. **금액은 한 건도 없다** — `원` 표기 0건. AMT 는 전부 NULL 이고, 정량은 자율휴가 `5일`·리조트 `34개` 둘뿐이다. 「동종업계 최고 수준 지향」(보상 이미지)은 서술이지 금액이 아니니 환산하지 말 것.
5. **출처 URL 은 `https://www.isc21.kr/careers/i-gle-life` 로 못 박고, 동명 도메인(`isc.co.kr`·`isc.kr`·`isc21.com`)은 출처 표기·재수집 폴백 어디에도 넣지 마라** — 셋 다 도메인 판매 페이지다. `isctech.co.kr` 은 같은 IP 에 있지만 정체를 확인하지 않았으니 쓰지 말 것. 재수집 감지는 `buildId`(`X9W3texYqERu4yjqhuC8L`) 또는 `benefitSection` HTML 해시로 하고, 인증서가 Let's Encrypt 90일(~2026-12-17)이라는 점을 갱신 주기 참고로 둔다.
