# 풍산 ((주)풍산 · POONGSAN CORPORATION · KOSPI 103140) — 복지 수집 가능성 실사 프로브

**판정: 가능** — 법인 자기 도메인 `www.poongsan.co.kr` 의 「채용정보 > 복지제도」 페이지에 **풍산이 주어인** 복지 목록 15항목이 서버렌더 HTML 표로 있다. robots 는 `*` 한 레코드로 전면 허용(해당 경로 무관), AI 봇 개별 차단 없음, 봇 차단·SPA·이미지 의존 없음. 지주 풍산홀딩스 자산은 하나도 쓰지 않았다.

- 정본 URL: `https://www.poongsan.co.kr/recruit/welfare` (GNB 채용정보 > 복지제도). 보조 1차 출처 = 같은 도메인이 호스팅하는 **2025 ESG 보고서 PDF p.66**(`/include/images/cs/2025_ESG_REPORT_KR.pdf`, GRI 401-2 지표가 이 페이지를 가리킨다)
- 항목 수: **웹 정본 15개 / 3카테고리**(회사생활 6 · 가족·건강 5 · 여가 4) + ESG 보고서 표 **9행**(중복 제외 신규 약 9항목) — 원 단위 금액 명시 **0건**
- 렌더 방식: **SSR**(Apache + PHP, `Content-Location: welfare.php`, `PHPSESSID`). 15항목 전부 최초 HTML 에 `<table><tr><td>` 텍스트로 존재. 헤드리스 렌더·이미지 판독·JSON API 불필요

---

- 법인: DART `corp_code 00684714` / 정식명 **(주)풍산** / 영문 POONGSAN CORPORATION / `stock_code 103140` / `corp_cls Y`(유가) / 법인등록번호 1313110083497 / 대표 류진·박우동 / **설립 2008-07-04** / DART 주소 경기도 평택시 포승읍 평택항로156번길 134
- DART `hm_url` = `www.poongsan.co.kr` → **생존, 오염 없음**(실응답 200, 아래 ④). 사이트 푸터 주소는 서울특별시 서대문구 충정로 23 풍산빌딩 · tel 02.3406.5114 · `copyright 2024 poongsan corporation`
- 프로브 일시: 2026-09-20 00:25~00:36 UTC
- 요청 수: `poongsan.co.kr`(www+apex) **HTTP 19회 + TLS 핸드셰이크 1회** / 타 호스트 5회(`poongsan.com` 2 · `poongsanholdings.co.kr` 3) / DART OpenAPI 3회 — 계약 한도(회사당 40회) 이내, 요청 간 ≥1초
- 내려받은 파일: `/tmp/claude-0/-home-ubuntu-loupit/f1d13a36-92d2-4e1c-9ce2-b6e64e0505ce/scratchpad/probe/poongsan/`
- 3자 사이트(사람인·잡코리아·잡플래닛·캐치·블라인드 등) **일절 조회하지 않았다.**

## ① 공식 채용/복지 페이지 URL — 자기 도메인 하나로 끝난다(그룹 채용사이트·ATS 없음)

| 경로 | 성격 | 복지 항목 |
|---|---|---|
| **`https://www.poongsan.co.kr/recruit/welfare`** | **복지제도** | **15개(HTML 표)** ← 정본 |
| `https://www.poongsan.co.kr/recruit/disclosure/system` | 인사제도 | **0** — 인사기본원칙 4개(보상·채용·경력개발·평가) 서술뿐. 계약 §3 기준 항목 0 |
| `https://www.poongsan.co.kr/recruit/talent` | 인재육성 | **0** — 교육 커리큘럼 4축(Value·Leadership·Professional·Expert). 「국내·외 MBA 연수 지원」·「석·박사 과정 지원」은 경계 항목이라 15개에 넣지 않았다(대한전선 선례와 같은 처리) |
| `https://www.poongsan.co.kr/recruit/disclosure/information` | 인재상 | 0 — 가치 5종(Challenge·Create·Change·Confirm·Communicate) |
| `https://www.poongsan.co.kr/recruit/disclosure/process` | 채용프로세스 | 0 — 전형 7단계 |
| `https://www.poongsan.co.kr/management/esg` | ESG보고서 | 보고서 PDF 3종(2023·2024·2025 국문) 다운로드 — 2025판 p.66 이 **보조 1차 출처** |
| `https://www.poongsan.co.kr/en/recruit/welfare` | 영문판 복지 | **진짜 404**(1,275B 오류 페이지) — 영문 복지 페이지는 없다 |

- **법인 귀속**: 자기 도메인 + 페이지 리드 문장이 「**풍산은** 임직원이 일과 삶의 균형을 유지하며 안정적으로 일할 수 있는 환경을 제공하기 위하여 다양한 복지제도를 운영하고 있습니다.」 — 주어가 법인명이다. 본문에 「계열사」·「상이」·그룹 공통 면책 문구 **0건**. 그룹 공통 채용사이트도, 외부 ATS(`recruiter.co.kr`·`greetinghr`·`midas` 등)도 전 페이지 링크 전수에 **없다**(채용 메뉴가 전부 자기 도메인으로 닫힌다).
- 채용 서브도메인 `recruit/careers/career/job/hr.poongsan.co.kr` 은 전부 **DNS 무응답**. 살아 있는 서브도메인은 `she2`(안전포털)·`srm`(전자구매)·`visit`(방문신청)뿐이고 복지와 무관하다.
- `sitemap.xml` = 200 · 19,009B · **128 URL 전부 `http://poongsan.co.kr` 한 호스트**(`/recruit/welfare` 포함). 다른 법인 호스트가 섞인 그룹 sitemap 이 아니다(이수페타시스형 유출 경로 없음). robots 에 `Sitemap:` 지시는 없다. 단, `<lastmod>` 가 `/recruit/*` 6쪽 모두 `2024-07-01T02:28:38+00:00` 로 동일 — 사이트 구축 시점의 일괄값이라 **갱신 시각 근거로는 못 쓴다.**

### ⚠ 동명 오염 — 실측으로 걷어낸 것 3건

| 대상 | 실측 | 결론 |
|---|---|---|
| `poongsan.com` / `www.poongsan.com` | 15.197.225.128(awselb) · `robots.txt` **404(0B)** · 루트는 `forsale.godaddy.com/forsale/POONGSAN.COM` 로 리다이렉트된 뒤 **403 Access Denied(406B)** | **GoDaddy 도메인 매물 파킹**. 풍산과 무관 — 쓰지 말 것 |
| `poongsanholdings.co.kr` | 211.202.2.206 · https 는 **인증서 만료(curl exit 60)** · http 루트 200(395B, EUC-KR)인데 `<title>나무묘목/멀구슬나무/편백, 동백 전문점 3fish`, 본문이 `top.location.href='http://3fish.kr'` | **풍산홀딩스 사이트가 아니다**(묘목 판매점으로 넘어가는 제3자 도메인). 지주 도메인을 이름으로 추측하면 여기에 걸린다 |
| 진짜 지주 도메인 | 풍산 사이트 푸터 Family Site 가 **「(주)풍산 홀딩스 → `http://www.poongsanhc.co.kr/`」** 로 명시 | 지주는 `poongsanhc.co.kr`. 이번 프로브는 **접속하지 않았고**, 수집에서도 배제 대상 |

- 같은 푸터의 관계사 6곳(`ipmx.com` PMX Industries · `poongsanfns.co.kr` · `pmcammo.com` · `poongsanms.co.kr` 풍산메탈서비스 · `poongsansm.co.kr` 풍산특수금속 · `hwadong.com` 풍산화동양행)도 별도 법인이다 — 이들 자산 역시 붙이면 안 된다.
- 풍산개·안동 풍산(지명)류 오염은 **이번 경로에 아예 들어오지 않았다**(검색 엔진을 쓰지 않고 자기 도메인만 따라갔다).

## ② 복지 항목 — 명시됨. 웹 15개(전부 HTML 텍스트) + ESG 보고서 9행

### 정본 `/recruit/welfare` — `<section class="welfare">` 안 `div.tab_body > div.div_tab > h5` + `table > tbody > tr > td[2]`

| 카테고리(`h5`) | # | 항목명(`td` 1) | 설명(`td` 2) |
|---|---|---|---|
| 회사생활 | 1 | 출퇴근 버스 | 임직원 통근 편의를 위해 사업장 별 특성과 교통편을 고려한 통근버스 운행 |
| 회사생활 | 2 | 사내식당운영 | 임직원 영양을 고려한 건강 식단을 제공하는 사내식당 운영 |
| 회사생활 | 3 | 장기근속 포상 | 10년 이상 장기근속자에게 근속 포상과 배우자 동반 해외 여행 제공 |
| 회사생활 | 4 | 공로 여행 | 정년 앞둔 장기근속 근로자에게 유급휴가 및 여행 경비 지원 |
| 회사생활 | 5 | 복지포인트 | 매년 지급되는 복지포인트를 개인 취향에 따라 사용 |
| 회사생활 | 6 | 자금 지원 | 사내 근로복지기금 운영 |
| 가족·건강 | 7 | 경조금 지원 | 임직원 본인 및 가족의 경조사 발생 시 축하금, 위로금 및 경조 물품지원 |
| 가족·건강 | 8 | 주거안정 지원 | 사원아파트 및 독신자 숙소 제공 |
| 가족·건강 | 9 | 자녀 학자금 지원 | 임직원 취학 자녀의 고교/대학교 학자금 지원 |
| 가족·건강 | 10 | 출산 지원 | 임직원 자녀 출산 시 장려금 지급 |
| 가족·건강 | 11 | 직장 어린이집 운영 | 일가정 양립을 위해 울산, 안강 사업장 사택 내 어린이집 운영 |
| 여가 | 12 | 휴가제도 | 하기휴가와 자기계발휴가 등 다양한 휴가제도 운영 |
| 여가 | 13 | 하계휴양비(체력단련비) 지급 | 하계휴양비(체력단련비) 지급: 전 직원에게 하계휴양비 지급 |
| 여가 | 14 | 휴양시설 운영 | 전국에 위치한 콘도를 회원가로 이용하고, 7~8월에는 각 사업장 별 별도의 휴양소를 설치, 운영 |
| 여가 | 15 | 동호회 활동 | 수십여개의 다양한 동호회 운영하고 동호회 지원금과 행사대관비를 지원 |

**예시 2개**: `장기근속 포상`(10년 이상 → 포상 + 배우자 동반 해외여행), `직장 어린이집 운영`(울산·안강 사업장 사택 내).

- 복지 블록 텍스트 **563자**(항목명+설명 합계), 섹션 마크업 4,640B.
- **금액 명시 0건** — 원/만원 정규식 검색 결과 0. 정량 표현은 「10년 이상」·「고교/대학교」·「7~8월」 세 건.
- **유령 항목 점검**: 페이지 HTML 주석 19개를 전수로 열어 `복지·복리·휴가·지원·경조·학자금·수당·포인트·검진` 키워드를 걸었을 때 **0건**. 주석 안에 숨은 옛 복지 블록은 없다(주석은 GNB 비활성 메뉴·푸터 배너 뿐). ATS 샘플 템플릿·타사 문구도 없다(회사명이 본문 주어).
- **CSS 숨김 점검**: 클래스명이 `tab_body` 지만 탭 위젯이 아니다. `sub.css` 실측 — `section.welfare .tab_body > .div_tab { margin-bottom:100px; display:flex; … }`, `> h5 {width:25%}`, `.basic_table {width:75%}` 로 **세 블록이 모두 나란히 보이는 레이아웃**이고, 이 선택자들에 `display:none` 규칙이 없다. 인라인 `style` 속성도 섹션 안 0개. **원본 HTML = 가시 텍스트**(넷마블형 숨김 탭 아님).
- 이미지 안 항목 없음(아이콘 없이 순수 표).

### 보조 1차 출처 — 2025 ESG 보고서 p.66 「복리후생 제도」 표 (같은 도메인 호스팅, 발행 주체 = 풍산)

`https://www.poongsan.co.kr/include/images/cs/2025_ESG_REPORT_KR.pdf` (200 · 19,133,673B · `Last-Modified: 2026-09-08` · 119쪽 · **텍스트 레이어 있음**, pypdf 로 추출 성공)

보고 범위(p.2 About This Report): 「비재무정보를 포함한 **풍산의** 지속가능경영 활동 및 성과는 서울 본사를 비롯하여 울산, 안강, 부산 등 **국내 주요 사업장**을 중심으로 수집하였으며, 일부 항목에는 **국외 자회사** 데이터를 포함」 · 문의처 `esg@poongsan.co.kr`, 서대문구 충정로 23 풍산빌딩 → **법인 단독 보고서**(지주·그룹 합산 보고서가 아니다). GRI Index p.109 가 `401-2 상근직 근로자를 위한 복리후생 → 66쪽` 으로 지정한다.

| # | 항목(p.66 표) | 내용 |
|---|---|---|
| 1 | 휴가 제도 | 경조휴가, 하계휴가, 자기계발 휴가, 정년임박자 공로여행 등 |
| 2 | 자녀 양육비 지원 | 자녀학자금, 자녀출산 장려금, **입학축하금** 지급 |
| 3 | 유연근무제 운영 | **시차출퇴근제, 선택적근로시간제 및 재택근무** 운영 |
| 4 | 건강관리 지원 | 전 임직원 대상 상해장애·상해사망·산재사망·질병사망 **보험금 지급** 및 **본인/배우자 종합건강검진** 지원 |
| 5 | 사택 지원 | **전사 1,109세대** 사택 제공 |
| 6 | 동아리활동 지원 | 전사 **약 50개** 동아리 활동 지원 |
| 7 | 직장 어린이집 운영 | 안강/울산사업장 직장 어린이집 운영 |
| 8 | 퇴직자 재취업 지원 | 퇴직 예정인 **만 50세 이상** 근로자 재취업 지원 서비스 |
| 9 | 기타 지원 | **통신비 지원**, 복지포인트 지급, **복지기금 대출**, 장기근속 표창, 경조금 지원 등 |

같은 p.65~66 서술에 **모성보호 제도**가 따로 있다 — 임신 축하 선물, 임산부의 날 도시락 제공, 출산 축하금, **사내 수유실**, 육아휴직 복귀자 대상 「맘토링」·「웰컴 프로그램」. (육아휴직·수유실은 법정 제도 경계이므로 SP-LEGAL 규칙 적용은 수집 단계 판단 몫이다.)

→ 웹 정본과 겹치지 않는 신규 항목은 **유연근무제 3종 · 단체보험 4종 · 종합건강검진(본인·배우자) · 통신비 · 복지기금 대출 · 입학축하금 · 퇴직자 재취업 지원 · 수유실 · 맘토링/웰컴 프로그램** 이고, 사택 1,109세대·동아리 약 50개 같은 **수치**도 여기서만 나온다.

## ③ robots — `*` 한 레코드 전면 허용, AI 봇 개별 차단 없음

`https://www.poongsan.co.kr/robots.txt` · **200 · 119B** · `Content-Type: text/plain; charset=UTF-8` · `Last-Modified: Mon, 01 Jul 2024 02:11:46 GMT` · **BOM 없음**(선두 바이트 `55 73 65` = `Use`)

```
User-agent: *
Allow:/
Disallow:/include/app/
Disallow:/admincyber/ 
Disallow:/admincyber_ori/
Disallow:/en/include/app/
```

- **레코드 단위 판정**: `User-agent` 줄이 하나뿐이라 그룹은 1개다(신한형 다중 UA 레코드 함정 해당 없음). `ClaudeBot`·`anthropic-ai`·`GPTBot`·`CCBot`·`Google-Extended` 를 이름으로 지정한 레코드는 **없다** → 전부 `*` 그룹을 따른다. **네패스형 AI 봇 선별 차단 아님.**
- RFC 9309 최장 일치로 이번에 건드린 경로를 판정: `/recruit/welfare`·`/recruit/*`·`/management/esg`·`/sitemap.xml`·`/include/css/sub.css`·`/include/images/cs/2025_ESG_REPORT_KR.pdf` 는 모두 `Allow:/` 에만 걸린다 → **허용**. 금지된 것은 `/include/app/`·`/admincyber/`·`/admincyber_ori/`·`/en/include/app/` 넷뿐이고 **이 프로브는 그 네 경로를 한 번도 요청하지 않았다**(robots 를 호스트별 단독으로 먼저 받고 본문은 그 뒤에 요청했다).
- `Disallow:/admincyber/ ` 줄 끝에 공백 1개가 있다(파서가 경로에 공백을 포함시키면 안 됨 — 우리가 갈 경로는 아니다). `Sitemap:` 지시 없음.
- apex `https://poongsan.co.kr/robots.txt` = **200 · 119B · 바이트 단위로 www 와 동일**(cmp 일치).
- 타 호스트: `www.poongsan.com/robots.txt` **404**(허용이지만 회사 사이트가 아니라 무의미) · `www.poongsanholdings.co.kr/robots.txt` https 는 **인증서 만료로 연결 실패**, http 로는 **404** — 둘 다 정본이 아니므로 본문 수집 대상에서 제외.

## ④ 접근성 — SSR·차단 없음·TLS 정상·UTF-8

| 항목 | 결과 |
|---|---|
| 정본 응답 | `GET /recruit/welfare` **200 · 109,010B · 0.30s**. 서버 `Apache`, PHP(`Content-Location: welfare.php`, `Set-Cookie: PHPSESSID`), `Cache-Control: no-store` |
| 렌더링 | **SSR**. 15항목이 최초 HTML 의 `<table>` 텍스트. `__NEXT_DATA__`·JSON API·SPA 없음(JS 는 jQuery·GSAP·Swiper 등 장식용). **헤드리스 렌더 불필요 → 실행하지 않았다** |
| 봇 차단 | 없음. WAF·캡차·429·302 디코이 미관측. **UA 를 지우고 기본 `curl/*` UA 로 받아도 200 · 109,010B** 이며 본문 차이는 JS 캐시버스터 쿼리(`?202609200926` → `?202609200928`, 분 단위 값)뿐 → **UA 문자열 차단 없음** |
| 차단 키워드·크기 | 본문 109KB, 「접근 차단·Access Denied·Request Blocked·비정상 접근」류 **0건**(한글·영문 모두). 2KB 미만 응답 없음 |
| HEAD/GET | HEAD 200 / GET 200 — 차이 없음(판정은 GET 기준) |
| soft-404 | 없음. `/recruit/welfare_zzz_probe` = **진짜 404** + 1,275B 자체 오류 페이지(「요청하신 페이지는 찾을 수 없습니다」) |
| TLS | Sectigo DV **와일드카드 `*.poongsan.co.kr`**(SAN 에 apex 포함), `notBefore 2026-02-12` ~ **`notAfter 2027-03-14`** → 수집 예정 시점에 만료 여지 없음. curl 검증 오류 0 |
| 인코딩 | UTF-8(헤더 `charset=utf-8` + `<meta charset="utf-8">`), 문서 BOM 없음. EUC-KR/cp949 혼용 없음 |
| www/apex | 둘 다 같은 IP `165.140.23.204`, 같은 본문. apex `GET /` 도 `302 → /main` 으로 **www 로 보내지 않는다**(호스트 유지). 사이트 내부 링크는 전부 상대경로. sitemap 만 `http://poongsan.co.kr` 절대경로라 스킴·호스트가 다르다 → **수집은 `https://www.` 로 고정**할 것 |
| ESG PDF | 200 · 19.1MB · `application/pdf` · 119쪽 · 텍스트 레이어 정상(스캔 이미지 아님) |

## ⑤ 재무·직원 3축 — DART 실측으로 확인(3축 모두 가능)

DART OpenAPI 3회 호출(`corp_code 00684714`, 키는 서버 `.env`):

- `company.json` → `status 000`, corp_name **(주)풍산**, stock_code **103140**, corp_cls **Y**, **est_dt 20080704**, hm_url `www.poongsan.co.kr`. **DART 가 부르는 법인·종목코드·홈페이지가 우리가 본 사이트와 일치한다**(hm_url 오염 없음).
- `list.json`(pblntf_ty=A, 2008-01-01~2026-09-20) → 정기공시 **74건**, 사업보고서 **(2008.12) ~ (2025.12)** 연속. 최신 `20260312 사업보고서 (2025.12)`. **5개년 재무 확보에 문제 없다.**
  ⚠ 설립일이 2008-07-04 이라 **2008년 이전 재무는 이 법인에 없다**(지주 풍산홀딩스가 별도 법인으로 존재하는 구조와 합치하지만, 분할 경위 자체는 이번 세션에서 원문으로 확인하지 않았다 — 장기 시계열 그래프를 그릴 때 리드가 확인할 것).
- `empSttus.json`(2025, 11011) → **4행**: 신동 남 1,114명(근속 15.3년·1인평균급여 98,141,000원) / 신동 여 80명(12.7년·90,613,000원) / 방산 남 2,439명(13.2년·91,890,000원) / 방산 여 121명(21.6년·91,419,000원). **합계 3,754명**.
  ⚠ **사업부문×성별 4행뿐이고 「전사」·「합계」 같은 총계 행이 없다** → NC·에스티팜형 합계행 2배 함정은 없지만, 직원 수·평균은 **4행을 직접 합산/가중평균**해야 한다.

---

## 수집 설계 시 유의

1. **정본은 `https://www.poongsan.co.kr/recruit/welfare` SSR 1회면 끝난다.** 파싱 단위는 `section.welfare` 안의 `div.div_tab` (→ `h5` 가 카테고리) × `table tbody tr` (→ `td` 두 칸이 항목명·설명). 헤드리스 렌더 금지(필요 없다), apex·`http` 대신 `https://www.` 로 고정, robots 금지 경로 4개(`/include/app/`·`/admincyber/`·`/admincyber_ori/`·`/en/include/app/`)는 건드리지 말 것. 영문 `/en/recruit/welfare` 는 404 라 국문 하나뿐이다.
2. **웹 15개만으로는 얕다 — 같은 법인의 2025 ESG 보고서 p.66(+p.65 모성보호)을 2차 출처로 함께 써라.** 유연근무 3종·단체보험 4종·본인/배우자 종합건강검진·통신비·복지기금 대출·입학축하금·퇴직자 재취업(만 50세 이상)·수유실이 거기서만 나오고, **사택 1,109세대·동아리 약 50개** 같은 유일한 수치도 거기 있다. 행마다 출처를 URL(웹) / PDF+페이지(보고서)로 **분리 기록**하고, 보고서 항목에는 **2025년 보고 기준**이라는 시점을 남겨라. PDF 는 19MB 이므로 1회만 받아 텍스트 레이어에서 파싱한다.
3. **협력사 복지를 임직원 복지로 끌어오지 말 것.** 같은 보고서 p.62~63 의 「협력사 교육 및 복리후생 지원」·「약 100억 원 규모 지원」·채용 격려금은 **협력사 대상**이다. 이 보고서에서 원 단위 금액이 나오는 유일한 대목이라 금액 욕심에 오수록되기 쉽다 — 임직원 복지 금액 명시는 웹·보고서 **둘 다 0건**이 맞다.
4. **지주·관계사 자산 전면 배제.** 풍산홀딩스의 실제 도메인은 푸터 기준 `poongsanhc.co.kr` 이고, 이름으로 추측한 `poongsanholdings.co.kr` 은 묘목 판매점(3fish.kr)으로 넘어가는 남의 도메인, `poongsan.com` 은 GoDaddy 매물 파킹이다. 관계사 6곳(PMX·풍산FNS·PMC Ammunition·풍산메탈서비스·풍산특수금속·풍산화동양행) 도메인도 마찬가지로 근거에 넣지 말 것.
5. **원문 그대로 옮길 때 걸리는 두 곳**: ① 13번 「하계휴양비(체력단련비) 지급」은 설명이 항목명을 그대로 되풀이한다(`하계휴양비(체력단련비) 지급: 전 직원에게 하계휴양비 지급`) — 카드 문장으로 쓰면 중복이 그대로 노출된다. ② 6번 「자금 지원」은 실체가 **사내 근로복지기금**이라 항목명만 보면 뜻이 서지 않는다. 둘 다 설명문 쪽을 살려 재코딩 대상으로 잡되, 원문 보존 필드는 그대로 둘 것. 사이트 `<lastmod>` 는 6쪽 모두 2024-07-01 일괄값이라 **게재 시점 근거로 쓰지 말 것.**
