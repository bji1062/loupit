# 웨이브 4 수집 계약 (W-1) — 2026-09-19

회사 1곳의 복지를 **공식 1차 출처에서만** 수집해 (a) 시드 SQL 1파일 (b) 근거표 1파일을 만든다. 신규 회사라 구본·앵커가 없다.
**DB 접속·기록 금지. `/home/ubuntu/loupit` 리포는 읽기만** — 산출물은 전부 스크래치패드에 쓴다.
스크래치패드 = `/tmp/claude-0/-home-ubuntu-loupit/f1d13a36-92d2-4e1c-9ce2-b6e64e0505ce/scratchpad` (아래 `…/scratchpad`).

**순서: SQL 을 먼저 완성해 쓰고 → evidence → 보고.** 웨이브 2에서 수집 13개 중 8개가 세션 한도로 끊겼는데, SQL 을 먼저 쓴 곳은 재투입 비용이 0 이었다.

## 산출물 (`…/scratchpad/wave4/`)

1. `wave4/<파일명>.sql` — 먼저 `db/seed/benefit/sql/삼성화재.sql`(그룹 채용사이트의 법인 전용 페이지)·`넷마블.sql`(컴퍼니 공통 각주)·`한국항공우주산업.sql`(이미지+CSR)·`심텍.sql`(라벨만)을 읽고 **그 형식 그대로**. 파일명은 리드가 준다.
   - 헤더 주석: `-- 출처: AI 파싱 (2026-09-19)` · `-- URL: <정본 URL>` · `-- badge: est` · `-- 참고:` 몇 줄(출처 경로·렌더 방식·판단 요약).
   - 1) `INSERT IGNORE INTO TCOMPANY (COMP_ENG_NM, COMP_NM, COMP_TP_ID, INDUSTRY_NM, LOGO_NM, CAREERS_BENEFIT_URL)` — **이 문장이 실제 등록**이다.
     `COMP_ENG_NM` = 리드가 준 snake_case slug(기존 138 과 충돌 금지 — `grep -h "VALUES ('" db/seed/benefit/sql/*.sql` 로 확인), `COMP_NM` = 표시명(DART 정식명 기준, 통용 표기 — 리드가 준 값),
     `COMP_TP_CD` ∈ large(대기업집단·대형 상장사) / mid(중견) / public(공기업) — 현 코퍼스 large 82·mid 56. 금융사·KOSPI 대형은 large, KOSDAQ 중소형은 mid.
     `INDUSTRY_NM` = **기존 값에 합류**한다(아래 목록). trending 폴백 쌍이 이 값으로 그룹핑되므로 「손해보험」처럼 새 변형을 만들면 쌍이 안 생긴다(웨이브 2 교훈). 정말 맞는 값이 없을 때만 짧은 새 값을 쓰고 evidence 에 사유를 적는다.
     `LOGO_NM` = **영문명 이니셜 대문자 1자**(코퍼스 관례: 고려아연 K·심텍 S·엘앤에프 L — 한글 초성 아님).
   - 2) `SET @comp_id` · 3) `UPDATE … CAREERS_BENEFIT_URL` · 4) `DELETE … BADGE_CD='est'` · 5) `INSERT INTO TCOMPANY_BENEFIT … ON DUPLICATE KEY UPDATE` — 심텍.sql 과 동일 구조(분할기 기준 5문장).
   - 행: `(@comp_id, 'code', '명칭', AMT|NULL, 'ctgr', 'est', NOTE|NULL, QUAL_YN, QUAL_DESC|NULL, SORT)`.
     카테고리 ∈ compensation·health·family·leisure·perks·growth·time_off·flexibility·work_env. SORT 는 카테고리별 10단위 섹션(첫 섹션 10, 다음 20 … 섹션 안은 +1). 섹션 순서는 원문 페이지 순서.
     금액 없는 행 = `NULL, 'est', NULL, TRUE, '<원문 근거 요약>'`. 금액 있는 행 = `AMT, 'est', '<명시값 근거>', FALSE, NULL`.
   - **주석·문자열에 홑따옴표·겹따옴표 금지**(load.py 분할기가 깨진다 — 「」 사용, 아포스트로피가 꼭 필요하면 U+2019 ’). BENEFIT_CD 는 회사당 UNIQUE(같은 코드 2행 금지 — 병합해 QUAL_DESC 에 양쪽 서술).
   - 다 쓴 뒤 분할기로 5문장인지 직접 확인: `cd /home/ubuntu/loupit && python3 -c "import sys; sys.path.insert(0,'.'); from db.seed import load; print(len(load._split_sql_statements(open(sys.argv[1],encoding='utf-8').read())))" <파일>`
2. `wave4/<파일명>.evidence.md` — `docs/handoff/2026-09-05-evidence/wave2/삼성화재.evidence.md`·`한국항공우주산업.evidence.md` 형식. 필수 절: 출처 표(채택/제외 URL·robots·렌더·해시) · 행별 인용표(행마다 원문 문장 그대로) · 코드 매핑 판단·어휘 함정 회피 · 법정 제도 미수록 확인 · 금액 · 제외 목록 · 이메일 도메인 관측 · 배포 시 주의.

### 기존 INDUSTRY_NM (2026-09-19 코퍼스, 빈도)
게임(9) · 바이오(7) · 반도체장비(7) · 보험(4) · 제약(4) · 전자부품(3) · 통신(3) · 배터리소재(3) · 반도체소재(3) · 바이오/제약(3) · 의료기기(3) · IT서비스(3) · 물류(2) · 건설(2) · 화장품(2) · 식품(2) · 화학(2) · 항공/방산(2) · 조선(2) · 자동차(2) · PCB(2) · 증권(1) · 은행(1) · 금융지주(1) · 금융/카드(1) · 전선/전력(1) · 전력기기(1) · 2차전지소재(1) · 철강(1) · 산업기계(1) · 로봇(1) · AI/의료(1) · IT/플랫폼(1) · 지주회사(1) · 건설/무역(1) · 에너지/화학(1) · 인프라/에너지(1) · 비철금속(1) 외 1회 값 다수.

## 규칙 (웨이브 1·2 계약 그대로 — 번호 유지)

1. **출처**: 회사 자기 도메인 또는 공식 홈에서 링크된 회사 브랜드 ATS 서브도메인, 또는 프로브가 귀속 경로를 확인한 그룹 채용사이트의 **법인 전용 페이지**만. SPA 면 번들·API 를 추적해 본문을 확보하고 경로를 기록(청크 해시는 index → 청크 순으로 추적). 이미지 안 항목은 내려받아 판독하고 URL·크기·sha256 기록. 3자 사이트(사람인·잡코리아·잡플래닛·캐치·인크루트·블라인드·원티드) 인용 0건. 검색엔진 스니펫도 근거가 아니다.
2. **원문 근거 필수**: 행마다 원문 문장을 인용. 없는 것을 만들지 말고, 뉴스 카드·인터뷰·인재상·비전 문구는 제도 근거가 아니다. HTML 주석 속 블록·샘플 템플릿·특정 직군 전용 처우 페이지·캐러셀 중복은 유령이다.
3. **법정 제도 미수록**: 4대보험·퇴직연금(DC/DB)·주5일·법정 연차·법정 육아휴직/육아기 단축·법정 출산휴가·난임휴가·근로자의 날은 행으로 만들지 않고, **다른 행의 QUAL_DESC 안에도 쓰지 않는다**(웨이브 2 감사: 법정 문구 4행 제거). 회사 재량 상회분(기간 연장·급여 보전·추가 휴가)만, 원문이 상회 조건을 밝힐 때 수록.
4. **금액은 명시값만**: 원문에 연 환산 가능한 금액이 있을 때만 BENEFIT_AMT(만원/년, SI-B2: 월액×12). 없으면 NULL — 타사 앵커·추정치 금지. 대출 원금 한도·비율·상한·1회성은 금액이 아니다(정성).
5. **어휘**: 새 코드를 만들기 전에 `docs/handoff/2026-09-19-evidence/_VOCAB.md`(87종) 에서 같은 뜻을 찾는다. 함정: `family_day`=조기퇴근(가족 행사는 `company_event`) · `long_service_leave`=휴가만(포상금은 `long_service_bonus`) · `work_tools`=IT 장비 · `insurance`=단체상해보험 · `pension_support`=개인연금 지원 · `uniform`=근무복(자율복장 아님) · `meal` 은 끼니·단가 명시 없으면 NULL · `smart_office`=업무 공간(스마트워크존 등). 대응 어휘가 정말 없을 때만 신규 코드(evidence 에 「신규 코드」절로 사유 기재 — 원문이 혜택 내용을 밝힐 때만).
6. **귀속**: 그룹 공통 복지를 개별 법인에 갖다 붙이지 않는다. 그룹 통합 페이지를 정본으로 쓸 수밖에 없으면 프로브가 확인한 귀속 경로를 헤더에 적고 **전 행 QUAL_DESC 말미에 「(그룹 통합 채용 기준)」 각주**(컴퍼니 공통이면 NOTE 에 「(○○컴퍼니 공통 채용 기준)」 — 넷마블 선례). 이 각주는 귀속의 전제라 절대 떨구지 않는다. 부문·자회사·임원/연구위원 한정 항목은 제외(한정을 지우면 허위).
7. **이메일 도메인 관측**: 채용 페이지·문의처에서 관측된 회사 공식 이메일 도메인을 evidence 에 기록(재직인증용, 없으면 「미관측」). 웹 도메인과 메일 도메인이 다를 수 있다(이수페타시스 선례 — MX 확인).
8. **비용 지원 = 복지 ○ / 회사 주도 교육 커리큘럼 = 복지 ×**. 명시적 비용 지원(학자금·자격증 취득비·어학비)만 growth 행. 사이버 연수원·사내 교육 프로그램 제공은 행이 아니다.
8-1. **파견형 육성**: 학위·해외 파견형(MBA·EMBA·석사·학술연수·지역전문가·주재원)은 `mba`(학위) 또는 `career`(파견·경력). 사내 아카데미·집중과정·직무 커리큘럼은 제외. 원문이 독립 항목이고 그 회사에 `career` 행이 있으면 `career`, 없으면 `mba` 행 서술에 흡수(행 신설 금지), MBA 와 한 복합 라벨이면 `mba`.
8-2. **어휘 함정 추가**: 예식장·웨딩홀 대관 = `event` 서술(`wedding` 은 현금 축하금+휴가) · `dormitory`=사택·기숙사 등 주거 **시설** / `housing_support`=임차비·주거비 **현금** / `housing_loan`=대출·이자 · `lounge`=휴게·카페 공간만(업무 공간은 `smart_office`, 옥외 조경·명상실 소개는 행 아님 — 명상실은 `nap_room` 서술) · 급여성 수당(학위 수당·자격 수당·초임)은 복지가 아니다 · 조직문화 담당자 역할(GWP·서포터즈·Change Agent)은 혜택이 아니다 · 상담실 「운영」만 있고 내용 없으면 `clinic` 아님 · 보훈·장애 관련 법정 의무 서술은 행 아님.
8-3. **사용자 노출 필드 문안**: QUAL_DESC_CTNT·NOTE_CTNT·BENEFIT_NM 은 `/api/v1/companies` 와 비교 엔진으로 그대로 노출된다. 허용 = 원문 라벨 + 출처 페이지명(「공식 채용 페이지 … 항목」) + 조건 + 미기재 사항. 금지 = 판단 근거·법조문·행 구조·「코퍼스」「선례」「병합」「판독」「이미지 판독 그대로」「규칙 N」「어휘」「수집」「검증」「⚠」. 그런 설명은 전부 evidence 로.
9. **행 수 목표 12~25**. 원문 항목이 그보다 적으면 억지로 늘리지 않는다(라벨만 있는 회사는 라벨 수 그대로). 병합은 같은 코드로 귀결되는 항목만.

## 예절

**robots.txt 는 호스트마다 단독으로 먼저 받아 경로 허용을 판정한 뒤에만 본문을 요청한다**(웨이브 4 프로브 4곳이 robots 와 본문을 한 배치로 보내 금지 경로에 샜다). 판정은 RFC 9309 기준(가장 긴 일치 규칙 우선 — 파이썬 `urllib.robotparser` 는 첫 일치라 `Disallow: /` + `Allow: /appsite/` 를 금지로 오판한다) · **헤드리스 렌더는 원본 HTML·데이터 JSON 에 항목이 없을 때만** — 렌더가 끌어오는 하위 리소스도 요청 수와 robots 판정에 든다(가온전선: 렌더가 금지 경로 JS 를 끌어옴 → 데이터 API 직접 호출) · UA 일반 브라우저(`Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0 Safari/537.36` — `curl` 기본 UA 는 차단되는 사이트가 있다) · `--max-time 20` · 요청 간 ≥1초 · 회사당 요청 ≤60회 · HEAD 대신 GET · 내려받은 파일은 `…/scratchpad/wave4/<eng>/` 아래. Playwright 가능(`from playwright.sync_api import sync_playwright`).

## 웨이브 4 추가 규칙 (웨이브 3 감사·데이터 정리에서 확정 — 규칙 본문과 같은 효력)

- **규칙 4 확장 — 1회성 지급·식대 일액은 AMT NULL.** 입학축하금·출산축하금처럼 한 번 주는 돈, 「중식 1만원」처럼 끼니 단가만 있는 값은 연 환산 금액이 아니다. 정성 행으로 쓴다. 비율(「본인 100%」·「50% 지원」)을 금액 칸에 넣지 않는다 — 웨이브 3 이전 데이터에서 현대제철 의료비 「본인 100%」가 `100`(=100만원)으로 라이브에 나갔다.
- **법정 휴가라도 쓰는 방식이 드물면 그 제도는 복지다**(사용자 결정 2026-09-20). 법정 연차를 **시간 단위·2시간 단위로 쪼개 쓰게 해주는 제도**는 대부분의 회사가 못 하는 것이라 회사 간 차이다 → 수록한다. 반대로 **연차·반차 자체는 법정**이라 문안에서 뺀다.
  - 화면에는 **시간으로 적는다** — 「반반차」는 내부 낱말이다. 반반차 = **2시간**, 회사가 별도 단위를 밝혔으면 그 단위(CJ올리브영 1시간 · 포스코퓨처엠 4시간 이내).
  - 예: `연차, 반차, 반반차` → **`2시간 단위 휴가 분할 사용`**.
- **휴가 코드 3분할**(웨이브 3 확정):
  - `refresh_leave` = 근속·사건 조건 **없이** 추가로 주는 휴가(리프레시·안식).
  - `long_service_leave` = 근속 연동 휴가(포상금은 `long_service_bonus`).
  - `leave_general` = 사건 보상(출장·심야근무 보상), 부여 조건이 불명한 집중휴가, 「○○데이」류.
- **규칙 8-1 보강**: 학위·파견형 육성(MBA·학술연수·지역전문가·주재원)은 **정본 페이지 안에 있으면 복리후생 블록 밖이어도 수록한다**(SK바이오팜·포스코퓨처엠 대칭). 사내 아카데미·직무 커리큘럼은 여전히 제외.
- **스톡옵션은 복지가 아니다** — 경영진·핵심인력 재량 선별 부여라 전 직원 제도가 아니다(루닛 REFUTED). **우리사주조합은 전원 대상이라 남긴다**(에스티팜 선례).
- **그룹 템플릿 공고**: 복리후생 블록이 그룹 채용 템플릿이면(형제 법인 공고와 문장이 같으면) 전 행 말미에 「(그룹 통합 채용 기준)」 각주. **조건 — 같은 템플릿을 쓰는 형제 법인을 같은 코퍼스에 또 등록하지 않는다**(더존비즈온 선례).
- **어휘 갱신**(2026-09-18 데이터 정리 #64 반영 — `_VOCAB.md` 87종이 정본):
  - `parent_care` 신설(부모 요양·부모 돌봄) — `parenting`(자녀 양육)과 다르다.
  - `remote_office` **코드 소멸** → 거점·공유 오피스는 `satellite_office`.
  - `welfare_point` 는 포인트·복지몰 한정. 자기계발비·개인 업무 지원비는 `self_development`(growth).
  - 주거: `housing_loan`(대출·이자) / `housing_support`(임차비·주거비 현금) / `dormitory`(사택·기숙사 시설) / `relocation`(이주·정착지원금). 정착지원금을 `housing_loan` 에 넣지 않는다.
  - 입학축하금은 `child_edu` 가 아니라 `parenting` 흡수(삼양식품·아이센스 선례), 보육비·보육수당도 `parenting`.
- **서술 어미**: `format.benefit_desc` 가 사용자 화면 문장을 만든다. QUAL_DESC 를 조사·부사형 어미(…에서·…까지·…회사가)로 끝내면 「회사가를 지원합니다」류가 나온다. 서술은 **명사로 끝내라**.

### 기존 INDUSTRY_NM (2026-09-19 코퍼스 138사, 77종)

게임 9 · 반도체장비 7 · 바이오 7 · 제약 6 · 보험 4 · IT서비스 4 · 의료기기 4 · 배터리소재 4 · 통신 3 · 전자부품 3 · 전선/전력 3 · 증권 3 · 반도체소재 3 · 바이오/제약 3 · 물류 2 · 화장품유통 2 · 식품 2 · 화학 2 · 자동차 2 · PCB 2 · 로봇 2 · AI/의료 2 · 건설 2 · 조선 2 · 화장품 2 · 항공/방산 2 — 나머지 51종은 1회(반도체·반도체패키징·반도체후공정·반도체기판·반도체계측·전력기기·에너지/발전·지주회사·타이어·철강 등).
**새 변형을 만들지 마라** — trending 폴백 쌍이 이 값으로 묶인다. 반도체 계열처럼 1회 값이 쪼개져 있는 축은 **가장 가까운 기존 값에 합류**시키고, 정말 없을 때만 짧은 새 값을 쓰고 evidence 에 사유를 적는다.
