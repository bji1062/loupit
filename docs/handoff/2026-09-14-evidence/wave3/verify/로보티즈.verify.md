# 로보티즈 (KOSDAQ 108490) — 웨이브 3 적대 검증 (R2-B, 2026-09-14)

## 요약

| CONFIRMED | SUSPECT | REFUTED | MISSED |
|---:|---:|---:|---:|
| 8 | 1 | 0 | 0 |

**결론: 반영 가능(조치 없이).** 9행 전부 원문(단일 법인 ATS Welfare 페이지)이 뒷받침하고, 샘플·초안 페이지 문구 혼입 0, 법정 문구 혼입 0, HTML 엔티티 잔재 0, 분할기 5문장. 유일한 다툼 지점은 SORT 30 「AI Tool 지원」의 코드 `work_tools`(소프트웨어 이용 지원을 IT 장비 축에 넣은 것)이며, 검수자가 유지/신규 코드 중 정하면 된다 — 나는 유지를 권고한다. 수집기가 뺀 「출산·육아·가족돌봄 지원」은 원문에 회사 재량 상회분이 한 글자도 없어 규칙 3 제외가 정당하다(MISSED 아님).

---

## 1. 독립 목록 (evidence 를 읽기 전에 원문에서 직접 추출)

- 요청: `robotisrecruiter.ninehire.site/robots.txt` GET 200 · 654B → `User-agent: *` Disallow 23줄 중 `/welfare` 에 일치하는 규칙 없음(RFC 9309 가장 긴 일치 — 일치 0 → 허용) → 1.5초 뒤 `/welfare` GET 200 · 224,011B. UA 일반 Chrome 문자열. **총 2회**, 헤드리스 0회.
- `welfare.html` sha256 `9492bee97c2befca1a7615ec6446a9b5a6ce89a0f5001cdd950b730923b122db`(수집기 사본 `d410f4…` 과 다름 — 페이지 전체엔 응답 시각 등 가변 요소가 있다). **welfare `sections` JSON(키 정렬·공백 없음) sha256 `a9ebc307a20f1b277d869a434755f32c2fae7c1904e45440cf3699f1b5b2e41c` · 23,811B — 수집기 evidence §1 S1 값과 바이트 단위로 일치.** 즉 수집 시점과 검증 시점의 원문이 같다.
- 렌더: Next.js SSR(buildId `r1qoSbaklERULZMHibntj`). `__NEXT_DATA__.props.pageProps.homepageProps.homepage.pages` 8개 중 `pageUrl == "welfare"` 1개만 읽었다. `info.companyName = 로보티즈` · `siteUrl = robotisrecruiter` · `status = published` · `companyId = beb507d0-12bd-11f1-a3b5-8d315495dba8` · `customDomains = []`. 이미지 블록 `imageFileKey` 접두사 전부 `beb507d0`(이 회사 경로). 푸터 `Address : 서울특별시 강서구 마곡중앙5로1길 37 로보티즈 … E-Mail : HR@robotis.com`. 스크립트 제외 본문 HTML 주석 0개. 복지 섹션 columns 전부 `invisible=False`.
- SSR 가시 텍스트 대조: 항목명 10개·설명 10개가 스크립트 제외 본문에 그대로 있다(`__NEXT_DATA__` 와 문자열 일치). `&amp;middot;` 본문 2회 → 화면에 「&middot;」가 글자로 보이는 이중 이스케이프 확인.

| # | 섹션(h5) | 항목명(원문) | 설명(원문 그대로) |
|---|---|---|---|
| 1 | 업무지원 | 식대지원 | 식비 부담 없는 넉넉한 식대 지원 / (일 18,000원 / 연장근로 시 10,000원 추가) |
| 2 | 업무지원 | 효율근무제 | 자율적인 출퇴근으로 업무 효율을 높이는 시차출퇴근제 운영 (07:00~10:00) |
| 3 | 업무지원 | AI Tool 지원 | 더 빠르고 효율적인 업무 수행을 위해 필요한 AI Tool 활용지원 |
| 4 | 업무지원 | 회식비 지원 | 팀 구성원의 선호를 반영한 자율적 회식 문화 |
| 5 | 생활지원 | 이사비용 지원 | 안정적인 정착을 돕기 위한 이사비용 지원 |
| 6 | 생활지원 | 경조사 | 구성원의 중요한 순간에 함께할 수 있도록 경조휴가와 경조금 지원 |
| 7 | 생활지원 | 생일자 쿠폰 지급 (굵은 `p`, `h6` 아님) | 생일을 맞은 구성원에게 축하 쿠폰 지급 |
| 8 | 생활지원 | 사내 동호회 | 문화, 운동, 자기계발 등 다양한 사내 동호회 활동지원 |
| 9 | 생활지원 | 하계휴가 | 충분한 재충전을 위해 연차 외 3일의 유급 하계휴가 제공 |
| 10 | 생활지원 | 출산&middot;육아&middot;가족돌봄 지원 (원문 리터럴) | 출산부터 육아, 가족돌봄까지 구성원이 일과 가정의 균형을 이룰 수 있도록 다양한 제도를 지원합니다. |
| — | 생활지원 | what is NEXT...? (굵은 `p`) | 더 나은 일터를 만들기 위한 고민은 계속됩니다. / 로보티즈의 다음 복지를 기대해주세요! — 예고 카드, 항목 아님 |

항목 **10개**(업무지원 4 · 생활지원 6) + 예고 카드 1. 섹션 [0] 머리 「로보티즈만의 특별함 / Welfare」, 섹션 [3] 「오시는 길」(지도)은 복지 아님. 부문·직군·임원 한정 표시 없음. 다른 7개 페이지(About·About(내꺼)·About(복사)·Jobs·FAQ·FAQ(복사)·개인정보처리방침 샘플)는 읽지 않았고, 아래 9행의 인용문은 전부 이 표 안에 있다 → **샘플·초안 문구 혼입 0**.

## 2. 행별 판정 (SQL 9행)

| SORT | 코드 | 판정 | 근거 원문(독립 목록 #) | 조치 권고 |
|---:|---|---|---|---|
| 10 | `meal` | **CONFIRMED** | #1 「식대 지원(일 18,000원 / 연장근로 시 10,000원 추가)」 | 유지. AMT NULL 은 규칙 4(연 환산 가능한 명시값만) 그대로 — 일액이라 근무일수 가정 없이는 연액이 안 나온다. **코퍼스 관례와의 어긋남 기록**: 코퍼스 `meal` 에는 「일 18,000원 x 240일」= 432 행이 10개사 이상 있고 그중 여러 행은 「(추정)」 표기도 없다. 그래서 식대 단가를 실제로 명시한 로보티즈가 단가를 짐작한 구내식당 회사들보다 meal 축 금액에서 낮게 보이는 역전이 생긴다. 이건 이 행의 잘못이 아니라 240일 관례(원문 없는 가정) 쪽 부채다. 검수자가 관례를 택하면 이 행만 `432, 'est', '일 18,000원 x 240일 환산 (추정)', FALSE, NULL` 로 바꾸면 되나, 규칙 4 문언상 권하지 않는다 |
| 11 | `team_dinner` | **CONFIRMED** | #4 라벨 「회식비 지원」 + 「팀 구성원의 선호를 반영한 자율적 회식 문화」 | 유지. 급부(회식비)는 라벨이 밝힌다. 코퍼스 `team_dinner` 2행(「팀 회식비 지원」·「부서 문화행사 지원」)과 같은 축, GWP 담당자 역할 유형 아님 |
| 12 | `relocation` | **CONFIRMED** | #5 「안정적인 정착을 돕기 위한 이사비용 지원」 | 유지. 코퍼스 `relocation` 4행(이사 비용·부임여비·초기 정착금)과 같은 축. `housing_support`(임차비 현금)·`housing_loan`·`dormitory` 아님 |
| 13 | `birthday_gift` | **CONFIRMED** | #7 「생일을 맞은 구성원에게 축하 쿠폰 지급」 | 유지 |
| 20 | `flex_work` | **CONFIRMED** | #2 「시차출퇴근제 운영 (07:00~10:00)」 | 유지. 전 구성원 대상 시차출퇴근 = 재량 제도(웨이브 2 감사가 지운 「시차 출근」은 임신기 §74⑨ 문맥이었고, 같은 감사가 일반 시차출퇴근제는 `flex_work` 에 유지했다). BENEFIT_NM 「효율근무제(시차출퇴근제)」는 라벨+설명 결합이라 허용 |
| 30 | `work_tools` | **SUSPECT (코드)** | #3 「더 빠르고 효율적인 업무 수행을 위해 필요한 AI Tool 활용지원」 | 원문은 실재하고 급부(회사가 AI 도구 이용을 지원)도 분명하다. 다툼은 코드뿐: 계약 규칙 5 함정 표기 `work_tools`=IT 장비이고 코퍼스 12행이 전부 노트북·모니터·스탠딩데스크·IT 기기 구매비 등 **하드웨어**다. AI 도구 이용(구독·라이선스)은 소프트웨어라 축이 엄밀히 같지 않다. **권고: 유지** — 어휘표 `work_tools` 의 뜻은 「업무 수행 도구 지급」이고 BENEFIT_NM 「AI Tool 지원」이 내용을 그대로 노출하므로 비교 화면에서 오독이 생기지 않으며, 1사 전용 신규 코드(`ai_tools`)는 쌍이 안 생겨 규칙 5 취지에 어긋난다. 검수자가 하드웨어 축을 지키고 싶다면 대안은 신규 코드 `ai_tools`(perks, evidence 에 「신규 코드」 절 추가)이지 제외가 아니다 |
| 40 | `event` | **CONFIRMED** | #6 「경조휴가와 경조금 지원」 | 유지. 경조휴가는 규칙 3 법정 목록 밖 |
| 50 | `club` | **CONFIRMED** | #8 「문화, 운동, 자기계발 등 다양한 사내 동호회 활동지원」 | 유지 |
| 60 | `summer_leave` | **CONFIRMED** | #9 「연차 외 3일의 유급 하계휴가 제공」 | 유지. 「연차 외」가 법정 연차와 별도라는 상회 근거를 원문이 직접 밝힌다(KAI `summer_leave` 「하기휴가(5일) 연차와 별도 제공」 유지 선례와 같은 형태). 휴가비 언급 없어 `summer_vacation_subsidy` 아님 |

### 공격 지점 점검 결과

- (a) 귀속: 공식 도메인 302 경로·companyName·이미지 경로·푸터 주소·「로보티즈만의 특별함」 모두 원문에서 직접 확인. 단일 법인 → 각주 불필요 판단 타당. 다른 7개 페이지 문구 인용 0.
- (b) 법정 혼입: 9행 QUAL_DESC 전수에 출산휴가·육아휴직·난임·가족돌봄·근로자의 날·4대보험·퇴직연금 0건. 「연차」 1건은 상회 조건(SORT 60).
- (c) 어휘: SORT 30 만 위 SUSPECT. 나머지는 어휘표 대표 명칭과 일치.
- (d) 금액: AMT 전부 NULL. 원문 금액 1건(식대 일액)의 처리는 SORT 10 참조.
- (e) 유령: 예고 카드 제외 확인. HTML 주석 0, 숨김 컬럼 0, 캐러셀 없음.
- (f) 병합·분리: 9코드 전부 상이, 억지 병합·분해 0.
- (g) 등록 INSERT: `('robotis', '로보티즈', mid, '로봇', 'R', …/welfare)` = `_ROSTER.md` 18행 지시값과 동일. `INDUSTRY_NM='로봇'` 은 레인보우로보틱스가 쓰는 기존 값(합류). `grep -h "VALUES ('" db/seed/benefit/sql/*.sql | grep -c "'robotis'"` = **0**(충돌 없음). 문장 구조는 심텍.sql 과 동일(COMP_TP_ID 서브셀렉트).
- (h) SQL 형식: `load._split_sql_statements` 실행 결과 **5문장**. 겹따옴표 0 · 주석 줄 홑따옴표 0 · HTML 엔티티(`&…;`·`&#`) 0 · 카테고리 9종 밖 0(perks 4·flexibility·work_env·family·leisure·time_off 각 1) · SORT 중복 0.
- (i) 노출 문안: QUAL_DESC 9행에 「코퍼스·선례·병합·판독·규칙·어휘·수집·검증·⚠·법정」 0건. 형식은 「원문 요약 (공식 채용 페이지 ○○ 항목 — 미기재 사항)」로 일관. 대체 문안 필요 없음.
- 3자 사이트·검색 스니펫 인용: SQL 0 · evidence 0(evidence 의 유일한 해당 단어 출현은 「인용 0건」 문장 자체).

## 3. MISSED 표

| 원문 항목 | 권고 코드 | 수집기 제외 사유 | 타당성 |
|---|---|---|---|
| #10 출산·육아·가족돌봄 지원 — 「출산부터 육아, 가족돌봄까지 구성원이 일과 가정의 균형을 이룰 수 있도록 다양한 제도를 지원합니다.」 | (없음 — 뒤집을 경우 `parenting`/family/SORT 41) | 세부 제도명 0·상회 조건 0·가리키는 영역이 전부 법정 제도 → 규칙 3 | **타당 (MISSED 아님).** 원문을 직접 읽었다: 급여 보전·기간 연장·추가 휴가·축하금·단축 근무 어느 것도 없고 「다양한 제도」뿐이다. 출산전후휴가·육아휴직·육아기 단축·가족돌봄휴가/휴직은 전부 사업주 의무라, 이 문장으로 `parenting` 「있음」을 주면 법정 최소만 운영하는 회사와 구분이 안 된다(비교 오염). 규칙 2(없는 것을 만들지 말 것)도 겹친다. 코퍼스에 「육아휴직 제도 운영」 같은 순수 법정 `parenting` 행이 남아 있지만 그건 웨이브 0 부채이지 따라갈 관례가 아니다. 검수자가 「회사가 복지 카드 10장 중 1장으로 게시했다」는 이유로 뒤집는다면 evidence §8-① 이 적어 둔 행 문안(법정 제도명 미기재) 그대로 1행 추가 — 10행 |
| what is NEXT...? 예고 카드 | (없음) | 제도 아님 | 타당 |

원문 10항목 = 9행 + 1 정당 제외. 그 밖에 놓친 항목 없음.

## 4. 수집기가 놓친 함정 (다음 웨이브에 남길 것)

1. 이 회사엔 없다 — 수집기가 evidence §9 에 적은 배선 6가지(welfare 페이지 필터·`h6`/굵은 `p` 혼용·예고 카드·`&amp;middot;`·`/api` robots 금지·SQL 주석 속 엔티티 세미콜론)가 이번 검증에서 전부 실재로 확인됐고 SQL 에는 하나도 새지 않았다.
2. 코퍼스 차원 부채 하나: `meal` 의 「일액 x 240일」 관례(추정 표기 없는 행 포함)가 실제 단가를 명시한 회사(로보티즈)를 NULL 로 만드는 역전을 낳는다. 규칙 4 와 구본 관례 중 하나로 정리하지 않으면 웨이브마다 같은 판단이 반복된다.
3. `work_tools` 의 뜻(하드웨어 한정인지 「업무 도구 지원」 일반인지)을 어휘표에 한 줄로 못박아 두면 AI 도구·SaaS 구독 지원 항목이 늘어날 때 흔들리지 않는다.
