# 가온전선 — 적대 검증 보고 (2026-09-15, verify-r3c)

## 요약

**CONFIRMED 12 / SUSPECT 1 / REFUTED 0 / MISSED 0** — **반영 가능**(SUSPECT 1건 = SORT 60 「집중 휴가제 (休weeks)」의 코드 선택, 행 존재는 원문이 뒷받침). 이번 세션 `POST getMainView` 1회에서 뽑은 menuCode 4600 `contents`(4,616자)가 수집기 사본과 **바이트 동일**(sha256 `c551d130…0962403`), `headerContents` 도 동일(`e0201460…`). 13 `li` → 분해 +1·병합 −1 = 13행 검산 일치. 벤더 샘플(마이다스아이티 4400·4500) 혼입 **0**, LS 그룹 공통 항목 혼입 **0**, 지어낸 설명 0, 법정 문구 0, 금액 창작 0, 3자 사이트 인용 0.

## 독립 목록 (evidence 를 읽기 전에 원문에서 직접 추출)

- robots(호스트별 단독 GET, 본문보다 먼저): `gaoncable.recruiter.co.kr/robots.txt` 200·112 B — `*` 레코드 `Disallow: /` + `Allow: /appsite/` + `Allow: /bbs/appsite/`, Googlebot·Yeti 레코드 `Allow: /`. RFC 9309 가장 긴 일치를 코드로 판정: `/appsite/company/getMainView` → `Allow: /appsite/`(9자) 승 → **허용**. `/upload/*`·`/resources*`·`/app/*` → `Disallow: /` 만 일치 → 금지(요청 0). `www.gaoncable.com/robots.txt` 200·22 B `Allow: /`.
- **S1-data** `POST https://gaoncable.recruiter.co.kr/appsite/company/getMainView`(`appsiteSn=2331&settingType=B`, 쿠키 없음) → 200 · 159,591 B · `application/json;charset=utf-8` · `ssl=0`. `companyInfo.companyName` **「가온전선(주)」** · `domainAddress gaoncable.recruiter.co.kr` · `companySubjectSn 56560`. 메뉴 트리 전수(34개)에서 `menuCode == "4600"`(useYn 1, menuSn 145488) 을 코드로 선택. `contents` sha256 **`c551d13082a1054d5b29bbf7df0614c06bf7ca80d101cf6efdbd5fa620962403`** = 수집기 `menu4600_contents.html` 과 `cmp` 동일. `headerContents` 「복리후생 / 가온전선은 구성원이 업무에만 몰입할 수 있게 다양한 복리후생을 지원하고 있습니다.」 sha256 `e020146046a0…` 동일. 4600 안 HTML 주석 0·`display:none` 0·`hidden` 0·「마이다스」 **0**.
- **S2 귀속** `https://www.gaoncable.com/` → 200 · 83,703 B — `<a href="https://gaoncable.recruiter.co.kr/" …>인재/채용</a>` **3회**(GNB 2 + 기업정보 1). `lsholdings` 는 푸터 Family Site·윤리 링크뿐.
- 헤드리스 렌더 **0회**. `lsholdings.com` 요청 **0회**. 이번 세션 요청: ATS 2회(robots·POST) · 자기 도메인 2회(robots·홈) = **4회**.

**4600 원문 6 h2 / 13 li(순서 그대로)**: [주거 지원] 지방근무자 사택지원 · 주택구입자금 및 전세자금 지원 / [생활 지원] 기념일 선물(생일선물, 추석선물) · 임직원 할인몰 운영 / [학비 지원] 고등학교/대학교 자녀 학비지원 / [여가 지원] 콘도미니엄, 팬션 운영 · 집중 휴가제 (休weeks) · 하기휴가, 하기휴가비 지급 · 사내동호회 지원 / [경조 지원] 경조 휴가 및 경조금 지급 · 경조물품 지원 / [의료 지원] 본인 의료비 지원 · 본인과 배우자에 대한 정기 종합 검진 (만 35세 이상). 설명문 0 · 금액 0 · 정량 조건 「만 35세 이상」 1.

**같은 JSON 의 다른 메뉴(행 근거 아님, 유령 점검)**: 4400 직급체계(useYn 0) 「마이다스아이티에서 직급별 근무연한을 채운 구성원은 상위 직급으로 자동 승진 … 종신고용제도」 · 4500 평가 및 보상(useYn 0) 「마이다스아이티의 연봉체계는 2014년 대졸 신입 기준 최소 4,000만원 … 제수당」 — 벤더 샘플. 4100 인사제도·4300 인재육성·4200 인재상(useYn 1)은 리드 한 문장 + **JPG 이미지만**(`/upload/56560/site/myImage/201808/*.jpg`, robots 금지 경로라 판독 불가).

**공격 지점 결과**
- **벤더 샘플 혼입**: SQL 13행 3필드에 「마이다스」「4,000」「자동승진」「종신」「상여」「호봉」 **0건**. 4400·4500 의 문구·금액 어느 것도 행·서술에 없다.
- **LS 그룹 공통 혼입**: 프로브가 그룹 페이지 전용으로 기록한 통근버스·단체정기보험·외국어 학원·사내복지기금·장기근속·복지카드 — SQL 에 **0건**. 13행 전부 4600 `li` 문구로 역추적된다.
- **SORT 60 refresh_leave**: 아래 SUSPECT.
- **SORT 61 summer_leave + 하기휴가비**: 원문이 한 `li`(「하기휴가, 하기휴가비 지급」). 코퍼스 같은 라벨 한국항공우주산업 `summer_leave` 1행(서술에 휴가비) 과 동일 처리. 규칙 9 「같은 코드로 귀결되는 항목만 병합」은 별개 항목 병합 금지 규칙이고 여기는 원문이 한 항목 → 위반 아님. `summer_vacation_subsidy` 는 코퍼스 1행(심텍, 휴가 항목 없는 단독 라벨)이라 분리하면 비교축이 오히려 어긋난다 → CONFIRMED.
- **SORT 21·30 분리**: 원문 괄호가 「생일선물, 추석선물」 두 계기를 명시. `birthday_gift`·`holiday_gift` 는 어휘표에서 다른 축(생일/명절) → 분리 타당. 설 선물을 만들지 않고 「추석 외 명절 선물 미기재」로 둔 것 확인.
- **SORT 71 「만 35세 이상」**: 원문 「본인과 배우자에 대한 정기 종합 검진 (만 35세 이상)」 — 괄호가 구 전체 뒤에 붙어 본인·배우자 어느 쪽에 걸리는지 원문이 말하지 않는다. QUAL_DESC 「대상 조건 만 35세 이상」은 적용 대상을 지정하지 않아 원문 이상으로 단정하지 않음 → 문안 통과.
- **지어낸 설명**: 13행 QUAL_DESC 전부 「<원문 li> (공식 채용 페이지 복리후생 <h2> 항목 — 조건, … 미기재)」 형식. 원문에 없는 서술은 「미기재」 나열뿐. 「팬션→펜션」·`\xa0`→공백 정규화만.
- **법정 문구**: 3필드에 `4대보험·퇴직연금·주5일·연차·육아휴직·출산휴가·난임·근로자의 날·법정` 0건. 경조휴가는 법정 아님.
- (g) 등록 INSERT: `gaon_cable` / `가온전선` / `mid` / `전선/전력`(기존 `LS.sql` 값) / `G` / 정본 URL — `_ROSTER.md` #12 와 일치. `grep -h "VALUES ('" db/seed/benefit/sql/*.sql` 126행 중 충돌 **0**.
- (h) 형식: `load._split_sql_statements` → **5문장** · 겹따옴표 0 · 주석 줄 홑따옴표 0 · 카테고리 7종 허용값 · 코드 중복 0 · SORT 중복 0 · 어휘표 밖 코드 0 · AMT 전부 NULL.
- (i) 노출 문안: 「코퍼스·선례·병합·판독·규칙·어휘·수집·검증·⚠·합침」 0건.

## 행별 판정

| SORT | 코드 | 판정 | 근거 원문 | 조치 권고 |
|---:|---|---|---|---|
| 10 | `dormitory` (work_env) | CONFIRMED | 「지방근무자 사택지원」 — 사택 = 주거 시설(규칙 8-2). 「지방근무자」는 근무지 조건이지 부문·직군 한정이 아니며 QUAL_DESC 에 조건 보존 | 유지 |
| 20 | `housing_loan` (perks) | CONFIRMED | 「주택구입자금 및 전세자금 지원」 — 동사가 「지원」이라 대출 단정 불가하나 QUAL_DESC 가 「대출·이자 지원 등 지원 방식 … 미기재」로 열어 둠. 현금 지급 문구 없어 `housing_support` 아님 | 유지 |
| 21 | `birthday_gift` (perks) | CONFIRMED | 「기념일 선물(**생일선물**, 추석선물)」 | 유지 |
| 22 | `discount` (perks) | CONFIRMED | 「임직원 할인몰 운영」 — 포인트 문구 없어 `welfare_point` 아님 | 유지 |
| 30 | `holiday_gift` (compensation) | CONFIRMED | 「기념일 선물(생일선물, **추석선물**)」 — 명칭 「추석 선물」, 설 미창작 | 유지(카테고리는 어휘표 지배 compensation) |
| 40 | `child_edu` (family) | CONFIRMED | 「고등학교/대학교 자녀 학비지원」 — 학교급 조건 보존 | 유지 |
| 41 | `event` (family) | CONFIRMED | 「경조 휴가 및 경조금 지급」 + 「경조물품 지원」 — 둘 다 경조 급부, 같은 코드 귀결(규칙 9 병합 요건 충족) | 유지 |
| 50 | `resort` (leisure) | CONFIRMED | 「콘도미니엄, 팬션 운영」 | 유지 |
| 51 | `club` (leisure) | CONFIRMED | 「사내동호회 지원」 | 유지 |
| 60 | `refresh_leave` (time_off) | **SUSPECT** | 「집중 휴가제 (休weeks)」 — 명칭뿐. 「집중 휴가제」는 코퍼스에서 S-Oil(「연 1회 2주간 휴가사용 의무화」)·두산 = `summer_leave`, 두산에너빌리티 「리프레시/집중 휴가」 = `refresh_leave` 로 갈린다. 이 회사는 「하기휴가」가 별도 `li` 라 `summer_leave` 는 막혀 있고, 남은 `refresh_leave` 는 「회사가 별도 부여하는 리프레시 휴가」를 뜻하는 축이다. 원문은 추가 부여인지 연차 몰아쓰기(休weeks = 주 단위 집중 사용)인지 말하지 않는다 — S-Oil 선례는 후자였다 | 검수자 결정. **권고 = `leave_general`(time_off) 재코딩**: 「휴가제도」 축이라 추가 부여를 주장하지 않고 행·SORT·문안은 그대로. 삭제는 과함(회사가 복리후생 항목으로 게시) |
| 61 | `summer_leave` (time_off) | CONFIRMED | 「하기휴가, 하기휴가비 지급」 — 한 `li`, 한국항공우주산업 같은 라벨 `summer_leave` 1행과 동일 | 유지 |
| 70 | `medical` (health) | CONFIRMED | 「본인 의료비 지원」 — 본인 한정 보존, 가족으로 안 넓힘 | 유지 |
| 71 | `health_check` (health) | CONFIRMED | 「본인과 배우자에 대한 정기 종합 검진 (만 35세 이상)」 — 조건 원문 범위 안에서 기재 | 유지 |

## MISSED 표

| 원문 항목 | 권고 코드 | 수집기 제외 사유 타당성 |
|---|---|---|
| (4600 안 13항목 전부 행에 귀속 — 제외 0) | — | — |
| 4300 인재육성 「가온전선은 인재육성을 위해 다양한 교육기회를 제공합니다.」 | (없음) | 타당 — 회사 주도 교육(규칙 8). 본문은 JPG 1장이라 비용 지원 문구 유무를 **확인할 수 없다**(robots 금지 경로) |
| 4500 「제수당 (기타수당)」·상여 | (없음) | 타당 — 벤더 샘플 유령 + 급여성 |

MISSED 0.

## 수집기가 놓친 함정

- 4100 인사제도(JPG 3장)·4300 인재육성(JPG 1장)의 본문이 이미지인데 evidence 는 「본문 전부 = 리드 한 문장」으로 적었다 — 실제로는 **이미지 본문이 있고 robots 금지 경로라 읽지 못한 것**이다. 재수집 때도 읽을 수 없으니 「교육비·보상 항목 미확인」으로 남길 것.
- 코퍼스의 「집중 휴가」 코드가 이미 둘로 갈려 있다(`summer_leave` 2 · `refresh_leave` 1). 웨이브 3 이후 어휘표에 「집중 휴가제 = 원문에 부여 일수 서술이 없으면 `leave_general`」 한 줄을 두면 같은 논쟁이 반복되지 않는다.
