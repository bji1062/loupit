# 2026-08-31 — 복지 에이전트 수집 파일럿(셀트리온·아이센스) 반영

## 무엇을

"DART 등록사 채용사이트 → 복지 수집" 타당성 검증(18개사 실측, 15/18 공식 복지 보유)에 이어,
**에이전트 팬아웃 수집 방법**(CJ 계열 7개사 때 방식의 정식화)을 파일럿 2개사로 실증하고 반영.

- **셀트리온**: 22 → **20행**. 출처를 공식 복지 페이지(`celltrion.com/ko-kr/careers/recruit/welfare`)로
  전면 교체. 근거 없는 8행 제외(마이그레이션), 신규 6행(mental·library·lang·birthday_gift·
  homecoming·guest_house). `CAREERS_BENEFIT_URL` NULL → 채움.
- **아이센스**: 5 → **29행**. 구본 5행은 **아이센스에프앤비(자회사) 오염 데이터**로 판명 — 96
  gold set 의 "CJ외 오매핑 잔존 점검" OI 가 실제 사례로 확인된 것. 공식 채용사이트
  (`recruit.i-sens.com`, Ninehire)의 `__NEXT_DATA__` SSR JSON 에서 추출, 채용공고 교차로
  금액 2건(추석 귀성여비 150·하계 복지포인트 60, stated) 확보. flex_work 는 FAQ
  "09:00~18:00" 고정 근무 명시로 반증·제외.

## 방법 (3레인 — 이후 100개사 확장의 표준)

1. **수집**: 회사당 에이전트. 계약 = 공식 도메인만 · 행마다 원문 인용 필수(없으면 행 생성 금지) ·
   정식 BENEFIT_CD 어휘 우선 · 금액은 명시값만 · 출력 = 기존 시드 SQL 형식 + evidence 로그.
2. **적대 검증**: 별도 에이전트가 **독립 재수집**해 행 단위 반증 시도. 이번 파일럿: 48행 중
   REFUTED 0 · MISSED 0 · SUSPECT 4(전부 해소 또는 명시 처리).
3. **검수·반영**: 사람은 SUSPECT 목록만 확인 → 시드 반영 → 서빙 적재 → 재생성·배포.

행별 원문 인용·검증 보고: [`2026-08-31-evidence/`](2026-08-31-evidence/).

## 결정

- **금액정책 (a) 유지**(사용자 확정): 새 출처에 금액이 없으면 기존 앵커 추정치를 보존.
  셀트리온 7건 복원(NOTE "(연 X만원 추정)" → DG-2 가 estimated 도출).
  **예외**: 아이센스 구본 앵커 3건은 오염 데이터라 승계하지 않음(파일 헤더에 명시).
- 신규 BENEFIT_CD 4개: `homecoming`(family) · `guest_house`(leisure) · `foundation_day_leave`
  (time_off) · `culture_day`(leisure) — 전부 원문 실존 + 기존 어휘 부재 확인.

## 반영 절차 (서빙)

시드 변경은 릴리스로 서빙에 반영되지 않는다(무접촉 게이트). 순서:

```
mysqldump ... LOUPIT TCOMPANY TCOMPANY_BENEFIT > ~/백업    # 1. 백업
mysql ... < db/migrations/20260831_remove_stale_benefit_rows.sql   # 2. 잔존 9행 삭제 (load 보다 먼저!)
python3 db/seed/load.py                                    # 3. 멱등 재적용(--fresh 아님)
bash infra/deploy/release.sh                               # 4. 정적 재생성·배포
```

`--fresh` 대신 재적용을 쓴 이유: 참조 5테이블 재생성(COMP_ID 재배정)·TCOMPARE_LOG TRUNCATE 를
피하고, 시드에서 빠진 행만 마이그레이션으로 표적 삭제(순서 주의 — 백필이 BADGE_SRC_CD 를
회사 단위로 덮기 전에 ai_parse 가드가 유효해야 한다).

## 함정(이 세션에서 확인)

- **멱등 재적용은 시드에서 뺀 행을 지우지 않는다** — 삭제는 항상 마이그레이션 동반.
- 기존 회사의 `CAREERS_BENEFIT_URL` 갱신은 INSERT IGNORE 로 안 된다(no-op) → UPDATE 문 필수.
- 96 gold set 에 CJ 외 오매핑이 실존한다(아이센스 사례) — 재수집 시 구본을 신뢰하지 말 것.

## 결과 수치

복지 1,465 → **1,487행**(+22: 셀트리온 −2, 아이센스 +24). 신선도: 두 회사 VERIFIED_DTM
2026-08-31, EXPIRES 2028-02-29(18개월).
