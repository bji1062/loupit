# 핸드오프 — 2026-07-29-B (재개 정본)

> **이 문서가 재개 정본이다.** `docs/HANDOFF-2026-07-29.md` 는 §3 남은 작업의 **1(도메인
> 커버리지)·2(백업)·3-②(베타 메일 모드)가 완료**되어 이력 문서로 강등됐다.
> **함정 목록 정본은 여전히 `-27-B` §5(①~⑬) · `-28` §4(⑭~⑲) · `-29` §4(⑳~㉗)** 이고,
> 이 문서는 **㉘~㊴** 를 잇는다.
>
> ## ★ 실사용자가 생겼다
>
> `TMEMBER` 에 **실회원 1행**이 있다(2026-07-29 확인). 이전 핸드오프의 "참여 데이터 전량
> 0행"은 더 이상 사실이 아니다. **함정 ⑳(정적 문안만 바꿀 땐 `release.sh` 금지)이 이제
> 가정이 아니라 실제 손실**이다 — 이번 세션에 시드를 반영할 때도 `release.sh` 를 쓰지 않고
> 시드 파일만 멱등 적용했다(§5 참조).

---

## 0. 한 눈에 보기

| 구분 | 상태 |
|---|---|
| 브랜치 | `main` = `origin/main` = `75a9755`, 작업트리 클린·푸시 완료 |
| 서비스 | mysql · nginx · loupit-api · loupit-beta-api · loupit-backup.timer · **loupit-ops-digest.timer** 전부 active |
| M9(로그인) | ✅ prod ON · beta ON. 클린 URL 8종 200 |
| **회사 도메인** | ✅ **93/95(97%)** — 31/95(33%)에서 이번 세션에 확장 |
| **백업** | ✅ 1차 `/var/backups/loupit`(14일) + **2차 `/db/backups/loupit`(7일, 별도 볼륨)** |
| 422 막다른 길 | ✅ 해소 — 등록 도메인 안내 + 수동 승인 폴백 |
| **메일 분리** | ✅ beta = `console`(자기 소유) · prod = `smtp`. 쿼터·발신 도메인 공유 끝 |
| 참여 데이터 | **TMEMBER 1행**(실회원) · 재직인증 0 · 재직승인요청 0 · **회사등록요청 1(pending)** |
| 테스트 | 백엔드 **485** · sc14 3 · 생성기 225 · 프론트 **626** |
| **대기 중 운영 액션** | 🔴 **회사 등록 요청 #1 (CJ ENM) pending** — 실사용자 대기 중(§3-0) |
| 운영자 알림 | ✅ 일일 요약 메일(`loupit-ops-digest.timer`, 08:00 KST) — 배달 실증 완료 |
| 미결 결정 | 백업 **자동화** 방식(수동 1부 확보 완료) · 관리자 페이지 착수 여부 |

---

## 1. 이번 세션에 한 일 (커밋 순)

| 커밋 | 내용 |
|---|---|
| `5322ad0` | **시드 가드 신설**(SED-1~6) — 오타 slug 의 조용한 no-op 차단 + 가드 자기검증 |
| `5f80045` | **백업 2차 사본을 `/db` 로**(BK-1~5) — 디바이스 장애 대비, 7일 보관 |
| `f6f59f0` | **422 막다른 길 해소** — 등록 도메인 안내 + 수동 승인 폴백 |
| `0998299` | **회사 도메인 커버리지 31→93/95** — 62개사 1차 출처 확증 |
| `75a9755` | **P3-② 해소** — 베타가 자기 메일 모드를 소유(`console`), 검증기에 `--host` 신설 |

**정본은 SPEC 이다**: `SPEC/13` **SP-AUTH-7.4**(도메인 화이트리스트 증거 기준) ·
SP-AUTH-7(422 계약) / `FRD/13`(422 응답 구조) / `docs/OPS-backup.md` §1-1b(백업 미러).

---

## 2. 🚨 이번 세션의 가장 큰 교훈 — 커버리지를 넓히면 막다른 길도 함께 넓어진다

도메인을 넣기만 하면 개선이라고 생각했는데 아니었다.

> **회사에 도메인이 하나라도 등록되면 그 회사는 409 `manual_required` 폴백을 잃는다.**

그 순간부터 계열사·자회사 주소를 쓰는 재직자는 422 "도메인이 일치하지 않아요" 한 줄만 보고
갈 곳이 없어진다. 31사 → 93사로 넓히면 그 막다른 길도 **3배로** 넓어진다. 순수한 개선이 아니라
**트레이드오프**였고, 데이터를 넣기 전에 먼저 열어야 했다.

발견 경로가 시사적이다 — `USECASE/09-참여.md` UC-2a 는 처음부터 **"필요 시 UC-74(수동 승인)
안내"** 로 규정하고 있었다. 명세가 옳았고 구현만 뒤처져 있었다. 기능을 확장하기 전에
**그 기능의 실패 경로가 명세대로 구현돼 있는지** 확인하라.

### 부수 교훈 — "위반이 없다"만 주장하는 테스트는 스스로를 증명하지 못한다

시드 가드 5개는 전부 "무료메일 없다 / 오타 없다" 형태다. 그런 테스트는 **파서가 조용히
아무것도 못 읽어도 초록**이다(빈 집합은 언제나 무해하다). 그래서 결함을 심어 실제로 빨개지는지
확인하는 SED-6 을 함께 넣었다. 함정 ㉒(스텁이 낸 거짓 초록)·㉔(같은 예외로는 원인 구분 불가)의
연장선이다.

---

## 3. 남은 작업 (우선순위)

### 0. 🔴 **지금 대기 중인 운영 액션 — 실사용자가 기다린다**

```
회사 등록 요청 1건:
  #1  [pending]  CJ ENM(커머스)  — 직장인-467027  2026-07-29 05:50:48
      참고 URL: https://cjonstylerecruit.ninehire.site/benefits
```

실회원이 가입 직후 회사 검색에서 못 찾고 넣은 요청이다 — SP-AUTH-17(검색 0건의 막다른 길)이
프로덕션에서 작동한 **첫 사례**. 사용자는 "확인 후 등록해 드릴게요" 안내를 받고 기다리는 중이다.

- 결정: `python3 -m server.ops decide-company-request 1 --approve|--reject --by 1 --note "…"`
- ⚠ **승인해도 회사가 만들어지지 않는다** — 상태만 바뀐다. 실제 등록은 `db/seed` 에 회사·복지
  데이터를 넣는 별도 작업이고, 그 데이터를 확보할 수 있는가가 판단의 핵심이다.
- ⚠ CJ ENM 을 등록하면 **`cj.net` 을 CJ올리브네트웍스와 함께 `IN (...)` 으로 합쳐야 한다**
  (§3-3, SED-5 가드가 안 합치면 빨개진다). 합치는 순간 삼성·SK 와 같은 그룹단위 인증이 되어
  "18사 중 1사" 비율 문제가 완화된다.
- ⚠ 참고 URL 이 **3자 채용 사이트**다(`ninehire.site`). 복지 데이터 근거로는 회사 공식 출처가
  필요하다 — SP-AUTH-7.4 의 도메인 증거 기준과 같은 원칙이다.

### 1. 백업 서버 밖 **자동화** — 수동 1부는 확보됨(2026-07-29 사용자 수행)

`/db` 미러는 **디바이스** 장애만 막는다. **머신을 잃으면 두 사본을 함께 잃는다.** 실회원이
생긴 지금 회원·재직인증·편집이력은 재현 불가다. 664K~728K 뿐이라 비용은 0에 가깝다.

**남은 일 중 유일하게 되돌릴 수 없는 위험이었다.** 사용자가 최신 덤프 1부를 서버 밖으로
내려받아 **"머신 손실 = 전손" 상태는 끝났다.** 남은 것은 그 사본이 계속 갱신되게 하는 자동화다
— 지금 사본은 2026-07-29 시점에 고정돼 있어 그 뒤 데이터는 여전히 서버 안에만 있다.

- **1분짜리 첫 걸음**(2026-07-29 사용자 안내): 본인 PC 에서 최신 덤프 한 부를 받아두면 "머신
  손실 = 전손" 상태가 끝난다. `scp <서버>:/db/backups/loupit/loupit-YYYYMMDD.sql.gz ~/`
  ⓘ **암호화는 불필요하다** — SSH 가 전송을 암호화하고 목적지가 본인 PC 라, 저장 시 암호를 걸면
  **암호 분실 위험만 새로 생긴다**. 암호화가 필요한 건 제3자 저장소(구글 드라이브 등)에 둘 때다.
- **자동화**(그다음): 이 서버엔 `gh`·`rclone`·`oci`·`aws` CLI 가 **전부 미설치**다(2026-07-29 확인).
  rclone + 클라우드 드라이브가 균형이 좋으나 **OAuth 승인은 사용자가 브라우저로 직접** 해야 한다.
  ⚠ Oracle Object Storage 는 자동화가 쉽지만 **같은 클라우드 계정**이라 계정 사고엔 함께 당한다.
- 부수: 복원 검증 자동화(`restore-drill.sh` + 주간 타이머). ⚠ 훈련 후 `loupit_test` 를 반드시 비울 것.

### ~~1-b. 운영자 알림~~ — ✅ **해소(`ops digest` + 타이머)**

> 실측이 동기였다: 요청 #1 이 **약 1시간 40분 방치**됐고 명령어를 치기 전까지 아무도 몰랐다.
> 알림 배선은 전무했다(코드에 notify 흔적 0건·타이머는 백업 하나·크론 없음).
> **알림 없는 큐는 큐가 아니다.**
>
> 배치: `loupit-ops-digest.timer`(일 1회 **23:00 UTC = 08:00 KST**) → `python3 -m server.ops
> digest --send`. 수신 `OPS_DIGEST_TO=jobchocontact@gmail.com`(처리방침 연락처와 같은 수신함으로
> "처리할 일"을 한 곳에 모은다 — 사용자 결정).
>
> 설계 판단 3가지는 `SPEC/13` **SP-AUTH-8.1** 이 소유한다. 요지:
> ① **건수·ID 만 보낸다**(내용을 실으면 위탁 발송자·외부 수신함을 거치는 새 개인정보 흐름이
> 생긴다. `collect_digest` 가 정수만 반환해 **자료형이 불변식을 보장**한다) · ② **큐가 비어도
> 보낸다**(침묵이 "대기 없음"과 "타이머 고장"의 두 뜻이 되면 안 된다 → 제목에 건수·🔴 마커) ·
> ③ **`--send` 없이는 발송 안 함**(사람이 확인차 칠 때마다 메일이 나가면 안 된다).
>
> 검증: OD-1~8 통과 · 실 DB SQL 확인 · **실발송 + 웹훅 `email.delivered` 로 배달 실증**
> (수신자 해시 일치) · **systemd 유닛으로도 발송 성공**(CLI 성공과 다른 검증 — 예전에
> `backup.sh` 가 systemd 최소 PATH 때문에 깨진 선례가 있다).

### 1-c. 관리자 페이지 (설계 논의 완료, 미착수)

**🚨 이 코드베이스엔 "관리자"라는 개념이 없다.** 그래서 이 작업의 본체는 UI 가 아니라
**없던 권한 개념을 새로 여는 일**이다.
- `DECIDED_BY_ID` 에는 **FK 가 없다**(같은 테이블의 `MBR_ID`·`COMP_ID` 는 있다). CLI 가 `--by N`
  으로 사람이 손으로 넣는 값이고 검증도 없다 → **현재 감사 기록은 자율신고**다. 관리자 페이지가
  세션에서 이 값을 자동으로 채우면 그제서야 감사가 진짜가 된다(부수 효과가 아니라 주된 이득).
- 레거시 `admin` 라우터는 `server/routers/__init__.py` 에 **영구 제외**로 못 박혀 있다.
- 권한 계층은 `require_member` 하나뿐 — "회원이냐 아니냐" 위가 없다.

결정할 것 4가지(2026-07-29 검토, 추천 포함):

| # | 결정 | 추천 | 근거 |
| --- | --- | --- | --- |
| ① 관리자 식별 | `server/.env` 이메일 화이트리스트 | 운영자 1명엔 최적. DB 변경 0이고 **바꾸려면 서버 접근 필요 = 그 자체가 방어**. `TMEMBER` 에 권한 컬럼을 넣으면 가입 경로와 권한 경로가 한 테이블을 공유해 실수 하나가 권한 상승이 된다. 운영자가 늘면 별도 `TOPERATOR` 로 |
| ② 인증 | 기존 세션 재사용 + 관문 한 겹 | 세션 쿠키가 이미 HttpOnly·Secure·SameSite=Lax·`Path=/api/v1`. 다만 이메일 코드만으로는 **관리자 메일함 하나가 유일한 열쇠**가 된다 |
| ③ **노출 범위** | **SSH 터널 전용** | 공개 `/admin` 은 봇이 즉시 찾는다. IP 제한은 IP 가 바뀌면 본인이 잠긴다. `ssh -L` 후 브라우저면 **공개 노출 0** 이고, 지금 CLI 쓰려고 SSH 하는 것과 수고가 같다 |
| ④ 범위 | 되돌릴 수 있는 것만 | 재직 승인/거부·회사 요청 결정·억제 해제까지. **복지 하드 삭제·인증 폐기는 CLI 에 남긴다** |

⚠ 위험 3가지 — **전부 사용자 입력이 화면에 뜨는 데서 온다**:
1. 증빙·회사명은 **사용자 입력 원문**이다 → `innerHTML` 금지, 노드 조립만(`f6f59f0` 의 422 문구와 같은 규칙).
2. 증빙엔 **외부 링크가 들어온다**(재직증명서 링크 등) → **자동 하이퍼링크 금지**. 관리자가 무심코
   누르면 IP 노출·피싱이다. 텍스트로 보여주고 복사하게 하라.
3. 감사: `DECIDED_BY_ID` 를 세션에서 자동 주입해야 자율신고를 벗어난다.

**순서 판단**: 지금까지 들어온 요청은 총 1건이고, §1-b(알림)로 방치 문제는 해소됐다. 관리자
페이지의 남은 값은 **처리 편의**와 **감사 진정성**(`DECIDED_BY_ID` 자동 주입)이다 — 요청량이
늘면 커진다. 지금은 서두를 이유가 없다.

⚠ **선행 결정과의 충돌을 먼저 정리하라**: `server/ops.py` 첫 문단과 `SPEC/13` SP-AUTH-8 이
**"웹 관리자 페이지는 두지 않는다"**를 설계 결정으로 명시하고 있다(근거: 공격면·관리자 인증
시스템 회피). 위 ③ SSH 터널 전용은 그 취지와 충돌하지 않지만, **문서를 고치지 않고 만들면
코드와 명세가 어긋난다.** 착수 시 그 결정을 개정하는 것이 첫 단계다.

### 2. P3 적대검토 잔여 **1건** (②는 `75a9755` 로 해소)

- **④ logrotate `size` 상한 없음** — `/etc/logrotate.d/nginx` 에 `maxsize 200M`. 라이브 파일이라
  별도 승인 필요. 현재 총 2.5M·루트 14G 여유라 급하지 않다.

> ✅ **② 해소(2026-07-29, `75a9755`)** — `.env.beta` 에 `MAILER_MODE=console` 명시. 베타가 prod
> 발신 도메인·무료 티어 쿼터를 더 이상 소모하지 않는다. ⚠ 대가로 console 은 인증 코드·수신
> 이메일을 **journald 에 평문**으로 남긴다(NFR31) — 베타 전용 DB 테스트 계정 한정이라는 전제다.
> 베타 실발송 e2e 가 다시 필요하면 **베타 전용 발신 서브도메인 + 별도 키**로 분리하라(한 줄로
> `smtp` 되돌리면 공유 문제가 그대로 돌아온다). 계약은 `verify_mail.py --host {prod|beta}` +
> `test_verify_mail_hosts.py` MV-1~5.

### 3. 도메인 커버리지 잔여 (이번 세션 산물)

- **HPSP · 덕산네오룩스 2사 보류** — 도메인 소유(`thehpsp.com`·`dsnl.co.kr`)와 자체 MX 는
  확인됐으나 **전 사이트에 게시된 이메일이 0건**이다(문의가 웹폼·전화뿐). 기준 미달로 보류.
  → 두 회사 재직자 문의가 오면 그때 완화 판단.
- **LIG 구 도메인 `lignex1.com` 보류** — MX 가 `ligdna.com` 과 완전히 동일(같은 회사)이나 게시
  이메일 0건. 사명변경 4개월차라 구 주소 사용자가 남아 있다. 인증 실패 문의 시 재검토 1순위.
- **CJ 계열사·컴투스홀딩스를 목록에 추가하면** `cj.net`·`com2us.com` 을 `IN (...)` 으로 합쳐야
  한다(SED-5 가드가 안 합치면 빨개진다). 그러면 삼성·SK 와 같은 그룹단위 인증이 된다.

### 4. DMARC `p=none` → `quarantine` → `reject` (`-29` §3-4 그대로)

Postmark 주간 요약을 몇 주 본 뒤 판단. ⚠ 데이터 없이 올리면 로그인 메일이 막힌다.
⚠ `aspf=r` 유지 / `ruf=` 금지.

### 5. P1-3 배달주소 기준 일일 상한 · P4 디스크 정리 (`-29` §3-5 그대로)

- 쿨다운이 창당 1통을 허용해 한 수신함 기준 `86400/60` = 1,440통/일이 가능하다.
- 디스크: `/data/mysql-8.0.42-*.tar` 1.6G 등 약 3.8G 회수 가능. 루트 35G/49G(72%).

### 6. 선택 — 처리방침 연락처를 도메인 주소로 (`-29` §3-6 그대로)

---

## 4. 함정 — 이번 세션에 추가된 것 (㉘~)

**①~⑬ `-27-B` §5 · ⑭~⑲ `-28` §4 · ⑳~㉗ `-29` §4 가 정본이다.** 아래는 그 뒤.

- ㉘ **커버리지를 넓히면 그 기능의 실패 경로도 함께 넓어진다.** 도메인을 등록하면 그 회사는
  `no_domains` 폴백을 잃는다(§2). 데이터를 넣기 전에 실패 경로부터 열어라. 그리고 그 실패
  경로가 **이미 명세에 있는지** 먼저 확인하라 — UC-2a 는 처음부터 옳았다.
- ㉙ **"위반이 없다"만 주장하는 가드는 스스로 초록일 수 있다.** 파서가 아무것도 못 읽으면 빈
  집합이라 언제나 통과한다. 결함을 심어 빨개지는지 확인하는 자기검증을 붙여라(SED-6).
- ㉚ **회사명·사이트 주소로 메일 도메인을 유추하지 마라.** 이번 조사 64사에서 "사이트 ≠ 메일"
  반례가 **9건**이었다. `silicon2.com` 은 빈 개발중 페이지이고, 검색엔진이 계속 노출하는
  `ecoprobm.co.kr` 은 **DNS 자체가 없다**(NXDOMAIN). 3자 표기를 믿지 마라.
- ㉛ **게시 위치가 아니라 소속을 봐라.** 현대제철 개인정보처리방침의 "기술책임자" 2인은
  외주 SI(지니시스템즈) 직원이라 `@partners-steel.kr` 이다. 회사 공식 페이지 게시물이지만
  임직원 도메인이 아니다 — 넣었으면 SI 직원이 현대제철 재직자가 됐다.
- ㉜ **도메인 소유 확증에는 회사가 통제하는 인프라 증거가 3자 사이트보다 강하다.**
  OV TLS 인증서 `O=` 필드 · M365 `getuserrealm.srf` 의 `FederationBrandName` · KISA `whois.kr`
  43번 포트 등록인 · MX/SPF/네임서버 공유 · Cloudflare `data-cfemail` XOR 복호.
  조회법과 실사례는 `SPEC/13` **SP-AUTH-7.4** 표에 있다.
- ㉝ **`INSERT ... SELECT ... WHERE COMP_ENG_NM = 'slug'` 는 오타 시 에러 없이 0행을 넣는다.**
  릴리스는 초록인데 그 회사만 영원히 수동 승인 경로에 남는다. SED-1 이 이걸 잡는다.
- ㉞ **미러·2차 사본은 1차가 확정된 뒤에만 손대라.** 순서를 뒤집으면 2차를 늘리려다 1차까지
  잃는다. 그리고 **미러 실패는 조용히 넘기지 마라** — 있다고 믿게 만드는 게 없는 것보다 나쁘다.
- ㉟ **로테이션 글롭이 남의 파일을 지운다.** `MIRROR_DIR` 을 `/db/backups` 직하로 뒀다면 7일
  규칙(`loupit-*.sql.gz`)이 거기 있던 `loupit-3db-premigrate-20260727.sql.gz` 를 지웠을 것이다.
  전용 하위 디렉터리를 써라.
- ㊱ **bash EXIT 트랩의 마지막 명령이 종료코드를 뒤집는다.** `[ -n "$VAR" ] && rm -f "$VAR"` 를
  트랩 끝에 두면 `VAR` 가 빈 값일 때 트랩이 1을 반환해 **성공한 스크립트가 실패로 보인다**.
  트랩은 `return 0` 으로 끝내라.
- ㊲ **케어젠 공식 사이트는 현재 전 세계에서 403 이다**(아임웹 호스팅 에러 페이지, 우리 IP
  차단 아님 — 독립 프록시로도 동일). 회사 사이트가 죽어도 메일 도메인은 살아 있을 수 있다.
  **웹 접근 실패를 도메인 미운영으로 단정하지 마라.**
- ㊳ **표현할 수 없는 계약은 지켜지지 않는다.** `verify_mail.py` 는 `server/.env` 만 읽고
  "`smtp` 가 아니면 실패"로 판정해서, **"베타는 실발송하면 안 된다"라는 반대 방향 불변식을
  표현할 수조차 없었다** — 베타가 prod 자격증명을 상속해 쓰는 동안에도 초록이었다. 검사기를
  만들 땐 "무엇이 정상인가"뿐 아니라 **"어느 호스트에서 무엇이 금지인가"** 를 표현할 수 있는지
  보라. 그리고 유효 설정은 **런타임과 같은 방식으로 합성**해야 한다(EnvironmentFile → 프로세스
  env → `server/.env` 폴백). `.env.beta` 만 읽으면 상속으로 채워지는 값을 놓친다.
- ㊴ **conftest 가 `MAILER_MODE=console` 을 강제하므로 pytest 안에서는 메일 모드 상속 결함을
  재현할 수 없다.** 실메일 차단을 위한 의도된 설정이지만, 그 결과 "상속이 일어나는가"를 묻는
  테스트는 원리적으로 불가능하다 → **결과 상태를 직접 심어** 판정만 검사하고 그 한계를 주석에
  남겨라(MV-4). 재현 못 하는 것을 재현한 척하면 그게 거짓 초록이다.

---

## 5. 도구·명령

```bash
# 테스트
DB_NAME=loupit_test python3 -m pytest server/tests -q -m "not sc14"   # 485
DB_NAME=loupit_test python3 -m pytest server/tests -q -m sc14         # 3
python3 -m pytest generator/tests -q                                   # 225
node --test 'web/**/*.test.js'                                         # 626

# 시드만 prod 에 멱등 적용 — ⚠ release.sh 금지(테스트 게이트가 서빙 스키마 DROP/CREATE)
#   드라이런이 기본이다. 무엇이 늘어날지 보고 --apply 를 붙여라.
python3 - <<'PY'   # 요지: load.py 의 _split_sql_statements 로 시드를 실행하고 전후를 대조
PY
#   이번 세션 스크립트: 트랜잭션 안에서 실행 → 신규/소실 행 + 회원 테이블 행수 출력 → 커밋/롤백

# 백업(2차 사본 포함) 수동 1회
sudo systemctl start loupit-backup.service && journalctl -u loupit-backup -n 3 --no-pager
ls -la /var/backups/loupit /db/backups/loupit

# 메일 설정 검증 — 호스트별 기대값이 다르다(prod=smtp 필수 / beta=console 필수)
python3 infra/deploy/verify_mail.py                   # prod: 연결·인증까지(발송 없음)
sudo python3 infra/deploy/verify_mail.py --host beta  # beta: 실발송 모드면 exit 1

# 도메인 소유 확증 도구(SP-AUTH-7.4)
curl -s "https://login.microsoftonline.com/getuserrealm.srf?login=test@<도메인>&xml=1"
python3 -c "import socket;s=socket.create_connection(('whois.kr',43));s.sendall(b'<도메인>\r\n');print(s.recv(65535).decode('utf-8','replace'))"
openssl s_client -connect <호스트>:443 -servername <호스트> </dev/null 2>/dev/null | openssl x509 -noout -subject

# 운영 CLI
python3 -m server.ops digest                          # 큐 3종 요약(발송 없음)
python3 -m server.ops list-pending                    # 재직 수동 승인 큐
python3 -m server.ops list-company-requests           # 회사 등록 요청 큐
python3 -m server.ops list-suppressed                 # 메일 발송 억제 목록

# nginx — ⚠ release.sh 는 conf 를 배포하지 않는다(함정 ⑭). 항상 손으로 배치
sudo install -m644 infra/nginx/loupit.conf /etc/nginx/sites-available/ && sudo nginx -t && sudo systemctl reload nginx
```

---

## 6. 참조

- **함정 정본**: `-27-B` §5(①~⑬) · `-28` §4(⑭~⑲) · `-29` §4(⑳~㉗) · 본 문서 §4(㉘~㊴)
- **SPEC 정본**: `SPEC/13` **SP-AUTH-7.4**(도메인 증거 기준)·SP-AUTH-7(422)·SP-AUTH-16(웹훅)·
  SP-AUTH-17(회사 등록 요청) / `SPEC/11` §3.4(리밋·게이트)
- **운영**: `docs/OPS-backup.md` §1-1b(백업 미러)
- **이력**: `docs/HANDOFF-2026-07-29.md`(M9 활성화) · `-28`(P3·P5·웹훅) · `-27-B`(리밋·메일)
- **감사**: `docs/AUDIT-2026-07-17.md`
