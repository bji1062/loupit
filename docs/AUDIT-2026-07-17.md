# 전체 코드베이스 감사 — 2026-07-17

> **목적**: 새 세션에서 결함 수정을 이어서 진행하기 위한 인수인계 문서.
> jobcho.wiki 컷오버 **완료 당일**(스모크 11/11, TLS 자동갱신 dry-run 성공) 시행한 전수 감사 결과다.
> 방법: 7개 렌즈(server API / generator / web 프론트 / infra / 스크립트·DB / 보안 / 계층간 계약) 병렬 리뷰
> → 중복제거 → **발견마다 독립 반증**(high는 2인, medium 1인) → 누락각도 크리틱. 총 89개 체크.
> 결과: 원시 39 → 중복제거 34 → **반증 통과 18 + 크리틱 3 = 확정 21건** (반증 기각 0건), low 16건은 미검증 보류.

## 재개 절차 (새 세션 첫 작업)

1. 이 문서 + 메모리 `loupit-deploy-host.md` 읽기.
2. ⚠️ **`run_tests.sh`/`release.sh`를 확정 #4 수정 전까지 절대 실행 금지** — 테스트 게이트가
   서빙 `TCOMPARE_LOG`(시드로 재현 불가한 유일한 운영 데이터)를 **복원 없이 삭제**한다(#4).
   백엔드 검증이 필요하면 먼저 `mysqldump ... LOUPIT TCOMPARE_LOG` 로 수동 백업하거나 #4부터 고칠 것.
   프론트 `cd web && npm test`(node --test, DB 무관)는 안전.
3. ⚠️ `server/.env`를 root로 편집하면 소유권이 바뀌어 API 두 개가 재시작 시 죽는다 —
   편집 후 `sudo chown ubuntu:ubuntu server/.env && sudo chmod 600 server/.env`.
4. 아래 "수정 우선순위"부터 착수. 수정마다: 원인 파일 확인 → 회귀 테스트 추가(RED) → 수정(GREEN) → 커밋.
5. 프론트 수정은 beta.loupit.co가 작업트리 `web/`을 직서빙하므로 브라우저로 즉시 확인 가능.

## 수정 우선순위 (제안)

**1순위 — 제품 정확성 (비교 결과가 틀리는 버그, 사이트 존재 이유):**
- #2 재택근무 점수 0 반영 (calc.js enum vs 불리언) — 수정 방향: 데이터 계약 결정 필요
  (불리언 true를 어느 절감액에 매핑할지 = 제품 결정. 엔진에 불리언 수용 추가 or 인테이크에서 enum 정규화)
- #3 필수값 결측 무시 → 0원 리포트 — 수정 방향: `report.ok`/`missing`을 ui.js 비교하기 버튼에서 소비
  (결측 안내 + 이동 차단 또는 결측 배지)
- #11 정성 복지 설명 미표시 — 수정 방향: 엔진·렌더의 `qual_desc` → `qual_desc_ctnt` 통일(또는 normalizeBenefit에서 매핑)

**2순위 — 데이터 보호:**
- #1 TCOMPARE_LOG 테스트마다 영구 삭제 — 수정 방향: run_tests.sh trap에 TCOMPARE_LOG dump/restore 추가
  + conftest DROP 목록 재검토 + load.py 복원 범위 결정
- #5 백업 체계 미가동 + #16 backup.sh 결함(PROCESS 권한·DB_NAME) — 타이머 설치 전에 스크립트부터 수정

**3순위 — 배포 파이프라인 안전화:** #4(release.sh 허위 서술+가드), #9(build.py --only 사고 방지),
#14(load.py --fresh 가드), #17(deploy-beta.sh 반배포), 런북 정정

**4순위 — 남용 방어·사업 준비:** #7(comparisons/log 레이트리밋·보존정책), #8(정책 4종 플레이스홀더 —
애드센스 심사 전 필수), #12(동의 배너 배선 — 광고 켜기 전 필수)

**결정 필요(제품 판단, 코드만으로 못 정함):** #2의 불리언 매핑값, 크리틱 #2·#3(최근비교·직접입력 —
완성할지 UI에서 제거할지), #8의 운영자 연락처·사업자 정보 실값

---

## 확정 발견 (반증 통과 18건, 심각도순)

### 1. [high] 테스트 게이트가 TCOMPARE_LOG(트렌딩 원천, 유일한 비재현 운영 데이터)를 실행할 때마다 영구 삭제 — 복원 범위에서 누락

- **위치**: `server/tests/conftest.py:59` · 렌즈: correctness/data-loss

**영향**: release.sh [4/7]이 run_tests.sh를 필수 경유하므로, 릴리스·테스트를 1회 돌릴 때마다 라이브 사용자들의 비교 로그(POST /comparisons/log 축적분)가 전량 삭제된다. 참조 5테이블은 시드로 복원되지만 이 로그는 시드로 재현 불가·백업 부재라 비가역 손실이고, '실시간 비교 TOP 10'(trending.py, 최근 7일 윈도우)이 릴리스 직후 빈 상태로 리셋된다.

<details><summary>증거 (리뷰어 원문)</summary>

```
conftest.py:53-61 TABLE_CREATE_ORDER = [... "TBENEFIT_PRESET", "TCOMPARE_LOG"] → :134-135 `for t in TABLE_DROP_ORDER + REMOVED_TABLES: cur.execute(f"DROP TABLE IF EXISTS {t}")` — 세션 픽스처 schema_db(:159-162)가 setup과 teardown 양쪽에서 TCOMPARE_LOG를 DROP한다. 복원을 담당하는 infra/deploy/run_tests.sh:36 `"$PY" "$ROOT/db/seed/load.py" --fresh`의 복원 범위는 db/seed/load.py:31 TABLE_CREATE_ORDER = ["TCOMPANY_TYPE", "TCOMPANY", "TCOMPANY_ALIAS", "TCOMPANY_BENEFIT", "TBENEFIT_PRESET"] — TCOMPARE_LOG 없음. schema.sql:136이 빈 테이블만 재생성. 쓰기 경로는 server/database.py:63 `INSERT INTO TCOMPARE_LOG ...` 뿐이고 백업은 미가동(별도 finding). 확인: `grep -n TCOMPARE_LOG server/tests/conftest.py db/seed/load.py` → conftest에만 존재.
```
</details>

**반증자 판정**: Confirmed by direct code trace. conftest.py:59 includes TCOMPARE_LOG in TABLE_CREATE_ORDER; _drop_all_tables (conftest.p / Confirmed end-to-end with live evidence. (1) conftest.py:59 puts TCOMPARE_LOG in the drop order and schema_db (conftest.


### 2. [high] 재택근무(remote)가 자율성 점수에 전혀 반영되지 않음 — 엔진 enum 계약 vs 실데이터/UI 불리언 불일치

- **위치**: `web/assets/js/calc.js:265` · 렌즈: correctness (engine-data contract)

**영향**: 기본 우선순위 축이 워라밸(app.js:24 `curPri: '워라밸'`)이고 vdWlb p2 '시간 자율성'이 autonomyScore로 승자를 판정하는데(calc.js:339-343), 재택 체크(사용자)든 회사 제안값(REF)이든 remote 기여가 항상 0이라 재택 유무만 다른 두 회사가 '거의 비슷합니다'(tie)로 오판정된다. 반면 유연근무 체크(+50)는 우연히 동작해 두 체크박스 효과가 비대칭 — 사용자 가시 오동작.

<details><summary>증거 (리뷰어 원문)</summary>

```
calc.js:120 `REMOTE_SAVE = { none: 0, partial: 72, hybrid: 120, free: 180 }`; calc.js:265 `const remote = (ws && REMOTE_SAVE[ws.remote]) || 0;` — enum 문자열 키만 조회. 그러나 실데이터는 전부 불리언: db/seed/company_meta.py:123 `"remote": bool(codes & {...})`, 라이브 확인 `curl http://127.0.0.1:8000/api/v1/reference/all` → 95/95 회사 remote/flex 타입 Counter({'bool': 95}). UI도 불리언 저장: ui.js:137-138 `cb.checked = !!ws[key]; cb.addEventListener('change', () => { state.wsState[slot][key] = cb.checked; })`. 재현: `node --input-type=module -e "import {autonomyScore} from './assets/js/calc.js'; console.log(autonomyScore({remote:true},null))"` → 0 (remote:'hybrid'면 120, flex:true는 50으로 동작). 테스트는 enum 문자열만 사용(calc.test.js:217 `autonomyScore({ remote: 'hybrid', ...})`, inputs.test.js:101 `work_style_val: { remote: 'hybrid' }`)해 불일치를 가림.
```
</details>

**반증자 판정**: Confirmed end-to-end with reproduction. calc.js:265 looks up REMOTE_SAVE[ws.remote] with enum-string keys only, but ever / Confirmed end-to-end with live evidence. Engine (calc.js:265 REMOTE_SAVE[ws.remote]) only scores enum strings, but every


### 3. [high] 필수값 결측(연봉·상승률)이 리포트 렌더에서 완전히 무시 — 0원 기반의 오도성 리포트가 정상처럼 표시

- **위치**: `web/assets/js/report.js:466` · 렌즈: correctness (missing-state dropped)

**영향**: (1) 연봉 미입력 상태에서 비교하기를 누르면 '총보상 0 만원', '실효연봉 차이 0 만원' 등 0 기반 리포트가 아무 안내 없이 정상 출력. (2) 상승률만 비워도 B 총액=복지합만 남아 vdSalary가 '총액 우위: 현재 직장'이라는 적극적으로 틀린 판정을 표시한다. 신규 사용자가 입력 화면을 대충 넘기는 흔한 흐름에서 발생.

<details><summary>증거 (리뷰어 원문)</summary>

```
엔진은 결측을 명시 반환: calc.js:469 `if (!salA.min && !salA.max) missing.push('salary')`, calc.js:513 `ok: missing.length === 0`. 그러나 렌더/배선 어디에서도 소비 안 함: `grep -n "missing\|\bok\b" web/assets/js/report.js` → sacrifice.ok(37행) 외 0건, ui.js:235-239 btn-compare 클릭은 무조건 `deps.runReport(...); deps.go('report')`, app.js:220-230 runReport도 report.ok 미확인. 또 상승률 미입력 시 calc.js:166 `if (!baseRange || !baseRange.min || selectedRate == null) return { min: 0, max: 0, mid: 0 }` → B 연봉이 0으로 계산되는데 이 상태는 missing에도 안 잡힘.
```
</details>

**반증자 판정**: Confirmed by code trace and live repro. (1) calc.js:469/513 sets missing/ok but no non-test consumer exists (grep: only  / Confirmed by live engine repro (node, pure module, no DB). (1) calc.js:469/513 sets ok:false+missing:['salary'] but no c


### 4. [high] release.sh: 프로덕션 서빙 DB를 비우는 [4/7] 게이트에 대한 가드/경고 전무 + '이전 산출물 유지' 주장이 허위(정적 스왑은 게이트 전에 이미 라이브) + RUNBOOK이 이를 표준경로로 권고하며 거짓 안전 서술

- **위치**: `infra/deploy/release.sh:37` · 렌즈: ops/runbook-contradiction

**영향**: '운영자 1인' 대상 문서(RUNBOOK:3)를 그대로 따르면 라이브 API가 백엔드 테스트~재시드 구간 동안 500/빈 응답을 내는 다운타임 창이 매 배포마다 생기고(사용자 가시), 그 구간에서 프로세스가 중단되면 서빙 DB가 빈 채로 남는다(run_tests.sh 스스로 문서화한 L-7). 런북의 '실패해도 이전 산출물 그대로'라는 보증을 믿고 재시도/방치하면 장애를 오판한다 — 정적 산출물에만 참인 문장을 전체 서비스로 일반화. 복구 명령도 프로덕션 API 재시작이 빠져 있어 절반짜리다.

<details><summary>증거 (리뷰어 원문)</summary>

```
release.sh:7-9 헤더: "[4/7] 테스트 게이트가 ... 실패한 산출물이 서비스에 반영되지 않는다(무중단 지향, 이전 산출물 유지)" — 그러나 (a) [3/7](:35) `generator.build --out web/dist`가 게이트 이전에 실행되고 nginx는 web/dist를 디스크에서 직접 서빙(infra/nginx/loupit.conf:105-118 `root /home/ubuntu/loupit/web/dist; try_files $uri.html`)하므로 새 정적 산출물은 게이트 실패와 무관하게 이미 반영돼 있다. (b) [4/7](:38)이 호출하는 run_tests.sh는 서빙 참조 테이블을 DROP/CREATE 후 재시드하는데 run_tests.sh:13-24 자체 주석이 이를 명시한다: "── C-1 안전장치(2026-07-12): 이 서버는 서빙 스키마 LOUPIT 를 테스트에도 재사용한다. 백엔드 테스트는 5개 참조 테이블을 DROP/CREATE 하므로 … ⚠ 비원자성(L-7…): load.py --fresh 는 DROP TABLE(DDL) … DROP~재시드 완료 사이에 프로세스가 죽으면 서빙이 빈 채로 남을 수 있다 — 원자 '보장'이 아니라 '시도'다." 그런데 release.sh에는 이 사실에 대한 경고·프로덕션 가드가 한 줄도 없다. 런북 측: docs/RUNBOOK-jobcho-cutover.md:89-91 "cd /home/ubuntu/loupit; bash infra/deploy/release.sh # schema→seed→generate→테스트 게이트→API 재시작→nginx reload→스모크"를 프로덕션 컷오버 절차로 그대로 권하고, :152는 "`release.sh`는 테스트 게이트 실패 시 [5/7] 이후를 실행하지 않으므로, 실패 시 서비스는 이전 산출물 그대로다"라고 단언 — DB·정적물 양쪽 모두에 대해 사실과 다르다. [1/7] schema·[2/7] seed도 게이트 이전에 이미 서빙 DB에 쓴다(release.sh:27-32). 라이브 확인: loupit-api가 이 DB를 실시간 서빙 중(`systemctl is-active loupit-api` → active, https://jobcho.wiki/api/v1/reference/all → 200, companies 95). 게이트 실패 시 수동 복구 안내(run_tests.sh:30-31, docs/RESUME.md:26)도 `sudo systemctl restart loupit-beta-api`로 베타 API만 재시작하며 프로덕션 loupit-api는 언급이 없다.
```
</details>

**반증자 판정**: 모든 주장 사실로 확인. (1) release.sh:7-9의 '이전 산출물 유지' 서술은 허위: [1/7] schema(:27-29)·[2/7] seed(:31-32)·[3/7] 정적 생성(:35)이 전부 [4/7] / Confirmed with concrete impact. (a) release.sh:27-38 runs schema/seed/static-generate before the [4/7] gate; nginx serve


### 5. [medium] 백업 체계 전체 미가동 — 타이머 미설치·어떤 스크립트/런북도 enable하지 않음, 백업 파일 0개

- **위치**: `infra/deploy/provision.sh:47` · 렌즈: ops/backup

**영향**: run_tests.sh/release.sh가 서빙 테이블을 상시 드랍-재시드하는 운영 구조인데 mysqldump 백업이 단 한 번도 만들어진 적 없는 상태다. TCOMPARE_LOG(익명 비교 로그 — 트렌딩 위젯의 원천, 라이브 유저 데이터) 등 시드로 재현 불가한 데이터는 어떤 사고에서도 복구 수단이 없고, '백업은 보조 안전장치'(backup.sh:2-3)라는 설계 전제 자체가 현재 거짓이다. 디스크 장애·오조작(테스트 게이트의 스키마 초기화 등) 시 복구 불능이며, 타이머를 지금 설치해도 위 2개 함정 때문에 첫 실행이 실패한다.

<details><summary>증거 (리뷰어 원문)</summary>

```
라이브 호스트 확인: `ls /etc/systemd/system | grep loupit` → loupit-api.service, loupit-beta-api.service 뿐(loupit-backup.* 미설치). `systemctl is-active loupit-backup.timer` → inactive / "No such file or directory". `systemctl list-timers --all | grep -i 'loupit\|backup'` → dpkg-db-backup.timer만. `ls /var/backups/loupit` → "No such file or directory". provision.sh:47-48은 `sudo cp .../loupit-backup.service|.timer /etc/systemd/system/` 복사만 하고 `systemctl enable --now loupit-backup.timer`가 리포 전체 어디에도 없다(`grep -rn 'enable' infra/deploy/*.sh` → deploy-beta의 beta-api enable뿐). 지금 타이머를 설치해도 실패하는 함정 2건: (a) backup.sh:9 `mkdir -p /var/backups/loupit`는 User=ubuntu로 실행돼 root 소유 /var/backups 밑 생성 실패(provision.sh 3단계가 선행 생성·chown해야 하나 이 호스트에서 미실행), (b) loupit-backup.service의 `EnvironmentFile=-/home/ubuntu/loupit/infra/env/backup.env`(옵셔널 `-` 접두)는 부재(`ls infra/env/` → server.env.example만) → SEED_PW 미설정으로 backup.sh:11-12 `${SEED_PW:?}`에서 즉시 abort. RUNBOOK-jobcho-cutover.md에는 백업 단계 자체가 없다.
```
</details>

**반증자 판정**: Fully confirmed on both repo and live host. provision.sh:47-48 only copies loupit-backup.service/.timer; no `systemctl e / 모든 세부 주장이 라이브 호스트·리포 양쪽에서 사실로 확인됨: loupit-backup.service/.timer 미설치(/etc/systemd/system에 api 유닛 2개뿐), /var/backups/loupi


### 6. [medium] provision_accounts.sql 런타임 계정이 SELECT 전용 — 서버 필수 쓰기(INSERT INTO TCOMPARE_LOG)와 모순, 리포대로 재프로비저닝하면 비교 로그 쓰기 전량 500·트렌딩 조용히 고갈

- **위치**: `infra/mysql/provision_accounts.sql:8` · 렌즈: correctness/deploy-drift (infra/mysql)

**영향**: 커밋된 provision_accounts.sql로 런타임 계정을 (재)프로비저닝하거나 파일 내 검증 지침대로 'SELECT만'으로 그랜트를 되돌리면, POST /comparisons/log가 매 호출 권한오류(ER1142) → main.py 전역 핸들러 500이 된다. 클라이언트(trending.js sendCompareLog)는 fire-and-forget이라 사용자에겐 조용히 실패하고 새 로그가 0건 누적되며, 트렌딩 쿼리는 WHERE INS_DTM >= NOW()-INTERVAL 7 DAY라 기존 행이 윈도우를 벗어나면 '실시간 비교 TOP 10' 위젯이 빈 목록→host hidden으로 사라진다. 새 호스트를 이 파일로 프로비저닝해도 동일하게 깨지고, 리포·라이브 어느 쪽이 정본인지도 상충(NFR20 최소권한 vs INV-1 개정). 어떤 테스트/스모크도 이 경로를 밟지 않아 회귀가 잡히지 않는다.

<details><summary>증거 (리뷰어 원문)</summary>

```
provision_accounts.sql:7-8 `CREATE USER IF NOT EXISTS 'loupit'@'127.0.0.1' ...; GRANT SELECT ON loupit.* TO 'loupit'@'127.0.0.1';` + 17행 검증 지침 "SHOW GRANTS ... → SELECT만 나와야 함(NFR20)". 그러나 server/database.py:63 `_SQL_INSERT_COMPARE_LOG = "INSERT INTO TCOMPARE_LOG (A_COMP_ID, B_COMP_ID) VALUES (%s, %s)"` 가 server/routers/trending.py:41-48 `await database.insert_compare_log(body.a, body.b)` (POST /api/v1/comparisons/log)에서 필수 실행됨. git 이력: provision 파일 최종 수정 a4dd223(2026-07-11, 커밋 #24) → 트렌딩 도입 a73bb27(INV-1 개정 'INSERT into TCOMPARE_LOG', 2026-07-14, 커밋 #50) — 그랜트가 먼저 커밋되고 이후 쓰기 기능이 추가됐으나 그랜트는 갱신되지 않음(git log --reverse로 확인). 시드는 TCOMPARE_LOG를 채우지 않고(grep db/seed → 0건) smoke.sh에도 POST 경로 테스트 없음(grep comparisons/log → 0건). 라이브 검증: `curl -s http://127.0.0.1:8000/api/v1/comparisons/trending` → {"items":[{..."cnt":5},{..."cnt":4},...]} — 시드 무주입인데 집계가 존재 = 라이브 런타임 계정이 실제로 INSERT에 성공 중 → 라이브 그랜트가 커밋된 SELECT-only에서 이미 이탈(config drift).
```
</details>

**반증자 판정**: 모든 주장 실증 확인. (1) provision_accounts.sql:7-8은 런타임 'loupit'@'127.0.0.1'에 GRANT SELECT만 부여하고 17행이 "SELECT만 나와야 함(NFR20)" 검증


### 7. [medium] 무인증 POST /comparisons/log — 레이트리밋·인증·보존정책 전무: 트렌딩 TOP10 조작 임계 6요청 + 커넥션풀(maxsize=10) 포화 DoS + TCOMPARE_LOG 무한 증가

- **위치**: `server/routers/trending.py:41` · 렌즈: abuse-resistance / 운영

**영향**: 인터넷 노출(https://jobcho.wiki/api/v1/comparisons/log) 익명 INSERT 엔드포인트에 완화장치(인증/스로틀/리텐션)가 하나도 없다. (a) 단순 curl 루프 6회로 홈 화면 '실시간 비교 TOP 10' 위젯 순위를 임의 조작 가능(위젯 클릭 시 비교 슬롯 프리필까지 유도 — app.js:120 onPick 배선), (b) 등록 comp_id 쌍으로 POST를 병렬 폭주시키면 요청당 풀 커넥션을 점유해 maxsize=10 풀을 포화시켜 search/reference/trending 등 정상 GET이 대기·타임아웃(사용자 가시 지연)에 빠질 수 있음, (c) 지속 대량 POST 시 TCOMPARE_LOG가 무한 증가(요청당 SELECT+INSERT 2회 DB 부하 + 단일 호스트 디스크 소진 경로, 정리 수단 전무 — 수동 DELETE뿐)이며 익명이라도 '무저장 지향'(FR-07 예외 한정) 원칙상 불필요 보관이다. 현재는 역설적으로 테스트 게이트의 전량 삭제 버그가 상한 역할을 하고 있어, 그 버그를 고치는 시점에 보존정책을 함께 넣어야 한다.

<details><summary>증거 (리뷰어 원문)</summary>

```
server/routers/trending.py:41-48
```python
@router.post("/comparisons/log", status_code=204)
async def log_comparison(body: CompareLogIn) -> Response:
    rows = await database.fetch_all(_SQL_PAIR_EXISTS, (body.a, body.b))
    ...
    await database.insert_compare_log(body.a, body.b)
```
요청당 SELECT+INSERT 2회 DB 왕복, 인증·토큰·중복제거 없음(body는 comp_id 쌍만). 방어 계층 전수 부재 확인: (1) 앱 계층 — server/ 전체에 rate-limit 미들웨어 없음(main.py는 CORS+예외핸들러뿐, CORS는 비브라우저 curl에 무력, 사전요청 없는 simple POST라 sendBeacon/스크립트로 무제한 호출 가능). (2) nginx 계층 — `grep -rn "limit_req\|limit_conn" infra/nginx /etc/nginx/sites-enabled/` → 0건, 배포 conf는 리포와 diff SAME이며 `location ^~ /api/v1/`(infra/nginx/loupit.conf:132)가 POST 포함 전 메서드를 proxy_pass만으로 프록시. (3) 데이터 계층 — TCOMPARE_LOG(db/schema.sql:136-144, CMP_LOG_ID BIGINT AUTO_INCREMENT PK)에 보존/파티션 없음, `grep -rn "DELETE\|PARTITION\|purge\|prune\|retention" db/ infra/` → 이 테이블 정리 로직 0건(backup.sh 14일 보관은 백업 파일 로테이션이지 테이블 정리가 아님), 소비는 trending.py:36 `WHERE l.INS_DTM >= NOW() - INTERVAL %s DAY`(trending_window_days=7, server/config.py:52)뿐 — 7일 지난 행은 어떤 쿼리도 읽지 않는데 무기한 축적. (4) 풀 상한 — config.py:36 db_pool_max=10, database.py:20-32 create_pool(maxsize=s.db_pool_max). 조작 임계 실측: 라이브 `curl http://127.0.0.1:8000/api/v1/comparisons/trending` → 1위 cnt=5 (삼성전자 vs SK하이닉스) — 유효 comp_id 목록은 /reference/all로 공개되므로 curl 6회면 임의 쌍을 홈 위젯 1위에 올릴 수 있음.
```
</details>

**반증자 판정**: Finding is factually accurate and the defect is real/reachable. Verified: POST /comparisons/log (trending.py:41-48) take


### 8. [medium] 정책 4종이 플레이스홀더·초안 배너 상태로 프로덕션 서빙·색인 중 (POLICY_*/ADSENSE env 미주입, 주입 경로도 부재)

- **위치**: `generator/config.py:29` · 렌즈: 운영 리스크(사용자 가시)

**영향**: 실사용자·검색엔진이 법적 고지 페이지에서 깨진 템플릿 플레이스홀더({...})와 초안 배너를 그대로 본다. 개인정보처리방침이 미완성 상태라 AdSense 심사 요건을 충족하지 못하고, 광고 슬롯 client id도 placeholder라 광고가 서빙될 수 없다. release.sh·systemd·env 어디에도 주입 지점이 없어 운영자가 수동 export 없이는 영구 지속된다.

<details><summary>증거 (리뷰어 원문)</summary>

```
config.py:29-43: policy_contact = os.environ.get("POLICY_CONTACT", "{운영자 정정·문의 연락처}") / policy_last_modified 기본 "{게시 시 운영자 확정}" / legal_reviewed 기본 false / adsense_client_id 기본 "ca-pub-XXXXXXXXXXXXXXXX". 재현: curl -s https://jobcho.wiki/privacy | grep -o '{운영자[^<]*' → "{운영자 정정·문의 연락처}" 출력, '최종 수정일: {게시 시 운영자 확정}' 및 초안 배너("본 문서는 초안입니다…")가 라이브 노출. grep -c 'ca-pub-XXXXXXXXXXXXXXXX' web/dist/company/kakao.html → 2 (전 페이지 광고슬롯 placeholder). grep -rn 'POLICY_CONTACT|ADSENSE_CLIENT_ID|POLICY_LEGAL' infra/env/ infra/systemd/ infra/deploy/release.sh → 무결과(주입 메커니즘 자체가 없음). sitemap.xml에 /privacy·/terms·/disclaimer·/ads 4종 포함(색인 대상).
```
</details>

**반증자 판정**: 라이브에서 전부 재현 확인. generator/config.py:29-43의 placeholder 기본값이 그대로 빌드에 들어갔고, generator/content/policy.py:77·158 등에서 legal_r


### 9. [medium] build.py --only(dev 필터)가 기본 --out=web/dist(프로덕션)와 결합 시 dist 전체를 부분집합으로 스왑 — 라이브 페이지 대량 소실

- **위치**: `generator/build.py:46` · 렌즈: --incremental/--only 변경 누락·운영 함정

**영향**: 개발용 플래그의 --out 기본값이 서빙 중인 web/dist라서, 오퍼레이터가 부분 렌더 확인 목적으로 --only를 실행하는 순간 프로덕션 사이트의 나머지 페이지 전부가 원자 스왑으로 삭제되고 sitemap도 축소된다. 게이트가 이를 잡지 못한다(회사 1페이지만 있어도 통과). 안전장치(--only 시 out_dir 강제 분리 또는 스왑 거부)가 없다.

<details><summary>증거 (리뷰어 원문)</summary>

```
build.py:46-47 `if only: pages = [p for p in pages if any(p.path.startswith(o) for o in only)]` 후 build.py:49-54에서 sitemap도 필터된 pages 기준으로 생성하고 stage_and_swap 호출. release.py:82 `_reset_dir(nxt)`(빈 스테이징에서 시작) → release.py:100 `_atomic_swap(out_dir, nxt)`(out_dir 통째 교체). 검증 게이트 checks.py:25-33(GC-2)은 company 페이지 ≥1 && ≠200만 확인하므로 통과. 논리 재현(실행 금지라 미실행): `python3 -m generator.build --bundle-json dump.json --only company/kakao` → web/dist에 company/kakao.html + sitemap(1~2 URL) + robots만 남고 나머지 ~100페이지·404.html 소멸. nginx는 error_page 404 /404.html을 dist에서 찾으므로(loupit.conf) 404 폴백도 깨짐.
```
</details>

**반증자 판정**: Confirmed in code: build.py:46-47 filters pages by prefix, --out defaults to CFG.out_dir="web/dist" (config.py:39) which


### 10. [medium] 참조 번들 로드 실패 시 오류 UI가 절대 보이지 않고 재시도 버튼도 미배선 (FR-E1 사실상 미구현)

- **위치**: `web/index.html:93` · 렌즈: correctness (error path UX)

**영향**: API(/api/v1/reference/all) 장애·타임아웃 시 사용자는 정적 히어로만 보고 비교 도구가 왜 없는지 아무 표시도 못 받으며, 설계된 '다시 시도' 경로가 이중으로(불가시+미배선) 죽어 있어 수동 새로고침 외 복구 수단이 없다.

<details><summary>증거 (리뷰어 원문)</summary>

```
#boot-error가 hidden 유지되는 #app 내부에 중첩: index.html:47 `<div id="app" hidden>` … index.html:93-96 `<div id="boot-error" hidden>…<button id="btn-boot-retry">` … 97행에서 #app 닫힘(compare/index.html:93-96 동일). styles.css:249 `[hidden] { display:none !important; }`. app.js:87-93 showBootError는 `box.hidden = false`만 수행하고 주석 그대로 '#app 계속 hidden(B-3)' — 조상 hidden이라 자식 unhide 무효. 추가로 app.js:98-101 `catch (err) { return showBootError(err); }`가 mountUI 이전에 return하므로 bindBootRetry(ui.js:285-288)가 실행되지 않아 재시도 버튼은 (보였더라도) 리스너 0개.
```
</details>

**반증자 판정**: 확인됨(반증 실패). (1) 불가시: web/index.html:47 `<div id="app" hidden>` 내부에 #boot-error(93-96행)가 중첩되고 97행에서 #app이 닫힘 — compare/in


### 11. [medium] 정성 복지 설명이 리포트에 절대 표시되지 않음 — qual_desc_ctnt(서버/정규화) vs qual_desc(엔진/렌더) 필드명 불일치

- **위치**: `web/assets/js/calc.js:214` · 렌즈: correctness (API-field contract)

**영향**: 리포트 '정성 복지' 섹션(FR-40 6번 카드)에서 691건의 설명('주 2일 이상 재택근무 지향' 등)이 전부 탈락하고 항목명만 나열된다 — 정성 복지 대비라는 섹션 목적 자체가 무력화되는 데이터 유실.

<details><summary>증거 (리뷰어 원문)</summary>

```
calc.js:214 `.map(b => ({ ..., qual_desc: b.qual_desc || '' }))` — 존재하지 않는 필드 참조. 서버는 qual_desc_ctnt만 전송(server/models/reference.py:38 `qual_desc_ctnt: str | None`), 클라이언트 정규화도 동일(inputs.js:45 `qual_desc_ctnt: b.qual_desc_ctnt || null`). 라이브 확인: reference/all에서 정성 복지 692건 중 691건이 qual_desc_ctnt 보유, `qual_desc` 키는 0건. 재현: `node --input-type=module -e "import {qualCompare} from './assets/js/calc.js'; ..."` → `{"benefit_nm":"재택근무",...,"qual_desc":""}`. 렌더 소비처 report.js:540 `if (it.qual_desc) li.append(...)` → 항상 false.
```
</details>

**반증자 판정**: Confirmed end-to-end. Server emits only qual_desc_ctnt (server/services/reference.py:37, server/models/reference.py:38);


### 12. [medium] 광고·동의 배선 사실상 부재: initConsentBanner 호출 0회, mountAds는 report 뷰('result')에서만 호출 — 랜딩 광고·동의 배너 dead code

- **위치**: `web/assets/js/ads.js:194` · 렌즈: correctness / 운영 리스크 (monetization wiring)

**영향**: 현재는 AD_CLIENT가 플레이스홀더(adsConfig.js:9)라 실광고 영향 0이지만, AdSense 승인 후 client id만 치환해도 랜딩·정적 페이지에서는 로더 주입·수동 슬롯·동의 배너(GDPR/개인화 신호 applyConsentSignal 포함)가 전혀 실행되지 않는다 — 수익모델 핵심 경로가 조용히 죽어 있는 운영 함정. 부가로 랜딩에 내용 없는 '광고' 점선 박스가 상시 노출.

<details><summary>증거 (리뷰어 원문)</summary>

```
`grep -rn "initConsentBanner\|mountAds" web/assets/js/*.js`(테스트 제외) → initConsentBanner는 ads.js:194 정의뿐 호출처 0. mountAds 호출은 ui.js:238 `deps.mountAds('result')` 단 한 곳(비교하기 클릭 시). app.js:85 `function bindGlobalUI() { /* 자리표시자 */ }`, boot()는 mountAds/initConsentBanner 미호출. index.html:24 `data-page-type="landing"`+110행 content_bottom 슬롯, 122행 #consent-banner가 존재하지만 채워지지 않음. dist 정적 페이지(company/vs/정책)는 스크립트 자체가 0개(`grep '<script'` → ld+json뿐). 랜딩의 광고 자리는 CSS(styles.css:489-498 `[data-ad-position]` 점선 박스 + ::before "광고")로만 그려진 영구 빈 박스.
```
</details>

**반증자 판정**: Confirmed on every point. initConsentBanner (ads.js:194) has zero production callers — only ads.test.js and HTML comment


### 13. [medium] 서빙 API와 DDL(드랍/시드)이 동일 server/.env 계정으로 수행 — 커밋된 최소권한 설계(provision_accounts.sql, NFR20)가 실환경에서 불이행

- **위치**: `infra/mysql/provision_accounts.sql:8` · 렌즈: security/least-privilege

**영향**: API 프로세스 침해·SQL 인젝션·크레덴셜 유출 시 읽기 유출에 그치지 않고 서빙 테이블 전체 드랍이 가능하다. 또한 provision_accounts.sql을 신뢰해 재구축하면(SELECT 전용 runtime + 소문자 loupit 스키마) 릴리스 파이프라인 전 단계가 권한 오류로 동작하지 않는, 문서-실환경 이중 괴리 상태다.

<details><summary>증거 (리뷰어 원문)</summary>

```
provision_accounts.sql:6-8 "런타임(앱): 읽기 전용 ... GRANT SELECT ON loupit.* TO 'loupit'@'127.0.0.1'" + :10 "시드/DDL(빌드타임 전용, .env와 분리 보관)". 그러나 실제로는: release.sh:20-29가 server/.env의 DB_USER로 `mysql < db/schema.sql`(DDL)을 실행하고, db/seed/load.py:75-84 `connect()`도 같은 server/.env로 DROP/INSERT를 수행하며, server/tests/conftest.py:120-135도 동일 env 계정으로 `DROP TABLE`을 실행해 전부 성공하는 것이 전제다(docs/RESUME.md:32 "별도 TEST DB 불가(APP_LOUPIT 권한) → LOUPIT 재사용" — 단일 계정 APP_LOUPIT). 즉 라이브 API(server/.env 사용)가 쥔 크레덴셜이 서빙 스키마에 대한 DROP/ALTER 권한을 보유한다.
```
</details>

**반증자 판정**: 전 증거 사슬 확인됨. (1) infra/mysql/provision_accounts.sql:6-13은 런타임 'loupit'@127.0.0.1에 GRANT SELECT만, DDL은 별도 'loupit_seed'로 


### 14. [medium] load.py --fresh: 확인 프롬프트·환경 가드 없이 CLI 플래그 하나로 서빙 5테이블 즉시 DROP — DDL 암묵커밋으로 rollback 무력

- **위치**: `db/seed/load.py:131` · 렌즈: ops/destructive-guard

**영향**: 셸 히스토리 재실행·오타 한 번이면 라이브 서빙 테이블이 즉시 사라지며, 시드 도중(예: 벤핏 SQL 95개 적재 중) 프로세스가 죽으면 부분 커밋된 빈/불완전 서빙 상태로 남는다. conftest의 pytest 가드(LOUPIT_ALLOW_SERVING_SCHEMA)와 달리 load.py 자체에는 어떤 안전장치도 없다.

<details><summary>증거 (리뷰어 원문)</summary>

```
load.py:130-131 `fresh_flag = "--fresh" in sys.argv[1:]` → :109-110 `if fresh: _drop_all_tables(cur)` → :87-91 `SET FOREIGN_KEY_CHECKS=0` 후 5개 테이블 `DROP TABLE IF EXISTS`. 확인 프롬프트, 대상 DB 확인 출력, 프로덕션 차단 환경변수 같은 가드가 전혀 없고 접속 대상은 server/.env(프로덕션 LOUPIT)가 기본이다. :120-122의 try/`conn.rollback()`은 DROP(DDL 암묵커밋) 이후 구간엔 무력 — run_tests.sh:18-24(L-7) 주석이 스스로 인정하는 비원자성. 그런데 docs/RESUME.md:26·run_tests.sh:31은 장애 복구 절차로 바로 이 `python3 db/seed/load.py --fresh` 맨손 실행을 지시한다.
```
</details>

**반증자 판정**: 모든 증거가 코드로 확인됨. load.py:131 --fresh 플래그 → :109-110 → :87-91에서 서빙 5테이블 DROP이며, load.py 전체에 확인 프롬프트·대상 DB 출력·환경 가드가 전무하다(c


### 15. [medium] 단독 load.py --fresh 실행 시 TCOMPARE_LOG 잔존 행이 재배정된 COMP_ID로 오귀속 — 트렌딩이 엉뚱한 회사쌍을 표시할 수 있음

- **위치**: `db/seed/load.py:31` · 렌즈: correctness/data-integrity

**영향**: RESUME.md:26이 지시하는 수동 복구(`load.py --fresh`)를 회사 로스터 변경 커밋 후에 실행하면, 살아남은 비교 로그가 무결성 오류 없이 조용히 다른 회사쌍으로 둔갑해 '실시간 비교 TOP 10'에 허위 데이터가 노출된다.

<details><summary>증거 (리뷰어 원문)</summary>

```
load.py:31 드랍 목록에 TCOMPARE_LOG 없음 → --fresh는 FK_CHECKS=0으로 부모 TCOMPANY를 DROP/재CREATE(AUTO_INCREMENT 1부터 재시작)하지만 TCOMPARE_LOG 행은 옛 COMP_ID를 그대로 보존한다(schema.sql:141-142 FK는 DROP TABLE에 CASCADE 미적용, 재생성 시 기존 행 재검증 없음). 회사 등록은 `sorted(BENEFIT_SQL_DIR.glob("*.sql"))`(load.py:114) 순 INSERT라 파일셋이 같을 때만 ID가 우연히 일치 — 회사 파일 추가/삭제/개명 시 이후 모든 회사의 COMP_ID가 밀리면서 기존 로그 쌍이 다른 회사로 재해석된다. server/routers/trending.py:29-38의 INNER JOIN은 고아 행은 숨기고 재사용된 ID는 그대로 다른 회사명으로 집계한다. (run_tests.sh 경유 시엔 conftest가 TCOMPARE_LOG를 아예 드랍해 이 경로 대신 전량 소실이 발생 — 별도 finding.)
```
</details>

**반증자 판정**: Confirmed end-to-end. load.py:31's drop list omits TCOMPARE_LOG (added to schema 2026-07-14; conftest.py:59 was updated 


### 16. [medium] backup.sh 정적 결함: 계정 PROCESS 권한 부재로 mysqldump 실패 예정 + DB_NAME 기본값이 서빙 스키마(LOUPIT)와 불일치 + 실패 시 잘린 백업 잔존 + 복원 절차 부재

- **위치**: `infra/deploy/backup.sh:9` · 렌즈: ops/backup

**영향**: 타이머를 살려도 현재 계정·기본값으로는 백업이 한 번도 성공할 수 없고, 부분 실패가 만든 잘린 gz를 재해 시점에 열어보고서야 복구 불가임을 알게 된다. 복원 절차가 없어 백업이 있어도 '복원 가능한 형태'인지 검증된 적이 없다.

<details><summary>증거 (리뷰어 원문)</summary>

```
backup.sh:9-12 `mysqldump --single-transaction ... -u "${SEED_DB_USER:-loupit_seed}" ... "${DB_NAME:-loupit}" | gzip -9 > "${OUT}"`. (a) 호스트 mysqldump 8.0.42(`mysqldump --version`) — 8.0.21+는 `--no-tablespaces` 미지정 시 PROCESS 권한 필요. infra/mysql/provision_accounts.sql:12-13의 loupit_seed 권한은 `SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, DROP, INDEX, REFERENCES`로 PROCESS 없음 → "Access denied; you need (at least one of) the PROCESS privilege(s)"로 항상 실패. (b) DB_NAME 기본 `loupit`(소문자)인데 서빙 스키마는 LOUPIT(schema.sql:3 "대상 스키마: LOUPIT ... lower_case_table_names=0", run_tests.sh:13-14, conftest.py:7-8) — lctn=0 리눅스에선 별개 스키마라 env 미주입 시 서빙이 아닌 다른/없는 DB를 덤프. server.env.example·provision_accounts.sql도 전부 소문자 loupit로 기록돼 재구축 시 그대로 빗나간다. (c) `set -euo pipefail`이라 덤프 중단 시 스크립트는 비0 종료하지만 이미 생성된 잘린 `loupit-YYYYMMDD.sql.gz`가 정리(trap rm)·무결성 검증(gunzip -t, 'Dump completed' 트레일러 확인) 없이 14일간 정상 백업처럼 남는다. (d) 복원 스크립트/절차가 리포·docs 어디에도 없음(`grep -rn '복원' docs/SPEC/11-인프라-배포.md` → 0건; docs 전체에서 DB restore 절차 부재).
```
</details>

**반증자 판정**: 확인됨(반증 실패). (a) backup.sh:9-12는 --no-tablespaces 없이 loupit_seed로 mysqldump 실행하는데 호스트 mysqldump는 8.0.42(직접 확인)이고 provisio


### 17. [medium] deploy-beta.sh: 인증서 부재를 경고만 하고 진행 — 깨진 vhost를 sites-enabled에 남긴 채 실패해 이후 nginx reload/재기동 전체가 막히는 반배포 상태

- **위치**: `infra/deploy/deploy-beta.sh:37` · 렌즈: ops/half-deploy

**영향**: nginx는 기동/재적재 시 sites-enabled 전체를 검증하므로, 이 잔존 심링크 하나로 이후 모든 `nginx -t`·reload가 실패하고(무관한 certbot deploy-hook reload-nginx.sh 포함), 서버 재부팅 시 nginx 자체가 뜨지 않아 프로덕션 jobcho.wiki vhost까지 전면 다운된다. 전제조건 실패는 파일 배치 전에 중단해야 한다.

<details><summary>증거 (리뷰어 원문)</summary>

```
deploy-beta.sh:25-28 인증서 없으면 경고(⚠)만 출력하고 exit하지 않음 → :37-38 `sudo cp .../loupit-beta.conf /etc/nginx/sites-available/` + `sudo ln -sf ... /etc/nginx/sites-enabled/loupit-beta.conf`로 활성화, :43 `systemctl enable loupit-beta-api`까지 수행 → :46-53 마지막에야 `nginx -t` 실패 시 exit 1. 이때 존재하지 않는 `/etc/letsencrypt/live/beta.jobcho.wiki/*.pem`을 참조하는 vhost 심링크가 enabled 상태로 잔존하며, 스크립트에 롤백(심링크 제거) 없음.
```
</details>

**반증자 판정**: Confirmed by code trace and live-state check. deploy-beta.sh:25-28 warns but does not exit on missing cert, then :37-38 


### 18. [low] infra/mysql/loupit.cnf 전체 미적용 — 라이브 MySQL은 tarball 설치(/data/mysql)로 /etc/my.cnf만 읽으며 리포 cnf와 실질 상이, provision.sh도 이 호스트에서 중도 abort

- **위치**: `infra/mysql/loupit.cnf:1` · 렌즈: deploy-drift/운영 함정 (infra/mysql)

**영향**: 리포가 기술하는 DB 서버와 실제 서버가 다른 물건이다: 슬로우쿼리 관측 불가, 6GB RAM 절약 튜닝 미적용, 리포 기준 트러블슈팅이 헛짚게 됨. 이 호스트에서 provision.sh를 재실행하면 mysql cnf 단계에서 중도 사망해 그 이후 단계(ssh 하드닝·방화벽)가 조용히 누락된다.

<details><summary>증거 (리뷰어 원문)</summary>

```
loupit.cnf:1 헤더 "정본: /etc/mysql/mysql.conf.d/loupit.cnf" 이나 `ls /etc/mysql/` → No such file or directory. 실제: `systemctl cat mysql` → ExecStart=/data/mysql/bin/mysqld_safe --defaults-file=/etc/my.cnf (MySQL 8.0.42 tarball, /data/data). 미적용 항목(알려진 bind-address 건 제외): slow_query_log=ON(리포 27행) → 라이브 미설정(OFF), performance_schema=OFF(24행) → 라이브 기본 ON, innodb_buffer_pool_size=512M(16행) → 라이브 미설정(기본 128M), max_connections=50 → 기본 151. /etc/my.cnf 고유 설정: skip_ssl, default_authentication_plugin=mysql_native_password, secure_file_priv=/log/load_data, transaction-isolation=READ-COMMITTED. (ngram_token_size=2·utf8mb4_0900_ai_ci는 MySQL 8 기본값과 우연히 일치 → 검색·문자셋 동작 무영향.) 연쇄: provision.sh:49 `sudo cp .../loupit.cnf /etc/mysql/mysql.conf.d/loupit.cnf`는 대상 디렉터리 부재로 실패 → set -euo pipefail로 그 지점에서 abort, 50행 sshd-hardening 배치·5단계 firewall.sh 부트스트랩까지 미실행.
```
</details>

**반증자 판정**: 핵심 사실은 확인됨: /etc/mysql/ 부재, 라이브 mysqld는 /data/mysql tarball 설치로 /etc/my.cnf만 읽으며(systemctl cat mysql + ps 확인) infra/mysq


---

## 크리틱 추가 발견 (누락각도 에이전트가 직접 검증, 3건)

### C1. [medium] '최근 비교'(FR-43~45) 기능 전체 미배선 — 저장 진입점 호출 0회 + 복원(onRestore) 콜백 미주입으로 리포트마다 영구 '저장된 비교가 없습니다' 섹션과 죽은 '불러오기' 버튼이 노출

- **위치**: `web/assets/js/report.js:560` · 렌즈: web 프론트 / 계층간 계약(미배선 dead feature)

**영향**: 비교 실행 후 리포트를 볼 때마다 '최근 비교' 섹션이 뜨지만 (1) 어떤 비교도 localStorage에 저장되지 않아 항상 '저장된 비교가 없습니다'로 비어 있고, (2) 만에 하나 레코드가 있어도 '불러오기' 버튼이 아무 동작도 하지 않는다. store.js의 봉투/FIFO/서명 로직(SP-FE-10)과 buildRecentRecord 전부가 도달 불가 코드 — ads.js 미배선과 동형의 기능 부재이나 이쪽은 빈 섹션이 사용자에게 항상 보인다.

<details><summary>증거 (리뷰어 원문)</summary>

```
report.js:557-561 renderReport 8단계가 모든 리포트에 최근비교 UI를 렌더: `if (ctx.recent !== false) { ... renderRecentUI(recentBody, ctx.recentCtx || {}); }` — 그러나 유일한 호출자 app.js:227은 `renderReportFn(report, el2, { benS: state.benS, matched: state.matched })`로 recentCtx를 절대 주입하지 않음. renderRecentUI(report.js:430,443)는 `const { listFn = recent.list, onRestore, onRemove } = ctx;` → 불러오기 클릭 시 `if (typeof onRestore === 'function') onRestore(r);` — onRestore가 항상 undefined라 무동작. 저장 쪽도 동일: app.js:233-236 `export function saveCurrentComparison(...) { ... return saveRecentComparison(state, r); }` 가 유일한 저장 진입점인데, `grep -rn "saveCurrentComparison\|saveRecentComparison" web --include=*.js --include=*.html | grep -v test` 결과 정의(app.js:233)와 report.js 내부 정의/import(app.js:5) 외 호출부 0곳 — compare/index.html에도 저장 버튼 마크업 없음(리포트 뷰 버튼은 btn-edit-input·btn-new-search 뿐, 76-84행). report.js:232 주석 '리포트 하단 UI가 호출'은 허위.
```
</details>


### C2. [medium] 직접 입력 모드(FR-17)가 UI에서 완전히 도달 불가 — setDirectType 호출부 0곳인데 검색 무결과 메시지는 '직접 입력으로 진행할 수 있습니다'라고 안내 (미등록 회사 사용자는 막다른 길)

- **위치**: `web/assets/js/inputs.js:139` · 렌즈: web 프론트 / 계층간 계약(미배선 dead feature)

**영향**: 등록 회사가 ~96개뿐이라(스키마 주석 TCOMPANY '~96개만 등록') 그 밖의 회사에 다니는 사용자는 검색 무결과가 기본 경험인데, UI가 명시적으로 약속한 '직접 입력' 경로가 존재하지 않아 비교 툴 사용이 그 자리에서 불가능해진다. TBENEFIT_PRESET 테이블·benefit_presets 번들·fillBenefits의 프리셋 분기(inputs.js:63-70)·서버 CompareLogIn의 직접입력 제외 규약 전부가 소비처 없는 dead path가 된다.

<details><summary>증거 (리뷰어 원문)</summary>

```
inputs.js:139-145 `export function setDirectType(state, slot, compTpCd)` — `grep -rn "setDirectType" web` 결과 정의(inputs.js)와 테스트 외 import/호출 0곳: ui.js는 search.js에서 `onSearchInput, selectCompany, clearSlot`만 import(ui.js:12), search.js 재노출도 `selectCompany, clearSlot`뿐(search.js:123), app.js는 `normalizeCompany, fillBenefits, initWsState, blankWs`만 import(app.js:7). 셸에도 유형 선택 컨트롤 마크업이 전혀 없고, 오히려 검색 무결과 메시지가 직접 입력을 안내: compare/index.html:56·63 및 index.html:56·63 `<p class="search-empty" ...>검색 결과가 없습니다. 직접 입력으로 진행할 수 있습니다.</p>`. 설령 호출돼도 setDirectType은 `state.matched[slot] = null`(inputs.js:141)로 두는데, 입력 뷰 전진 게이트 ui.js:69-74 `maybeAdvance`는 `if (state.matched.a && state.matched.b)`라 직접 입력 슬롯으로는 입력 뷰에 진입할 수 없다(입력 뷰 도달 경로는 maybeAdvance·프리필 뿐).
```
</details>


### C3. [low] 호스트 방화벽이 리포의 firewall.sh(nftables)와 무관한 OCI 기본 iptables뿐이며 IPv6 계층은 필터 0줄로 전면 개방 — mysqld는 3306+33060(X 프로토콜)을 듀얼스택 와일드카드로 청취 (현재는 글로벌 IPv6 주소 부재로 미노출)

- **위치**: `infra/deploy/firewall.sh:20` · 렌즈: infra 보안(잠재 위험) / 재부팅 지속성

**영향**: 알려진 이슈 #8('방화벽이 외부 차단 중')의 실체는 리포가 커밋한 firewall.sh가 아니라 OCI 이미지 기본 IPv4 룰 하나다. 호스트에 IPv6가 활성화되는 순간(OCI VNIC IPv6 부여 등) 3306·33060·8000 등 모든 포트가 호스트 계층에서 무필터가 되며, 방어는 검증 불가능한 클라우드 Security List 단 한 겹만 남는다. firewall.sh를 실제 적용·영속화(스크립트 말미 안내대로 /etc/nftables.conf 갱신 + nftables enable)하면 v4/v6 모두 drop 정책으로 닫힌다.

<details><summary>증거 (리뷰어 원문)</summary>

```
firewall.sh는 `table inet loupit_filter`(policy drop, v4/v6 공통)를 적용하도록 작성돼 있으나 실호스트 미적용: `sudo nft list ruleset`에 loupit_filter 부재 — 활성 룰은 OCI 스톡 `table ip filter`(INPUT policy accept + 말미 `reject with icmp type host-prohibited`)뿐이고, `table ip6 filter`는 `chain INPUT { type filter hook input priority filter; policy accept; }` 룰 0줄로 전면 개방. 영속 파일도 동일: /etc/iptables/rules.v6 = `:INPUT ACCEPT` 외 규칙 없음(netfilter-persistent enabled → 재부팅 후에도 IPv6 무필터 상태가 복원됨). `systemctl is-enabled nftables` = disabled, /etc/nftables.conf 는 2022-03 스톡 기본(빈 accept 테이블). `ss -tln`: `*:3306`, `*:33060`(mysqlx — 알려진 이슈 #8에 미언급 포트) 듀얼스택 와일드카드 청취. 단 `ip -6 addr show scope global` 출력 없음 — 현재 글로벌 IPv6 미할당으로 실노출 없음.
```
</details>


---

## 미검증 low 16건 (착수 시 반증 먼저)

1. `server/routers/reference.py:33` — reference/all 계약검증 우회 — model_validate는 검증만 하고 응답 바디는 미검증 원시 dict를 직렬화
   - services/reference.py의 SELECT에 컬럼이 추가되거나 DB 타입이 드리프트하면(예: 문자열화된 숫자), '계약 위반은 500으로 막는다'는 H-1 가드(주석 28-31행)가 통과된 채 비계약 형태의 JSON이 클라이언트·1h 캐시·CDN까지 전파된다. 현재 스키마/시드 기준 실동작 오류는 없음(라이브 응답 정상 확인) — 가드가 명목상만 유효한 잠재 위험.
2. `server/routers/reference.py:25` — TTL 캐시 만료 시점 동시 재빌드(dogpile) — 미스 판정과 set 사이 await 구간에 in-flight 중복 억제 없음
   - reference TTL 3600s·trending TTL 60s 만료 경계마다 동시 요청 수만큼 중복 DB 부하 발생. 현 데이터 규모(~96사, 라이브 응답 606KB)에서는 체감 영향이 작으나, 트래픽 증가 시 만료 시점 스파이크로 나타나는 전형적 thundering-herd 함정. TCOMPARE_LOG가 무한 증가하는 finding과 결합되면 trending 집계 비용이 커져 악화된다.
3. `generator/pages/company.py:66` — 정성 복지(qual_yn=True)인데 qual_desc NULL이면 값 셀이 빈칸 렌더 — 빌드 검증 없음, 프로덕션에 실존
   - qual_yn=True+qual_desc NULL 또는 qual_yn=False+amt NULL인 복지 행은 값 칸이 완전히 비어 사용자에게 정보 없는 행으로 보인다(라이브 amorepacific 페이지에서 확인). 생성기·검증 게이트 어디에서도 이 퇴화 조합을 거르거나 폴백(note로 대체 등)하지 않아 데이터 입력 실수가 그대로 프로덕션 HTML로 통과한다.
4. `generator/build.py:48` — sitemap lastmod가 전 103 URL에 빌드일로 일괄 기록 — 미변경 페이지도 매 빌드 갱신
   - --incremental이 바이트 동일 페이지를 이전본 복사로 보존하더라도 sitemap은 전 URL의 lastmod를 오늘로 선언한다. 검색엔진이 매 빌드마다 전 페이지가 변경된 것으로 인식해 lastmod 신호 신뢰가 무의미해지고 재크롤 예산이 낭비된다(잘못된 결과라기보다 SEO 신호 훼손).
5. `generator/release.py:71` — _atomic_swap의 2회 rename 사이 out_dir 부재 창 — 스왑 순간 전 생성물(404.html 포함) 일시 404
   - 배포(release.sh → generator.build) 시점마다 밀리초 수준의 창 동안 유입 요청이 nginx 기본 404로 떨어진다. 빈도·지속시간이 극히 짧아 low이나, 코드 주석의 '원자적 스왑' 보장과 달리 단일 원자 연산이 아니다(심볼릭 링크 전환 방식이면 제거 가능).
6. `generator/release.py:92` — --incremental이 manifest sha만 신뢰하고 이전 파일 내용 미검증 복사 — 변조·손상 시 영속 전파 + html/.gz 불일치 가능
   - 증분 모드의 '변경 없음' 판정이 실파일이 아닌 manifest 기록에만 근거한다. dist가 수동 조작·디스크 손상 등으로 manifest와 어긋난 순간부터 증분 빌드는 이를 감지하지 못하고 stale/손상 콘텐츠를 계속 승격하며, 사전압축본과 원본이 갈라져 클라이언트별로 다른 응답을 줄 수 있다.
7. `web/assets/js/app.js:233` — '최근 비교' 저장/복원 기능 미배선 — 저장 진입점 호출 0회, '불러오기' 버튼 no-op
   - 리포트 하단에 '최근 비교' 섹션이 항상 '저장된 비교가 없습니다.'로 렌더되고(저장이 불가능하므로), localStorage에 레코드가 있어도(구버전 잔존 등) '불러오기' 클릭이 아무 동작도 하지 않는다 — UI에 노출된 기능이 전부 무반응.
8. `web/assets/js/calc.js:166` — 연봉 최소값 0 입력 시("0-5000") B 연봉이 무조건 {0,0,0}으로 붕괴
   - 현재 연봉을 '0~5000'처럼 하한 0으로 입력한 사용자의 이직처(B) 연봉·총보상·시급이 전부 0으로 계산돼 salary/benefits 축 판정이 A 쪽으로 왜곡된다.
9. `web/assets/js/inputs.js:114` — selectCompany 폴백 fetch에 최신성 가드 없음 — 연타 시 이전 선택의 늦은 응답이 최신 선택을 덮음
   - REF 번들이 부팅 스냅숏이라 이후 추가된 회사는 검색에만 잡혀 폴백 경로를 타는데, 이 조건+연타에서 사용자가 마지막에 고른 회사가 아닌 이전 회사로 비교가 진행된다. 발생 조건이 좁아 low.
10. `web/assets/js/app.js:125` — 해시 딥링크/새로고침(#report·#company)이 상태 없는 빈 뷰를 그대로 표시
   - 비교 완료 후 새로고침(흔한 행동)이나 #report/#company URL 공유 시 제목과 버튼('입력 수정'/'새 비교')만 있는 빈 페이지가 보인다. 크래시·무한로딩은 없고 버튼으로 복귀 가능해 low.
11. `infra/systemd/loupit-api.service:16` — ExecReload가 SIGHUP 전송 — uvicorn은 SIGHUP 미처리(기본동작=종료)라 `systemctl reload loupit-api`가 그레이스풀 리로드가 아닌 강제종료+약 5초 502 유발
   - 오퍼레이터가 무중단 의도로 `systemctl reload loupit-api`를 실행하면 uvicorn이 즉사하고 RestartSec=5 동안 nginx /api/v1/* 프록시가 502를 반환한다(인플라이트 요청 드랍 포함). reload 명령은 성공(0)으로 보고돼 다운타임이 은폐된다. 현행 배포 스크립트는 restart만 사용(release.sh:41)하므로 수동 reload 시에만 발현.
12. `infra/nginx/loupit-beta.conf:87` — 리포 loupit-beta.conf에 /dist.prev/ 차단 블록 부재(프로덕션 conf에는 있음) — beta.jobcho.wiki 배포 시 이전 빌드 사본 노출
   - 리포 소스대로 beta.jobcho.wiki를 배포하면 /dist.prev/company/*.html 등 stale 이전 빌드가 200으로 그대로 서빙된다. X-Robots-Tag noindex(loupit-beta-security.conf)로 색인은 차단되므로 SEO 영향은 없고, 구버전 콘텐츠가 스테이징에서 열람 가능해지는 수준 — 프로덕션 conf가 같은 이유로 명시 차단한 것과의 단순 비대칭.
13. `infra/deploy/firewall.sh:27` — firewall.sh: inet 테이블 policy drop에서 ICMPv6 미허용 — IPv6 활성 시 NDP까지 차단되어 IPv6 전면 불통
   - 호스트에 IPv6가 프로비저닝되면 Neighbor Discovery(ICMPv6)가 drop되어 IPv6 주소로는 80/443 규칙이 있어도 통신 자체가 성립하지 않는다. AAAA 레코드를 추가하는 순간 해당 경로 사용자에게 사이트가 죽은 것으로 보인다. 현재 v4 전용 운영이면 잠재 위험.
14. `db/seed/load.py:96` — load.py verify_counts가 사실상 무검증 — 시드 결과 하한 검증이 릴리스 [2/7] 경로에 없음
   - 벤핏 SQL 파일 손상·부분 유실 상태로 release.sh를 돌리면 [2/7]이 조용히 성공 처리되고, [3/7] 생성기가 축소된 데이터로 정적 페이지를 만들어 게이트의 다른 테스트가 우연히 잡아주지 않는 한 축소분이 그대로 배포될 수 있다. docstring의 '하한 assert(방어적 스모크)' 의도와 구현이 불일치.
15. `docs/RUNBOOK-jobcho-cutover.md:97` — RUNBOOK '빠른 부분 배포' 명령이 존재하지 않는 venv 경로(server/venv/bin/python)를 하드코딩 — 현 서버는 venv 미프로비저닝(시스템 python3 사용)이라 문서대로 하면 즉시 실패
   - 스키마·시드 무변경 시의 안전한 부분 배포 경로(정적 재생성만)가 문서대로는 'No such file or directory'로 즉사한다. 운영자가 이 실패 후 문서상 유일한 대안인 전체 release.sh(서빙 DB 접촉 경로)로 우회하도록 유도되는 2차 효과가 있다.
16. `docs/RUNBOOK-jobcho-cutover.md:12` — RUNBOOK 전제 작업 흐름(ssh ubuntu → git pull → release.sh)이 현 서버의 리포 소유권(root:root)과 충돌 — ubuntu 사용자로는 git pull이 'dubious ownership'으로, web/dist 쓰기(generator 스왑)가 권한 부족으로 실패
   - 문서의 사전 조건 단계(§0)와 배포 단계(§3)가 문서에 명시된 사용자(ubuntu)로는 현 서버에서 한 줄도 진행되지 않는다. 운영자는 결국 전부 root로 실행하게 되고(현 root 소유 상태가 그 결과), root 실행은 리포·산출물 소유권을 계속 root로 고착시켜 User=ubuntu로 도는 loupit-api와의 권한 비대칭을 영속화한다. 문서에 sudo 필요 여부 또는 소유권 정리(chown/safe.directory) 절차가 빠져 있다.

---

## 참고: 이번 세션에서 이미 수정·배포된 것 (재수정 불필요)

- nginx `/compare` 301 쿼리스트링 승계(`$is_args$args`) — 커밋 21c4648, prod 반영·검증 완료
- `/dist.prev/` 노출 차단 — 커밋 346e890, prod 반영
- smoke.sh SM-5 기본 슬러그 `samsung-elec` — 커밋 359a53e
- loupit-api.service 시스템 python3 전환 — 커밋 8475314, 설치·가동 중
- 컷오버 자체: jobcho.wiki 라이브, jobchoice(구 loupit.co) stop+disable, TLS 자동갱신 dry-run 성공
- loupit.co는 **의도적 방치 확정**(실고객 없었음 — 사용자 확인). 301 승계·인증서 정리 제안 금지.

감사 산출 원본(JSON): 세션 스크래치패드 `audit-final.json` (세션 종료 시 소실 — 이 문서가 정본).
