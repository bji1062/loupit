# 핸드오프 — 2026-07-27 세션 B (메일 레이트리밋 배선 + Resend 실메일 전환 + 적대검토 후속)

> **이 문서가 재개 정본이다.** `docs/HANDOFF-2026-07-27.md`(세션 A)는 §3 재개 절차가 **전부 완료**되어
> 이력 문서로 강등됐다. 세션 A 는 배경(Resend 선택 근거·DNS·`.env` 함정)을 볼 때만 참조하라.

> **다음 세션 첫 줄**: §2 의 **미결 결정 1건**(라이브 nginx 반영)을 사용자에게 물어라.
> 그것 말고는 막힌 것이 없다. 커밋·푸시는 전부 끝났고(`origin/main` = `e67b0f0`) 작업트리는 클린이다.

---

## 0. 한 눈에 보기

| 구분 | 상태 |
|---|---|
| Resend 도메인 인증 | ✅ 완료(실측: `MAIL FROM`/`RCPT TO` → `250 Accepted`) |
| 메일 실발송 | ✅ **켜짐** — beta 가 Resend SMTP 로 실제 발송 중 (`MAILER_MODE=smtp`) |
| nginx 메일 레이트리밋 | ✅ 라이브 적용됨(1차분) / ⏸ **2차 보정분은 리포에만 있고 라이브 미반영** |
| 커밋·푸시 | ✅ 8건 커밋, `origin/main` 동기화 완료 |
| 테스트 | ✅ 베이스 **381** · sc14 **3** · 생성기 **211** · 프론트 **423** · `SMOKE PASS` |
| 미결 결정 | ⏸ **1건** — §2 |

---

## 1. 이 세션에서 한 일

### 1-1. §3-A 메일 레이트리밋 배선 (라이브 적용 완료)

`SPEC/11` 이 `loupit_mail` 존을 13회 명세했으나 `/etc/nginx` 전체에 **0건**이었다. Resend 실전환
직전이라 이대로 켜면 단일 IP 로 분당 300통 — 무료 티어 20초 소진 + 도메인 평판 손상.

공격 경로(실측): `POST /members/login-code` 는 세션 불필요, `require_csrf` 는 공개 상수 헤더의
**존재만** 검사, 앱 쿨다운은 이메일 해시 스코프라 **주소를 바꾸면 안 걸린다** → IP 상한이 유일한 방어.

배선한 것:
- `conf.d/loupit-limits.conf` — `loupit_mail` 존 `3r/m`
- `loupit.conf`·`loupit-beta.conf` — 메일 2종에 `=` 전용 블록(`loupit_api` 이중 + `X-Loupit-Client` 재선언)
- `loupit-beta.conf` — `limit_req_status 429` + `^~ /api/v1/` 에 `loupit_api`
  ↳ **beta 에는 `limit_req` 가 하나도 없었다.** M9 가 켜져 실메일이 나가는 유일한 호스트가
    프로덕션보다 방어가 약했다.

**실측 검증**: 무헤더 `403` / prod 헤더포함 6연속 `404·404·404·429·429·429` / 21초 후 토큰 회복
`204` / 베타 페이지·자산 무회귀 / `SMOKE PASS`

> 🚨 **사고를 하나 막았다**: 라이브 `loupit-beta.conf` 에는 리포에 없는 M9 클린 URL 블록 5개
> (`/login`·`/mypage`·`/verify`·`/edit`·`/edits`)가 있었다. 통째 복사했으면 e2e 대상 로그인
> 페이지가 전부 404 였다. → 리포로 역이식 + 파일 상단에 "통째 복사 금지" 경고 추가.

### 1-2. §3-B Resend 실메일 전환 (완료)

**핸드오프 A 의 §3-B 2번이 부정확했다.** `loupit-beta-api` 는 `server/.env` 가 아니라
**`server/.env.beta`** 를 `EnvironmentFile` 로 읽는다. 다만 pydantic `env_file` 이 항상
`server/.env` 를 보고 `.env.beta` 에 `MAILER_MODE`·`SMTP_*` 가 없어 **결과적으로는 `server/.env`
값이 적용**된다(우선순위 실측: 프로세스 env > `.env` 파일).

`server/.env` 쪽을 바꾼 이유 — `.env.beta` 에만 넣으면 나중에 prod M9 를 켤 때 `MAILER_MODE` 가
`console` 로 남아 **인증코드가 평문으로 journald 에 쌓인다**(코드가 명시적으로 경계하는 실패 모드).
prod 는 `validate_mail_config` 가 `m9_enabled` 부재 시 즉시 반환하므로(`main.py:97-98`) 무영향.

절차: `.env` 수정 **전에** 발송 선검증(`MAILER_MODE=smtp python3 infra/deploy/verify_mail.py <주소>`,
6단계 전부 통과) → `.env` 전환 → bash `source`·python-dotenv **양쪽 파싱 검증**(§2-4 ① 함정,
`SMTP_PASS` 36자 유실 없음) → `loupit-beta-api` 재시작 → e2e.

**e2e 결과**: `204` / `ConsoleMailer` 누적 로그 **15건 → 15건(증가 0)** / `"인증 코드 메일 발송 실패"`
로그 **0건** / `TAUTH_CODE` 행 생성(해시 64자만, 평문 없음, 5분 TTL) = SMTP 경로 확정.

### 1-3. 적대검토(5렌즈 → 지적 29건) 후속 수정

라이브 프로덕션 엣지를 건드렸으므로 적대검토를 돌렸고, 확증된 것을 전부 수정했다.

| 결함 | 심각도 | 조치 | 커밋 |
|---|---|---|---|
| `deploy-beta.sh` 가 beta conf 를 **통째 cp** → M9 페이지 5종 404. `nginx -t` 통과라 롤백 가드가 못 잡음 | blocker | docroot 보존 + **배치 전 내용 검증** 2중 차단 | `3fb20a1` |
| `conf.d` 산출물(`limits`·`botdefense`)을 배치하는 스크립트가 **리포에 전무** → 신규 프로비저닝 시 nginx 기동 불가 | blocker | `provision.sh`·`deploy-beta.sh` 에 추가 | `3fb20a1` |
| **롤백이 조용히 무효화** — `limits` 만 되돌리면 `[emerg] zero size shared memory zone`, 그런데 `reload` 가 **exit 0** 반환하고 구설정 계속 서빙 | blocker | `rollback-nginx.sh` 신설(존 정합성 검사·의존 순서 강제·실반영 확인) | `963ae36` |
| **`+태그`/도트로 쿨다운 우회** → 한 수신함에 4,320통/일 | major | 계정키/배달주소키 분리(`_delivery_address`) | `0112320` |
| `login-code burst=2` 가 온보딩 입구 과조임(공유 NAT 8명 중 4명 429) | major | `burst=5`(= verify-code) | `c14add3` |
| beta `^~ /api/v1/` 에 **Layer A 게이트 부재** — SPEC 3.4.3 주장이 라우트가 실재하는 호스트에서 0 | major | 게이트 + `= /api/v1/health` 예외 신설 | `c14add3` |
| `login-test.html` 이 **GET 에 CSRF 헤더 미부착** → 위 게이트 반영 시 403 | blocker(선결) | 조건 제거 | `e67b0f0` |
| SPEC §3.4 가 beta 미포함 / 선배포 오서술 / SM-ID 충돌 / 공유버킷 미서술 | minor×4 | SPEC/11 정정 | `e4e06da`·`e67b0f0` |
| `AU-5` 설정 테스트가 배포 자격증명 누출로 RED | — | 환경 격리(`monkeypatch.delenv`) | `f48e19b` |

### 1-4. 커밋 목록 (전부 `origin/main` 푸시 완료)

```
e67b0f0 fix(SC14): beta Layer A 반영 선결 조건 — login-test.html 이 GET 에 CSRF 헤더를 안 붙였다
ac3fbbb docs(SP-DB): TAUTH_CODE.TARGET_HASH_VAL 컬럼 주석 정정 — 계정키가 아니라 수신함키
e4e06da docs(SPEC/11): SP-INFRA-3.4 정정 4건
0112320 fix(SC14): 메일 폭탄 우회 차단 — +태그·도트 변형이 쿨다운을 통째로 무력화했다 (GREEN)
963ae36 feat(infra): nginx 롤백 스크립트 — "조용히 무효화되는 롤백" 차단 (GREEN)
c14add3 fix(SC14): 메일 리밋 적대검토 확증 3건 — burst 역전·beta Layer A 부재·공유버킷 오서술
f48e19b fix(test): AU-5 설정 테스트 환경 격리 (GREEN)
3fb20a1 fix(infra): 배포 스크립트 blocker 2건 (GREEN)
4fd61a2 feat(SC14): 메일 발송 레이트리밋 nginx 배선 — prod·beta 라이브 적용 (GREEN)
```

---

## 2. ⏸ 미결 결정 — 라이브 nginx 반영 (다음 세션 첫 작업)

**리포가 라이브보다 앞서 있다.** 1차분(`4fd61a2`)은 라이브에 있고, 적대검토 보정분(`c14add3`)은
**리포에만** 있다.

```
conf.d/loupit-limits.conf   리포가 12줄 앞섬 (공유버킷 주석 — 동작 무관)
sites-available/loupit.conf 리포가 13줄 앞섬 (burst 2→5 + 주석)
sites-available/loupit-beta.conf 리포가 65줄 앞섬 (burst + Layer A + health 예외 + 주석)
```

### 반영할 동작 변경 3건

| # | 변경 | 근거(실측) |
|---|---|---|
| 1 | `login-code` burst **2 → 5** (prod·beta) | 공유 NAT 뒤 8명이 3초 간격 요청 → `204·204·204·429·429·429·429·204` = **8명 중 4명이 21초간 로그인 시작 불가**. `rate 3r/m` 불변이라 스팸 방어는 안 약해지고 순간 동시성만 완화 |
| 2 | beta `^~ /api/v1/` 에 **Layer A 게이트** | SPEC 3.4.3 이 "SC14 라우트 10종은 엣지에서 CSRF 충족"이라 주장하는데 그 게이트가 prod 에만 있었고, **10개 라우트는 M9 가 켜진 beta 에만 실재**(prod 404) |
| 3 | beta `= /api/v1/health` 예외 블록 | 2번의 부작용 방지 — 없으면 무헤더 헬스체크 403 |

**격리 검증 완료**: `nginx -t` 통과 / burst=5 → **6건 통과 후 429**(`1+burst`) / health 무헤더 **200**
/ `reference/all` 무헤더 **403**, 유헤더 **200**.

### 반영 절차 (승인 후)

```bash
cd /home/ubuntu/loupit
D=$(date +%Y%m%d-%H%M%S)

# 0) ⚠ 선결: beta 가 서빙하는 별도 체크아웃의 테스트 페이지를 먼저 동기화
#    (게이트를 먼저 넣으면 login-test.html 의 GET 3종이 403)
cp infra/beta-test/login-test.html /home/ubuntu/loupit-fe/infra/beta-test/login-test.html

# 1) 백업
sudo cp -a /etc/nginx/conf.d/loupit-limits.conf        /etc/nginx/conf.d/loupit-limits.conf.bak-$D
sudo cp -a /etc/nginx/sites-available/loupit.conf      /etc/nginx/sites-available/loupit.conf.bak-$D
sudo cp -a /etc/nginx/sites-available/loupit-beta.conf /etc/nginx/sites-available/loupit-beta.conf.bak-$D

# 2) prod·limits 는 리포와 베이스가 같으므로 그대로 복사
sudo install -o root -g root -m 644 infra/nginx/loupit-limits.conf /etc/nginx/conf.d/loupit-limits.conf
sudo install -o root -g root -m 644 infra/nginx/loupit.conf        /etc/nginx/sites-available/loupit.conf

# 3) ⚠ beta 는 docroot 가 다르다 — 통째 복사 금지. 변환 후 "제거 줄 0" 확인 필수
sed 's#/home/ubuntu/loupit/web#/home/ubuntu/loupit-fe/web#g;
     s#/home/ubuntu/loupit/infra/beta-test#/home/ubuntu/loupit-fe/infra/beta-test#g' \
    infra/nginx/loupit-beta.conf > /tmp/beta.new
diff /etc/nginx/sites-available/loupit-beta.conf /tmp/beta.new | grep -c '^<'   # ← 0 이어야 안전
sudo install -o root -g root -m 644 /tmp/beta.new /etc/nginx/sites-available/loupit-beta.conf

# 4) 검증 → 반영
sudo nginx -t && sudo systemctl reload nginx

# 5) 사후 확인
bash infra/deploy/smoke.sh                       # SMOKE PASS 여야 함
curl -s -o /dev/null -w '%{http_code}\n' https://beta.loupit.co/api/v1/health              # 200
curl -s -o /dev/null -w '%{http_code}\n' https://beta.loupit.co/api/v1/reference/all       # 403
curl -s -o /dev/null -w '%{http_code}\n' -H 'X-Loupit-Client: web' \
     https://beta.loupit.co/api/v1/reference/all                                            # 200
for p in /login /mypage /verify /edit /edits /login-test; do                                # 전부 200
  curl -s -o /dev/null -w "$p %{http_code}\n" https://beta.loupit.co$p; done
```

**롤백**: `bash infra/deploy/rollback-nginx.sh <접미사>` (먼저 `DRY_RUN=1` 로 확인).
파일 단위 수동 롤백은 하지 마라 — §5-② 참조.

---

## 3. 남은 작업 (우선순위 순)

### P1 — 메일 경로 마무리

1. **기업 게이트웨이 수신 확인** (미완). 개인 메일(gmail)은 도달 확인했으나 `@samsung.com` 같은
   **기업 게이트웨이가 실제 위험**이다. 사용자에게 회사 주소를 받아 `verify_mail.py <주소>` 로 확인.
   실패하면 DMARC 레코드 추가·전용 서브도메인 검토가 다음 수순.
2. **DMARC 레코드 부재**. 현재 SPF·DKIM 만 있다. `_dmarc.jobcho.wiki` TXT 미설정 — 기업
   게이트웨이 통과율에 직결된다. 가비아에서 `v=DMARC1; p=none; rua=...` 부터 시작 권장.
3. **배달주소 기준 일일 상한 미구현**(§1-3 의 정직한 상한). 쿨다운은 창당 1통을 허용하므로
   한 수신함 기준 `86400/60` = **1,440통/일**은 여전히 가능하다. 낮추려면 `mail_resend_cooldown_sec`
   상향 또는 별도 카운터가 필요(리텐션 퍼지가 `TAUTH_CODE` 행을 지워 행 카운트로는 못 셈).

### P2 — M9 공개 경로

4. **`m9-frontend` → `main` 병합 시점**(이전 세션부터 미결). 병합하면 beta docroot 드리프트가
   해소되고 `loupit-beta.conf` 의 "통째 복사 금지" 경고를 지울 수 있다.
   정정: "병합 = M9 공개"는 과대평가 — prod nginx 에 `location = /login` 류가 **없어** 클린 URL 은 404.
5. **prod M9 활성화**. `server/.env` 에 `M9_ENABLED=1` **한 줄**이면 즉시 실메일 경로가 열린다
   (nginx 리밋은 이미 선배포됨 — 그게 선배포한 이유). 단 그 전에 **정책 문안**(약관 T5·개인정보 P7,
   SP-POL)과 **pepper 3종 주입**(NFR30, `session_hash_pepper`·`comp_email_hmac_pepper`·
   `login_code_hmac_pepper` — 현재 전부 빈 문자열)이 선행돼야 한다. 체크리스트는
   `docs/HANDOFF-로그인기능-ML-A.md` §H-5.
   ⚠ pepper 를 `.env` 에 넣으면 `test_config.py::test_AU5_...` 가 또 터질 뻔했으나 `f48e19b` 에서
   구조적으로 격리했다 — 이제 안 터진다.

### P3 — 적대검토 잔여(미판정)

6. `verify-rest` 에이전트가 미판정 지적을 반증 중이었으나 세션 종료로 결론 미수령. 원본 29건은
   `/tmp/.../scratchpad/findings.json`(**세션 휘발성**). 아래는 그중 **아직 판정도 수정도 안 된** 것들:
   - 경로 정규화·인코딩(`//api/v1/...`, `%6Cogin-code`, 후행 슬래시)으로 `=` 블록 우회 가능 여부
   - `X-Forwarded-For` 로 `$binary_remote_addr` 키를 흐릴 수 있는지
   - nginx `429` 에 `Retry-After` 부재 + 프론트 복구 안내가 리밋된 엔드포인트를 다시 누르게 함
   - `error.log` 에 리밋 거부가 쌓이는데 logrotate size 상한 없음(디스크 고갈로 형태만 바뀜)
   - `conf.d/*.bak-*` 백업이 include 대상 디렉터리 안에 있음(현재는 이름 덕에 무해)
   - 라이브 리밋 동작을 재확인할 **회귀 수단 없음** — 검증이 일회성 curl 이었다.
     → `smoke.sh` 에 SM-22(메일 리밋) 추가가 자연스러운 해법. SPEC §11.2 에 이미 정의돼 있다.

### P4 — 이월 인프라 (세션 A 에서 넘어옴)

7. `/data/mysql-8.0.42-*.tar` **1.6G** 잔여물 — 지금 지워도 무해(루트 72% 사용 중)
8. `/data/data`(93M)·`/log`(2.1G) MySQL 이전 전 원본 — 며칠 정상 가동 확인 후 삭제하면 2.2G 회수
9. **MySQL root 비밀번호 없음** → `mysqldump --all-databases` 불가 → 일일 백업이 `mysql`·`JOB`·
   `jobchoice` DB 를 못 담는다
10. `mysql.service` `Restart=on-failure` **실동작 미검증**(테스트하려면 `systemctl kill -s SIGKILL mysql`, 5~10초 다운)
11. journald 평문 인증코드 **15건** — 사용자 판단으로 **그대로 두기로 결정**(7/23~25 베타 기록,
    5분 TTL 로 전부 만료, 운영자 본인 주소). SMTP 전환으로 신규 축적은 이미 중단됐다.

---

## 4. 현재 상태 스냅샷 (2026-07-27 세션 B 종료 시점)

```
서비스   mysql·nginx·loupit-api·loupit-beta-api·loupit-backup.timer  전부 active
디스크   /    49G 중 35G 사용(72%)      /db  98G 중 2.2G 사용(3%)
메모리   4G/11G · 스왑 0G/7G · 코어 2개(⚠ 워크플로 동시 상한이 min(16, 코어-2)라 사실상 1)
M9      prod /members/me → 404(OFF)    beta → 401(ON)
메일    MAILER_MODE=smtp — beta 가 Resend 로 실발송 중
브랜치   main = e67b0f0 (origin/main 동기화 완료) · m9-frontend = dc1b285(미병합)
백업    /etc/nginx/**.bak-20260727-130123 (1차 반영 직전 상태)
```

---

## 5. 이 세션에서 배운 함정 (반드시 읽어라)

**① `limit_req` 는 PREACCESS 페이즈다 — `return 204` 로 끝내는 location 에서는 평가되지 않는다.**
레이트리밋 테스트 하네스를 짤 때 반드시 더미 업스트림 + `proxy_pass` 를 써야 한다.
1차 측정에서 이걸로 **위양성**을 냈다("리밋이 안 걸린다" → 실제로는 하네스 결함).

**② nginx 롤백은 `reload` 의 exit 0 을 믿으면 안 된다.**
존 정의만 되돌리고 참조가 남으면 `[emerg] zero size shared memory zone` 인데, `systemctl reload`
는 **성공을 보고**하고 마스터는 **구설정으로 계속 서빙**한다. 운영자는 "롤백 완료"를 보지만 아무것도
안 바뀌었다. → `infra/deploy/rollback-nginx.sh` 를 써라(존 정합성 검사 + 워커 pid 교체 확인).

**③ `nginx -t` 는 `root` 디렉터리·파일의 존재를 검사하지 않는다.**
docroot 가 틀려도 문법은 완벽히 유효하다 → 배포 스크립트의 `nginx -t` 롤백 가드가 **못 잡는다**.
`deploy-beta.sh` 는 이제 배치 **전에** docroot 안에 실제 페이지가 있는지 직접 확인한다.

**④ 라이브 `loupit-beta.conf` 를 리포에서 통째 복사하지 마라.**
docroot 가 `/home/ubuntu/loupit-fe/web`(별도 체크아웃)이다. 반영 후 반드시
`diff <라이브> <변환본> | grep -c '^<'` = **0** 을 확인하라.

**⑤ `Settings(_env_file=None)` 은 `.env` 파일만 끊고 `os.environ` 은 못 끊는다.**
`conftest.py:36` 의 `load_dotenv` 가 배포 설정을 `os.environ` 에 붓기 때문에, "선언된 기본값"을
보려는 테스트가 **배포 상태에 따라 RED** 가 된다. 격리하려면 `monkeypatch.delenv` 로 필드명을
대문자로 뒤집어 지워라(`f48e19b` 참조).

**⑥ 이메일 정규화는 두 종류다 — 섞지 마라.**
계정 식별키(`_normalize_email`, `+태그` 보존)와 폭탄방지 배달주소키(`_delivery_address`, `+태그` 제거)는
목적이 다르다. 도트 제거는 **구글 계열 한정**이어야 한다 — 전역 적용하면 제3자가
`v.ictim@naver.com` 을 요청해 `victim@naver.com` 의 로그인을 막는 **반대 방향 사고**가 된다.

**⑦ `server/.env` 는 bash `source` 로도 읽힌다**(`release.sh:39`).
`SMTP_FROM` 은 반드시 따옴표로 감싸라 — 따옴표 없는 `<` 는 리다이렉션으로 파싱돼 그 뒤
`SMTP_PASS` 까지 통째로 유실된다. 주석(`#`)은 안전하다. `.env` 를 고쳤으면
`bash -c 'set -a; source server/.env; echo ${#SMTP_PASS}'` 로 **양쪽 파서를 다 확인**하라.

**⑧ 이 머신은 코어가 2개다.** Workflow 툴의 동시 상한이 `min(16, 코어수-2)` 라 **사실상 직렬**로
돈다(29건 반증이 기어갔다). 병렬이 필요하면 **Agent 툴로 팬아웃**하라 — 그 상한과 무관하고,
이 작업들은 CPU 가 아니라 API 대기라 2코어가 병목이 아니다(로드 0.17 관측).

---

## 6. 참조

- **세션 A 핸드오프**(배경·Resend 선택 근거·DNS·서버 인프라): `docs/HANDOFF-2026-07-27.md`
- **SC14 정본 핸드오프**: `docs/HANDOFF-로그인기능-ML-A.md` (§H = `M9_ENABLED` 스위치, §H-5 = 활성화 체크리스트)
- **관련 SPEC**: `docs/SPEC/11-인프라-배포.md` §3.4(레이트리밋·게이트·**§3.4.5 beta 계약 신설**)·
  §7(SMTP 키) · `docs/SPEC/13-참여-로그인.md` SP-AUTH-11(메일러)
- **도구**:
  ```
  python3 infra/deploy/verify_mail.py [수신주소]      # 메일 설정 6단계 검증(주소 주면 실발송)
  DRY_RUN=1 bash infra/deploy/rollback-nginx.sh <접미사>  # nginx 롤백(먼저 드라이런)
  bash infra/deploy/smoke.sh                          # 라이브 스모크(릴리스 게이트)
  ```
- **테스트 실행**:
  ```
  DB_NAME=loupit_test python3 -m pytest server/tests -q -m "not sc14"   # 381
  DB_NAME=loupit_test python3 -m pytest server/tests -q -m sc14         # 3
  python3 -m pytest generator/tests -q                                   # 211
  node --test 'web/**/*.test.js'                                         # 423
  ```
