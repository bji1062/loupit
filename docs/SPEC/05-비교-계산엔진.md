# 05. 비교 계산 엔진 (클라이언트 순수모듈) — SPEC

> **이력**: 브랜드 축(성장성·안정성)은 2026-07-20 제거됐다. 우선순위는 연봉·워라밸·복지 3종.
> 경위·파급범위: `docs/ANALYSIS-성장성-용어-2026-07-20.md` §6-1 · `docs/HANDOFF-2026-07-19.md` §K.


**목적**: FRD `05-비교엔진.md`(FR-30~FR-39)와 `06-리포트-표현.md`(FR-40·FR-42의 계산 인용)를 **구현이 추측 없이 따라 만들 수 있는 기술 계약**으로 확정한다. 비교 리포트(F3)의 계산 로직을 **UI와 완전히 독립된 순수 함수 모듈** `web/assets/js/calc.js`(ES module, 부수효과 0)로 명세한다. 입력 타입(슬롯 a/b 상태 구조)·함수 시그니처·수식·상수·출력 타입·테스트 케이스를 픽셀 단위까지 고정한다. 본 문서는 **SP-ENGINE** 대역을 소유한다(SP-ARCH-8: 문서 07 SP-CALC 슬롯 = 본 문서).

**참조 상위문서**: 브리프 `CONTEXT_BRIEF.md` §7(비교 알고리즘)·§4(데이터 모델)·§6(REF 번들), FRD `05-비교엔진.md`(FR-30~39·통합 파이프라인·레거시 대비표), FRD `06-리포트-표현.md`(FR-40 표현·계산 분리, FR-42 재계산 트리거), RESEARCH `benefit-scraping.md`(§4.2 불확실성 밴드·DEC-2), SPEC `01-개요와-아키텍처.md`(SP-ARCH-6 디렉토리 `calc.js`, SP-ARCH-10 INV-4·INV-5, T5), FRD `01-개요와-FR규칙.md`(§5 상태 모델·§5.1 프로파일러 제거). 알고리즘 원형: `/home/ubuntu/job_change/app.js`(`compare()` L989~, `calc()` L826, `getBenTotal` L818, `getSalRange/getOfferRange` L821-822, `getWSHours` L602, `getOTPay` L603-611, `sumBenUncertainty` L701, `benUncertainty` L699, `renderBenCatCompare` L838, `renderQualCompare` L934, 희생비용 L1252-1265).

**상위 추적**: SPEC → FRD 05·06 → USECASE 04(UC-30~36) → PRD F3 → 브리프 §7. 각 SP-ENGINE 항목은 구현하는 FR-3x/NFR/§를 인용한다.

**전역 전제(상속)**: 계산은 100% 클라이언트(§2-8, NFR16·NFR20·NFR26). 서버 전송·저장 0. 프로파일러 벡터(`pfResult`)는 엔진에서 **완전 제거**(§2-3, 01 §5.1, SP-ENGINE-16). 우선순위·희생은 사용자 직접 선택값(`curPri`/`curSacrifice`)만. 밴드 계수는 **DEC-2**(금액 신뢰도 `amt_source` 기준: 명시 ±5% / 추정 ±20% / 만료 +15% 가산, 출처 배지와 디커플링). 회사 등록 = 복지 실데이터 ~96개. 프리셋 = 직접 입력 모드 템플릿용.

**범위 경계**: 본 문서는 **엔진(계산 규칙)만** 소유한다. (1) 입력 수집·검증·초기값·프리셋 폴백 UI = SP-FE(06). (2) 리포트 렌더·배지 표시·XSS 이스케이프·"최근 비교" localStorage = SP-RPT(08). (3) 엔진은 **HTML을 반환하지 않는다** — 순수 데이터(숫자·판정 구조체)만 반환하고, 렌더는 SP-RPT가 소비한다(표현·계산 분리, FRD 06 공통규약). (4) 회사·복지 문자열은 엔진이 가공하지 않고 그대로 전달하며 이스케이프는 렌더 계층 책임(NFR21).

---

## 1. 모듈 계약 · 파일 배치

### [SP-ENGINE-1] 순수 모듈 계약 (`calc.js`)
비교 계산 전체를 단일 ES module `web/assets/js/calc.js`로 분리한다(SP-ARCH-6). 브라우저는 `<script type="module">`로, 테스트는 `node --test`로 **동일 파일을 import**한다.

**불변 계약 (테스트로 강제, INV-4):**
1. **부수효과 0**: `document`·`window`·`fetch`·`localStorage`·`XMLHttpRequest`·전역 변수 읽기/쓰기 금지. 모든 입력은 인자로 받고, 모든 출력은 반환값으로 낸다.
2. **결정성**: 동일 입력 → 동일 출력. 시간 의존(만료 판정)은 `now` 인자 주입으로 제거한다(기본값 `Date.now()`는 오케스트레이터에서만 바인딩, 순수 함수는 `now`를 명시 인자로 받음).
3. **불변 입력**: 인자 객체를 변형(mutate)하지 않는다. 새 객체를 반환한다.
4. **DOM/서버 미의존**: 참조 데이터(회사 메타 `matched` 등)는 인자로 주입한다(REF에서 UI가 추출해 전달).
5. **에러 대신 미산출**: 필수값 결측은 throw 하지 않고 `null`/`missing[]`로 표기한다(FR-30, UC-35 1a).

**파일 배치:**

| 파일 | 역할 | 소유 |
| --- | --- | --- |
| `web/assets/js/calc.js` | ★순수 계산 모듈(본 문서 전체). `export` 상수·함수 | SP-ENGINE |
| `web/assets/js/calc.test.js` | `node:test` 단위 테스트(SP-ENGINE-17) | SP-ENGINE·SP-TEST |
| `web/assets/js/package.json` | `{"type":"module","private":true}` — **node 테스트 전용** ESM 해석용(브라우저 무영향, 무빌드 유지) | SP-ENGINE |
| `web/assets/js/report.js` | 소비자: 반환 구조체 → DOM 렌더(본 문서 아님) | SP-RPT(08) |
| `web/assets/js/inputs.js` | 생산자: 폼 → 아래 `CompareState` 조립(본 문서 아님) | SP-FE(06) |

- **추적**: FR-30, NFR16·NFR20·NFR26, SP-ARCH-6·INV-4, T5.

---

## 2. 입력·출력 타입 (슬롯 a/b 상태 구조)

### [SP-ENGINE-2] 타입 정의 (JSDoc `@typedef`, `calc.js` 상단)
FRD 01 §5 상태 모델을 엔진 입력 계약으로 고정한다. 슬롯 `s ∈ {'a','b'}`(a=현직, b=이직처). 금액 단위는 **만원 정수**, 시간당 가치만 **원(₩)**.

```js
/** @typedef {{ min:number, max:number, mid:number }} Range  // 만원 정수, min≤max */

/** @typedef {'compensation'|'flexibility'|'work_env'|'time_off'|'health'
 *          |'family'|'growth'|'leisure'|'perks'} CtgrCode */

/** @typedef {{
 *   benefit_cd:   string,
 *   benefit_nm:   string,
 *   benefit_amt:  number|null,          // 만원. 정성이면 null
 *   benefit_ctgr_cd: CtgrCode,
 *   badge_cd:     'official'|'est',      // 출처 신뢰도(밴드에 미사용, DEC-2)
 *   amt_source:   'stated'|'estimated'|'none',  // ★밴드 기준(금액 신뢰도)
 *   checked:      boolean,               // 체크 안 하면 전 합산 제외
 *   qual_yn:      boolean,               // true=정성(금액 없음)
 *   expires_dtm:  string|null,           // ISO8601. 경과 시 밴드 +0.15
 *   qual_desc?:   string,                // 정성 설명(렌더 계층이 이스케이프)
 *   note_ctnt?:   string
 * }} BenefitItem */

/** @typedef {{
 *   ot:     'low'|'mid'|'high'|null,     // 야근 빈도 → 주간근무시간
 *   wage:   'inclusive'|'separate'|null, // 포괄/비포괄
 *   remote: 'none'|'partial'|'hybrid'|'free'|null,
 *   flex:   'none'|'stagger'|'flexible'|null
 * }} WorkStyle */

/** @typedef {{
 *   comp_id:    number|null,
 *   comp_nm:    string|null,
 *   comp_tp_cd: string|null,             // 기업유형 코드(직접입력 프리셋용)
 *   work_style_val: { unlimitedPTO?:boolean, remote?:boolean,
 *                     flex?:boolean, refreshLeave?:string }|null
 * }} SlotMeta */

/** @typedef {'salary'|'wlb'|'benefits'} PriKey */  // 엔진 정규 키(§2.1)

/** @typedef {{
 *   salStr:        string|null,          // 슬롯 a 연봉 "lo-hi"(만원). 예 "5000-7000"
 *   selectedRate:  number|null,          // 슬롯 b 상승률(%). b = a×(1+rate/100)
 *   benS:          { a:BenefitItem[], b:BenefitItem[] },
 *   wsState:       { a:WorkStyle, b:WorkStyle },
 *   com:           { a:number, b:number },  // 편도 통근(분) ≥0
 *   curPri:        PriKey,
 *   curSacrifice:  PriKey|null,           // ≠ curPri
 *   matched:       { a:SlotMeta|null, b:SlotMeta|null }
 * }} CompareState */
```

### 2.1 우선순위 키 정규화 (레거시·상태 모델 정합)
FRD 01 §5는 값 도메인을 한국어 라벨 `{연봉,워라밸,복지}`로 표기하나, **엔진은 안정 영어 키 `PriKey`만 소비**한다. UI(SP-FE)가 선택을 아래 표로 매핑해 `curPri`/`curSacrifice`에 넣는다(레거시 `app.js` 키와 일치).

| 한국어 라벨 | `PriKey` | 비고 |
| --- | --- | --- |
| 연봉 | `salary` | |
| 워라밸 | `wlb` | |
| 복지 | `benefits` | |

### 2.2 출력 타입 (엔진 반환 구조체 — 데이터만, HTML 없음)
```js
/** @typedef {{
 *   salRange:  Range,        // FR-30
 *   ben:       number,       // 체크된 전 복지 합(만원) FR-31
 *   net:       number,       // 체크된 비정성 복지 합(순복지) FR-31
 *   eff:       Range,        // 실효연봉 = salRange + net  FR-32
 *   wsHours:   number,       // 주간근무시간(0=미입력) FR-33
 *   otPay:     number,       // 야근수당(만원/년, ≥0) FR-34
 *   total:     number,       // eff.mid + otPay  FR-34/32
 *   hourly:    number|null,  // 원/시간(null=미산출) FR-35
 *   autonomy:  number,       // 자율성 점수 FR-36
 *   sumBand:   number,       // ± 만원(불확실성) FR-38
 *   totalRange:[number,number], // [total-sumBand, total+sumBand]
 *   commuteMin:number        // 편도 분
 * }} SlotResult */

/** @typedef {{
 *   ok:        boolean,      // false면 필수값 결측(missing 참조)
 *   missing:   string[],     // 예 ['salary']
 *   a:         SlotResult,
 *   b:         SlotResult,
 *   deltas:    { effMid:number, effMin:number, effMax:number, salMid:number,
 *                totalDiff:number, hourlyDiff:number|null, benDiff:number },
 *   catDelta:  Array<{ ctgr:CtgrCode, sumA:number, sumB:number, delta:number }>,
 *   qual:      { a:Array<{benefit_nm,benefit_ctgr_cd,qual_desc}>,
 *                b:Array<{benefit_nm,benefit_ctgr_cd,qual_desc}> },
 *   commute:   { a:number, b:number, annA:number, annB:number, winner:'a'|'b'|'tie' },
 *   vdCard:    VdCard,       // FR-39 (§13)
 *   sacrifice: SacrificeCost|null, // FR-39 (§14)
 *   rest:      Array<{ axis:PriKey, winner:'a'|'b'|'tie', value:object }>, // 나머지 요약
 *   warnings:  string[]      // 규칙 코드(문구 아님). 예 ['eff_shrink','both_inclusive']
 * }} Report */

/** @typedef {{ salRange:{a:Range,b:Range}, net:{a:number,b:number},
 *              eff:{a:Range,b:Range} }} LightSummary */  // calc() 경량 반환
```

- **추적**: FR-30·31·32, FRD 01 §5, §4(BenefitItem 필드), §6(REF).

---

## 3. 엔진 상수

### [SP-ENGINE-3] 고정 상수 (`export const`, `calc.js`)
FRD 05 §1.3 상수표를 코드 상수로 확정한다. 하위 어떤 함수도 임의값을 쓰지 않는다.

```js
export const OT_HRS          = { low:40, mid:45, high:54 };      // 주당 총 근무시간(h) L372
export const LEGAL_WEEK_HRS  = 40;                               // 법정 기준주(초과=연장근로)
export const REMOTE_SAVE     = { none:0, partial:72, hybrid:120, free:180 }; // 연간 통근절약(만원) L377
export const FLEX_BONUS      = 50;   // flex≠none 자율성 가산
export const PTO_BONUS       = 80;   // unlimitedPTO 자율성 가산
export const MONTHLY_STD_HRS = 209;  // 월 소정근로시간(통상시급 분모) L608
export const OT_MULT         = 1.5;  // 연장근로 가산율
export const WEEKS_PER_MONTH = 4.33; // 월 평균 주수 L610
export const WEEKS_PER_YEAR  = 52;   // 연간 근무시간 환산 L1106
export const WON_PER_MANWON  = 10000;// 만원→원
export const COMMUTE_ROUND_TRIP = 2; // 왕복 계수 L1158
export const COMMUTE_WORKDAYS   = 240;// 연 근무일(통근 연환산) L1158
export const WORKDAY_HRS     = 8;    // 근무일 환산(연시간→근무일)
export const BENEFIT_SAT_THRESHOLD = 1200; // 총보상 차 ≤이면 "복지 우선" 판단(만원) L1076

// ★DEC-2 밴드 계수 — 금액 신뢰도(amt_source) 기준, 출처 배지와 디커플링(RESEARCH §4.2)
export const BAND_BASE   = { stated:0.05, estimated:0.20, none:0 };
export const BAND_EXPIRE = 0.15;  // 만료 시 base에 가산(치환 아님)

export const BENEFIT_CATEGORIES = ['compensation','flexibility','work_env','time_off',
  'health','family','growth','leisure','perks']; // 9종 고정 순서(§4)
```

> **레거시와의 차이(반드시 준수)**: 레거시 `benUncertainty`(L699)는 **배지 상태**(official 0.05 / stale 0.15 / est 0.20)로 밴드를 정했다. 본 엔진은 **DEC-2로 교체**: `amt_source`(stated/estimated/none) 기준 + 만료 **가산**(치환 아님). 이로써 "출처는 official이나 금액은 estimated" 항목이 ±20%를 유지한다(INV-5).

- **추적**: FR-30~39 §1.3, RESEARCH §4.2·DEC-2, INV-5.

---

## 4. 연봉 Range (FR-30)

### [SP-ENGINE-4] `parseSalRange` · `deriveOfferRange`
슬롯 a는 `"lo-hi"` 문자열을 파싱, 슬롯 b는 a range × (1+rate/100). DOM 접근 없이 문자열·숫자만 받는다(레거시 `getSalRange` L821 / `getOfferRange` L822에서 DOM 제거).

```js
/** 슬롯 a 연봉 문자열 → Range. 미입력/파싱불가 → {0,0,0}. */
export function parseSalRange(salStr) {
  if (!salStr) return { min:0, max:0, mid:0 };
  const [lo, hi] = String(salStr).split('-').map(Number);
  if (!Number.isFinite(lo) || !Number.isFinite(hi)) return { min:0, max:0, mid:0 };
  return { min:lo, max:hi, mid:Math.round((lo+hi)/2) };
}

/** 슬롯 b 연봉 = 슬롯 a range × (1+rate/100). base 미입력·rate null → {0,0,0}. */
export function deriveOfferRange(baseRange, selectedRate) {
  if (!baseRange || !baseRange.min || selectedRate == null) return { min:0, max:0, mid:0 };
  const mult = 1 + selectedRate/100;
  return { min:Math.round(baseRange.min*mult),
           max:Math.round(baseRange.max*mult),
           mid:Math.round(baseRange.mid*mult) };
}
```

- **규칙**: `mid=round((min+max)/2)`. b는 세 값 각각에 배율 적용 후 반올림. `rate=0`(동결)은 유효(항등 배율). `min≤max` 가정(입력 검증은 SP-FE).
- **추적**: FR-30(연봉 range 규칙), §7.

---

## 5. 복지 합산 · 카테고리 · 정성 (FR-31)

### [SP-ENGINE-5] `benTotal` · `benByCat` · `benCatCompare` · `qualCompare`
체크된 항목만 대상. 정성(`qual_yn=true`)은 금액 합산·카테고리 델타에서 제외하고 별도 구성(레거시 `getBenTotal` L818, `renderBenCatCompare` L838, `renderQualCompare` L934의 **계산부만** 추출; HTML 생성부는 SP-RPT).

```js
/** 슬롯 복지 합산. ben=체크 전 합, net=체크된 비정성 합(순복지). */
export function benTotal(list) {
  let ben=0, net=0;
  for (const b of (list||[])) {
    if (!b.checked) continue;
    const amt = Number(b.benefit_amt) || 0;   // 정성(null)→0
    ben += amt;
    if (!b.qual_yn) net += amt;
  }
  return { ben, net };
}

/** 카테고리별 합(체크·비정성만). 9종 전부 키 존재(미보유=0). */
export function benByCat(list) {
  const acc = Object.fromEntries(BENEFIT_CATEGORIES.map(c => [c, 0]));
  for (const b of (list||[])) {
    if (!b.checked || b.qual_yn) continue;
    const c = BENEFIT_CATEGORIES.includes(b.benefit_ctgr_cd) ? b.benefit_ctgr_cd : 'perks';
    acc[c] += Number(b.benefit_amt) || 0;
  }
  return acc;
}

/** 9카테고리 a·b 합산 + 델타(sumB−sumA). 미상 카테고리는 perks 폴백(FR-D8). */
export function benCatCompare(listA, listB) {
  const A = benByCat(listA), B = benByCat(listB);
  return BENEFIT_CATEGORIES.map(ctgr => ({ ctgr, sumA:A[ctgr], sumB:B[ctgr],
                                           delta:B[ctgr]-A[ctgr] }));
}

/** 정성 항목(체크·qual_yn) a/b 목록. 금액 없이 설명 대비(원문은 렌더 계층이 이스케이프). */
export function qualCompare(listA, listB) {
  const pick = list => (list||[])
    .filter(b => b.checked && b.qual_yn)
    .map(b => ({ benefit_nm:b.benefit_nm, benefit_ctgr_cd:b.benefit_ctgr_cd,
                 qual_desc:b.qual_desc || '' }));
  return { a:pick(listA), b:pick(listB) };
}
```

- **비즈니스 규칙**: (1) 정성은 net·카테고리 금액에 **절대 미포함**(§7, UC-31 1a). (2) 미체크는 전 합산 제외. (3) 미상 카테고리 코드는 `perks`로 폴백(FR-40 검증규칙·FR-D8). (4) 단위 만원.
- **추적**: FR-31, UC-31·UC-32, §4(BENEFIT_CTGR_CD·QUAL_YN)·§7.

---

## 6. 실효연봉 (FR-32)

### [SP-ENGINE-6] `effSalary`
```js
/** 실효연봉 = 연봉 range + 순복지(net). 세 값에 동일 가산(L996-997). */
export function effSalary(salRange, net) {
  return { min:salRange.min+net, max:salRange.max+net, mid:salRange.mid+net };
}
```

- **경고 규칙(warnings에 코드로)**: `salDiffMid>0 && effDiffMid<salDiffMid && benA>benB` → `'eff_shrink'`("연봉이 올라도 실질 보상 차이가 줄어든다", L1083). 문구 생성은 SP-RPT.
- **비즈니스 규칙**: 순복지만 가산, 정성·미체크 미가산(FR-31). 연봉 미입력 슬롯은 `net`만 남아 eff.mid=net일 수 있으나, 상위(SP-ENGINE-15)에서 `missing:['salary']`로 지표를 미산출 처리한다.
- **추적**: FR-32, UC-31, §7.

---

## 7. 주간 근무시간 (FR-33)

### [SP-ENGINE-7] `getWSHours`
```js
/** 주간근무시간 = OT_HRS[ot]. ot 미선택(null) → 0(미입력). L602 */
export function getWSHours(ws) { return OT_HRS[ws && ws.ot] || 0; }
```

- **파생**: 연장근로시간 = `max(0, wsHours − LEGAL_WEEK_HRS)`(FR-34·35 재사용). 연간근무시간 = `wsHours × WEEKS_PER_YEAR`(FR-35).
- **규칙**: 매핑은 `OT_HRS` 고정값만. 0(미입력)이면 시간 기반 비교(시간당·"적게 일하기") 미산출.
- **추적**: FR-33, UC-31·UC-33, §7.

---

## 8. 야근수당 — 포괄/비포괄 분기 (FR-34)

### [SP-ENGINE-8] `getOTPay`
임금유형(`wage`)으로 분기. 포괄임금은 **결코 가산하지 않는다**(핵심). 레거시 `getOTPay`(L603-611)와 동치이되 DOM 대신 `salRange`를 인자로 받는다.

```js
/** 야근수당(만원/년, ≥0). 포괄임금 or ot='low' → 0. 비포괄+야근 → 시급×1.5 기반. */
export function getOTPay(ws, salRange) {
  if (!ws || ws.wage !== 'separate' || ws.ot === 'low' || ws.ot == null) return 0;
  const mid = salRange && salRange.mid;
  if (!mid) return 0;                                     // 연봉 미입력 → 0
  const hourlyBase = mid * WON_PER_MANWON / 12 / MONTHLY_STD_HRS; // 통상시급(원)
  const extraHrs   = OT_HRS[ws.ot] - LEGAL_WEEK_HRS;             // 주당 연장근로(h)
  return Math.round(extraHrs * hourlyBase * OT_MULT * WEEKS_PER_MONTH * 12 / WON_PER_MANWON);
}
```

**분기표(FR-34 확정):**

| 조건 | 반환 |
| --- | --- |
| `wage='inclusive'`(포괄) | **0** (야근수당 연봉 포함) |
| `wage=null` | **0** |
| `wage='separate'` & `ot∈{low,null}` | **0** (연장근로 없음/미입력) |
| `wage='separate'` & `ot∈{mid,high}` & `salRange.mid=0` | **0** (연봉 미입력) |
| `wage='separate'` & `ot∈{mid,high}` & `salRange.mid>0` | 위 수식 |

- **비즈니스 규칙**: 통상시급 분모 209h 고정, 가산율 1.5 고정, 음수 불가(연장근로 음수 시 0). 포괄+야근 슬롯은 상위에서 `'inclusive_ot'` 경고 코드 부여(시간당 하락, §9·L1116).
- **추적**: FR-34, UC-31, §7(getOTPay 포괄/비포괄).

---

## 9. 시간당 실질 가치 (FR-35)

### [SP-ENGINE-9] `hourlyValue`
```js
/** 시간당 가치(원). 분모(연간근무시간)=0이면 null(0나눗셈 방지). L1105-1110 */
export function hourlyValue(effMid, otPay, wsHours) {
  const annHrs = wsHours * WEEKS_PER_YEAR;
  if (annHrs <= 0) return null;                          // 근무시간 미입력 → 미산출
  return Math.round((effMid + otPay) * WON_PER_MANWON / annHrs);
}
```

- **승자·차이(상위 조립)**: `hvDiff=hourly(b)−hourly(a)`; 승자 = `>0→'b' / <0→'a' / =0→'tie'`; 차이 % = `round(|hvDiff| / min(hvA,hvB) × 100)`. **한쪽이라도 `null`이면 시간당 비교 미산출**(deltas.hourlyDiff=null).
- **비즈니스 규칙**: 연간근무시간 = `wsHours×52` 고정. 분모 0 → null(throw 금지). 총액 우위와 시간당 우위가 엇갈릴 수 있으며 vdCard가 대비(FR-39).
- **추적**: FR-35, UC-31, §7·NFR26.

---

## 10. 워라밸 · 자율성 (FR-36)

### [SP-ENGINE-10] `autonomyScore`
재택 절약 가치(만원) + 유연 가산 + 무제한휴가 가산(레거시 L1049-1050 동치).

```js
/** 자율성 점수 = REMOTE_SAVE[remote] + (flex≠none?50) + (unlimitedPTO?80). */
export function autonomyScore(ws, slotMeta) {
  const remote = (ws && REMOTE_SAVE[ws.remote]) || 0;
  const flex   = (ws && ws.flex && ws.flex !== 'none') ? FLEX_BONUS : 0;
  const pto    = (slotMeta && slotMeta.work_style_val && slotMeta.work_style_val.unlimitedPTO)
                 ? PTO_BONUS : 0;
  return remote + flex + pto;
}
```

- **입력 출처**: `remote`·`flex`는 `wsState[s]`, `unlimitedPTO`는 `matched[s].work_style_val`(회사 미선택 슬롯은 `matched[s]=null` → PTO 가산 0).
- **승자**: `autonomy(a)>autonomy(b)?'a':autonomy(b)>autonomy(a)?'b':'tie'`. 근거 요인(재택 라벨·유연 유형·자율휴가)은 SP-RPT가 값으로 구성.
- **규칙**: REMOTE_SAVE 고정, +50/+80 고정. 근무시간(FR-33 "적게 일하기")과 **별개 축**(FR-39 워라밸 p1/p2).
- **추적**: FR-36, UC-31·UC-33, §7·§4(WORK_STYLE_VAL).

---

## 11. 통근 (FR-37)

### [SP-ENGINE-11] `commuteCompare`
통근의 **금전 가치는 재택(REMOTE_SAVE, FR-36)으로만** 반영. 여기서는 **편도 시간 비교 + 연환산 시간**만 산출(레거시 L1158-1160).

```js
/** 편도 통근(분) a/b 비교 + 연환산 시간(round(min×2×240/60)). 더 짧은 쪽 winner. */
export function commuteCompare(comA, comB) {
  const ann = m => Math.round((m||0) * COMMUTE_ROUND_TRIP * COMMUTE_WORKDAYS / 60); // 연간 시간
  const a = comA||0, b = comB||0;
  return { a, b, annA:ann(a), annB:ann(b),
           winner: a===b ? 'tie' : a<b ? 'a' : 'b' };   // 짧을수록 유리
}
```

- **규칙**: 통근시간은 표시·워라밸 보조 지표. 총보상 금액 합산에 직접 미포함(통근 절감은 REMOTE_SAVE로만 금전화). 분 단위 비음수. 미입력(0)이면 비교 생략은 SP-RPT.
- **추적**: FR-37, UC-31, §7·FR-36.

---

## 12. 복지 불확실성 밴드 — DEC-2 (FR-38)

### [SP-ENGINE-12] `bandCoeff` · `sumBand`
밴드는 **금액 신뢰도(`amt_source`) 기준**이며 출처 배지(`badge_cd`)와 **무관**(DEC-2). 만료는 **가산(+0.15)**. 레거시 `benUncertainty`/`sumBenUncertainty`(L699-701)를 DEC-2로 교체.

```js
/** 항목 밴드 계수. 정성(none)→0. 만료면 base+0.15. now 주입(결정성). */
export function bandCoeff(item, now) {
  if (!item) return 0;
  const base = BAND_BASE[item.amt_source];
  if (base == null) return item.amt_source === 'none' ? 0 : BAND_BASE.estimated; // 미상→estimated
  if (item.amt_source === 'none') return 0;
  const expired = item.expires_dtm != null && Date.parse(item.expires_dtm) < now;
  return base + (expired ? BAND_EXPIRE : 0);
}

/** 체크·비정성 항목의 ± 절대 합(만원). = Σ |amt| × bandCoeff. */
export function sumBand(list, now) {
  return (list||[])
    .filter(b => b.checked && !b.qual_yn)
    .reduce((acc, b) => acc + (Number(b.benefit_amt)||0) * bandCoeff(b, now), 0);
}
```

**계수 진리표(DEC-2, INV-5):**

| `amt_source` | 만료 아님 | 만료(EXPIRES 경과) |
| --- | :---: | :---: |
| `stated` (공식 명시) | 0.05 | 0.05+0.15 = **0.20** |
| `estimated` (앵커 추정) | 0.20 | 0.20+0.15 = **0.35** |
| `none` (정성) | 0 | 0 (밴드 미적용) |

- **총보상 범위**: `[total − sumBand, total + sumBand]`(SlotResult.totalRange).
- **DEC-2 핵심**: `badge_cd='official'`이어도 `amt_source='estimated'`면 ±20% 유지. **밴드는 `badge_cd`를 읽지 않는다**(디커플링). 만료는 치환이 아니라 가산.
- **규칙**: 항목별 산정 후 합산(단일 % 일괄 금지). 정성·미체크 제외. `now`는 상위가 `Date.now()` 주입(테스트는 고정값).
- **추적**: FR-38, UC-34, RESEARCH §4.2·DEC-2, INV-5, §4(amt_source·EXPIRES_DTM).

---

## 13. 우선순위 판정 vdCard (FR-39)

### [SP-ENGINE-13] `buildVdCard` — 3축 분기 (데이터 반환)
사용자 선택 `curPri` 축으로 두 관점(p1·p2)을 평가. **엔진은 승자·수치만 반환**하고 문구·색은 SP-RPT가 구성(FRD 06 표현·계산 분리). 레거시 vdCard(L1019-1077)의 salary/wlb/benefits 계산(FR-39). `pfResult` 가중 없음(§2-3).

```js
/** @typedef {{ label:string, winner:'a'|'b'|'tie', detail:object }} VdPersp */
/** @typedef {{ axis:PriKey, p1:VdPersp, p2:VdPersp, tie:boolean }} VdCard */

export function buildVdCard(axis, m) {
  // m = { totalA,totalB, hourlyA,hourlyB, wsHoursA,wsHoursB,
  //       autoA,autoB, benA,benB }
  switch (axis) {
    case 'salary':   return vdSalary(m);
    case 'wlb':      return vdWlb(m);
    case 'benefits': return vdBenefits(m);
    default:         return vdWlb(m);   // 안전 폴백(기본 워라밸)
  }
}
```

**축별 계약(FR-39 표 확정):**

| `axis` | p1 (관점1) | p2 (관점2) | 승자 규칙 | detail 필드 |
| --- | --- | --- | --- | --- |
| **salary** | 총액 `total` | 시간당 `hourly` | 각 관점 큰 값 유리(시간당은 양쪽 non-null일 때만) | p1:{totalA,totalB,diff}; p2:{hourlyA,hourlyB,diff,pct} 또는 winner='tie'·미산출 |
| **wlb** | 적게 일하기 `wsHours` 적은 쪽 | 시간자율성 `autonomy` 큰 쪽 | p1 근무시간↓, p2 자율성↑ (양쪽 wsHours>0일 때만 p1) | p1:{wsA,wsB,weekDiff,annDiff,annDays}; p2:{autoA,autoB} |
| **benefits** | 복지항목 `ben` 큰 쪽 | 총보상 `total` 큰 쪽 | 각 큰 값 유리 | p1:{benA,benB}; p2:{totalA,totalB,diff, satisfy:|diff|<1200} |

```js
function vdSalary(m) {
  const diff = m.totalB - m.totalA;
  const p1 = { label:'총액', winner: diff>0?'b':diff<0?'a':'tie',
               detail:{ totalA:m.totalA, totalB:m.totalB, diff } };
  let p2;
  if (m.hourlyA != null && m.hourlyB != null) {
    const hd = m.hourlyB - m.hourlyA;
    const pct = Math.round(Math.abs(hd) / Math.min(m.hourlyA, m.hourlyB) * 100);
    p2 = { label:'시간당 가치', winner: hd>0?'b':hd<0?'a':'tie',
           detail:{ hourlyA:m.hourlyA, hourlyB:m.hourlyB, diff:hd, pct } };
  } else {
    p2 = { label:'시간당 가치', winner:'tie', detail:{ missing:true } };
  }
  return { axis:'salary', p1, p2, tie: p1.winner==='tie' && p2.winner==='tie' };
}

function vdWlb(m) {
  const both = m.wsHoursA>0 && m.wsHoursB>0;
  const wd = Math.abs(m.wsHoursA - m.wsHoursB), annDiff = wd*WEEKS_PER_YEAR;
  const p1 = { label:'적게 일하기',
    winner: !both ? 'tie' : m.wsHoursA<m.wsHoursB?'a':m.wsHoursB<m.wsHoursA?'b':'tie',
    detail: both ? { wsA:m.wsHoursA, wsB:m.wsHoursB, weekDiff:wd,
                     annDiff, annDays:Math.round(annDiff/WORKDAY_HRS) }
                 : { missing:true } };
  const p2 = { label:'시간 자율성',
    winner: m.autoA>m.autoB?'a':m.autoB>m.autoA?'b':'tie',
    detail:{ autoA:m.autoA, autoB:m.autoB } };
  return { axis:'wlb', p1, p2, tie: p1.winner==='tie' && p2.winner==='tie' };
}

function vdBenefits(m) {
  const bd = m.benB - m.benA, td = m.totalB - m.totalA;
  const p1 = { label:'복지 항목', winner: m.benA>m.benB?'a':m.benB>m.benA?'b':'tie',
               detail:{ benA:m.benA, benB:m.benB } };
  const p2 = { label:'총보상 포함', winner: td>0?'b':td<0?'a':'tie',
               detail:{ totalA:m.totalA, totalB:m.totalB, diff:td,
                        satisfy: Math.abs(td) < BENEFIT_SAT_THRESHOLD } };
  return { axis:'benefits', p1, p2, tie:false };
}
```

### 13.4 ~~[SP-ENGINE-13b] `getCompanyType` · `brandProjection`~~
→ **폐기(2026-07-20)**: 브랜드 축 제거. `getCompanyType`·`brandProjection`·`PROJECTION_YEARS`·
`GROWTH_RATE_FALLBACK`은 `calc.js`에서 삭제됐고, 엔진은 `companyTypes`를 더 이상 주입받지 않는다.
`reference/all`의 `company_types` 원소는 `comp_tp_id`/`comp_tp_cd`/`comp_tp_nm` 3필드뿐이다.
경위: `docs/ANALYSIS-성장성-용어-2026-07-20.md` §6-1.

- **규칙**: (1) 판정 입력 = `curPri` + 지표값뿐, **프로파일러 벡터 없음**(§2-3, INV). (2) 근소 차이 표기(`tie`/차이%)로 과잉 단정 회피(UC-33 3a) — 문구는 SP-RPT. (3) 우선순위 미선택 처리(UC-33 1a)는 SP-FE 게이트(엔진은 유효 `curPri` 전제).
- **추적**: FR-39, UC-33, §2-3·§6(REF).

---

## 14. 희생요소 포기비용 (FR-39)

### [SP-ENGINE-14] `sacrificeCost`
`curSacrifice` 선택 시 포기 비용 정량화(레거시 L1252-1265). 데이터만 반환.

```js
/** @typedef {{ axis:PriKey, ok:boolean, detail:object }} SacrificeCost */

export function sacrificeCost(sacrifice, m) {
  // m 재사용(§13) + benA/benB
  switch (sacrifice) {
    case 'salary': {
      const td = Math.abs(m.totalB - m.totalA);
      return { axis:'salary', ok:true, detail:{
        annual:td, monthly:Math.round(td/12),
        daily:Math.round(td*WON_PER_MANWON/365),   // 원
        better: m.totalB>m.totalA?'b':'a' } };
    }
    case 'wlb': {
      if (!(m.wsHoursA>0 && m.wsHoursB>0))
        return { axis:'wlb', ok:false, detail:{ missing:true } }; // 야근 미입력
      const wd = Math.abs(m.wsHoursA - m.wsHoursB), ann = wd*WEEKS_PER_YEAR;
      return { axis:'wlb', ok:true, detail:{
        weekDiff:wd, annHours:ann, annDays:Math.round(ann/WORKDAY_HRS),
        more: m.wsHoursA>m.wsHoursB?'a':'b' } };
    }
    case 'benefits': {
      const d = Math.abs(m.benA - m.benB);
      return { axis:'benefits', ok:true, detail:{ diff:d, better: m.benA>m.benB?'a':'b' } };
    }
    default: return null;
  }
}
```

- **규칙**: `curSacrifice≠curPri`(SP-FE 보장). wlb는 양쪽 근무시간 입력 시만 `ok:true`, 아니면 안내(`missing`).
- **추적**: FR-39(희생 포기비용), UC-33, §7.

---

## 15. 오케스트레이터 — `compare` · `calc` (FR-30)

### [SP-ENGINE-15] `compare(state, now)` · `calc(state)`
FR-30 진입점. 하위 순수 함수를 조립해 전체 리포트/경량 요약을 반환. **서버 호출·저장·throw 없음**(NFR16·NFR20·NFR25). `now`는 만료 판정용 주입(기본 `Date.now()`).

```js
/** 전체 리포트(FR-30). 필수값(연봉) 결측이면 ok:false + missing. */
export function compare(state, now = Date.now()) {
  const salA = parseSalRange(state.salStr);
  const salB = deriveOfferRange(salA, state.selectedRate);
  const missing = [];
  if (!salA.min && !salA.max) missing.push('salary');   // 연봉 필수(UC-30 2a)

  const R = {};   // 슬롯 원시 결과
  for (const s of ['a','b']) {
    const sal = s==='a' ? salA : salB;
    const ws  = state.wsState[s];
    const { ben, net } = benTotal(state.benS[s]);
    const eff = effSalary(sal, net);
    const wsHours = getWSHours(ws);
    const otPay   = getOTPay(ws, sal);
    const total   = eff.mid + otPay;
    const hourly  = hourlyValue(eff.mid, otPay, wsHours);
    const auto    = autonomyScore(ws, state.matched[s]);
    const band    = sumBand(state.benS[s], now);
    R[s] = { salRange:sal, ben, net, eff, wsHours, otPay, total, hourly,
             autonomy:auto, sumBand:band,
             totalRange:[total-band, total+band], commuteMin:state.com[s]||0 };
  }

  const m = { totalA:R.a.total, totalB:R.b.total, hourlyA:R.a.hourly, hourlyB:R.b.hourly,
              wsHoursA:R.a.wsHours, wsHoursB:R.b.wsHours, autoA:R.a.autonomy, autoB:R.b.autonomy,
              benA:R.a.ben, benB:R.b.ben };

  const warnings = [];
  const effDiffMid = R.b.eff.mid - R.a.eff.mid, salDiffMid = salB.mid - salA.mid;
  if (salDiffMid>0 && effDiffMid<salDiffMid && R.a.ben>R.b.ben) warnings.push('eff_shrink');
  const incA = R.a.otPay===0 && state.wsState.a.wage==='inclusive' && state.wsState.a.ot!=='low';
  const incB = R.b.otPay===0 && state.wsState.b.wage==='inclusive' && state.wsState.b.ot!=='low';
  if (incA && incB) warnings.push('both_inclusive');
  else if (incA) warnings.push('inclusive_a');
  else if (incB) warnings.push('inclusive_b');

  const hourlyDiff = (R.a.hourly!=null && R.b.hourly!=null) ? R.b.hourly-R.a.hourly : null;

  return {
    ok: missing.length===0, missing,
    a:R.a, b:R.b,
    deltas: { effMid:effDiffMid, effMin:R.b.eff.min-R.a.eff.min, effMax:R.b.eff.max-R.a.eff.max,
              salMid:salDiffMid, totalDiff:R.b.total-R.a.total, hourlyDiff,
              benDiff:R.b.ben-R.a.ben },
    catDelta: benCatCompare(state.benS.a, state.benS.b),
    qual:     qualCompare(state.benS.a, state.benS.b),
    commute:  commuteCompare(state.com.a, state.com.b),
    vdCard:   buildVdCard(state.curPri, m),
    sacrifice:state.curSacrifice ? sacrificeCost(state.curSacrifice, m) : null,
    rest:     restSummary(state.curPri, state.curSacrifice, m, R),
    warnings
  };
}

/** 경량 실시간 요약(FR-30). 편집 중 salRange/net/eff만. vdCard·밴드 미산출. */
export function calc(state) {
  const salA = parseSalRange(state.salStr);
  const salB = deriveOfferRange(salA, state.selectedRate);
  const nA = benTotal(state.benS.a).net, nB = benTotal(state.benS.b).net;
  return { salRange:{a:salA,b:salB}, net:{a:nA,b:nB},
           eff:{ a:effSalary(salA,nA), b:effSalary(salB,nB) } };
}

/** 나머지 기준 요약(curPri·curSacrifice 아닌 축). L1268-1281. */
export function restSummary(pri, sac, m, R) {
  const keys = ['salary','wlb','benefits'].filter(k => k!==pri && k!==sac);
  return keys.map(axis => {
    if (axis==='salary') { const d=m.totalB-m.totalA;
      return { axis, winner:d>0?'b':d<0?'a':'tie', value:{ diff:d } }; }
    if (axis==='wlb') { const both=m.wsHoursA>0&&m.wsHoursB>0;
      return { axis, winner: both ? (m.wsHoursA<m.wsHoursB?'a':m.wsHoursB<m.wsHoursA?'b':'tie') : 'tie',
               value: both ? { wsA:m.wsHoursA, wsB:m.wsHoursB }
                           : { commuteA:R.a.commuteMin, commuteB:R.b.commuteMin, missing:true } }; }
    // benefits(남는 유일한 축)
    const d = m.benB-m.benA;
    return { axis, winner:d>0?'b':d<0?'a':'tie', value:{ diff:d } };
  });
}
```

**재계산(FR-42/UC-35)**: SP-RPT/SP-FE가 입력 변경 시 `compare(state)`를 재호출한다. `curPri`만 바뀌면 지표값 동일 → 재호출해도 `a/b/deltas`는 같고 `vdCard`만 전환된다(성능상 SP-RPT가 vdCard만 교체 가능하나 엔진은 항상 전체 반환; INP≤200ms는 순수·경량으로 충족).

- **비즈니스 규칙**: (1) 입력·중간값·결과 서버 전송 0(NFR16·NFR20). (2) 필수값 결측은 `missing`으로 부분 미산출, 리포트 전체 무효화·throw 금지(FR-40 2a). (3) `pfResult` 미참조(§2-3). (4) 반환은 순수 데이터(HTML 없음).
- **추적**: FR-30·FR-42, UC-30·UC-35, NFR2·16·20·26·27, §7·§2-8.

---

## 16. 프로파일러 벡터 제거 (명시)

### [SP-ENGINE-16] 레거시 프로파일러 완전 제거 (계약)
`calc.js`는 프로파일러 계열 상태·가중을 **어떤 함수에서도 읽거나 쓰지 않는다**(§2-3, 01 §5.1).

| 레거시(app.js) | 처리 | 근거 |
| --- | --- | --- |
| `pfResult`·`pfJob`·`pfAnswers`·`pfCur`·`pfShuffled` | 엔진에 **부재**(참조·주입 금지) | 01 §5.1 |
| L1016 프로파일러 콜아웃(`pfResult` 가중 리포트) | **제거** | §2-3 |
| L1283 가치관 테스트 CTA | **제거**(프로파일러 없음) | §2-3 |
| 판정(vdCard) 입력 | `curPri`/`curSacrifice`(사용자 직접) + 지표값 **뿐** | FR-39 |
| `computeDataTrust`·`reportBen`·서버 저장(`saveDraft`/`saveComparison`)·`openAuth` 게이트 | **제거**(런타임 쓰기·로그인 없음) | §2-1·§2-4, NFR20 |

**검증(테스트 T-ENGINE-19)**: `calc.js` 소스에 `pfResult`·`profiler`·`fetch`·`localStorage`·`document`·`openAuth` 토큰이 **0회** 등장. `state`에 프로파일러 키를 주입해도 결과 불변.

- **추적**: §2-3, 01 §5.1, FR-30·FR-39, INV-4, T5.

---

## 17. 테스트 명세 (TDD)

### [SP-ENGINE-17] 단위 테스트 — `web/assets/js/calc.test.js` (`node --test`)
브리프 §10. 순수 함수이므로 러너·모킹 없이 `import`→assert. 도구: `node:test` + `node:assert/strict`. 실행: `node --test web/assets/js/`. 각 케이스는 대표 입력→기대 출력을 고정한다(경계·0나눗셈·밴드 포함).

**17.1 대표 케이스 표 (입력→기대):**

| # | 대상 함수 | 입력 | 기대 출력 |
| --- | --- | --- | --- |
| T-ENGINE-1 | `parseSalRange` | `"5000-7000"` | `{min:5000,max:7000,mid:6000}` |
| T-ENGINE-2 | `parseSalRange` | `""` / `null` / `"abc"` | `{0,0,0}` (0나눗셈·NaN 방어) |
| T-ENGINE-3 | `deriveOfferRange` | `({5000,7000,6000}, 10)` | `{min:5500,max:7700,mid:6600}` |
| T-ENGINE-4 | `deriveOfferRange` | `(base, 0)` (동결) | base와 동일 값 |
| T-ENGINE-5 | `deriveOfferRange` | `({0,0,0}, 10)` / `(base, null)` | `{0,0,0}` |
| T-ENGINE-6 | `benTotal` | 체크3(정성1 amt=null) + 미체크1 | `ben`=체크 합, `net`=비정성 체크 합(정성·미체크 제외) |
| T-ENGINE-7 | `benCatCompare` | a: perks 200, b: perks 300 | perks 행 `{sumA:200,sumB:300,delta:100}`, 9행 반환 |
| T-ENGINE-8 | `benCatCompare` | 미상 ctgr 코드 `'xxx'` amt 50 | `perks`에 50 폴백 합산 |
| T-ENGINE-9 | `qualCompare` | 정성 항목 a1/b0 | `{a:[{benefit_nm,...}], b:[]}` |
| T-ENGINE-10 | `effSalary` | `({5000,7000,6000}, 600)` | `{5600,7600,6600}` |
| T-ENGINE-11 | `getWSHours` | `{ot:'low'}`/`{ot:'mid'}`/`{ot:'high'}`/`{ot:null}` | `40`/`45`/`54`/`0` |
| T-ENGINE-12 | `getOTPay` | `({wage:'inclusive',ot:'high'}, {mid:6000})` | `0` (포괄 미가산) |
| T-ENGINE-13 | `getOTPay` | `({wage:'separate',ot:'low'}, {mid:6000})` | `0` (연장근로 없음) |
| T-ENGINE-14 | `getOTPay` | `({wage:'separate',ot:'mid'}, {mid:6000})` | `932` (수식: 5h×(6000만÷12÷209)×1.5×4.33×12÷1e4) |
| T-ENGINE-15 | `getOTPay` | `({wage:'separate',ot:'high'}, {mid:0})` | `0` (연봉 미입력) |
| T-ENGINE-16 | `hourlyValue` | `(6600, 0, 45)` | `28205` (round(6600×1e4÷2340)) |
| T-ENGINE-17 | `hourlyValue` | `(6600, 0, 0)` | `null` (★0나눗셈 방지) |
| T-ENGINE-18 | `autonomyScore` | `({remote:'hybrid',flex:'flexible'}, {work_style_val:{unlimitedPTO:true}})` | `120+50+80 = 250` |
| T-ENGINE-19 | `autonomyScore` | `({remote:'none',flex:'none'}, null)` | `0` (미선택·회사없음) |
| T-ENGINE-20 | `commuteCompare` | `(30, 60)` | `{a:30,b:60,annA:240,annB:480,winner:'a'}` |
| T-ENGINE-21 | `bandCoeff` | `{amt_source:'stated',expires_dtm:null}` | `0.05` |
| T-ENGINE-22 | `bandCoeff` | `{amt_source:'estimated',expires_dtm:null}` | `0.20` |
| T-ENGINE-23 | `bandCoeff` | `{amt_source:'none'}` | `0` (정성) |
| T-ENGINE-24 | `bandCoeff` | `{amt_source:'stated',expires_dtm:'2020-01-01'}`, `now=Date.now()` | `0.20` (0.05+0.15 만료 가산) |
| T-ENGINE-25 | `bandCoeff` | `{amt_source:'estimated',expires_dtm:'2020-01-01'}` (만료) | `0.35` (0.20+0.15) |
| **T-ENGINE-26** | `bandCoeff` (★DEC-2·INV-5) | `{badge_cd:'official', amt_source:'estimated'}` | `0.20` (배지 official이어도 추정 금액이면 ±20% — `badge_cd` 무시) |
| T-ENGINE-27 | `sumBand` | 체크 estimated 200 + 체크 stated 100 + 미체크 estimated 500 + 정성 | `200×0.2 + 100×0.05 = 45` |
| ~~T-ENGINE-28~~ | ~~`getCompanyType`~~ | — | **폐기(2026-07-20)**: 브랜드 축 제거로 함수 삭제 |
| ~~T-ENGINE-29~~ | ~~`brandProjection`~~ | — | **폐기(2026-07-20)**: 브랜드 축 제거로 함수 삭제 |
| ~~T-ENGINE-30~~ | ~~`brandProjection`(폴백)~~ | — | **폐기(2026-07-20)**: 브랜드 축 제거로 함수 삭제 |
| T-ENGINE-31 | `buildVdCard('salary', …)` | totalA=6000,totalB=7000,hourly 양쪽 | `p1.winner:'b'`, `p2` 시간당 승자·pct |
| T-ENGINE-32 | `buildVdCard('salary', …)` | hourlyA=null | `p2.winner:'tie'`, `detail.missing:true` |
| T-ENGINE-33 | `buildVdCard('wlb', …)` | wsA=40,wsB=45, autoA<autoB | `p1.winner:'a'`(적게), `p2.winner:'b'`(자율) |
| T-ENGINE-34 | `buildVdCard('wlb', …)` | wsA=0(미입력) | `p1.winner:'tie'`, `p1.detail.missing:true` |
| T-ENGINE-35 | `buildVdCard('benefits', …)` | benA>benB, |totalDiff|<1200 | `p1.winner:'a'`, `p2.detail.satisfy:true` |
| ~~T-ENGINE-36~~ | ~~`buildVdCard('brand', …)`~~ | — | **폐기(2026-07-20)**: 브랜드 축 제거로 `vdBrand` 삭제 |
| T-ENGINE-37 | `sacrificeCost('salary', …)` | totalA=6000,totalB=7200 | `{annual:1200,monthly:100,daily:round(1200×1e4/365),better:'b'}` |
| T-ENGINE-38 | `sacrificeCost('wlb', …)` | wsA=0 | `{ok:false, detail.missing:true}` |
| T-ENGINE-39 | `sacrificeCost('benefits', …)` | benA=800,benB=500 | `{diff:300,better:'a'}` |
| T-ENGINE-40 | `compare` (통합) | 정상 state | `ok:true`, `a/b` SlotResult, `deltas`, `vdCard`, `catDelta.length===9` |
| T-ENGINE-41 | `compare` (필수 결측) | `salStr:null` | `ok:false`, `missing:['salary']`, throw 없음 |
| T-ENGINE-42 | `compare` (warnings) | 양쪽 포괄+야근 | `warnings` 포함 `'both_inclusive'` |
| T-ENGINE-43 | `compare` (eff_shrink) | b연봉↑지만 a복지↑로 실질 축소 | `warnings` 포함 `'eff_shrink'` |
| T-ENGINE-44 | `calc` (경량) | 정상 state | `{salRange,net,eff}`만, `vdCard` 키 부재 |
| T-ENGINE-45 | 순수성(INV-4) | `import` 후 전역 미주입 호출 | `document`/`fetch`/`localStorage` 미접근, 인자 미변형(입력 객체 동결 후 호출 시 예외 없음) |
| T-ENGINE-46 | 프로파일러 제거(§16) | state에 `pfResult` 주입 | 결과 T-ENGINE-40과 동일(무영향), 소스에 `pfResult` 토큰 0 |
| T-ENGINE-47 | `totalRange` (밴드 적용) | total=6600, sumBand=45 | `[6555, 6645]` |
| T-ENGINE-48 | `deriveOfferRange` 반올림 경계 | `({5001,...}, 33)` | 각 값 `Math.round` 일치(부동소수 경계) |

**17.2 검증 전략:**
- **경계값**: 0 연봉, `ot:null`, 빈 복지 배열, `rate:0`(동결)·음수(감봉 허용 시), 회사 미선택(`matched=null`).
- **0 나눗셈**: `hourlyValue` wsHours=0 → `null`(T-17), `%` 계산 `min(hvA,hvB)` 분모 방어(양쪽 non-null 전제).
- **밴드(DEC-2)**: T-21~27, 특히 **T-26**(official 배지 × estimated 금액 = 0.20)이 INV-5 회귀 게이트. `now` 주입으로 만료 결정성.
- **미산출 정합**: 필수 결측 시 `missing` 채우고 나머지 지표는 산출(부분 미산출), `compare`가 throw하지 않음(T-41).
- **결정성**: 동일 `(state, now)` 반복 호출 → deep-equal(T-45 보강).

**17.3 릴리스 게이트**: T-ENGINE-1~48 중 **폐기분(28·29·30·36)을 제외한 44케이스** 전부 green이어야 SP-ARCH-9 4단계 통과. 특히 T-26(INV-5)·T-45(INV-4)·T-41(무크래시)은 필수 차단 케이스.

- **추적**: 브리프 §10, NFR2·NFR16·NFR26, INV-4·INV-5, T5, FR-30~39.

---

## 18. 추적성 매트릭스 (본 문서)

| SP-ENGINE | 구현 대상(함수) | 상위 FR | 충족 UC | 관련 NFR/INV | 브리프/근거 |
| :---: | --- | :---: | --- | --- | --- |
| SP-ENGINE-1 | 모듈 계약(calc.js) | FR-30 | UC-30 | NFR16·20·26, INV-4 | SP-ARCH-6·T5 |
| SP-ENGINE-2 | 입력·출력 타입 | FR-30·31·32 | UC-30 | — | 01 §5·§4·§6 |
| SP-ENGINE-3 | 상수 | FR-30~39 | — | INV-5 | §1.3·RESEARCH §4.2 |
| SP-ENGINE-4 | parseSalRange·deriveOfferRange | FR-30 | UC-30 | — | §7 |
| SP-ENGINE-5 | benTotal·benCatCompare·qualCompare | FR-31 | UC-31·32 | NFR21(렌더계층) | §4·§7 |
| SP-ENGINE-6 | effSalary | FR-32 | UC-31 | NFR26 | §7 |
| SP-ENGINE-7 | getWSHours | FR-33 | UC-31·33 | — | §7 |
| SP-ENGINE-8 | getOTPay(포괄/비포괄) | FR-34 | UC-31 | — | §7 |
| SP-ENGINE-9 | hourlyValue | FR-35 | UC-31 | NFR26 | §7 |
| SP-ENGINE-10 | autonomyScore | FR-36 | UC-31·33 | — | §7·§4 |
| SP-ENGINE-11 | commuteCompare | FR-37 | UC-31 | — | §7 |
| SP-ENGINE-12 | bandCoeff·sumBand(DEC-2) | FR-38 | UC-34 | INV-5 | RESEARCH §4.2·DEC-2 |
| SP-ENGINE-13 | buildVdCard(3축) | FR-39 | UC-33 | NFR20 | §2-3·§6 |
| SP-ENGINE-14 | sacrificeCost | FR-39 | UC-33 | — | §7 |
| SP-ENGINE-15 | compare·calc·restSummary | FR-30·42 | UC-30·35 | NFR2·16·20·26·27 | §7·§2-8 |
| SP-ENGINE-16 | 프로파일러 제거 | FR-30·39 | — | INV-4 | §2-3·01 §5.1 |
| SP-ENGINE-17 | 테스트 명세(44케이스, 28·29·30·36 폐기) | FR-30~39 | UC-30~35 | INV-4·5 | §10·T5 |

**커버리지**: FR-30(SP-ENGINE-1·2·4·15), FR-31(5), FR-32(6), FR-33(7), FR-34(8), FR-35(9), FR-36(10), FR-37(11), FR-38(12), FR-39(13·14), FR-42 재계산(15). 레거시 대비 제거(FRD 05 §4): 로그인 게이트·신고·서버 저장·프로파일러·`computeDataTrust` 전부 SP-ENGINE-16에서 배제, 밴드 계수 기준은 배지→`amt_source`로 교체(SP-ENGINE-3·12, DEC-2/INV-5).
</content>
</invoke>
