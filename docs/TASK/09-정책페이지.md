# TASK 09 — 정책·고지 페이지 (SP-POL)

> SPEC 계약: [SPEC/09-정책-페이지.md](../SPEC/09-정책-페이지.md) (SP-POL). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M4(광고·정책 기반) — 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md). 콘텐츠 소스·문안·상수·content 단위 테스트(PC-1·2·6·11·13)는 M4에서 완결. **생성물·푸터 PC 테스트(PC-3·4·5·7·8·9·10·12)는 렌더 파이프라인(SP-GEN)이 착지한 뒤 green** — TDD로 M4에 RED 작성, 그린은 SP-GEN(M5) 소비 시점.
> 선행 의존: **SP-ARCH**(디렉토리 경계 `generator/content/` 신설 허용 SP-ARCH-6·버전 pin SP-ARCH-7·라우트 확정 SP-ARCH-2 `= /privacy … = /ads`·불변식 INV-3·4·5·6 SP-ARCH-10·`generator/config.py` 존재). **소비처(하위, 순환 아님)**=SP-GEN(정책 4종 본문 문안 렌더·SEO·canonical·sitemap 엔트리·`_footer.html`·정정/초안 배너 마크업 — TASK 07 T-07.8.2가 본 도메인 `build_policy_docs`를 import·소비). 확정 순서는 00이 소유.
> 테스트: 러너=**pytest** 8.3.x(SP-ARCH-7) · 위치=`generator/tests/`(`test_policy_content.py`·`test_policy_pages.py`·`test_footer_links.py`) · 케이스 대역=PC-1~PC-13. content 단위 = `generator.content.policy` 직접 import, 생성물 = SP-GEN 픽스처(`generator/tests/fixtures.py` fake 번들 + `build_policy_docs`)로 4종 HTML 렌더·파싱, 푸터 = `web/index.html`·`web/compare/index.html` 수기 셸 파싱. 실행 `pytest generator/tests/test_policy_content.py generator/tests/test_policy_pages.py generator/tests/test_footer_links.py -v`. 릴리스 게이트(SP-ARCH-9 4단계·SP-GEN-11)에 PC-1~13 포함.

## 진행 요약
- 리프 총 18 / 완료 9 / 진행 9 / 미착수 0 (2026-07-11)
- Tier-0 게이트 리프: T-09.2.2 (PC-13 이스케이프/렌더 안전, NFR21 — 깨지면 배포 차단) — **green**(`generator/tests/test_policy_content.py`, Jinja2 autoescape 재현 렌더로 검증)
- 선결정 게이트: 없음 (SPEC 자족적 — 문안 초안·config 키·4종 라우트·밴드 계수 모두 SPEC 확정). **별도 승인 게이트(코드 게이트 아님, M8)**: 법률 자문 검토 완료 시 `legal_reviewed=True` 주입→초안 배너 해제(PC-11 `=True` 경로). 운영 승인으로만 전환, 구현 차단 아님.
- **콘텐츠 단위 완결(9리프, PC-1·2·6·11·13 green)**: `generator/content/policy.py`(`PolicySection`·`PolicyDoc`·`_S`·`build_policy_docs`·`POLICY_KEYS`·`REQUIRED_ITEMS`·`DRAFT_BANNER`·`POLICY_FOOTER_LINKS`·4종 문안) + `generator/config.py`(`GenConfig` 3키) 신설. `pytest generator/tests/ -q` → **21 passed**(RED 사전 확인: 문안 함수 스텁화 시 16 failed로 회귀 재현 후 원복 확인).
- **생성물 의존 9리프는 `- [-]`(진행중, 가짜 green 금지)**: T-09.1.3(policy_contact·last_modified의 PC-9·12 소비분)·T-09.7.1(PC-9)·T-09.8.1(PC-10)·T-09.9.1~9.9.6(PC-5·PC-7·PC-4·PC-3·PC-8·PC-12) — SP-GEN(M5) `templates/policy.html`·`_footer.html`·`web/dist` 렌더 및 SP-FE(M6) `web/index.html`·`web/compare/index.html` 수기 셸이 아직 존재하지 않아 렌더·파싱 대상이 없다. `test_policy_pages.py`·`test_footer_links.py`는 **의도적으로 미작성**(SP-POL-10.1 파일 배치는 SP-GEN 착지 시점에 함께 신설 — 지금 스텁만 두면 방치·부실화 위험). SP-GEN 착지 후 즉시 작성해 RED→GREEN 전환할 것.

## T-09.1 콘텐츠 소스 모듈 골격 — 데이터 구조·로더·설정 키  — (SP-POL-2.1·2.2, FR-80~84, NFR22·29, SP-ARCH-6·7)
- [v] T-09.1.1 PolicySection·PolicyDoc dataclass + _S 헬퍼 (구현+PC-1·PC-2 소비 green; `cross_route`의 PC-10 교차검증은 M5 연기)
  - 구현: `generator/content/policy.py` 신설(SP-ARCH-6 `generator/` 하위 파일 추가 허용, 마크다운 파서 등 신규 의존성 금지 SP-ARCH-7) — `@dataclass(frozen=True) class PolicySection`(`req_id`·`anchor`·`toc_label`·`heading`·`paragraphs: tuple[str,...]`·`cross_route: str|None=None`) + `@dataclass(frozen=True) class PolicyDoc`(`key`·`route`·`filename`·`title`·`meta_description`·`sections: tuple[PolicySection,...]`·`related: tuple[tuple[str,str],...]`·`show_correction=True`·`ads_none=True`·`draft=True`) + `_S(req_id, anchor, toc_label, heading, paragraphs, cross_route=None)->PolicySection` 생성 헬퍼.
  - 테스트: 필드 직접 케이스 없음 — 소비로 강제(`PolicyDoc`/`PolicySection` 필드→PC-1·PC-2, `cross_route`→PC-10). 모듈/픽스처 부재 시 import 수집 실패로 RED (pytest `test_policy_content.py`) — RED 먼저
  - refs: SP-POL-2.1 · FR-80~84 · SP-ARCH-6·7 · NFR29
- [v] T-09.1.2 build_policy_docs 로더 + POLICY_KEYS + REQUIRED_ITEMS (PC-1) — green
  - 구현: `generator/content/policy.py` `build_policy_docs(cfg)->list[PolicyDoc]` = `[_privacy(cfg), _terms(cfg), _disclaimer(cfg), _ads(cfg)]`(확정 순서) + `POLICY_KEYS=("privacy","terms","disclaimer","ads")`(순서·집합 정본) + `REQUIRED_ITEMS`(dict: `privacy→{P1..P6}`·`terms→{T1..T4}`·`disclaimer→{D-1..D-7}`·`ads→{A-1..A-5}`). `route`/`filename`은 SP-POL-1 표(`/privacy`→`privacy.html` … `/ads`→`ads.html`)와 1:1 매핑·SP-ARCH-2 Nginx location 정합.
  - 테스트: PC-1(정확히 4 doc·`key` 집합 == `{privacy,terms,disclaimer,ads}`·각 `route`/`filename` == SP-POL-1 표) (pytest `test_policy_content.py`) — RED 먼저
  - refs: SP-POL-2.2·SP-POL-1 · FR-80 · SP-ARCH-2
- [-] T-09.1.3 config 정책 키 3종 (policy_contact·policy_last_modified·legal_reviewed) — 구현 완료(`generator/config.py` 신설), `legal_reviewed`는 PC-11 green. `policy_contact`·`policy_last_modified`의 소비 검증(PC-9·PC-12)은 SP-GEN 렌더 착지 후(M5)
  - 구현: `generator/config.py` `GenConfig`에 3키 추가(SP-GEN 소유 파일·본 키는 SP-POL 요구): `policy_contact`(env `POLICY_CONTACT`, 기본 플레이스홀더 `"{운영자 정정·문의 연락처}"`)·`policy_last_modified`(env `POLICY_LAST_MODIFIED`, 기본 `"{게시 시 운영자 확정}"` `YYYY.MM.DD`)·`legal_reviewed`(env `POLICY_LEGAL_REVIEWED=="true"`, 기본 `False`). 실 DB 자격·PAT·AdSense id 미포함(NFR22); `policy_contact` 실값=운영자 env 주입·저장소 커밋 금지.
  - 테스트: 소비로 강제(`policy_contact`→PC-9·PC-12, `legal_reviewed`→PC-11) (pytest) — RED 먼저
  - refs: SP-POL-2.2 · FR-85 · NFR22

## T-09.2 초안 배너·이스케이프 렌더 안전  — (SP-POL-2.3·2.4, NFR21, INV-3, FRD10 공통)
- [v] T-09.2.1 DRAFT_BANNER 상수·draft 게이팅 계약 (PC-11) — green
  - 구현: `generator/content/policy.py` `DRAFT_BANNER` 상수(초안 고지 문안, 창작 없이 사실만: "본 문서는 초안입니다. 실제 게시 전 법률 자문 검토가 필요하며, 구체 문구·연락처·최종 수정일은 운영자가 확정합니다."). `PolicyDoc.draft`는 `cfg.legal_reviewed`가 False인 동안 4종 전부 True. 배너 마크업 `{% if doc.draft %}<aside class="policy-draft" role="note">⚠ {{ DRAFT_BANNER }}</aside>` = SP-GEN `policy.html`(위임). 확정 게시(`legal_reviewed=True`)→draft=False·미노출.
  - 테스트: PC-11(`legal_reviewed=False` 렌더→4종 전부 초안 배너"법률 자문 검토 필요" 노출; `=True`→미노출) (pytest `test_policy_content.py`) — RED 먼저. 렌더 경로는 SP-GEN policy.html 초안 블록 소비.
  - refs: SP-POL-2.3 · FR-80~84 · FRD10 공통(창작 금지·초안)
- [v] T-09.2.2 XSS 이스케이프·비-JS 렌더 안전 검증 (PC-13) (Tier0) — green(Jinja2 autoescape 재현 렌더; 실 `policy.html` 교체는 SP-GEN 착지 시 동일 계약 유지)
  - 구현: `generator/tests/test_policy_content.py` — `PolicySection.paragraphs`에 `"<script>alert(1)</script>"` 주입 변형 doc를 `policy.html`(SP-GEN `make_env` autoescape 경유, SP-GEN-4.1)로 렌더→출력에 실행 가능한 `<script>alert` 부재·`&lt;`로 이스케이프 표시. 구현측 담보=Jinja autoescape + `paragraphs` 원문 삽입(본문에 별도 `|safe` 없음, `|safe`는 조립 blob 전체 1회만). **Tier-0 NFR21 회귀 게이트**.
  - 테스트: PC-13(XSS 표본→실행 `<script>alert` 부재·본문 텍스트로 이스케이프) (pytest `test_policy_content.py`) — RED 먼저
  - refs: SP-POL-2.4·SP-POL-10 · **NFR21** · INV-3

## T-09.3 개인정보처리방침 문안 (_privacy, P1~P6)  — (SP-POL-3, FR-81, INV-4)
- [v] T-09.3.1 _privacy(cfg) — P1~P6 sections — PC-2 green(+ P1·P2 INV-4 정합 회귀 테스트). PC-10 교차검증은 M5
  - 구현: `generator/content/policy.py` `_privacy(cfg)->PolicyDoc` — key=`privacy`·route=`/privacy`·filename=`privacy.html`·title="개인정보처리방침"·`meta_description`(로그인·회원 없음·서버 미저장·광고 쿠키·개인화 동의 요지), sections P1(수집·저장 안 함)·P2(localStorage 한정)·P3(광고 쿠키 Google AdSense)·P4(제3자 처리·개인화 동의/거부, `cross_route="/ads"`)·P5(거부해도 이용 가능)·P6(재직자 식별정보 미수집), `related=(("이용약관","/terms"),("광고·제휴 고지","/ads"))`. 문안=초안, 브리프·상위문서 근거 사실만(창작 금지), 미확정 값 플레이스홀더.
  - 테스트: PC-2(`{s.req_id for s in sections}` ⊇ `{P1..P6}`·anchor 유일·`^[a-z0-9-]+$`) (pytest `test_policy_content.py`) — RED 먼저. P1·P2 실구현 정합(INV-4)·P3·P4 단일 진실은 PC-10 소비.
  - refs: SP-POL-3 · FR-81 · INV-4 · §2-1·§2-2 · RESEARCH §5-7

## T-09.4 이용약관 문안 (_terms, T1~T4)  — (SP-POL-4, FR-82, §1)
- [v] T-09.4.1 _terms(cfg) — T1~T4 sections — PC-2 green
  - 구현: `generator/content/policy.py` `_terms(cfg)->PolicyDoc` — key=`terms`·route=`/terms`·filename=`terms.html`·title="이용약관", sections T1(서비스 성격: 복지·연봉·근무조건 비교 정보 도구, 비-채용 플랫폼)·T2(로그인·계정 없음)·T3(참고용·의사결정 책임, `cross_route="/disclaimer"` — 면책 상세 중복 없이 링크 위임)·T4(일반 이용 조건, 운영자 확정·법률 검토), `related=(("개인정보처리방침","/privacy"),("데이터 정확성 면책조항","/disclaimer"))`.
  - 테스트: PC-2(`{s.req_id}` ⊇ `{T1..T4}`·anchor 규칙) (pytest `test_policy_content.py`) — RED 먼저
  - refs: SP-POL-4 · FR-82 · §1 · PRD03 §6 · D4.1

## T-09.5 데이터 정확성 면책조항 문안 (_disclaimer, D-1~D-7)  — (SP-POL-5, FR-83·DEC-2, INV-5)
- [v] T-09.5.1 _disclaimer(cfg) — D-1~D-7 sections — PC-2 green
  - 구현: `generator/content/policy.py` `_disclaimer(cfg)->PolicyDoc` — key=`disclaimer`·route=`/disclaimer`·filename=`disclaimer.html`·title="데이터 정확성 면책조항", sections D-1(참고용·실제와 다를 수 있음)·D-2(출처 신뢰도 배지 공식/추정)·D-3(출처 신뢰도 ≠ 금액 신뢰도)·D-4(불확실성 밴드; 문안 정본=T-09.5.2)·D-5(검증일·만료 재확인)·D-6(브라우저 계산·우열 무단정)·D-7(정정 요청 경로→SP-POL-7), `show_correction=True`, `related=(("개인정보처리방침","/privacy"),("이용약관","/terms"))`.
  - 테스트: PC-2(`{s.req_id}` ⊇ `{D-1..D-7}`·anchor 규칙) (pytest `test_policy_content.py`) — RED 먼저. D-2·D-3 디커플링 정합은 PC-6 인접.
  - refs: SP-POL-5 · FR-83 · FR-D9 · D4.1·§7
- [v] T-09.5.2 D-4 불확실성 밴드 문안 정합 (PC-6) — green(±5%·±20%·+15%·"금액 신뢰도" 존재, "출처 배지 기준" 부재, SP-CALC `BAND_BASE`/`BAND_EXPIRE`와 계수 일치 확인)
  - 구현: `_disclaimer` D-4 문단 문안 확정 — "밴드는 출처 배지가 아니라 **금액 신뢰도** 기준, **명시 ±5%·추정 ±20%·만료 +15% 가산**"으로 서술. SP-CALC `BAND={"stated":0.05,"estimated":0.20}`·만료 가산 `+0.15`(치환 아닌 가산)와 문자열·의미 일치. "출처 배지(`badge_cd`) 기준" 서술 금지·'배지 official + 금액 estimated' 과소 고지 금지(DEC-2·R3).
  - 테스트: PC-6(D-4 문안에 "±5%"·"±20%"·"+15%"·"금액 신뢰도" 존재·계수가 SP-CALC `BAND`(0.05·0.20)·만료 +0.15와 일치·"출처 배지 기준" 서술 부재) (pytest `test_policy_content.py`) — RED 먼저
  - refs: SP-POL-5 · FR-83·DEC-2 · **INV-5** · SP-CALC/FR-38·FR-D9(위임: 밴드 산출)

## T-09.6 광고·제휴 고지 문안 (_ads, A-1~A-5)  — (SP-POL-6, FR-84, NFR19)
- [v] T-09.6.1 _ads(cfg) — A-1~A-5 sections — PC-2 green
  - 구현: `generator/content/policy.py` `_ads(cfg)->PolicyDoc` — key=`ads`·route=`/ads`·filename=`ads.html`·title="광고·제휴 고지", sections A-1(수익 모델: AdSense+affiliate, 구독·결제 없음)·A-2("광고" 표기·미은폐)·A-3(제휴 수수료 관계)·A-4(제3자 광고 쿠키·개인화 동의, `cross_route="/privacy"`)·A-5(공정위 표시·광고 규정 정합), `related=(("개인정보처리방침","/privacy"),("데이터 정확성 면책조항","/disclaimer"))`.
  - 테스트: PC-2(`{s.req_id}` ⊇ `{A-1..A-5}`·anchor 규칙) (pytest `test_policy_content.py`) — RED 먼저. A-2("광고" 라벨)·A-4(단일 진실)는 PC-7·PC-10 소비.
  - refs: SP-POL-6 · FR-84 · §2-5 · NFR19 · MON11

## T-09.7 정정요청 임시 창구 (연락 경로·SLA·서버 무쓰기)  — (SP-POL-7, FR-85, INV-4, NFR16)
- [-] T-09.7.1 정정 경로 게시·SLA·서버 무쓰기 검증 (PC-9) — 계약 확정(`show_correction`·`policy_contact` 스킴 분기 규칙), 마크업 미존재(SP-GEN M5 위임)로 PC-9는 렌더 후 green 예정. `test_policy_pages.py` 미작성(사유: 위 진행 요약)
  - 구현: 정정·문의 연락 경로 계약(마크업=SP-GEN `policy.html` C2 위임): `{% if doc.show_correction %}` 섹션에서 `cfg.policy_contact` 스킴 분기(`mailto:`/`http://`/`https://`→`<a href rel="nofollow">`, `@` 포함→`mailto:` 링크, else 플레이스홀더 텍스트만) + SLA 문구("접수→검토→다음 빌드에 반영, 즉시 반영 아님·배포 주기 종속"). 스킴 화이트리스트=`mailto:`/`http://`/`https://`(그 외 링크화 금지, NFR21). 처리 흐름=서버 무쓰기·빌드타임 재생성(D4.2). 본 리프=계약+PC-9 검증(마크업 실구현 위임).
  - 테스트: PC-9(정정·문의 연락 경로 게시[면책조항 필수]+SLA 한계 문구 존재; `<form>`·`action=`·서버 mutation `fetch`/`XMLHttpRequest` 부재) (pytest `test_policy_pages.py`) — RED 먼저
  - refs: SP-POL-7.1·7.2 · FR-85 · **INV-4** · NFR16

## T-09.8 동의 배너 ↔ 정책 단일 진실 (정책 측 계약)  — (SP-POL-8, FR-87, MON13·14·15)
- [-] T-09.8.1 동의 단일 진실 검증 (PC-10) — 문안(P3·P4·P5·A-4) 확정 완료, 배너 링크 도달성 검증은 SP-GEN 렌더 후(M5). `test_policy_pages.py` 미작성(사유: 위 진행 요약)
  - 구현: 동의 배너↔정책 단일 진실 계약(검증 전용 — 문안 실구현은 T-09.3(P3·P4·P5)·T-09.6(A-4), 배너 표시·저장·링크 동작은 SP-ADS 위임): 배너 링크 대상 `/privacy`·`/ads` 존재·도달(SP-POL-1); `/privacy` P3·P4·P5 + `/ads` A-4가 배너 참조 사실(쿠키·제3자 Google·광고 목적·동의 선택권)과 **동일**. 동의 상태=브라우저 한정(P5·A-4 고지, INV-4). 중복 정의 불일치 금지.
  - 테스트: PC-10(배너 링크 대상[`/privacy`·`/ads`] 존재·도달·404 아님; `/privacy` P3·P4·P5 + `/ads` A-4 문안 존재·배너 참조 사실과 동일) (pytest `test_policy_pages.py`) — RED 먼저
  - refs: SP-POL-8 · FR-87 · INV-4 · MON13·14·15

## T-09.9 전역 푸터 링크·색인·무광고·생성물 검증  — (SP-POL-9·2.2, FR-80·86, NFR6·9·13·22·24·29, MON7-6)
- [-] T-09.9.1 POLICY_FOOTER_LINKS 정본 상수 — 구현 완료(`generator/content/policy.py`, 라우트 집합 정합 자체 검증 green). 생성 페이지·수기 셸 소비 검증(PC-5)은 M5·M6 착지 후
  - 구현: `generator/content/policy.py` `POLICY_FOOTER_LINKS = (("개인정보처리방침","/privacy"),("이용약관","/terms"),("데이터 정확성 면책조항","/disclaimer"),("광고·제휴 고지","/ads"))`. 전역 푸터 정책 4종 링크 정본 상수. 소비처=SP-GEN `_footer.html`(순회 렌더, 생성 페이지 전체)·SP-FE 수기 셸(하드코딩, import 불가) — 본 상수를 정본으로 양측 일치를 테스트가 강제.
  - 테스트: 소비 PC-5(생성 푸터↔`POLICY_FOOTER_LINKS` 라벨·라우트 일치) (pytest `test_footer_links.py`) — RED 먼저
  - refs: SP-POL-9.1 · FR-80 · SP-FE-2.1(위임)
- [-] T-09.9.2 푸터 링크·404 금지 일치 검증 (PC-5) — `web/index.html`·`web/compare/index.html`(SP-FE, M6)·생성 페이지(SP-GEN, M5) 모두 미존재. `test_footer_links.py` 미작성(사유: 위 진행 요약)
  - 구현: `generator/tests/test_footer_links.py` — (a) 전 생성 페이지 푸터가 `POLICY_FOOTER_LINKS` 4링크 순회 렌더·라벨·라우트 일치, (b) 수기 셸 `web/index.html`·`web/compare/index.html` 푸터 4링크(`/privacy /terms /disclaimer /ads`) 존재·라우트 일치, (c) 각 링크 대상이 실제 생성 dist 파일로 매핑(미존재 링크 0·404 금지). 수기 셸 하드코딩=SP-FE 위임, 본 테스트가 양측 정합 강제.
  - 테스트: PC-5(생성+수기 셸 푸터 4정책 링크 존재·대상 매핑·`POLICY_FOOTER_LINKS` 일치·미존재 0) (pytest `test_footer_links.py`) — RED 먼저
  - refs: SP-POL-9.1 · FR-80·87 · NFR24 · INV-3
- [-] T-09.9.3 무광고·"광고 없음" 표식 검증 (PC-7) — `ads_none` 필드 존재(기본 True), 렌더 HTML 부재로 검증은 SP-GEN 후(M5)
  - 구현: `generator/tests/test_policy_pages.py` — 각 정책 HTML(4종): `.ad-slot` 요소 **0**·"광고 없음" 표식(`doc.ads_none`→"이 페이지에는 광고가 없습니다") 존재·AdSense `<script>`·실 client id 부재(placeholder만). `page_type=policy` 자동광고 OFF·수동 슬롯 0·SCR-62 미마운트=SP-ADS 게이팅 위임.
  - 테스트: PC-7(각 정책 HTML `.ad-slot` 0·"광고 없음" 표식 존재·AdSense script/client id 부재) (pytest `test_policy_pages.py`) — RED 먼저
  - refs: SP-POL-9.2 · FR-86 · MON7-6·MON8 · NFR22
- [-] T-09.9.4 정책 SEO·접근성 검증 (PC-4) — `title`/`meta_description` 필드 확정, `<h1>`·`lang`·`title` 접미 등 head 렌더는 SP-GEN 소유(M5 후 검증)
  - 구현: `generator/tests/test_policy_pages.py` — 각 정책 HTML 파싱: 단일 `<h1>`(정책명)·`<html lang="ko">`·`<meta charset="utf-8">`·고유 `<title>`(`"{title} | loupit"`)·`meta description`·4종 간 title 중복 0. SEO head·title 접미·description 절단 부여=SP-GEN 위임, 본 테스트가 정책 특화 검증.
  - 테스트: PC-4(단일 `<h1>`·`lang="ko"`·charset utf-8·고유 title/desc·4종 title 중복 0) (pytest `test_policy_pages.py`) — RED 먼저
  - refs: SP-POL-9.2 · NFR6·13·29 · SP-GEN-4·6(위임)
- [-] T-09.9.5 정책 4종 게시·sitemap 등록 검증 (PC-3·PC-8) — `web/dist`·`sitemap.xml` 산출은 SP-GEN 소유(M5 후 검증)
  - 구현: `generator/tests/test_policy_pages.py` — 렌더 후 `web/dist`에 `privacy.html`·`terms.html`·`disclaimer.html`·`ads.html` 4파일 존재(PC-3, 누락·404=0) + `sitemap.xml` `<loc>`에 `/privacy /terms /disclaimer /ads` 4종 포함(PC-8, https·canonical 일치). 렌더·sitemap 엔트리=SP-GEN 위임(T-07.8.2·T-07.9.1), 본 테스트가 정책 4종 게시·색인 특화 검증.
  - 테스트: PC-3(4파일 존재)·PC-8(sitemap `<loc>` 4종 포함·https·canonical 일치) (pytest `test_policy_pages.py`) — RED 먼저
  - refs: SP-POL-9.2·SP-POL-1 · FR-80 · NFR9 · SP-GEN-9(위임)
- [-] T-09.9.6 시크릿 부재 검증 (PC-12) — 콘텐츠 모듈(`generator/content/policy.py`·`generator/config.py`) 자체는 실 client id·DB 자격·PAT 0(코드 리뷰 확인, 자동화 테스트는 M5 렌더 HTML 포함 시 함께). `policy_contact` 미주입 시 플레이스홀더 반환은 `GenConfig` 기본값으로 보장
  - 구현: `generator/tests/test_policy_pages.py` — 4종 HTML·`generator/content/policy.py`에 실 AdSense client id·DB 자격·PAT **0**; `policy_contact` 미주입(env 부재) 시 플레이스홀더 텍스트만 노출(링크 없음). 시크릿 부재(NFR22).
  - 테스트: PC-12(4 HTML·콘텐츠 모듈 시크릿 0·`policy_contact` 미주입 시 플레이스홀더) (pytest `test_policy_pages.py`) — RED 먼저
  - refs: SP-POL-2.2·SP-POL-10 · NFR22

## 위임 케이스 (다른 SP 스위트에 실구현 — 본 도메인 이중 구현 없음)
- **SP-GEN(07) 위임**: `generator/pages/policy.py::render_all`·`templates/policy.html`(구조화 `sections` 순회 렌더·초안 배너 블록·정정 섹션 C2 마크업·`{{ policy_body | safe }}` 조립)·`partials/_head_meta.html`(정책 SEO head·canonical·OG)·`partials/_footer.html`(`POLICY_FOOTER_LINKS` 순회)·`partials/_disclaimer.html`·`404.html`·sitemap 엔트리·`page_type=policy` 렌더. 본 도메인은 문안·상수·계약·PC 검증만(TASK 07 T-07.8.2·T-07.9.1 소비).
- **SP-ADS(08) 위임**: 동의 배너(SCR-61) 표시·저장·개인화/폴백 동작, "광고" 라벨 부착·광고 슬롯 게이팅·`page_type=policy` 무광고 게이팅 매트릭스, `rel="sponsored nofollow"`. 본 도메인은 정책 문서의 내용·존재·도달성(A-2·A-4·PC-7·PC-10)만.
- **SP-CALC≡SP-ENGINE(05) 위임**: 불확실성 밴드 계수 산출(`BAND` 0.05·0.20·만료 +0.15, DEC-2·FR-38). 본 도메인 D-4는 그 계수를 **고지**만(문자열·의미 일치 강제, PC-6).
- **SP-FE(06) 위임**: 수기 셸 `web/index.html`·`web/compare/index.html` 푸터 4정책 링크 **하드코딩**(무빌드 정적 HTML, 상수 import 불가). 본 도메인은 `POLICY_FOOTER_LINKS` 정본 + PC-5 정합 강제.
- **SP-SEED(03)·SP-ARCH(01) 위임**: 정정요청 반영(`TCOMPANY_BENEFIT` 수정/무효화→재시드→정적 재생성→재배포 SP-ARCH-9). 본 도메인은 서버 무쓰기·빌드타임 반영·SLA 한계 고지(PC-9)만.
- **별도 승인 게이트(M8, 코드 게이트 아님)**: 법률 자문 검토 완료 시 `legal_reviewed=True` env 주입→초안 배너 해제(PC-11 `=True` 경로). 운영 승인으로만 전환하며 어떤 리프도 이 승인 전에 구현이 차단되지 않는다(초안 배너 노출 상태로 완결).

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-09.1.1 | SP-POL-2.1 | 소비 PC-1·2·10 | FR-80~84 · SP-ARCH-6·7 |
| T-09.1.2 | SP-POL-2.2·1 | PC-1 | FR-80 · SP-ARCH-2 |
| T-09.1.3 | SP-POL-2.2 | 소비 PC-9·11·12 | FR-85 · NFR22 |
| T-09.2.1 | SP-POL-2.3 | PC-11 | FR-80~84 · FRD10 공통 |
| T-09.2.2 | SP-POL-2.4·10 | PC-13 (Tier0) | **NFR21** · INV-3 |
| T-09.3.1 | SP-POL-3 | PC-2 (정합→PC-10) | FR-81 · INV-4 · §2-1·§2-2 |
| T-09.4.1 | SP-POL-4 | PC-2 | FR-82 · §1 |
| T-09.5.1 | SP-POL-5 | PC-2 | FR-83 · FR-D9 |
| T-09.5.2 | SP-POL-5 | PC-6 | FR-83·DEC-2 · **INV-5** · SP-CALC/FR-38 |
| T-09.6.1 | SP-POL-6 | PC-2 (라벨→PC-7·10) | FR-84 · NFR19 |
| T-09.7.1 | SP-POL-7.1·7.2 | PC-9 | FR-85 · **INV-4** · NFR16 |
| T-09.8.1 | SP-POL-8 | PC-10 | FR-87 · INV-4 · MON13·14·15 |
| T-09.9.1 | SP-POL-9.1 | 소비 PC-5 | FR-80 · SP-FE-2.1 |
| T-09.9.2 | SP-POL-9.1 | PC-5 | FR-80·87 · NFR24 · INV-3 |
| T-09.9.3 | SP-POL-9.2 | PC-7 | FR-86 · MON7-6 · NFR22 |
| T-09.9.4 | SP-POL-9.2 | PC-4 | NFR6·13·29 |
| T-09.9.5 | SP-POL-9.2·1 | PC-3·8 | FR-80 · NFR9 |
| T-09.9.6 | SP-POL-2.2·10 | PC-12 | NFR22 |
