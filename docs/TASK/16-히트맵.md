# TASK 16 — 복지·실적 히트맵 (SP-HEAT)

> SPEC 계약: [SPEC/16-히트맵.md](../SPEC/16-히트맵.md). 리프 완료(- [v]) = 구현 + 테스트 green. 진행중 - [-], 미착수 - [ ].
> 마일스톤: **M12(히트맵)** — SC16(2026-08-27). 벤치마크 비교판 3종(대분류 10 / 업종 67 / KRX 20)을 실데이터로 만들어 **KRX 업종 + 첫 디자인**으로 확정(사용자 결정).

## 진행 요약
- 리프 총 **12** / 완료 **12** — 카테고리 모드 배포 완료(PR #14, 2026-08-27). 1차 8개는 2026-08-27 배포 완료( — PR #12, nginx 블록 배치, 재생성, 스모크: `/heatmap` 200 · 복지 102칸·실적 92칸 × 가로/세로 · 탭 · sitemap).

---

## T-16.1 데이터·배치 — (SP-HEAT-2·3)
- [v] T-16.1.1 KRX 업종 시드 `generator/data/krx_sector.csv`(99코드) + `sector.py`(비상장 폴백)
- [v] T-16.1.2 `treemap.py` squarify·nested_layout — 면적 비례·경계·겹침 0·작은 그룹 타일 보존(HM 3)

## T-16.2 페이지 — (SP-HEAT-4·5)
- [v] T-16.2.1 `pages/heatmap.py` 뷰모델 — 복지/실적 모드·7단계 색·가로/세로 배치·금융업 제외·미적재 시 실적 생략(HM 5)
- [v] T-16.2.2 `heatmap.html` + `base.html css_extra` + `heatmap.css`(토큰) — JS 없이 완성·범례·읽는 법
- [v] T-16.2.3 `heatmap.js` 탭 전환(activate 순수 함수, node 3)
- [v] T-16.2.4 상단 탭 `히트맵` — GNB_TABS + 셸 8개 + test_gnb_tabs 현재 탭
- [v] T-16.2.5 전역 가드 편입 — build.py 배선·GC-20·SEO·sitemap·FN-6 + 금지 어휘 테스트

## T-16.4 카테고리 모드 (2026-08-27 후반, 목업 `mock-category-v1` 확정)
- [v] T-16.4.1 뷰모델 — 9 카테고리 패널 × 항목 묶음 × 복지 행, 출처 3분류·최소 칸·수정 안내(HM +4)
- [v] T-16.4.2 템플릿·CSS — 칩·패널·출처 색·범례, JS 없이 9패널 세로 나열
- [v] T-16.4.3 `heatmap.js` `activatePanel` + `?mode=c&cat=` 복원(node +2)
- [v] T-16.4.4 재생성·배포·스모크(`/heatmap?mode=c` 카테고리 9패널·1,465칸)

## T-16.3 반영
- [v] T-16.3.1 nginx `= /heatmap` prod·beta 배치 + 재생성 + 스모크(`/heatmap` 200·탭·102칸) + STATE
