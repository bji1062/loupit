# TASK 03 — 데이터 시드·백필 (SP-SEED)

> SPEC 계약: [SPEC/03-데이터-시드-백필.md](../SPEC/03-데이터-시드-백필.md) (SP-SEED). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M1(데이터 계층) — 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: **SP-DB**(schema.sql 5테이블·`AMT_SOURCE_CD VARCHAR(10) NOT NULL DEFAULT 'estimated'`·9카테고리·2배지 값집합·`uq_comp_benefit`/`uq_comp_alias`/UNIQUE `COMP_TP_CD`·`COMP_ENG_NM`·`COMP_NM`). 소스(읽기전용, 편집 금지): `/home/ubuntu/job_change` 의 96개 `benefit/sql/*.sql`·`seed.py`(`COMPANY_TYPES`·`BEN_PRESETS`)·`companies_kospi_1/2.py`·`companies_kosdaq_1/2.py`(200-seed). 확정 순서는 00이 소유(SP-DB-10: schema→reference→95개→backfill).
> 테스트: 러너=pytest + pymysql + MySQL 8(`loupit_test`, CI docker `mysql:8.0`) · 위치=`server/tests/`(`conftest.py`·`test_seed_counts.py`·`test_seed_integrity.py`·`test_seed_badge_backfill.py`·`test_seed_idempotency.py`) · 케이스 대역=SD-1~7 · SI-1~8 · SB-1~10 · SM-1~4. `conftest.py` 가 `load.main(fresh=True)` 로 스키마→시드→백필 전체 적용 후 슬라이스 검증.

## 진행 요약
- 리프 총 21 / 완료 0 / 진행 0 / 미착수 21
- Tier-0 게이트 리프: T-03.3.4 (SD-3 회사 95≠200), T-03.5.1 (SB-1 official 전량 승격), T-03.5.3 (SB-6 DEC-2 디커플링 실재)
- 선결정 게이트: DG-1(T-03.5.5 위), DG-2(T-03.5.2 위), DG-3(T-03.4.3 위) — SPEC이 후보값을 채택했으나 오케스트레이터 지시에 따라 구현 전 AskUserQuestion 확인 필수(임의 확정 금지)

## T-03.1 시드 하네스·오케스트레이터 진입점  — (SP-SEED-9, SP-SEED-3, SP-SEED-11, INV-1)
- [ ] T-03.1.1 시드 테스트 픽스처 `seeded_db`(load.main fresh)
  - 구현: server/tests/conftest.py 에 `seeded_db` 픽스처 추가 — `load.main(fresh=True)` 호출로 schema→reference→95개 benefit→company_meta→backfill 전체 적용 후 pymysql 커넥션 yield. **위임**: 기반 pymysql 커넥션·`loupit_test` drop/create·`SET NAMES utf8mb4` 세션은 SP-DB(T-02.1.1). 본 리프는 `load.main` 오케스트레이션 픽스처만 추가(SP-DB seeded_db 는 순수 SQL 백필 경로, 본 픽스처는 loader `backfill_dec2.py` 프로버넌스 경로)
  - 테스트: 픽스처 smoke — 5개 테이블 존재 + 시드 후 `COUNT(*) TCOMPANY > 0` (pytest server/tests/) — RED 먼저
  - refs: SP-SEED-11 · SP-SEED-9 · INV-1
- [ ] T-03.1.2 load.py 진입점 골격 (`main`/`connect`/`run_sql_file`/`--fresh`)
  - 구현: server/seed/load.py — `def main(fresh=False)`, `connect()`(동기 pymysql, aiomysql 아님), `run_sql_file(cur, path)`(다중문장 `;` 분할·단일 커넥션에서 `SET @var` 세션 유지 순차 실행), `--fresh`→`DROP TABLE IF EXISTS`(FK 역순)→`schema.sql`. CLI `python server/seed/load.py [--fresh]`, 종료코드 0=성공·예외시 `conn.rollback()`+비0(단일 트랜잭션). 우선 단계 1(schema)만 배선
  - 테스트: load smoke — `main(fresh=True)` 예외 없이 5개 테이블 생성, `run_sql_file` 다중문장·`SET @var` 세션 유지 단위 (pytest) — RED 먼저
  - refs: SP-SEED-9 · SP-SEED-3

## T-03.2 reference.sql — 기업유형 6종 + 프리셋 28  — (SP-SEED-4, SP-SEED-3 단계2, FR-D2·D3, INV-6)
- [ ] T-03.2.1 기업유형 6종 `INSERT IGNORE`
  - 구현: server/seed/reference.sql 상단 `SET NAMES utf8mb4;` + `INSERT IGNORE INTO TCOMPANY_TYPE (COMP_TP_CD,COMP_TP_NM,GROWTH_RATE_VAL,GROWTH_LABEL_NM,STABILITY_SCORE_NO) VALUES …` 6행(large 0.0400/90·mid 0.0270/70·public 0.0300/95·startup 0.1000/30·foreign 0.0500/60·freelance 0.0200/20, SP-SEED-4.1 확정값). load.py 단계2 배선(`reference.sql`)
  - 테스트: SD-1(`COUNT(*) TCOMPANY_TYPE = 6`) (pytest server/tests/test_seed_counts.py) — RED 먼저
  - refs: SP-SEED-4.1 · SP-DB-2 · FR-D2 · D2.1
- [ ] T-03.2.2 유형별 복지 프리셋 28행 full-refresh
  - 구현: reference.sql 에 `DELETE FROM TBENEFIT_PRESET;` + 전량 `INSERT`(large 8·mid 4·public 8·startup 2·foreign 6·freelance 0 = 28행). 필드맵 `{key→BENEFIT_CD, name→BENEFIT_NM, val→BENEFIT_AMT, cat→BENEFIT_CTGR_CD, badge→BADGE_CD, checked→DEFAULT_CHECKED_YN}`·`SORT_ORDER_NO`=배열 인덱스·`COMP_TP_ID=(SELECT … WHERE COMP_TP_CD=?)`. 소스=`seed.py` `BEN_PRESETS`(재게시 아님, 큐레이션 상수). `reference_gen.py`(선택) 또는 수기
  - 테스트: SD-2(`COUNT(*) TBENEFIT_PRESET = 28`)·SD-7(freelance 제외 5개 유형 프리셋 ≥1행·freelance=0행) (pytest test_seed_counts.py) — RED 먼저
  - refs: SP-SEED-4.2 · SP-DB-6·9 · FR-D3 · INV-6

## T-03.3 96개 복지 SQL 재이식 (`reingest_benefit_sql.py`)  — (SP-SEED-5, SP-SEED-1, SP-SEED-7, SP-SEED-2.2, FR-D4)
- [ ] T-03.3.1 재이식 코어 루프 + 모비스 중복 skip (95 파일 생성)
  - 구현: server/seed/reingest_benefit_sql.py — `for f in SRC/*.sql`(96개, `SRC=/home/ubuntu/job_change/server/seed/benefit/sql`, `DST=/home/ubuntu/loupit/server/seed/benefit/sql`): `basename=='모비스.sql'`→skip(재이식 제외, SP-SEED-1.2 중복), 그 외 94개 **바이트 무변경 복사**(헤더 `출처:`/`URL:` 보존=백필 프로버넌스 소스), `CJ.sql`은 T-03.3.2. `assert count(DST/*.sql)==95`
  - 테스트: 단위 — `count(DST)==95`·`모비스.sql` 미생성; SI-3(부분: 로드 후 `COMP_ENG_NM='mobis'` 회사 부재) (pytest server/tests/test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-5.1 · SP-SEED-1.1·1.2 · SP-SEED-7 · D2.4
- [ ] T-03.3.2 CJ 등록명 교정 (리터럴 치환)
  - 구현: reingest 내 `replace_company_nm(text, old='CJ그룹', new='CJ올리브네트웍스')` — 정규식 앵커 `VALUES \('cj', '\KCJ그룹`(자기등록 튜플 2번째 리터럴만 치환), `eng_nm='cj'` 유지(`@comp_id` 조회 키 불변). 헤더 주석 동반 갱신 권장. `CJ ENM`은 미시드
  - 테스트: SI-1(`COMP_NM='CJ올리브네트웍스'` 1건·`'CJ그룹'` 및 `'CJ ENM'` 부재) (pytest test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-5.1 · SP-SEED-1.3 · SP-SEED-7 · D2.7 · OI-1b
- [ ] T-03.3.3 복지 SQL 포맷 계약 보존 검증 (3부구조·`AMT_SOURCE_CD` DEFAULT·eng↔복지 정합)
  - 구현: 바이트 무변경 복사가 SP-SEED-5.2 3부구조 보존 확인(자기등록 6컬럼 `INSERT IGNORE TCOMPANY`→`SET @comp_id`→`DELETE … BADGE_CD='est'`(official 보존)→`INSERT … ON DUP KEY`). `AMT_SOURCE_CD` INSERT 목록 미기재→컬럼 DEFAULT `'estimated'` 로드. load.py 단계3 배선(`sorted(glob(benefit/sql/*.sql))` 95개 순차)
  - 테스트: SI-4(`TCOMPANY_BENEFIT.COMP_ID` 고아 0·14개 eng-상이 회사(SP-SEED-2.2)도 복지 정상 연결)·SI-5(`BENEFIT_CTGR_CD NOT IN 9종=0`, benefit+preset 양쪽) (pytest test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-5.2 · SP-SEED-2.2 · SP-DB-7.2·9 · FR-D5
- [ ] T-03.3.4 회사 등록 카운트 95 (Tier0)
  - 구현: 재이식 95개 + load.py 단계3 순차 적용 결과 `TCOMPANY=95`(96 소스 − 모비스 중복). 200개 목업 아님·복지 보유 회사만 등록(§5). 검증 리프(구현은 T-03.3.1~3·단계3 배선)
  - 테스트: SD-3(`COUNT(*) TCOMPANY = 95`, Tier0)·SI-8(`TCOMPANY` 회사수=95 ≠ 200)·SD-5(복지 0개 회사 수=0) (pytest test_seed_counts.py, test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-1 · D2.6 · §5 · SD-3
- [ ] T-03.3.5 복지 총행 카운트 하한
  - 구현: 재이식 후 `TCOMPANY_BENEFIT` 총행 정확값 고정(1330 − 모비스 파일 복지행수)·방어 하한 `≥1200` assert. 검증 리프(구현은 단계3 로드)
  - 테스트: SD-4(`COUNT(*) TCOMPANY_BENEFIT = 1330 − 모비스행`, `≥1200` 하한) (pytest test_seed_counts.py) — RED 먼저
  - refs: SP-SEED-11.1 · SP-SEED-1.2 · SD-4

## T-03.4 회사 메타 보강 (`company_meta.py` + `companies.py`)  — (SP-SEED-6, SP-SEED-2, D2.3·D1.6·D2.7, FR-D4)
- [ ] T-03.4.1 별칭 보강 `build_company_meta()` + `apply_company_meta()`
  - 구현: server/seed/company_meta.py `build_company_meta()→dict{eng:{aliases[],work_style{}}}` — 200-seed 4파일 로드(`by_name={rec["name"]:rec}`), `parse_header_insert(f)`로 95 SQL의 `(eng,comp_nm,type)` 파싱, **정본 조인 키=`COMP_NM`**(SP-SEED-2.1), 실패시 `aliases=[comp_nm]` 폴백+warn. server/seed/companies.py `apply_company_meta(cur, meta)` — `id_of={eng:cid}` from `SELECT COMP_ID,COMP_ENG_NM`, alias당 `INSERT IGNORE TCOMPANY_ALIAS (COMP_ID,ALIAS_NM)`(`uq_comp_alias` 멱등), 미등록 eng warn skip. load.py 단계4 배선
  - 테스트: SD-6(별칭 0개 회사 수=0)·SI-7(회사 내 중복 별칭 0, `uq_comp_alias` 위반 없음) (pytest test_seed_counts.py, test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-6.1·6.3 · SP-SEED-2.1·2.2 · D2.3·D2.6 · FR-D4
- [ ] T-03.4.2 `WORK_STYLE_VAL` 도출·UPDATE (`derive_work_style`)
  - 구현: company_meta.py `derive_work_style(f)` — 복지행 스캔 파생(보수 기본): `remote`(`BENEFIT_CD∈{remote_work,telecommute,wfh}`→true, else false)·`flex`(`flex_work`→true, else false)·`unlimitedPTO`(`unlimited_pto` 또는 QUAL_DESC "무제한/자율 휴가"→true, else false)·`refreshLeave`(`refresh_leave/long_service_leave`의 `QUAL_DESC_CTNT` ≤60자, else null)·`overtime`(null). companies.py `UPDATE TCOMPANY SET WORK_STYLE_VAL=json.dumps(m["work_style"], ensure_ascii=False)`(멱등). `WORK_STYLE_OVERRIDE` 상수는 선택
  - 테스트: SI-6(`WORK_STYLE_VAL` JSON 키가 `{remote,flex,unlimitedPTO,refreshLeave,overtime}` 부분집합·불리언 3키 타입) (pytest test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-6.2 · D1.6 · SI-6
- [ ] T-03.4.3 등록 예외 메타 override (엔씨소프트·CJ올리브·현대모비스)
  - 선결정 DG-3: 엔씨소프트 시드 메타(별칭·산업·work_style — 200 목록 밖) 확보 — SPEC이 `NCSOFT_ALIASES`(엔씨소프트·NCSOFT·NC·엔씨·리니지)·work_style 후보를 제시하나 **구현 전 AskUserQuestion 확인**(임의 확정 금지, OI-1a)
  - 구현: company_meta.py 예외 override — `meta["cj"]["comp_nm"]="CJ올리브네트웍스"`·`aliases=CJ_OLIVE_ALIASES`(CJ올리브네트웍스·올리브네트웍스·CJ OliveNetworks·cj_olive_networks); `meta["ncsoft"]["aliases"]=NCSOFT_ALIASES`(200-seed 부재 예외); `meta["hyundai_mobis"]["aliases"] += ["모비스"]`(모비스 중복 흡수)
  - 테스트: SI-2(`COMP_ENG_NM='ncsoft'` 회사 존재·별칭 ≥1)·SI-3(부분: `hyundai_mobis` 별칭에 `'모비스'` 포함·`mobis` 회사 부재) (pytest test_seed_integrity.py) — RED 먼저
  - refs: SP-SEED-6.1 · SP-SEED-7 · SP-SEED-1.2·1.4 · D2.7 · OI-1a/1b

## T-03.5 DEC-2 백필 오케스트레이션 (`backfill_dec2.py`)  — (SP-SEED-8, DEC-2, D3, INV-5)
- [ ] T-03.5.1 출처 신뢰도 `official` 승격 (Tier0)
  - 구현: server/seed/backfill_dec2.py `def backfill(cur)->dict` 단계5. `UPDATE TCOMPANY_BENEFIT SET BADGE_CD='official' WHERE BADGE_CD='est'`(96=95등록분 전량 승격). 멱등(대상 0 재실행 no-op). load.py 단계5 배선(`stats=backfill(cur)`)
  - 테스트: SB-1(백필 후 등록 복지 중 `BADGE_CD='est'` = 0, 전량 official, Tier0) (pytest server/tests/test_seed_badge_backfill.py) — RED 먼저
  - refs: SP-SEED-8.1 · DEC-2 · D3 · SB-1
- [ ] T-03.5.2 금액 신뢰도 `AMT_SOURCE_CD` 도출 (`derive_amt_source`)
  - 선결정 DG-2: OI-6 amt_source 판별규칙 — SPEC이 결정규칙(`qual_yn`/`amt is None`→none; note "추정"/"환산" 또는 note 없음→estimated; note 명시금액·추정표기 없음→stated)을 확정했으나 기존 `(추정)` 표기의 stated/estimated 귀속을 **구현 전 AskUserQuestion 확인**(임의 확정 금지, OI-6)
  - 구현: backfill_dec2.py `derive_amt_source(benefit_amt, qual_yn, note_ctnt)->str`(SP-SEED-8.2 결정규칙) + 행별 `UPDATE AMT_SOURCE_CD`. 순수 SQL 등가(SP-DB-13.2)와 동치
  - 테스트: SB-2(`AMT_SOURCE_CD NOT IN ('stated','estimated','none')` = 0)·SB-3(`QUAL_YN=TRUE AND (BENEFIT_AMT NOT NULL OR AMT_SOURCE_CD<>'none')` = 0)·SB-4(`'none' AND BENEFIT_AMT NOT NULL` = 0)·SB-5(`'estimated'` 행 ≥1)·SB-7(삼성전자 `health_check`(note '(추정)')·`meal`(note '환산')→estimated; 명시금액·추정표기 없는 표본→stated) (pytest test_seed_badge_backfill.py) — RED 먼저
  - refs: SP-SEED-8.2 · SP-DB-13.2 · OI-6 · SB-2·3·4·5·7
- [ ] T-03.5.3 DEC-2 디커플링 실재 (official × estimated 공존) (Tier0)
  - 구현: 별도 구현 없음 — T-03.5.1(출처축 `BADGE_CD`)·T-03.5.2(금액축 `AMT_SOURCE_CD`) 독립 유지의 조합 검증 리프. DEC-2 서명(공식 출처라도 추정 금액은 넓은 밴드)
  - 테스트: SB-6(`BADGE_CD='official' AND AMT_SOURCE_CD='estimated'` 행 ≥1, Tier0) (pytest test_seed_badge_backfill.py) — RED 먼저
  - refs: SP-SEED-8.1·8.2 · DEC-2 · INV-5 · SB-6
- [ ] T-03.5.4 출처 유형·URL (`BADGE_SRC_CD`·`BADGE_SRC_URL_CTNT`)
  - 구현: backfill_dec2.py 헤더 `URL:` 실측 반영 — 실 http URL 5건→`BADGE_SRC_CD='scrape_official'`+헤더 URL; `수동 입력`류 91건→`'ai_parse'`+`NULL`. 값 존재시 보존(멱등). 프로버넌스=`provenance={eng_nm:{scraped_at,url}}`를 reingest가 `provenance.json` emit 또는 backfill이 `benefit/sql/*.sql` 헤더 재파싱(회사→복지 `COMP_ID` 조인 전파)
  - 테스트: SB-10(`BADGE_SRC_CD NOT NULL AND NOT IN 5종` = 0; 실 URL 5건=`scrape_official`+URL non-null, 그 외 `ai_parse`) (pytest test_seed_badge_backfill.py) — RED 먼저
  - refs: SP-SEED-8.3 · SP-SEED-8.5 · SB-10
- [ ] T-03.5.5 신선도·만료 TTL (`VERIFIED_DTM`·`EXPIRES_DTM`)
  - 선결정 DG-1: OI-3 카테고리별 만료 TTL — SPEC이 확정값(compensation 6·perks 9·time_off/flexibility 12·work_env/health/growth/leisure/family 18 개월, `benefit_service.py` 상충 종결)을 채택했으나 **구현 전 AskUserQuestion 확인**(임의 확정 금지, OI-3)
  - 구현: backfill_dec2.py `VERIFIED_DTM`=헤더 `출처: AI 파싱 (YYYY-MM-DD)` 수집일(파싱 실패→`2026-07-10` 폴백). `TTL_MONTHS={"compensation":6,"perks":9,"time_off":12,"flexibility":12}`(그 외 18)·`expires(verified,ctgr)=verified+relativedelta(months=TTL_MONTHS.get(ctgr,18))`. SP-DB-13 `CASE … MONTH` 표와 동치
  - 테스트: SB-8(`VERIFIED_DTM IS NULL` = 0)·SB-9(카테고리 표본 `EXPIRES_DTM = VERIFIED_DTM + TTL`·`EXPIRES_DTM > VERIFIED_DTM` 전량) (pytest test_seed_badge_backfill.py) — RED 먼저
  - refs: SP-SEED-8.4 · SP-DB-13 · OI-3 · SB-8·9

## T-03.6 오케스트레이터 완성·멱등성  — (SP-SEED-9, SP-SEED-10, SP-SEED-3)
- [ ] T-03.6.1 load.py 전체 오케스트레이션·단일 트랜잭션·`verify_counts`
  - 구현: load.py `main()` 단계 1~5 완결 배선(schema→reference→95 benefit(sorted glob)→`apply_company_meta(build_company_meta())`→`backfill(cur)`)·`conn.commit()` 단일 트랜잭션·예외시 rollback+비0·`verify_counts(stats)` 하한 assert·`print(f"seed done: companies=…, benefits=…, {stats}")`. `--fresh`=DROP(FK 역순)→재시드, 무플래그=멱등 재적용
  - 테스트: SM-1(`load.main()` 2회 연속 실행 후 각 테이블 행수·`BADGE_CD` 분포·`AMT_SOURCE_CD` 분포가 1회와 동일·회사 95 불변·official 전량 유지) (pytest server/tests/test_seed_idempotency.py) — RED 먼저
  - refs: SP-SEED-9 · SP-SEED-3 · SM-1
- [ ] T-03.6.2 프리셋 full-refresh 멱등
  - 구현: reference.sql `DELETE FROM TBENEFIT_PRESET;` + 전량 재삽입(스키마 `(COMP_TP_ID,BENEFIT_CD)` UNIQUE 부재로 `INSERT IGNORE` 부적합 → full-refresh 필수). 검증 리프(구현은 T-03.2.2)
  - 테스트: SM-2(2회 실행 후 `TBENEFIT_PRESET = 28`, 중복 증식 없음) (pytest test_seed_idempotency.py) — RED 먼저
  - refs: SP-SEED-10 · SP-SEED-4.2 · SM-2
- [ ] T-03.6.3 est↔official 순환·official 보존 멱등
  - 구현: 복지 upsert `DELETE … BADGE_CD='est'`(official 파괴 안 함, est 없으면 no-op) + `ON DUP KEY UPDATE BADGE_CD=VALUES(BADGE_CD)` 후 단계5 재승격. 파이프라인 단위(단계 3+5)로만 멱등 — load.py 는 항상 3 뒤 5 실행(부분 실행 금지)
  - 테스트: SM-3(1회 시드→백필(official) 후 단계3(복지 SQL) 재적용→단계5 재실행 시 다시 official; `DELETE est`가 official 파괴 안 함) (pytest test_seed_idempotency.py) — RED 먼저
  - refs: SP-SEED-10 · SP-SEED-8.1 · SM-3
- [ ] T-03.6.4 부분실행 감지 가드
  - 구현: 단계5(백필) 미실행 시 `BADGE_CD='est'` 잔존 → 최종상태 아님 가드 assert(테스트 전용, 중간상태 최종 취급 금지 규약 검증)
  - 테스트: SM-4(백필 미실행 시 `BADGE_CD='est'` 잔존 → 최종상태 아님 assert) (pytest test_seed_idempotency.py) — RED 먼저
  - refs: SP-SEED-10 · SM-4

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-03.1.1 | SP-SEED-11·9 | 픽스처 smoke | INV-1 |
| T-03.1.2 | SP-SEED-9·3 | load smoke | — |
| T-03.2.1 | SP-SEED-4.1 | SD-1 | FR-D2 · D2.1 |
| T-03.2.2 | SP-SEED-4.2 | SD-2·SD-7 | FR-D3 · INV-6 |
| T-03.3.1 | SP-SEED-5.1·1.2 | SI-3(부분) | D2.4 |
| T-03.3.2 | SP-SEED-5.1·1.3 | SI-1 | D2.7 · OI-1b |
| T-03.3.3 | SP-SEED-5.2·2.2 | SI-4·SI-5 | FR-D5 |
| T-03.3.4 | SP-SEED-1 | SD-3 (Tier0)·SI-8·SD-5 | D2.6 · §5 |
| T-03.3.5 | SP-SEED-11.1·1.2 | SD-4 | — |
| T-03.4.1 | SP-SEED-6.1·6.3·2.1 | SD-6·SI-7 | FR-D4 · D2.3·D2.6 |
| T-03.4.2 | SP-SEED-6.2 | SI-6 | D1.6 |
| T-03.4.3 | SP-SEED-6.1·7·1.4 | SI-2·SI-3(부분) | D2.7 · OI-1a/1b · [DG-3] |
| T-03.5.1 | SP-SEED-8.1 | SB-1 (Tier0) | DEC-2 · D3 |
| T-03.5.2 | SP-SEED-8.2 | SB-2·3·4·5·7 | OI-6 · [DG-2] |
| T-03.5.3 | SP-SEED-8.1·8.2 | SB-6 (Tier0) | DEC-2 · INV-5 |
| T-03.5.4 | SP-SEED-8.3·8.5 | SB-10 | — |
| T-03.5.5 | SP-SEED-8.4 | SB-8·SB-9 | OI-3 · [DG-1] |
| T-03.6.1 | SP-SEED-9·3 | SM-1 | — |
| T-03.6.2 | SP-SEED-10·4.2 | SM-2 | INV-6 |
| T-03.6.3 | SP-SEED-10·8.1 | SM-3 | INV-5 |
| T-03.6.4 | SP-SEED-10 | SM-4 | — |

### 위임(중복 구현 금지) 표기
- **SP-DB 위임**: schema.sql(5테이블 DDL·`AMT_SOURCE_CD` 컬럼 정의·값집합 제약)·`migrations/20260710_backfill_dec2.sql`(순수 SQL 백필 판본)·conftest.py 기반 pymysql 커넥션·`loupit_test` drop/create 픽스처. T-03.1.1 은 `seeded_db(load.main)` 픽스처만 추가하고, backfill_dec2.py(loader 프로버넌스 판본)와 SQL 판본은 동일 불변식(DC-13/14 ≡ SB-1/SB-9) 만족.
- **SP-API 위임**: `AMT_SOURCE_CD→amt_source` 와이어 필드맵·번들 직렬화·검색 쿼리·캐시. 본 도메인은 근거 컬럼 값만 백필.
- **SP-ENGINE/SP-FE 위임**: 불확실성 밴드 계산(±5/±20/+15)·만료 판정(`EXPIRES_DTM`으로 클라 +15% 확대)·`value_source` 프로버넌스·`WORK_STYLE_VAL` 소비(autonomyScore). 본 도메인은 근거만 저장.
- **SP-SEED 내부 비중복**: `reference.sql` 프리셋 값은 T-03.2.2 단독 구현, T-03.6.2 는 멱등만 검증. `official` 승격은 T-03.5.1 단독, T-03.5.3·SM-3 은 결과 조합·보존만 검증.
