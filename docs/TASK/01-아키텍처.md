# TASK 01 — 개요·시스템 아키텍처 (SP-ARCH)

> SPEC 계약: [SPEC/01-개요와-아키텍처.md](../SPEC/01-개요와-아키텍처.md) (SP-ARCH). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M0(스캐폴드: SP-ARCH-6 디렉토리·SP-ARCH-7 버전 pin) + M8(통합 스모크 T7·T8) — 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: 없음(최상위 — 6종 디렉토리·버전 pin·SPEC 인덱스가 모든 도메인에 선행). 단 회귀·스모크 리프 T-01.3.1(T3)·T-01.4.1(T7)·T-01.5.1(T8)은 다운스트림(SP-API·SP-GEN·SP-SEED·SP-INFRA) 구현 후 M8에서 green이 된다(테스트는 여기서 RED로 먼저 소유).
> 테스트: 러너=pytest(`httpx.ASGITransport`) + node:test + bash/curl · 위치=`server/tests/test_arch.py`·`infra/deploy/smoke.sh` · 케이스 대역=T1~T8(고유 T1·T3·T7·T8 / 위임 T2·T4·T5·T6).

## 진행 요약
- 리프 총 7 / 완료 3 / 진행 1 / 미착수 3
- Tier-0 게이트 리프: 없음 (SP-ARCH가 참조하는 회귀 게이트 케이스 T2·T4·T5·T6은 **위임 대역** — SP-API/SP-ENGINE/SP-GEN 스위트가 실구현·Tier-0 게이트 소유; SP-TEST-7. 본 도메인 고유 케이스 T1·T3·T7·T8은 Tier-0 목록에 없음)
- 선결정 게이트: DG-4 (T-01.2.1 위 — MySQL 인증 플러그인 → `cryptography` 포함 여부; 전역 레지스트리 00 §4의 **DG-4**로 통합, 구현 전 AskUserQuestion. 전역 DG-1은 OI-3 만료 TTL)

## T-01.1 모노레포 스캐폴드·컴포넌트 경계 (M0)  — (SP-ARCH-6, SP-ARCH-1, FR-01, NFR4·NFR20·NFR21, AS3)
- [v] T-01.1.1 최상위 6종 디렉토리 생성 + 배포 산출물·비밀 경계(.gitignore)
  - 구현: 리포 루트 `/home/ubuntu/loupit`에 §5 정본 트리 최상위 경계 6종 생성 — `web/ server/ db/ generator/ infra/ docs/`. `.gitignore`에 배포 산출물·비밀 등록(`web/dist/`·`__pycache__/`·`*.pyc`·`.env`). 컴포넌트별 서브디렉토리(`server/routers`·`db/seed` 등)는 소유 SP-*가 추가하며 **경계·경로는 불변**(SP-ARCH-6 규칙).
  - 테스트: T1 (pytest — `server/tests/test_arch.py`) — `web/ server/ db/ generator/ infra/ docs/` 6 디렉토리 `os.path.isdir` assert — RED 먼저(디렉토리 부재)
  - refs: SP-ARCH-6 · SP-ARCH-1 · FR-01 · NFR4·NFR20 · AS3

## T-01.2 기술 스택 버전 pin·의존성 매니페스트 (M0)  — (SP-ARCH-7, F8, NFR16·NFR20·NFR23·NFR29, AS1·AS3·AS4)
- 선결정 DG-4(로컬 — 전역 레지스트리 00 §4 DG-4): MySQL 인증 플러그인 결정 — `caching_sha2_password`(→`cryptography>=42.0.0` 포함) vs `mysql_native_password`(→`cryptography` 생략). SP-ARCH-7 "조건부" 항목. 구현 전 AskUserQuestion 확인.
- [v] T-01.2.1 server/requirements.txt + .env.example (API·테스트 의존 pin, 인증 라이브러리 제거)  — DG-4 확정(cryptography 포함)·pymysql 추가, 완료
  - 구현: `server/requirements.txt` — pin `fastapi==0.115.6`·`uvicorn[standard]==0.34.0`·`aiomysql==0.2.0`·`pydantic==2.10.4`·`python-dotenv==1.0.1`·`pytest==8.3.*`·`httpx==0.28.*`·`pytest-asyncio==0.24.*`(+ DG-4 확정 시 `cryptography>=42.0.0`). **금지(사용 금지)**: `python-jose[cryptography]`·`passlib[bcrypt]`·`smtplib`/SMTP·OAuth 키. `server/.env.example` — DB·CORS·캐시 TTL 변수만(JWT/OAuth/SMTP 키 부재).
  - 테스트: §9.2 전용 케이스 없음 — `pip install -r server/requirements.txt` 성공 + 금지 라이브러리 부재 grep(bash 린트); 런타임 무인증·무쓰기 회귀는 **T2 위임**(SP-API TS-1·TS-2·TM-1, Tier-0)로 강제 — RED 먼저(매니페스트 부재)
  - refs: SP-ARCH-7 · F8 · NFR16·NFR20·NFR23·NFR29 · AS1 · DG-4
- [v] T-01.2.2 generator/requirements.txt + web/package.json (생성기·프론트 테스트 pin, 무빌드)
  - 구현: `generator/requirements.txt`(`Jinja2==3.1.*`·`pytest==8.3.*`) + `web/package.json`(`"scripts":{"test":"node --test"}`만, 번들러·`build` 스크립트·runtime deps **부재**=무빌드 AS3·NFR4). `assets/fonts/` Pretendard woff2 self-host 경로 고정(NFR5).
  - 테스트: §9.2 전용 케이스 없음 — `node --test` 부트 성공 + `package.json`에 build/bundler 부재 확인(bash 린트); 생성기 렌더 검증은 **GC 위임**(SP-GEN) — RED 먼저
  - refs: SP-ARCH-7 · NFR4·NFR5·NFR21 · AS3·AS4

## T-01.3 참조 번들 단일 소스 계약 회귀  — (SP-ARCH-4, FR-92, FR-D*, INV-2 지지)
- [-] T-01.3.1 build_reference_bundle 단일 소스 회귀 (함수 실구현 위임 SP-API)  — T3 테스트 작성·skip(M2 SP-API 구현 시 해제)
  - 구현: 함수 실구현은 **위임**(SP-API `server/services/reference.py::async def build_reference_bundle(conn) -> dict`, 순수 조립·최상위 키 3종). 본 리프는 **단일 소스 회귀만 소유**: `server/tests/test_arch.py`에서 런타임 라우터(FR-92)와 빌드타임 generator(C2)가 **동일** `build_reference_bundle` 심볼을 import함을 assert.
  - 테스트: T3 (pytest — `server/tests/test_arch.py`) — `reference.build_reference_bundle` 임포트 후 라우터·generator 참조 동일성 검사 — RED 먼저(중복 구현/부재 시 실패)
  - refs: SP-ARCH-4 · FR-92·FR-D* · INV-2 지지 (build_reference_bundle 실구현 위임 SP-API)

## T-01.4 런타임 읽기전용·무전송 불변식 스모크 (M8)  — (SP-ARCH-5·SP-ARCH-10, INV-4, FR-07, NFR16·NFR17·NFR20)
- [ ] T-01.4.1 T7 사용자 데이터 무전송 통합 스모크
  - 구현: `infra/deploy/smoke.sh`에 T7 절 추가 — 로컬 스택 기동 후 비교 흐름 수행 중 `/api/v1`로의 POST/PUT/PATCH/DELETE·본문 업로드 **0건**을 Nginx 접근 로그/네트워크 관찰로 검증. 셸 스크립트 상세·서비스 기동은 SP-INFRA와 공유.
  - 테스트: T7 (bash+curl — `infra/deploy/smoke.sh`, M8) — 쓰기 메서드/업로드 0건 assert — RED 먼저(스택 미기동)
  - refs: SP-ARCH-5 · SP-ARCH-10 · INV-4 · NFR16·NFR17·NFR20 · FR-07 (계산 순수성 T5/T-ENGINE-45는 위임 SP-ENGINE, Tier-0)

## T-01.5 배포 스모크·릴리스 파이프라인 순서 (M8)  — (SP-ARCH-9·SP-ARCH-2·SP-ARCH-3, INV-3·INV-7, FR-96, NFR1·NFR3·NFR22·NFR23·NFR28)
- [ ] T-01.5.1 T8 배포 스모크 + 릴리스 순서(1~7단계) 최종 게이트
  - 구현: `infra/deploy/smoke.sh`에 T8 절 — 릴리스 순서 계약(SP-ARCH-9: schema→seed→generate→검증→restart api→nginx reload→smoke) 실행 후 `curl /`(200 HTML)·`curl /api/v1/health`(200)·`curl /api/v1/reference/all`(200 + `Cache-Control: public, max-age=3600`)·`curl /company/{slug}`(200 정적) 검증. `release.sh`·`nginx/loupit.conf`·`systemd/loupit-api.service` 스크립트/설정 상세는 **위임 SP-INFRA**(순서 계약만 SP-ARCH 소유).
  - 테스트: T8 (bash+curl — `infra/deploy/smoke.sh`, M8) — 4 엔드포인트 200 + `reference/all` 캐시 헤더 assert — RED 먼저
  - refs: SP-ARCH-9 · SP-ARCH-2 · SP-ARCH-3 · INV-3·INV-7 · NFR1·NFR3·NFR22·NFR23·NFR28 · FR-96·FR-91·FR-92

## T-01.6 SPEC 문서군 인덱스·ID 대역 레지스트리  — (SP-ARCH-8, 전 FR)
- [ ] T-01.6.1 SPEC 인덱스·안정 ID 대역 지도·별칭 정합 확정
  - 구현: SPEC 문서군 인덱스(SP-ARCH-8 §7: 12영역·12파일 `docs/SPEC/`, 대역-파일 1:1) + 별칭 정합 레지스트리 고정 — `SP-CALC≡SP-ENGINE`·`SP-DSN≡SP-DS`·구 `SP-RPT` 흡수(값=SP-ENGINE, 렌더/저장=SP-FE). 산출물 = `docs/SPEC/*.md`(12) + `docs/SPEC.md`(얇은 인덱스).
  - 테스트: MT-2 (node:test — `web/test/harness.test.js`) 필수 테스트 파일 존재 린트로 대역-파일 1:1 간접 강제 — **위임 SP-TEST**(SP-TEST-10). 대역 별칭 정합은 문서 교차 리뷰.
  - refs: SP-ARCH-8 · 전 FR(인덱스) · (MT-2 위임 SP-TEST)

## 위임 케이스 (다른 SP 스위트에 실구현 — 본 도메인 이중 구현 없음)
SP-ARCH §9.2의 위임 4종. INV-1~7 회귀 게이트는 아래 대역이 소유·구현하며, 본 문서는 계약(INV)만 고정한다.

| 위임 케이스 | 검증 INV | 실구현 대역·케이스 | 계층/러너 |
| --- | --- | --- | --- |
| T2 API 표면=GET 4종·무쓰기·무인증 | INV-1 | SP-API TS-1·TS-2·TM-1 (Tier-0) | pytest+httpx ASGI |
| T4 reference/all 계약(3키·프로파일러 키 부재·1h 캐시) | INV-2 | SP-API TR-1~3·TR-5 (Tier-0) | pytest+httpx ASGI |
| T5 계산 순수성·밴드 계수(±5/±20/만료+15) | INV-4·INV-5 | SP-ENGINE T-ENGINE-26·45·41 (Tier-0) | node:test |
| T6 생성물 개수(≈95≠200)·비-JS 본문·XSS | INV-3·INV-6 | SP-GEN GC-2·10·21 (Tier-0) | pytest(fake 번들) |

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-01.1.1 | SP-ARCH-6·SP-ARCH-1 | T1 (pytest) | FR-01 · NFR4·NFR20 · AS3 |
| T-01.2.1 | SP-ARCH-7 | 설치 린트(bash) · T2 위임 | F8 · NFR16·NFR20·NFR23·NFR29 · DG-4 |
| T-01.2.2 | SP-ARCH-7 | 부트 린트(bash) · GC 위임 | NFR4·NFR5·NFR21 · AS3·AS4 |
| T-01.3.1 | SP-ARCH-4 | T3 (pytest) | FR-92·FR-D* · INV-2 지지 |
| T-01.4.1 | SP-ARCH-5·SP-ARCH-10 | T7 (bash/curl, M8) | INV-4 · NFR16·NFR17·NFR20 · FR-07 |
| T-01.5.1 | SP-ARCH-9·SP-ARCH-2·SP-ARCH-3 | T8 (bash/curl, M8) | INV-3·INV-7 · NFR1·NFR3·NFR22·NFR23·NFR28 · FR-96 |
| T-01.6.1 | SP-ARCH-8 | MT-2 (위임 SP-TEST) | 전 FR(인덱스) |
| (위임) | SP-ARCH-10 / INV-1 | T2 → SP-API TS-1·TS-2·TM-1 | NFR20 · FR-90 |
| (위임) | SP-ARCH-10 / INV-2 | T4 → SP-API TR-1~3·TR-5 | NFR3·NFR16 · FR-92 |
| (위임) | SP-ARCH-5·10 / INV-4·INV-5 | T5 → SP-ENGINE T-ENGINE-26·45·41 | DEC-2 · FR-05 · §2-8 |
| (위임) | SP-ARCH-10 / INV-3·INV-6 | T6 → SP-GEN GC-2·10·21 | NFR1·NFR24 · FR-06 |
