# TASK 06 — 프론트엔드 구조(SPA) (SP-FE)

> SPEC 계약: [SPEC/06-프론트엔드-구조.md](../SPEC/06-프론트엔드-구조.md) (SP-FE). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M6(프론트 SPA) — 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: SP-ENGINE(`calc.js` 순수 계산 — `compare`/`calc`/`deriveOfferRange`, 어댑터가 소비만) · SP-API(`GET /reference/all`·`companies/search`·`companies/{id}` 응답 형태·캐시 헤더) · SP-DS(`styles.css` 디자인 토큰 — 색·배지/밴드/광고 라벨) · SP-ADS(`mountAds` DOM 계약 — 결과 뷰 하단 슬롯). `web/` 디렉토리 트리는 SP-ARCH-6 정본(파일 추가만, 경계 불변). 확정 순서는 00이 소유.
> 테스트: 러너=`node --test`(node:test, ES module import — 순수 로직 UT-*) + **최소 스텁**(in-memory `localStorage`/`fetch`/`document` 목) · 위치=`web/assets/js/*.test.js` · 케이스 대역=UT-ESC·SORT·NORM·FILL·WS·ROUTE·PRE·STORE·STATE·REF·ASM(20) 자동 + MB-1~15(15 수동 브라우저 체크리스트). 브라우저 통합은 수동(자동 아님).

## 진행 요약
- 리프 총 46 / 완료 36 / 진행 0 / 미착수 10 (2026-07-11 순수모듈 전량 + 2026-07-13 **통합 배선(`ui.js`)·T-06.2.1** 완료. node --test 'web/**/*.test.js' **300 pass**·회귀 0). T-06.14.1~14.10(MB-1~15 수동)은 리프상 `- [ ]` 유지이나, **상당수가 이제 자동 검증됨**: `ui.test.js`(jsdom)가 MB-3·4·5·6·8·15 대역을, gstack 라이브 e2e가 MB-1·3·5·6·8·14·15를 실제 확인(2026-07-13). 잔여 순수 수동(MB-9 최근저장 UX·MB-10 사생활·MB-11 프리필·MB-12 XSS·MB-13 비-JS 육안)은 릴리스 QA에서.
- Tier-0 게이트 리프: T-06.9.1 (UT-ESC-1 escapeHtml), T-06.9.2 (UT-ESC-2 safeUrl), T-06.9.3 (UT-ESC-3 el html 차단) — 전량 green 유지(재구현 없음)
- 선결정 게이트: 없음 (SP-FE는 SPEC이 시그니처·상수·필드매핑까지 확정 — 개방 결정/DG 없음)
- 설계 메모(SP-FE-1.2 규칙3 적용, 임의결정): `search.js`·`inputs.js`의 함수(`onSearchInput`·`selectCompany`·`fillBenefits` 등)는 App.state를 **함수 인자**로 받는다(SPEC 예시 코드는 `App.state`를 모듈 스코프처럼 암묵 참조하나, app.js가 이미 search.js/inputs.js를 import하므로 그 반대 방향 import는 순환이 되어 SP-FE-1.2 규칙4 위반). 규칙3이 "함수 인자 또는 App 네임스페이스" 두 선택지를 명시하므로 함수 인자 경로를 택함 — 의존 방향 app.js→{search,inputs,boot,store,report,ads,calc,dom}는 그대로 단방향 유지, 순수성·테스트 용이성도 향상.
- ~~미배선 항목(리프 범위 밖, 후속 통합 과제)~~ → **✅ 구현 완료(2026-07-13, `cd20712`)**: `compare/index.html`의 DOM 이벤트 배선이 미구현이라 비교툴이 정적 셸만 뜨고 조작 불가였다(gstack 헤드리스 브라우저 QA로 발견 — 순수 유닛 300개는 green이나 실제 브라우저에선 죽어 있던 갭, 곧 MB-* 수동 대역). **`web/assets/js/ui.js` 신규**로 통합 계층 구현: 검색입력→`onSearchInput`, 후보클릭→`selectCompany`, 선택→입력뷰 전진(`maybeAdvance`), **입력뷰 컨트롤 렌더**(연봉레인지·상승률·복지 체크박스·근무형태·통근·우선순위 피커 — 셸의 빈 `#input-slot-*`·`#priority-picker`를 채움), "비교하기"→`runReport`+`go('report')`+`mountAds('result')`, 리포트 내비, 검색상태→셸 메시지 반영. `app.js boot()`가 `mountUI` 호출(+프리필 `go('input')` 덮임 버그 수정). `bindGlobalUI()`는 헤더/푸터/동의배너 자리표시자로 유지(별개 대역). **회귀 방지**: `web/assets/js/ui.test.js`(jsdom 실DOM 통합 14개)가 run_tests.sh 게이트에 편입 — "유닛만 green이고 실제 안 됨" 재발 차단. (시각 폴리시 styles.css는 옵션1대로 별도.)

## T-06.1 모듈 스캐폴드·의존 그래프  — (SP-FE-1, FR-02·03, SP-ARCH-6, INV-4)
- [v] T-06.1.1 프론트 모듈 파일 스캐폴드·공개 심볼 예약·비순환 의존 방향
  - 구현: `web/assets/js/`에 `app.js`·`api.js`·`boot.js`·`search.js`·`inputs.js`·`dom.js`·`store.js`·`report.js` 생성(각 SP-FE-1.1 공개 심볼 export 자리표시자). `calc.js`(위임 SP-CALC)·`ads.js`(위임 SP-ADS)·`styles.css`(위임 SP-DSN)는 생성 금지. 셸은 `app.js` 단일 `<script type=module defer>`만 로드하고 `app.js`가 나머지를 import. 의존 방향(SP-FE-1.2): `calc.js`·`dom.js`는 앱 모듈 import 0, `report.js`는 `dom.js`만, `App.state` 단일 소유는 `app.js`(순환 금지).
  - 테스트: 모듈 import 스모크(`node --test` — `dom.js`·`store.js`·`inputs.js`·`search.js` 순수부가 브라우저 없이 import 성립·순환 없음) — RED 먼저. SP-FE-12.1 전용 UT 없음(구조 스모크); 순수성 회귀는 각 모듈 node:test import 성공으로 지속 검증
  - refs: SP-FE-1.1·1.2 · FR-02·FR-03 · SP-ARCH-6 · INV-4

## T-06.2 SPA 셸·점진적 향상  — (SP-FE-2, FR-02, NFR12·NFR24, SCR-02·94)
- [v] T-06.2.1 `compare/index.html` 셸 골격·3뷰 컨테이너·`<noscript>` 폴백 (2026-07-13: 셸 + `ui.js` 통합 배선을 `ui.test.js` jsdom 실DOM 통합테스트로 자동 검증 — 셸 로드·뷰 전진·컨트롤 렌더 green)
  - 구현: `web/compare/index.html` — `<main id="app" hidden>` + 3 view section(`#view-search`/`#view-input` hidden/`#view-report` hidden, `data-screen`) + `#boot-error` hidden(FR-E1 재시도 자리) + `<noscript id="nojs">`(정적 안내 + `/company/*`·`/vs/*`·랜딩 내부 링크) + `<header data-global>`/`<footer data-global>` + `<script type=module src=/assets/js/app.js defer>`. PE-1~4: `#app` 초기 hidden, 셸에 사용자 데이터·PII 0(정적), 광고 자리는 결과 뷰 하단만.
  - 테스트: MB-1(부팅 시 `#app` 표시)·MB-13(비-JS `<noscript>` 본문·정적 링크 노출) 수동 — 정적 HTML, 자동 UT 대상 아님(수동 게이트 T-06.14.1·T-06.14.8)
  - refs: SP-FE-2.1·2.2 (PE-1~4) · FR-02 · NFR12·NFR24 · SCR-02·SCR-94

## T-06.3 화면 라우팅(`go`·해시/History·popstate)  — (SP-FE-3, FR-02·03, NFR14, FLOW-20·30·40·41)
- [v] T-06.3.1 `parseHash`·`SCREENS` 해시 파서
  - 구현: `app.js` — `const SCREENS=['search','input','report']`; `parseHash()` = `location.hash` 선행 `#` 제거 후 `SCREENS.includes` → 유효 screenId 아니면 `null`.
  - 테스트: UT-ROUTE-1 (`#input`→`'input'`·`#zzz`→`null`·빈문자→`null`) (`node --test`, `location` 스텁) — RED 먼저
  - refs: SP-FE-3.2 · FR-02·FR-03 · FLOW-20·40
- [v] T-06.3.2 `go(screenId)` 뷰 토글·미지뷰 가드·pushState·popstate 핸들러
  - 구현: `app.js` — `go(screenId,{push=true})` = 미지 screenId→`'search'` 정규화, `App.state.ui.screen` 세팅, `SCREENS` 순회 `#view-*.hidden` 토글(재마운트 없음 — 입력 상태 보존), `push`면 `history.pushState({screen},'','#'+screenId)`, `scrollTo(0,0)`, `focusFirstHeading`(NFR14). `popstate` 리스너 = `e.state.screen||parseHash()||'search'`로 `go(...,{push:false})`. 전이 규칙 SP-FE-3.3(`input→report`는 항상 허용, 미지 해시→`search`).
  - 테스트: UT-ROUTE-2 (`go('nope')` → screen `'search'` 정규화) (`node --test`, in-memory `document`/`history` 스텁) — RED 먼저. 뷰전환·뒤로가기 상태보존은 MB-8 수동(T-06.14.5)
  - refs: SP-FE-3.2·3.3 · FR-02·FR-03 · NFR14 · SCR-03~05

## T-06.4 전역 클라이언트 상태 모델  — (SP-FE-4, FR-03·D8, INV-2·INV-4)
- [v] T-06.4.1 `App.state` 초기 shape·`blankWs`·프로파일러 키 부재 (UT-STATE-1)
  - 구현: `app.js` — `App.state` 객체(SP-FE-4.1): `REF:null`·`matched{a,b:null}`·`benS{a,b:[]}`·`wsState{a,b:blankWs()}`·`salS.a{low,high:null}`(연봉, 슬롯 a만 FR-20)·`selectedRate:null`(상승률 FR-24)·`cmtS`(통근 FR-24)·`curPri:'워라밸'`·`curSacrifice:null`(우선순위 FR-25)·`chosenType`·`inputMode{a,b:'company'}`·`ui{screen,searchTimers,searchAborts,searchState}`. `blankWs()`={ot,wage,remote,flex:null}. 각 필드는 입력 위젯이 직접 세팅(controlled input→state, 어댑터가 소비 T-06.10.1). `pfShuffled/pfCur/pfAnswers/pfResult/pfJob` 키 **부재**(SP-FE-4.3).
  - 테스트: UT-STATE-1 (초기 `App.state` 키집합에 프로파일러 키 부재·`curPri==='워라밸'`) (`node --test`) — RED 먼저. 입력→상태 바인딩은 MB-15 흐름(T-06.14.10)
  - refs: SP-FE-4.1·4.2·4.3 · FR-03·FR-20·FR-24·FR-25·FR-D8 · INV-2·INV-4

## T-06.5 부팅 시퀀스·`reference/all` 로드·캐시  — (SP-FE-5, FR-02·E1, SP-API-9, NFR3·26)
- [v] T-06.5.1 `loadReference`·`assertRefShape` 3키 검증 (UT-REF-1)
  - 구현: `boot.js` — `export async function loadReference()` = `getReference()`(api.js) → `assertRefShape(ref)` → 반환. `assertRefShape` = `Array.isArray(company_types)` && `benefit_presets` object && `Array.isArray(companies)` 아니면 `throw new Error('REF_SHAPE')`(손상 번들→부팅 오류 전이).
  - 테스트: UT-REF-1 (3키 정상→통과 / `companies` 결측→throw) (`node --test`) — RED 먼저
  - refs: SP-FE-5.2 (B-1) · FR-02·FR-E1 · SP-API-9 · INV-2
- [v] T-06.5.2 `boot()` 오케스트레이션·`showBootError` 재시도(FR-E1)
  - 구현: `app.js` — `async function boot()` = `bindGlobalUI()` → `try App.state.REF=await loadReference()` `catch return showBootError(err)`(#boot-error 표시·#app 계속 hidden·재시도 버튼이 `loadReference` 재호출 후 나머지 이어감, B-3·B-4) → `restoreFromPrefill()` → `#app.hidden=false` → `go(parseHash()||'search',{push:false})`. `DOMContentLoaded→boot`. 부팅당 REF 정확히 1회(B-1), 브라우저 캐시 1h 흡수(B-2).
  - 테스트: MB-1(REF 1회 로드·Network)·MB-2(REF 실패→#boot-error·무크래시) 수동(T-06.14.1·T-06.14.2). SP-FE-12.1 전용 UT 없음(브라우저 오케스트레이션); shape 실패 전이는 UT-REF-1(T-06.5.1)이 커버
  - refs: SP-FE-5.1 (B-1~B-5) · FR-02·FR-E1 · SP-API-9 · NFR3·NFR26

## T-06.6 읽기전용 API 클라이언트(`apiFetch`, 인증 헤더 0)  — (SP-FE-6, FR-02, SP-API-9~13, NFR16·NFR20, INV-1·INV-4)
- [v] T-06.6.1 `apiFetch` 전송 계약·`ApiError`·타임아웃/abort·무인증
  - 구현: `api.js` — `apiFetch(path,{signal,timeout=8000})` = `AbortController`+`setTimeout(abort,timeout)`, 외부 `signal.abort`→ctrl.abort 연결(경합 취소), `fetch(API_BASE+path,{method:'GET',headers:{Accept:json},credentials:'omit'})`(Authorization/Cookie 미부착 NFR16), `!res.ok→throw new ApiError(status,path)`, `finally clearTimeout`. `class ApiError extends Error`. **쓰기 헬퍼(post/put/delete) 미노출**(INV-1·NFR20). `API_BASE='/api/v1'`(동일 오리진).
  - 테스트: `apiFetch` 유닛(global `fetch` 목: 200 json 반환·비200→`ApiError`·timeout→abort·요청 헤더에 Authorization/Cookie 부재·`credentials:'omit'`) (`node --test`) — RED 먼저. SP-FE-12.1 전용 UT 없음; 무전송 불변식 게이트는 MB-14 수동(T-06.14.9)
  - refs: SP-FE-6.1 (규칙1·2·4) · FR-02 · SP-API-9 · NFR16·NFR20 · INV-1·INV-4
- [v] T-06.6.2 엔드포인트 래퍼 `getReference`·`searchCompanies`·`getCompany`·`encodeURIComponent`
  - 구현: `api.js` — `getReference=(opt)=>apiFetch('/reference/all',opt)`, `searchCompanies=(q,opt)=>apiFetch('/companies/search?q='+encodeURIComponent(q),opt)`, `getCompany=(id,opt)=>apiFetch('/companies/'+encodeURIComponent(id),opt)`. 3종 GET 소비(health 제외), 무결과(200 `[]`)와 오류(예외/비200)를 호출부가 구분(규칙5).
  - 테스트: 래퍼 유닛(`apiFetch` 목: `searchCompanies`가 `q`를 `encodeURIComponent`로 인코딩·경로 조립·`getCompany` id 인코딩) (`node --test`) — RED 먼저. SP-FE-12.1 전용 UT 없음; 서버 실계약(TSE-*·TC-*)은 SP-API 위임
  - refs: SP-FE-6.1·6.2 (규칙3·5) · FR-02·FR-10 · SP-API-10·11·12

## T-06.7 검색 오케스트레이션(디바운스·경합·후보 렌더)  — (SP-FE-7, FR-10~13, SP-API-10, NFR14·NFR21)
- [v] T-06.7.1 `sortCandidates` 결정적 정렬 (UT-SORT-1)
  - 구현: `search.js` — `sortCandidates(items,q)` = `rank(완전일치 0 < 접두 1 < 그외 2)` → `comp_nm.length` 오름 → `localeCompare(...,'ko')`(가나다). `rank(it,nq)` 헬퍼(소문자 비교). 서버 순서 비의존(FR-12 R3).
  - 테스트: UT-SORT-1 (완전일치·접두·기타 혼합 `q='삼성'` → 완전일치→접두→길이→가나다 결정적 순서) (`node --test`) — RED 먼저
  - refs: SP-FE-7.2 (S-5) · FR-12 · SP-API-10
- [v] T-06.7.2 `dedupById` 방어적 중복 제거 (UT-SORT-2)
  - 구현: `search.js` — `dedupById(items)` = `Set`으로 `comp_id` 최초 1건만 유지(선순위 보존, FR-11 R3).
  - 테스트: UT-SORT-2 (동일 `comp_id` 3건 → 1건 잔존) (`node --test`) — RED 먼저
  - refs: SP-FE-7.2 (S-5) · FR-11
- [v] T-06.7.3 `onSearchInput` 디바운스·빈쿼리 가드
  - 구현: `search.js` — `DEBOUNCE_MS=300`; `onSearchInput(slot,raw)` = `q=raw.trim()`, `clearTimeout(searchTimers[slot])`, `q.length<1`→`closeCandidates(slot)`+return(미호출 S-2), 아니면 `setSearchState(slot,'loading')`+`setTimeout(runSearch,300)`(슬롯 독립 타이머). `setSearchState`/`closeCandidates` 헬퍼.
  - 테스트: 디바운스 유닛(fake timer: `<1`자 미호출·300ms 후 1회·연속 입력 시 타이머 리셋) (`node --test`, timer 목) — RED 먼저. SP-FE-12.1 전용 UT 없음; 브라우저 확인 MB-3(T-06.14.3)
  - refs: SP-FE-7.1 (S-1·S-2) · FR-10·FR-11
- [v] T-06.7.4 `runSearch` 경합 폐기·무결과/오류 구분
  - 구현: `search.js` — `runSearch(slot,q)` = 이전 `searchAborts[slot].abort()`, 새 `AbortController` 저장, `await searchCompanies(q,{signal})`, `searchAborts[slot]!==ctrl`면 폐기(최신 아님), `sortCandidates(dedupById(items))`, `length? renderCandidates : setSearchState(slot,'empty')`(FR-13 무결과), `catch`에서 `aborted`면 무시(경합) 아니면 **`bundleFallbackSearch(slot,q)` 시도(FR-E2, T-06.7.6)** — 폴백 후보 있으면 렌더, 없으면 `setSearchState(slot,'error')`(오류≠무결과 S-3).
  - 테스트: `runSearch` 유닛(`searchCompanies` 목: `[]`→`'empty'`·throw→폴백 시도·stale 응답 폐기) (`node --test`) — RED 먼저. SP-FE-12.1 전용 UT 없음; 브라우저 확인 MB-4(T-06.14.3)
  - refs: SP-FE-7.1 (S-1·S-3) · FR-12·FR-13·FR-E2 · SP-API-10
- [v] T-06.7.5 `renderCandidates` 안전 렌더·20 상한·키보드
  - 구현: `search.js` — `renderCandidates(slot,items,q)` = `list.replaceChildren()`, `items.slice(0,20)`(상한 S-5), `el('li',{class,role:'option',tabindex:'0'})`에 `cand-nm text=comp_nm`·`cand-meta text=typeLabel+' · '+industry_nm`(모두 textContent NFR21), `click`/`keydown Enter`→`selectCompany(slot,comp_id)`, `setSearchState(slot,'results')`. `typeLabel(comp_tp_cd)` 헬퍼.
  - 테스트: 후보 렌더 유닛(`document` 스텁: 20 상한·`textContent` 삽입·`comp_id` 클릭 바인딩) (`node --test`) — RED 먼저. 안전삽입 회귀는 UT-ESC-3(el, T-06.9.3); XSS 무실행 MB-12(T-06.14.7)
  - refs: SP-FE-7.3 (S-4·S-6) · FR-11·FR-12 · NFR14·NFR21
- [v] T-06.7.6 `bundleFallbackSearch` 검색 API 실패 시 REF 번들 폴백 매칭 (UT-SEARCH-FB-1) — FR-E2
  - 구현: `search.js` — `bundleFallbackSearch(slot,q)` = REF 로드 후 `companies/search` 실패 시 클라이언트 폴백. `App.state.REF.companies`에서 `comp_nm`/`comp_eng_nm`/`aliases` 대상 `q` 완전·접두·부분 일치 매칭 → `sortCandidates(dedupById(items),q)` → `length? (renderCandidates(slot,items,q)+setSearchState(slot,'results')) : setSearchState(slot,'error')`. 네트워크 무의존(NFR26 오프라인 계산 연동)·읽기전용·무전송(INV-4). `runSearch`(T-06.7.4) 오류 경로에서 호출. 동작 정본 = FRD 12 FR-E2("REF 로드 시 번들 폴백 매칭", 무결과와 구분), SPEC.md §3 SP-FE=FR-E1~E3 배정(SPEC/06 미상세분 보완).
  - 테스트: UT-SEARCH-FB-1 (`searchCompanies` throw 목 + `REF.companies` 스텁 → 이름/별칭 일치 후보 반환·`'results'` 전이 / 폴백 0건→`'error'`) (`node --test`) — RED 먼저. 브라우저 확인 MB-4(오류 vs 폴백, T-06.14.3)
  - refs: SP-FE-7.1 (S-3 확장) · FR-E2·FR-D6·FR-D11 · NFR26 · UC-91 · INV-4

## T-06.8 회사 선택·정규화·프리셋 채움(inputs.js)  — (SP-FE-8, FR-14~17·21·23, FR-D8·D7·D11, FR-06)
- [v] T-06.8.1 `normalizeCompany`·`normalizeBenefit` 통일 shape·폴백 (UT-NORM-1·2)
  - 구현: `inputs.js` — `normalizeCompany(raw)` = API/번들/프리셋 회사를 동일 shape로(comp_id..logo_nm·work_style_val·aliases[]·benefits.map(normalizeBenefit)). `normalizeBenefit(b)` = `qual_yn` 불리언 강제, `benefit_amt` 정성이면 null, `benefit_ctgr_cd` 미상→`'perks'`(CTGR9 set), `badge_cd` 미상→`'est'`, `amt_source` 미상→`estimated`(정성이면 `none`, AMT3 set), `checked/value_source` undefined(fill 단계 부여). `CTGR9`/`AMT3` 상수 set.
  - 테스트: UT-NORM-1 (카테고리·배지 미상·`qual_yn=1`→`perks`·`est`·`true`·`benefit_amt=null`)·UT-NORM-2 (`amt_source` 미상 비정성→`estimated`, 정성→`none`) (`node --test`) — RED 먼저
  - refs: SP-FE-8.2 (N-1·N-3) · FR-D8.2·FR-14 · DEC-2
- [v] T-06.8.2 `fillBenefits` 회사/프리셋/빈 분기 (UT-FILL-1·2)
  - 구현: `inputs.js` — `fillBenefits(slot)` = `matched[slot]`이면 `benefits.map(b=>({...b,checked:true,value_source:'real'}))`(회사 실데이터 UC-14); `chosenType[slot]`이면 `REF.benefit_presets[type].map(p=>({...normalizeBenefit(p),checked:!!p.default_checked_yn,value_source:'preset'}))`(직접입력 프리셋 FR-06); 둘 다 아니면 `[]`. `value_source`(real/preset/user)와 `amt_source`(밴드용) 독립(N-3).
  - 테스트: UT-FILL-1 (회사 지정 → 각 항목 `checked===true`·`value_source==='real'`)·UT-FILL-2 (직접입력+유형 → `checked=default_checked_yn`·`value_source==='preset'`) (`node --test`, `App.state` 스텁) — RED 먼저. 항목 체크 토글·편집(FR-22)은 `benS[slot][i].checked` 세터·MB-15 흐름
  - refs: SP-FE-8.3 (N-2·N-4) · FR-21·FR-22·FR-06·FR-D8.1·FR-E4
- [v] T-06.8.3 `initWsState` 근무형태 제안 (UT-WS-1)
  - 구현: `inputs.js` — `initWsState(slot)` = `ws=matched[slot]?.work_style_val`; `{ot:null,wage:null,remote: ws?.remote? proposeRemote(ws.remote):null, flex: ws?.flex? proposeFlex(ws.flex):null}`(야근·임금유형은 사용자 입력 미선택, 원격/유연은 회사값 제안 FR-23). `proposeRemote`/`proposeFlex` 헬퍼.
  - 테스트: UT-WS-1 (`work_style_val.remote='hybrid'` → `remote` 제안값 세팅·`ot/wage=null`) (`node --test`) — RED 먼저
  - refs: SP-FE-8.3 · FR-23
- [v] T-06.8.4 `selectCompany` 반영·`sameCompanyGuard`·`clearSlot`
  - 구현: `search.js`/`inputs.js` — `selectCompany(slot,compId)` = `REF.companies.find`(인라인 우선, 호출 0) → 실패 시 `getCompany`(폴백 FR-D7) → catch `showSlotError`(FR-D11 미선택 유지) → `sameCompanyGuard`(양 슬롯 동일 comp_id 보류 FR-15) → `matched=normalizeCompany`·`inputMode='company'`·`chosenType=null`·`fillBenefits`·`initWsState`·`reflectSlotLabel`·`closeCandidates`·`maybeAdvance`. `sameCompanyGuard(slot,compId)` = 반대 슬롯 동일 comp_id면 `notify`+`true`(반영 보류). `clearSlot(slot)` = 초기값 복귀(FR-15 해제).
  - 테스트: `selectCompany` 유닛(REF 인라인 매칭·동일회사 보류·미존재 폴백 실패 시 미선택 유지) (`node --test`, `REF`/`getCompany` 목) — RED 먼저. 정규화는 UT-NORM(T-06.8.1); 브라우저 확인 MB-5·MB-6(T-06.14.4)
  - refs: SP-FE-8.1 (N-5·N-6) · FR-14·FR-15·FR-16 · FR-D7·FR-D11
- [v] T-06.8.5 `setDirectType` 직접입력 모드 유형 선택(FR-17)
  - 구현: `inputs.js` — `setDirectType(slot,comp_tp_cd)` = `inputMode='direct'`·`matched[slot]=null`(회사와 상태 구분 N-4)·`chosenType=comp_tp_cd`·`fillBenefits(slot)`(프리셋 채움)·`initWsState(slot)`(matched null→ws 미제안). 직접입력 모드는 회사 선택과 상태상 구분(matched null 유지).
  - 테스트: `setDirectType` 유닛(`inputMode==='direct'`·`matched===null`·`chosenType` 세팅) (`node --test`) — RED 먼저. 프리셋 채움 회귀는 UT-FILL-2(T-06.8.2); 브라우저 확인 MB-7(T-06.14.4)
  - refs: SP-FE-8.3 (N-4) · FR-17·FR-E4

## T-06.9 렌더 안전 유틸(`dom.js`)  — (SP-FE-9.1·9.2, FR-45, NFR21)
- [v] T-06.9.1 `escapeHtml`·`h` 태그드 템플릿 (UT-ESC-1) (Tier0)
  - 구현: `dom.js` — `escapeHtml(s)` = `String(s??'')`의 `& < > " '` 치환. `h(strings,...values)` = 정적부는 그대로·보간값은 `escapeHtml` 후 결합(정적 innerHTML에 안전). 데이터 문자열은 `h`/`textContent`로만 삽입(R-1·R-2).
  - 테스트: UT-ESC-1 (`<img src=x onerror=alert(1)>` → `&lt;img …&gt;`, `<`/`>`/`"`/`'`/`&` 전부 치환) (`node --test`) — RED 먼저
  - refs: SP-FE-9.1·9.2 (R-1·R-2·R-5) · FR-45 · NFR21
- [v] T-06.9.2 `safeUrl` 스킴 화이트리스트 (UT-ESC-2) (Tier0)
  - 구현: `dom.js` — `safeUrl(u)` = `new URL(u,location.origin)`, `protocol∈{http:,https:}`면 `url.href` 아니면 `null`(파싱 실패도 null). `javascript:` 등 위험 스킴 링크 미생성(R-3).
  - 테스트: UT-ESC-2 (`javascript:alert(1)`→`null` / `https://x.co/a`→원 URL) (`node --test`) — RED 먼저
  - refs: SP-FE-9.1·9.2 (R-3) · FR-45·FR-41 · NFR21
- [v] T-06.9.3 `el` 안전 DOM 빌더·raw html 차단 (UT-ESC-3) (Tier0)
  - 구현: `dom.js` — `el(tag,opts,...children)` = `createElement`, `text`→`textContent`(데이터는 항상 textContent), `html`→`throw new Error`(데이터 보간 innerHTML 차단), `class`→`className`, 그외 `setAttribute`(자동 이스케이프), children `append`.
  - 테스트: UT-ESC-3 (`el('div',{html:'<b>'})` → 예외 던짐) (`node --test`) — RED 먼저
  - refs: SP-FE-9.1·9.2 (R-2) · FR-45 · NFR21

## T-06.10 엔진 호출·상태 조립(`assembleCompareState`)  — (SP-FE-9.3, FR-40·42, SP-ENGINE-1·2, FR-D8)
- [v] T-06.10.1 `assembleCompareState`·`salToStr`·`PRI_KEY` 어댑터 (UT-ASM-1)
  - 구현: `app.js` — `PRI_KEY={'연봉':'salary','워라밸':'wlb','복지':'benefits','브랜드':'brand'}`. `salToStr({low,high})` = `low`/`high` null이면 `null` 아니면 `low+'-'+high`. `assembleCompareState(state)` = App.state→`CompareState`(SP-FE-9.3 필드매핑): `salStr:salToStr(salS.a)`(슬롯 a만), `selectedRate`(슬롯 b는 rate 파생 — 절대연봉 미전달 A-2), `benS`/`wsState`/`matched` pass-through, `com:{a:cmtS.a??0,b:cmtS.b??0}`(null→0 A-4), `curPri:PRI_KEY[curPri]||'wlb'`, `curSacrifice:PRI_KEY[...]||null`, `companyTypes:REF.company_types`. 유일 변환점(A-1).
  - 테스트: UT-ASM-1 (`salS.a={5000,7000}`·`cmtS={a:null,b:30}`·`curPri='워라밸'`·`selectedRate=10` → `salStr==='5000-7000'`·`com.a===0`·`com.b===30`·`curPri==='wlb'`·`companyTypes===REF.company_types`·슬롯 b 절대연봉 미전달) (`node --test`) — RED 먼저
  - refs: SP-FE-9.3 (A-1~A-5) · FR-40·FR-42 · SP-ENGINE-1·2·2.1·4 · FR-D8
- [v] T-06.10.2 `runReport` 재계산 오케스트레이션(조립→compare→렌더)
  - 구현: `app.js` — `runReport()` = `compare(assembleCompareState(App.state))`(SP-ENGINE-2.2 Report) → `renderReport(report,#view-report,{benS,matched})`. `compare`/`calc`는 `calc.js`에서 import(**위임 SP-ENGINE — 재구현 금지**), `renderReport`는 `report.js`(T-06.11). FR-42 입력 변경 시 재조립·재계산.
  - 테스트: `runReport` 유닛(`compare` 목·`renderReport` 목: 어댑터→계산→렌더 호출 순서·인자) (`node --test`) — RED 먼저. SP-FE-12.1 전용 UT 없음; 엔진 값 정확도는 SP-ENGINE(T-ENGINE-*) 위임, 통합 MB-15(T-06.14.10)
  - refs: SP-FE-9.3 · FR-42 · SP-ENGINE-2.2

## T-06.11 리포트 DOM 렌더(`report.js` — 구 SP-RPT 흡수)  — (SP-FE-9.4, FR-40·41·42·45, NFR21, SP-ENGINE-2.2·13)
- [v] T-06.11.1 `renderReport` 골격·멱등 재구성·카드 순서(FR-40)
  - 구현: `report.js` — `renderReport(report,mountEl,ctx)` = `mountEl.replaceChildren()`(멱등) 후 FR-40 순서로 블록 조립: 판정카드→총보상+밴드→시간조정 가치→워라밸·자율성→카테고리 델타표→정성표→항목 배지/밴드→경고 배너. `calc.js` import 금지(값은 이미 계산됨), `dom.js`(`el`/`h`/`safeUrl`)만 사용. 모든 데이터 문자열 `el({text})`/`h`로 삽입(RP-4).
  - 테스트: `renderReport` 유닛(`document` 스텁: 블록 존재·`replaceChildren` 멱등 재호출) (`node --test`) — RED 먼저. 데이터 이스케이프 회귀 UT-ESC(T-06.9); 값 정확도 SP-ENGINE 위임; 통합 MB-15(T-06.14.10)
  - refs: SP-FE-9.4 (렌더 구성표·RP-4) · FR-40 · NFR21 · SP-ENGINE-2.2
- [v] T-06.11.2 `renderVdCard` 판정카드·승자색·tie/limited
  - 구현: `report.js` — `renderVdCard(vdCard,mountEl)` = 축 라벨·두 관점(p1/p2) 승자→색(승 `--green`/열위 `--red`/`tie` `--t3`/브랜드 `--purple`, SP-DSN A3 — **색 토큰 위임**), 엔진 `detail` 수치로 문구 조립(엔진은 수치만), `tie`/근소차 단정 회피(UC-33 3a), `vdCard.limited`(brand)면 "한 회사 미선택 — 제한" 주석. `report.sacrifice` 병기.
  - 테스트: `renderVdCard` 유닛(`document` 스텁: 승자 클래스·`tie` 표기·`limited` 주석) (`node --test`) — RED 먼저. SP-FE-12.1 전용 UT 없음; 판정 수치는 SP-ENGINE-13 위임; MB-15(T-06.14.10)
  - refs: SP-FE-9.4 (RP-5) · FR-42 · SP-ENGINE-13 · SP-DSN A3
- [v] T-06.11.3 `renderCatDelta` 9카테고리 델타표
  - 구현: `report.js` — `renderCatDelta(catDelta,mountEl)` = `report.catDelta[]{ctgr,sumA,sumB,delta}`를 9개 카테고리 **고정 순서** a·b 합·델타 표로. 미상→`perks`는 엔진(`benByCat`)이 이미 처리.
  - 테스트: `renderCatDelta` 유닛(`document` 스텁: 9행 고정 순서·sumA/sumB/delta 셀) (`node --test`) — RED 먼저. SP-FE-12.1 전용 UT 없음; MB-15(T-06.14.10)
  - refs: SP-FE-9.4 · FR-40 · SP-ENGINE-2.2
- [v] T-06.11.4 `renderBands` 항목 배지·밴드 표시·`safeUrl` 출처(FR-41)
  - 구현: `report.js` — `renderBands(slotResult,benItems,mountEl)` = 항목 배지 `badge_cd`(`official`→"공식"·`est`→"추정")+만료(`expires_dtm` 경과→"만료" 경고색 RP-2), 밴드는 엔진 `totalRange`/`sumBand`(amt_source 기준 ±5/±20/만료+15 DEC-2) **표시만**(계수 재계산 금지 RP-1), 출처 `badge_src_url_ctnt`→`safeUrl` `http/https`만 링크화(RP-3).
  - 테스트: `renderBands` 유닛(`document` 스텁: 배지 라벨·만료 경고·`safeUrl` 링크화/위험스킴 텍스트) (`node --test`) — RED 먼저. `safeUrl` 회귀 UT-ESC-2(T-06.9.2); 밴드 계수 SP-ENGINE 위임; MB-15(T-06.14.10)
  - refs: SP-FE-9.4 (RP-1·RP-2·RP-3) · FR-41·DEC-2 · SP-DSN SP-DS-3
- [v] T-06.11.5 경고 배너 코드→문구 매핑·정성 복지표
  - 구현: `report.js` — `WARN_COPY` 매핑(`eff_shrink`→"연봉이 올라도 실질 보상 차이가 줄어든다"·`both_inclusive`→"양측 포괄임금"…) `report.warnings[]` 코드→문구(미지 코드 방어), 코드→문구 매핑은 **본 절 소유**(엔진은 코드만). 정성 복지표 `report.qual.a/b[]{benefit_nm,benefit_ctgr_cd,qual_desc}`(`benefit_nm`·`qual_desc` 이스케이프 FR-45).
  - 테스트: 경고문구 매핑 유닛(순수 `WARN_COPY[code]` 분리: 코드→문구·미지코드 방어) (`node --test`) — RED 먼저. 정성표 이스케이프 회귀 UT-ESC(T-06.9); MB-15(T-06.14.10)
  - refs: SP-FE-9.4 (경고배너·정성표·RP-4) · FR-40·FR-45 · NFR21
- [v] T-06.11.6 "최근 비교" 저장/불러오기 UI 렌더(리포트 하단, RP-6)
  - 구현: `report.js` — 리포트 하단 "최근 비교" 요약 콘텐츠·저장/불러오기/삭제 UI 렌더. `recent.save/list/removeById`(SP-FE-10, T-06.12) 호출. `RecentComparison`의 `input`/`result` **필드 구성은 FR-43** 소유(경계), 봉투 무결성·FIFO·시그니처는 T-06.12. 복원 문자열은 신뢰 불가로 이스케이프(L-6).
  - 테스트: 저장/불러오기 UI 렌더 유닛(`document`+localStorage 목: 목록 표시·복원 클릭 콜백) (`node --test`) — RED 먼저. 봉투 무결성 회귀 UT-STORE(T-06.12); 브라우저 확인 MB-9(T-06.14.6)
  - refs: SP-FE-9.4 (RP-6) · FR-43 · SP-FE-10

## T-06.12 localStorage(`store.js`)·"최근 비교" 봉투  — (SP-FE-10, FR-07·43·44, NFR16·17·25)
- [v] T-06.12.1 `store` 저수준 래퍼 try/catch 흡수 (UT-STORE-4)
  - 구현: `store.js` — `store.get(key)`(getItem→`JSON.parse` try/catch→null), `store.set(key,value)`(`JSON.stringify`+setItem try/catch→false, QuotaExceeded/비활성 흡수 FR-44), `store.remove(key)`(try/catch), `store.available()`(probe write/remove→bool). 모든 접근 try/catch(L-5).
  - 테스트: UT-STORE-4 (`setItem` throw 목 → `set` `false` 반환·앱 계속) (`node --test`, in-memory `localStorage` 목) — RED 먼저
  - refs: SP-FE-10.1 (L-1·L-5) · FR-07·FR-44·FR-E3 · NFR16·NFR25
- [v] T-06.12.2 `recent.save` FIFO·dedup·봉투 v:1 (UT-STORE-1·2)
  - 구현: `store.js` — `RECENT_KEY='loupit.recentComparisons'`·`RECENT_V=1`·`MAX_RECENT=10`. `recent.save(record)` = `store.available()` 아니면 false(FR-44), `list()`, `signatureOf` dedup 시 `splice` 제거, `unshift`(신규 선두), `while length>MAX_RECENT pop`(말미 축출 FIFO), `store.set({v:1,items})`. `signatureOf(r)` = 양 슬롯 회사(comp_id)/direct + `result.priAxis`.
  - 테스트: UT-STORE-1 (11건 저장 → 길이 10·최오래건 축출·최신 선두)·UT-STORE-2 (동일 시그니처 재저장 → 추가 없이 갱신·선두 이동·길이 불변) (`node --test`) — RED 먼저
  - refs: SP-FE-10.2 (L-3·L-4) · FR-43 R1·R2·R3
- [v] T-06.12.3 `recent.list` 손상 폐기·`isValidEnvelope`/`isValidRecord` (UT-STORE-3)
  - 구현: `store.js` — `recent.list()` = `store.get`, `isValidEnvelope`(`v===1`&&`Array.isArray(items)`) 아니면 `store.remove`+`[]`(R4 손상/버전불일치 폐기), `filter(isValidRecord)`(개별 손상 무시). `isValidRecord(r)` = `id`/`savedAt` string·`slots`/`input`/`result` 존재. `recent.removeById`/`recent.clear`.
  - 테스트: UT-STORE-3 (`{v:1,items:[불량레코드]}`→불량 제외 / `{v:2,...}`→봉투 폐기 빈배열 / 비-JSON→빈배열 무크래시) (`node --test`) — RED 먼저
  - refs: SP-FE-10.2 (L-2·L-6) · FR-43 R4·FR-44 1c

## T-06.13 URL 파라미터 프리필(정적 CTA → 비교 툴)  — (SP-FE-11, FR-16·D11, INV-4)
- [v] T-06.13.1 `resolveCompanyToken` 지시자 해석 (UT-PRE-1)
  - 구현: `app.js` — `resolveCompanyToken(token)` = `/^\d+$/`이면 `comp_id===Number` find; 아니면 `comp_eng_nm` 소문자 일치 → `comp_nm` 완전일치 → `aliases.includes` → `null`. REF 인라인 우선(상세 API 호출 없이 프리필 P-1).
  - 테스트: UT-PRE-1 (`'42'`/영문식별자/정식명/별칭/미존재 → 각 매칭 회사 / `null`) (`node --test`, `REF` 스텁) — RED 먼저
  - refs: SP-FE-11.2 (P-1) · FR-16·FR-D4
- [v] T-06.13.2 `restoreFromPrefill` 슬롯 반영·`go('input')`
  - 구현: `app.js` — `restoreFromPrefill()` = `URLSearchParams(location.search)`, `a`/`b` 각 `token`→`resolveCompanyToken`→성공 시 `matched=normalizeCompany`+`fillBenefits`+`initWsState`+`reflectSlotLabel`(FR-14 동일 정규화 P-2), 미해석은 미선택 유지(정상 검색 폴백 P-3), `matched.a||matched.b`면 `go('input',{push:false})`. GET·읽기전용·무전송(P-5). `slot` 파라미터/CTA 링크 **생성은 SP-GEN 위임**.
  - 테스트: `restoreFromPrefill` 유닛(`location.search` 스텁: 유효/무효 지시자·프리필 시 input 진입·무효는 미선택) (`node --test`) — RED 먼저. 해석 회귀 UT-PRE-1(T-06.13.1); 브라우저 확인 MB-11(T-06.14.5)
  - refs: SP-FE-11.2 (P-2·P-3·P-4·P-5) · FR-16·FR-D11 · INV-4

## T-06.14 수동 브라우저 체크리스트(SP-FE-12.2 — 자동 아님)  — (SP-FE-12, FR-02·10~17·43·44·45·E1, NFR14·21·25·26, INV-2·4)
- [ ] T-06.14.1 MB-1 부팅·REF 1회 로드·`#app` 표시
  - 구현: (수동 검증 — 신규 코드 없음) `/compare` 진입 시나리오 실행.
  - 테스트: MB-1 (수동 브라우저) — Network 탭 `reference/all` 1회·`#app` 표시·검색 뷰 진입. 릴리스 게이트 항목
  - refs: SP-FE-2·5 · FR-02 · SP-API-9
- [ ] T-06.14.2 MB-2 REF 실패 무크래시(#boot-error)
  - 구현: (수동 검증) 오프라인/5xx 강제 후 부팅.
  - 테스트: MB-2 (수동 브라우저) — `#boot-error` 재시도 UI·`#app` 숨김·크래시 없음. **릴리스 게이트**(핵심 안전 FR-E1)
  - refs: SP-FE-5.1 (B-3·B-4) · FR-E1 · NFR26
- [ ] T-06.14.3 검색 디바운스·경합·무결과 vs 오류 (MB-3·MB-4)
  - 구현: (수동 검증) 슬롯 타이핑·연속 입력·없는 검색어·서버 중단 시나리오.
  - 테스트: MB-3(300ms 후 1회·최신만 반영)·MB-4(무결과 문구+직접입력 안내 vs 오류 문구+재시도, 구분 FR-13) (수동 브라우저) — MB-4 **릴리스 게이트**
  - refs: SP-FE-7 · FR-10·FR-13 · SP-API-10
- [ ] T-06.14.4 회사 선택·동일회사·직접입력 (MB-5·MB-6·MB-7)
  - 구현: (수동 검증) 회사 선택·양 슬롯 동일 회사·직접입력+유형 시나리오.
  - 테스트: MB-5(입력란=정식명·복지 자동채움·근무형태 제안)·MB-6(반영 보류+다른 회사 안내 FR-15)·MB-7(matched=null 유지·프리셋 채움 FR-17) (수동 브라우저) — MB-6 **릴리스 게이트**
  - refs: SP-FE-8 · FR-14·FR-15·FR-17
- [ ] T-06.14.5 뷰 전환·뒤로가기·프리필 (MB-8·MB-11)
  - 구현: (수동 검증) `search↔input↔report` 해시·`popstate`·`?a=..&b=..` 프리필 시나리오.
  - 테스트: MB-8(해시 반영·`popstate` 뒤로가기 뷰 복원·상태 보존)·MB-11(`?a=삼성전자&b=SK하이닉스` 두 슬롯 지정·입력 뷰 진입 FR-16) (수동 브라우저)
  - refs: SP-FE-3·11 · FR-02·FR-03·FR-16
- [ ] T-06.14.6 최근 비교 저장/복원/삭제·사생활 모드 (MB-9·MB-10)
  - 구현: (수동 검증) 저장→목록→복원→11번째 축출·localStorage 차단 시나리오.
  - 테스트: MB-9(저장 후 목록·복원 시 입력 재현·11번째 최오래건 축출)·MB-10(사생활 모드 저장 UI 비활성·비교/리포트 정상 FR-44) (수동 브라우저) — MB-10 **릴리스 게이트**
  - refs: SP-FE-9.4·10 · FR-43·FR-44 · NFR25
- [ ] T-06.14.7 MB-12 XSS 페이로드 무실행
  - 구현: (수동 검증) 회사명/복지명에 `<script>`·`<img onerror>` 주입 후 렌더.
  - 테스트: MB-12 (수동 브라우저) — 문자 그대로 표시·스크립트 미실행. **릴리스 게이트**(NFR21). 자동 회귀는 UT-ESC-1~3(T-06.9, Tier0)
  - refs: SP-FE-9.1·9.2 (R-5) · NFR21
- [ ] T-06.14.8 MB-13 비-JS 폴백 노출
  - 구현: (수동 검증) 스크립트 차단 후 `/compare` 진입.
  - 테스트: MB-13 (수동 브라우저) — `<noscript>` 본문·정적 링크 노출·빈 화면 아님(SCR-94)
  - refs: SP-FE-2.2 (PE-2) · FR-E5 · NFR12·NFR24 · SCR-94
- [ ] T-06.14.9 MB-14 무전송 불변식(쓰기 메서드 0)
  - 구현: (수동 검증) 비교 흐름 전체 중 Network 관찰.
  - 테스트: MB-14 (수동 브라우저) — `/api/v1`로 POST/PUT/PATCH/DELETE·본문 업로드 0. **릴리스 게이트**(INV-4)
  - refs: SP-FE-6.1 · INV-4·NFR16 · SP-API
- [ ] T-06.14.10 MB-15 리포트 렌더·curPri 부분 갱신
  - 구현: (수동 검증) 비교 실행 후 리포트 확인·`curPri` 전환.
  - 테스트: MB-15 (수동 브라우저) — 판정카드(승자 색)·총보상+밴드·시간조정·워라밸·9카테고리 델타표·정성표·항목 배지(공식/추정/만료) 표시, `curPri` 변경 시 판정카드만 갱신(FR-42)
  - refs: SP-FE-9.4 · FR-40·FR-41·FR-42 · SP-ENGINE-2.2

## 위임 케이스 (다른 SP 스위트에 실구현/검증 — 본 도메인 이중 구현 없음)
- **SP-ENGINE(SP-CALC) 위임**: `calc.js` 순수 계산(`compare`/`calc`/`deriveOfferRange`·실효연봉·시간가치·밴드 계수 ±5/±20/만료+15 DEC-2·`buildVdCard` 판정 수치·희생). 본 도메인은 `assembleCompareState`(T-06.10.1)로 입력을 조립하고 `Report`(SP-ENGINE-2.2)를 **소비·렌더만**(T-06.11). 밴드·vdCard 수치 정확도 회귀는 T-ENGINE-*(SP-ENGINE).
- **SP-API 위임**: `GET /reference/all`·`companies/search`·`companies/{id}` 서버 계약·캐시 헤더·q 검증(TSE-*·TC-*·TR-*). 본 도메인 무 DB 테스트는 `fetch` 목 캔드 주입, 실 응답 스모크는 SP-API/SP-ARCH M8.
- **SP-DS(SP-DSN) 위임**: `styles.css` 디자인 토큰(`--green`/`--red`/`--t3`/`--purple` 승자색 A3·배지/밴드/광고 라벨 색 SP-DS-3). 본 도메인은 클래스명 부여만·색 값 재정의 금지(T-06.11.2·T-06.11.4).
- **SP-ADS 위임**: `ads.js` `mountAds`·`adPolicy` 게이팅(결과 뷰 하단 1개, 검색·입력 뷰 무광고 PE-3). 본 도메인은 마운트 지점(#view-report 하단 DOM 자리)만 제공.
- **SP-GEN 위임**: 정적 회사/조합/정책 페이지(F4·F5·F7) 생성·CTA `/compare?a=..&b=..` **링크 생성**. 본 도메인은 넘어온 URL 파라미터를 **소비**(파싱·해석·슬롯 반영, T-06.13)만.
- **FR-43 필드 경계**: `RecentComparison`의 `input`(F2 스냅샷)·`result`(요약) **필드 구성은 FRD 06 FR-43** 소유. 본 도메인(T-06.11.6·T-06.12)은 렌더·봉투 무결성·FIFO·시그니처만.

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-06.1.1 | SP-FE-1.1·1.2 | import 스모크(구조) | FR-02·03 · INV-4 |
| T-06.2.1 | SP-FE-2.1·2.2 | MB-1·MB-13 (수동) | FR-02 · NFR12·24 |
| T-06.3.1 | SP-FE-3.2 | UT-ROUTE-1 | FR-02·03 |
| T-06.3.2 | SP-FE-3.2·3.3 | UT-ROUTE-2 | FR-02·03 · NFR14 |
| T-06.4.1 | SP-FE-4.1·4.2·4.3 | UT-STATE-1 | FR-03·20·24·25·D8 · INV-2·4 |
| T-06.5.1 | SP-FE-5.2 | UT-REF-1 | FR-02·E1 · INV-2 |
| T-06.5.2 | SP-FE-5.1 | MB-1·MB-2 (수동) | FR-02·E1 · NFR3·26 |
| T-06.6.1 | SP-FE-6.1 | apiFetch 유닛 / MB-14 | FR-02 · NFR16·20 · INV-1·4 |
| T-06.6.2 | SP-FE-6.1·6.2 | 래퍼 유닛(encode) | FR-02·10 · SP-API-10·11 |
| T-06.7.1 | SP-FE-7.2 | UT-SORT-1 | FR-12 |
| T-06.7.2 | SP-FE-7.2 | UT-SORT-2 | FR-11 |
| T-06.7.3 | SP-FE-7.1 | 디바운스 유닛 / MB-3 | FR-10·11 |
| T-06.7.4 | SP-FE-7.1 | runSearch 유닛 / MB-4 | FR-12·13·E2 |
| T-06.7.5 | SP-FE-7.3 | 렌더 유닛 / MB-12 | FR-11·12 · NFR14·21 |
| T-06.7.6 | SP-FE-7.1 | UT-SEARCH-FB-1 / MB-4 | FR-E2·D6·D11 · NFR26 · INV-4 |
| T-06.8.1 | SP-FE-8.2 | UT-NORM-1·2 | FR-D8.2·14 · DEC-2 |
| T-06.8.2 | SP-FE-8.3 | UT-FILL-1·2 | FR-21·22·06·D8.1·E4 |
| T-06.8.3 | SP-FE-8.3 | UT-WS-1 | FR-23 |
| T-06.8.4 | SP-FE-8.1 | selectCompany 유닛 / MB-5·6 | FR-14·15·16·D7·D11 |
| T-06.8.5 | SP-FE-8.3 | setDirectType 유닛 / MB-7 | FR-17·E4 |
| T-06.9.1 | SP-FE-9.1·9.2 | UT-ESC-1 (Tier0) | FR-45 · NFR21 |
| T-06.9.2 | SP-FE-9.1·9.2 | UT-ESC-2 (Tier0) | FR-45·41 · NFR21 |
| T-06.9.3 | SP-FE-9.1·9.2 | UT-ESC-3 (Tier0) | FR-45 · NFR21 |
| T-06.10.1 | SP-FE-9.3 | UT-ASM-1 | FR-40·42 · SP-ENGINE-2 · FR-D8 |
| T-06.10.2 | SP-FE-9.3 | runReport 유닛 / MB-15 | FR-42 |
| T-06.11.1 | SP-FE-9.4 | renderReport 유닛 / MB-15 | FR-40 · NFR21 |
| T-06.11.2 | SP-FE-9.4 | renderVdCard 유닛 / MB-15 | FR-42 · SP-ENGINE-13 |
| T-06.11.3 | SP-FE-9.4 | renderCatDelta 유닛 / MB-15 | FR-40 |
| T-06.11.4 | SP-FE-9.4 | renderBands 유닛 / MB-15 | FR-41·DEC-2 |
| T-06.11.5 | SP-FE-9.4 | 경고문구 매핑 유닛 | FR-40·45 · NFR21 |
| T-06.11.6 | SP-FE-9.4 | 저장UI 유닛 / MB-9 | FR-43 |
| T-06.12.1 | SP-FE-10.1 | UT-STORE-4 | FR-07·44·E3 · NFR16·25 |
| T-06.12.2 | SP-FE-10.2 | UT-STORE-1·2 | FR-43 R1·2·3 |
| T-06.12.3 | SP-FE-10.2 | UT-STORE-3 | FR-43 R4·44 1c |
| T-06.13.1 | SP-FE-11.2 | UT-PRE-1 | FR-16·D4 |
| T-06.13.2 | SP-FE-11.2 | restoreFromPrefill 유닛 / MB-11 | FR-16·D11 · INV-4 |
| T-06.14.1 | SP-FE-2·5 | MB-1 (수동) | FR-02 |
| T-06.14.2 | SP-FE-5.1 | MB-2 (수동·게이트) | FR-E1 · NFR26 |
| T-06.14.3 | SP-FE-7 | MB-3·MB-4 (수동) | FR-10·13 |
| T-06.14.4 | SP-FE-8 | MB-5·MB-6·MB-7 (수동) | FR-14·15·17 |
| T-06.14.5 | SP-FE-3·11 | MB-8·MB-11 (수동) | FR-02·03·16 |
| T-06.14.6 | SP-FE-9.4·10 | MB-9·MB-10 (수동) | FR-43·44 · NFR25 |
| T-06.14.7 | SP-FE-9.1·9.2 | MB-12 (수동·게이트) | NFR21 |
| T-06.14.8 | SP-FE-2.2 | MB-13 (수동) | FR-E5 · NFR12·24 · SCR-94 |
| T-06.14.9 | SP-FE-6.1 | MB-14 (수동·게이트) | INV-4·NFR16 |
| T-06.14.10 | SP-FE-9.4 | MB-15 (수동) | FR-40·41·42 |
| (위임) | SP-ENGINE-2·13 | T-ENGINE-* | FR-30~39·42 |
| (위임) | SP-API-9·10·11 | TR-*·TSE-*·TC-* | FR-92·93·94 |
| (위임) | SP-DSN A3·SP-DS-3 | UT-CONTRAST 등 | NFR15 |
| (위임) | SP-ADS mountAds | UT-ADS-GATE-1 | FR-70~79 |
| (위임) | SP-GEN CTA 생성 | GC-* | FR-16 R1 |
