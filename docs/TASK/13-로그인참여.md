# TASK 13 — 참여·로그인·재직인증 (SP-AUTH)

> SPEC 계약: [SPEC/13-참여-로그인.md](../SPEC/13-참여-로그인.md) (SP-AUTH). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M9(참여·로그인) — **SC14 증분, 라이브(M8) 이후**. 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: SP-DB(참여 7테이블 DDL·`BADGE_CD='verified'`·`_CNT` 접미 비준·`TMEMBER` 제거 해제 — 본 M9의 DB 확장으로 처리) · SP-API(`server/` 앱·`main.py`·`database.py`·`config.py` 재사용, 라우터/미들웨어 등록 지점) · SP-INFRA(nginx `loupit_mail` 존·`SMTP_*` 정의 허용 재명세). `server/` 스캐폴드 중복 생성 금지. 확정 순서는 00이 소유.
> 테스트: 러너=pytest 8.3 + httpx 0.28(`ASGITransport`, 무 러닝 서버) + pytest-asyncio 0.24 + pymysql(스키마·CLI 통합) · node --test(프론트 `apiSend`) · 위치=`server/tests/`·`web/test/` · 케이스 대역=AU · AL · AS · AM · AE · AB · AH · AC · AX · AO.

## 진행 요약
- 리프 총 **41** / 완료 **28** / 진행 **2** / 미착수 **11** — **M9: 표면(14)+로그인(6)+계정관리(3)+재직인증(5) 세그먼트 GREEN**. **DB-less 서버 165 base GREEN(회귀 0)** + 도메인 시드 loupit_test 검증 PASS.
  - ▸**표면**(2026-07-22): 라우터3+main.py+`database.execute/transaction`·AU-6/AU-7 베이스 흡수.
  - ▸**로그인**(2026-07-23): 무비밀번호 이메일코드(발송→검증→세션쿠키). T-13.5.1·6.1·6.2·6.3·6.4·12.1. **+적대적 보안점검(15에이전트)→6수정**: ①코드검증 원자화(TOCTOU 상한우회·다중세션 차단, 조건부 UPDATE) ②섀도잉 내성(CODE_HASH_VAL 직접 매칭) ③코드 HMAC(`login_code_hmac_pepper`, 운영 필수) ④메일러 fail-closed ⑤가입 이메일 레이스 500 해소 ⑥422 이메일·코드 반향 제거.
  - ▸**계정관리**(2026-07-23): `deps.require_member`(Cookie→resolve_session, 401) + `member.py` GET/PUT `/members/me`·POST `/members/logout`·DELETE `/members/me`(탈퇴=이메일 NULL·전세션 폐기·재직인증 HMAC 파기·닉네임/이력 존치, 원자 트랜잭션). T-13.7.1·7.2. AM-1~4 + 닉네임 제약(금칙어 422·중복 409).
  - ▸**재직인증**(2026-07-23): 블라인드식 도메인 자동 인증 + CLI 수동 폴백. `services/employment.py`(도메인 화이트리스트 매칭·HMAC·원문 파기·코드) + `deps.require_employment`(403 IDOR) + `employment.py` POST verify-code(409 manual_required/422/204)·verify(201·HMAC 중복 409)·requests(수동 요청 202/409). T-13.5.2·8.1·8.2·8.3·9.1·**8.4(도메인 시드 31매핑, 그룹공용 samsung/sk 포함, loupit_test 검증 PASS)**. AE-1~5. **잔여**: T-13.9.2 `ops.py` 운영자 CLI(수동 승인 처리).
  - **AU-1(T-13.1.2) RED 이월** — 남은 쓰기 2(benefits POST·PUT)·GET 1(edits) 미착지(복지편집 T-13.10·T-13.11). **잔여 이월**: CSRF `X-Loupit-Client`(T-13.13.1)·발송 rate-limit(T-13.13.2, 섀도잉 지속 DoS 완전차단)·`ops.py` CLI(T-13.9.2).
- Tier-0 게이트 리프(배포 차단, 00 §5 #28~31): **AU-3(T-13.3.9)·AU-4(T-13.3.10) GREEN**(loupit_test) · **AU-6(T-13.1.4)·AU-7(T-13.5.3) 베이스 게이트 흡수 GREEN**(test_package·test_database 재명세, sc14 마커 해제·구 테스트 삭제) · AU-2(T-13.1.3 = TS-2 원문 유지, 상시 green) · **AU-1(T-13.1.2) RED 이월** — 참여 쓰기 11·공개 GET 2가 app.routes에 실제 등록돼야 green(라우트 본체 T-13.6~11 완료 시 + 베이스 TS-1 재명세). 배포 차단 게이트는 M9 GREEN 완료 시 정본 활성.
- 최우선 선행: **T-13.2.1** — **완료(④, 커밋 `af6635e`)**. 존재검사 no-op이라 참여 테이블 생성 전엔 무영향.
- **테스트 DB 제약(M9 실행 환경)**: DB 의존 리프(스키마 AU-3/4·서비스)는 별도 `loupit_test` MySQL 또는 DB 접근 환경 필요 — 현 셸엔 root/생성권 없고 서빙 `LOUPIT` 뮤테이션 금지(C-1·docroot hazard). DB-less 리프(표면·config·db 심볼)는 dummy DB_NAME 로 검증 가능.
- 선결정 게이트: **DG-5**(재직 인증 회사 도메인 화이트리스트 시드) 미해소 — T-13.8.x 착수 전 확인(00 §4). 그 외 config 값(세션 30일·코드 10분·시도 5·재직 365일)은 SPEC/13 SP-AUTH-2 기본값 확정.
- 러너: (미구현 — 착수 시 `pytest server/tests/ -q` + `node --test web/`).

---

## T-13.1 모듈 스캐폴드·앱 조립·표면 게이트  — (SP-AUTH-1, FR-100, INV-1·INV-9, T2·T10)

- [v] T-13.1.1 라우터 3종 스캐폴드 + main.py 등록(미들웨어 0) — **완료(2026-07-22)**: member·employment·benefit_edit 빈 `APIRouter` 3파일 + `main.py` `include_router` 3종. `app.user_middleware==['CORSMiddleware']` 불변 검증(스모크). 라우트 본체(로그인·재직·편집)는 T-13.6~11이 채운다.
  - 구현: `server/routers/member.py`·`employment.py`·`benefit_edit.py`(`APIRouter`) + `server/main.py`에 `include_router` 3종 추가. **`app.add_middleware` 추가 금지**(세션·재직=의존성). 라우터 파일명 `auth` 금지(→`member.py`). `server/` 스캐폴드는 SP-ARCH T-01.2.1 재사용.
  - 테스트: import 스모크 + `member/employment/benefit_edit` 모듈 존재·`auth` 모듈 부재 grep (pytest — `server/tests/test_package.py`) — RED 먼저
  - refs: SP-AUTH-1 · FR-100 · INV-1·INV-9 · T10
- [-] T-13.1.2 AU-1 참여 쓰기 라우트 열거·그 외 쓰기 0 (TS-1 확장) (Tier0) — RED 이월(③ `test_TS1_sc14`, @pytest.mark.sc14 유지). 스캐폴드(T-13.1.1) 완료했으나 참여 라우트 **본체 미등록**이라 여전히 RED — 쓰기 11·공개 GET 2가 `app.routes`에 실제 등록되는 **T-13.6~11 완료 시 GREEN + 베이스 TS-1 재명세**(현 익명 표면 게이트 불변).
  - 구현: `app.routes` 순회 시 쓰기 라우트 = `POST /comparisons/log` + SC14 기여 라우트(로그인·재직·편집) **열거집합**뿐, 그 외 POST/PUT/DELETE=0임을 강제. 라우트 실구현은 T-13.6~11이 채우고 본 리프는 **Tier-0 표면 회귀 게이트**만 소유. SP-ARCH T2가 이 케이스로 확장 위임됨.
  - 테스트: AU-1 (pytest+httpx — `server/tests/test_surface.py`) — `app.routes` 순회, 익명 GET 5종 + 익명 로그 POST + 기여 쓰기 열거집합 + 공개 GET 2종(me·edits)뿐·그 외 쓰기 0 — RED 먼저
  - refs: SP-AUTH-1·14 · INV-1 · FR-100 · NFR20 (SP-ARCH T2 확장 위임)
- [ ] T-13.1.3 AU-2 미들웨어=CORS 1종·무세션 미들웨어 (TS-2 원문 유지) (Tier0)
  - 구현: SC14 라우터 등록 후에도 `app.user_middleware == ['CORSMiddleware']`(인증/세션 미들웨어 0). 세션·재직 검증이 `Depends`로만 주입됨을 표면 수준에서 고정(INV-9). TS-2 어서션 원문 유지.
  - 테스트: AU-2 (pytest+httpx — `server/tests/test_surface.py`) — `app.user_middleware == ['CORSMiddleware']`, 익명 GET dependant 트리에 `require_member` 부재 — RED 먼저
  - refs: SP-AUTH-1·4·14 · INV-9 · T2·T10 (SP-ARCH T2 위임 수신)
- [v] T-13.1.4 AU-6 라우터 allowlist 확장·`auth` 모듈 부재 (test_package 재명세) — **완료(2026-07-22)**: 베이스 `test_T04_1_1_routers_package_file_allowlist`를 8파일로 갱신·`auth` FORBIDDEN 유지·trending 보존, 구 `test_AU6_sc14` 흡수·삭제. GREEN.
  - 구현: `test_package` allowlist에 `member`·`employment`·`benefit_edit` **추가**, `FORBIDDEN_MODULE_NAMES`에 **`auth` 유지**. SP-API가 소유한 기존 allowlist를 SC14로 확장(중복 정의 금지·SP-API leaf와 정합).
  - 테스트: AU-6 (pytest — `server/tests/test_package.py`) — 라우터 모듈 집합 ⊇ {member,employment,benefit_edit}, `auth` 부재 — RED 먼저
  - refs: SP-AUTH-1·14 · T10

## T-13.2 테스트 하네스·DB 격리 확장  — (SP-AUTH-14, SP-TEST·SP-INFRA 위임)

- [v] T-13.2.1 `run_tests.sh` 참여 7테이블 백업/재주입 확장 (**최우선 선행**) — 완료(커밋 `af6635e`, 존재검사 no-op·FK-ON·`test_runner_backup.py` 9가드·적대검증)
  - 구현: `infra/deploy/run_tests.sh`의 기존 TCOMPARE_LOG 백업/재주입 장치를 **참여 7테이블(TMEMBER·TSESSION·TAUTH_CODE·TCOMPANY_EMAIL_DOMAIN·TEMPLOY_VERIFICATION·TEMPLOY_VRF_REQUEST·TBENEFIT_EDIT_LOG)로 확장**. 공유 스키마 DROP/CREATE 게이트 실행이 실 회원 데이터를 전멸시키지 않도록 dump→테스트→restore. SP-INFRA(SP-ARCH-9)·SP-TEST와 정합.
  - 테스트: 백업/재주입 대상 테이블 집합에 참여 7종 포함 grep + dry-run (bash·pytest — `server/tests/test_runner_backup.py`) — RED 먼저
  - refs: SP-AUTH-14 · SP-INFRA · SP-TEST-7 · INV-8
- [ ] T-13.2.2 conftest 참여 테이블 픽스처·격리
  - 구현: `server/tests/conftest.py`에 참여 7테이블 생성/틴다운 픽스처 추가(동 스키마 `LOUPIT` 테이블 재생성 격리, 00 §4 DG-4 방식 계승). httpx `ASGITransport` 앱 픽스처에 SC14 라우터 포함.
  - 테스트: 픽스처 로드 스모크 (pytest — `server/tests/conftest.py` 소비 테스트) — RED 먼저
  - refs: SP-AUTH-14 · SP-DB T-02.1.1(위임)

## T-13.3 참여 7테이블 DDL·스키마 게이트  — (SP-AUTH-3, SP-DB, FR-111, INV-8·T9)

> 테이블=리프 패턴(SP-DB T-02.2.* 계승). DDL 정본은 **SP-DB(SPEC 02)**가 소유하며 M9에서 확장한다. 전 테이블 `ENGINE=InnoDB CHARSET=utf8mb4`·`IF NOT EXISTS`·감사 4컬럼·한국어 COMMENT.

- [v] T-13.3.1 TMEMBER DDL (무비밀번호·PII 최소) — db/schema.sql 추가, AU-3 green(loupit_test)
  - 구현: `db/schema.sql`에 `CREATE TABLE IF NOT EXISTS TMEMBER` — `MBR_ID` PK·`LOGIN_EMAIL_NM VARCHAR UNIQUE`(탈퇴 시 NULL)·`NICKNAME_NM VARCHAR UNIQUE`·`STATUS_CD`·감사 4. **비밀번호·소셜 컬럼 정의 금지**(PWD/PROVIDER 부재).
  - 테스트: SC(TMEMBER 존재·PII 컬럼=이메일·닉네임 2종·비밀번호 컬럼 부재)·CN(UNIQUE 위반→IntegrityError) (pytest — `server/tests/test_schema_load.py`,`test_constraints.py`) — RED 먼저
  - refs: SP-AUTH-3 · SP-DB · FR-111 · INV-8 · NFR30
- [v] T-13.3.2 TSESSION DDL (토큰 SHA-256 해시) — db/schema.sql 추가, AU-4 green
  - 구현: `TOKEN_HASH_VAL CHAR(64) UNIQUE`·`MBR_ID` FK·`EXPIRES_DTM`·`REVOKED_DTM`·감사 4. **토큰 원문 컬럼 부재**.
  - 테스트: SC(TOKEN_HASH_VAL만·원문 부재)·CN(FK) (pytest — `test_schema_load.py`) — RED 먼저
  - refs: SP-AUTH-3·4 · INV-8 · T9 · NFR30
- [v] T-13.3.3 TAUTH_CODE DDL (코드 해시·시도) — db/schema.sql 추가, AU-4 green
  - 구현: `CODE_HASH_VAL`·`TARGET_HASH_VAL`·`PURPOSE_CD`(login|employ_verify)·`ATTEMPT_CNT`·`COMP_ID`(널)·`MBR_ID`(널)·`EXPIRES_DTM`·`CONSUMED_DTM`·감사 4. **코드·이메일 원문 컬럼 부재**.
  - 테스트: SC(해시 컬럼만·원문 부재·ATTEMPT_CNT)·CN (pytest — `test_schema_load.py`) — RED 먼저
  - refs: SP-AUTH-3·5 · INV-8 · T9 · NFR30
- [v] T-13.3.4 TCOMPANY_EMAIL_DOMAIN DDL (회사↔도메인 화이트리스트) — db/schema.sql 추가(적용 검증)
  - 구현: `DOMAIN_ID` PK·`COMP_ID` FK·`EMAIL_DOMAIN_NM`·`ACTIVE_YN`·감사 4 + `uq_comp_domain (COMP_ID, EMAIL_DOMAIN_NM)`. 시드 데이터는 **DG-5** 확정 후(T-13.8).
  - 테스트: SC·CN(uq) (pytest — `test_schema_load.py`,`test_constraints.py`) — RED 먼저
  - refs: SP-AUTH-3·7 · SP-DB
- [v] T-13.3.5 TEMPLOY_VERIFICATION DDL (회사 이메일 HMAC) — db/schema.sql 추가, AU-4 green
  - 구현: `EMPLOY_VRF_ID` PK·`MBR_ID` FK·`COMP_ID` FK **ON DELETE SET NULL**(IDOR 근거)·`VRF_METHOD_CD`(domain|manual)·`COMP_EMAIL_HASH_VAL UNIQUE`(HMAC)·`EXPIRES_DTM`·`REVOKED_DTM`·감사 4. **회사 이메일 원문 컬럼 부재**.
  - 테스트: SC(HMAC 컬럼만·원문 부재)·CN(UNIQUE 중복→409 근거·FK SET NULL) (pytest — `test_schema_load.py`) — RED 먼저
  - refs: SP-AUTH-3·7 · INV-8 · T9 · NFR30
- [v] T-13.3.6 TEMPLOY_VRF_REQUEST DDL (수동 승인 큐) — db/schema.sql 추가(적용 검증)
  - 구현: `VRF_REQUEST_ID` PK·`MBR_ID`·`COMP_ID` FK·`STATUS_CD`(pending|approved|rejected)·`EVIDENCE_CTNT`(이스케이프)·`DECIDED_BY_ID`·`DECIDED_DTM`·`DECIDE_NOTE_CTNT`·감사 4 + `idx_status (STATUS_CD, INS_DTM)`.
  - 테스트: SC·CN (pytest — `test_schema_load.py`) — RED 먼저
  - refs: SP-AUTH-3·8 · INV-8 · NFR21
- [v] T-13.3.7 TBENEFIT_EDIT_LOG DDL (append-only 편집 이력) — db/schema.sql 추가(append-only, INS_DTM만)
  - 구현: `EDIT_LOG_ID` PK·`BENEFIT_ID` FK CASCADE·`COMP_ID`·`ACTOR_MBR_ID` FK **SET NULL**(탈퇴 후 로그 보존)·`EDIT_TYPE_CD`(create|update|delete)·`BEFORE_VAL JSON`·`AFTER_VAL JSON`·`EDIT_NOTE_CTNT`·`INS_DTM`(불변·MOD 없음) + `idx_comp_ins (COMP_ID, INS_DTM)`·`idx_actor_ins (ACTOR_MBR_ID, INS_DTM)`.
  - 테스트: SC(append-only·감사 4 아님·INS_DTM만)·CN(FK CASCADE/SET NULL) (pytest — `test_schema_load.py`) — RED 먼저
  - refs: SP-AUTH-3·9·10 · INV-8 · NFR21
- [-] T-13.3.8 TCOMPANY_BENEFIT `BADGE_CD='verified'` 추가·`TMEMBER` 제거 해제 (SP-DB 확장) — TMEMBER 제거 해제 완료(③ conftest.REMOVED_TABLES), `BADGE_CD='verified'`는 값집합(ENUM 금지)이라 benefit_edit 구현 시 test_data_contract BADGE_CODES 확장
  - 구현: SP-DB(SPEC 02)에서 `TCOMPANY_BENEFIT.BADGE_CD` COMMENT/값집합에 **3번째 값 `verified`** 추가(DC-6 게이트 official/est 2값→3값), `_CNT` 접미 비준(SP-DB-1.2), `REMOVED_TABLES`에서 `TMEMBER` **제거**, SP-ARCH-8 인덱스 'DDL 5→7테이블'. **선행 필수**(미선행 시 DC-6·SC-6 실패).
  - 테스트: DC-6 확장(verified 허용)·`_CNT` 접미 화이트리스트·`TMEMBER` 부재 단언 해제 (pytest — `test_schema_load.py`,`test_data_contract.py`) — RED 먼저
  - refs: SP-AUTH-3·9 · SP-DB-5·9·11 · INV-5
- [v] T-13.3.9 AU-3 TMEMBER PII 컬럼 정확집합 (Tier0) — **GREEN**(loupit_test, DDL 적용 후 `test_AU3_sc14` 통과)
  - 구현: `TMEMBER`의 PII 컬럼 = 정확히 `{LOGIN_EMAIL_NM, NICKNAME_NM}`(그 외 이메일/전화/실명 컬럼 부재)를 스키마 로드로 강제. INV-8·T9의 회귀 게이트.
  - 테스트: AU-3 (pytest+pymysql — `server/tests/test_schema_load.py`) — TMEMBER PII 컬럼 정확집합, REMOVED_TABLES에서 TMEMBER 제거됨 — RED 먼저
  - refs: SP-AUTH-3·14 · INV-8 · T9
- [v] T-13.3.10 AU-4 인증/세션/재직 테이블 원문 컬럼 부재·`*_HASH_VAL`만 (Tier0) — **GREEN**(loupit_test, `test_AU4_sc14` 통과)
  - 구현: `TSESSION`·`TAUTH_CODE`·`TEMPLOY_VERIFICATION`에 원문 토큰·코드·회사 이메일 컬럼이 **부재**하고 `*_HASH_VAL`만 존재함을 강제. NFR30 at-rest 회귀 게이트.
  - 테스트: AU-4 (pytest+pymysql — `server/tests/test_schema_load.py`) — 3테이블 원문 비밀값 컬럼 0·해시 컬럼만 — RED 먼저
  - refs: SP-AUTH-3·14 · INV-8 · T9 · NFR30

## T-13.4 config·금지 substring 재명세  — (SP-AUTH-2, T10)

- [v] T-13.4.1 Settings 확장 + AU-5 금지 substring — 완료(`server/config.py` SC14 필드 14종·AU-5 green·base test_config green, no-DB 검증)
  - 구현: `server/config.py Settings`에 `mailer_mode`·`smtp_*`·`session_ttl_days`·`login_code_ttl_min`·`code_max_attempts`·`mail_resend_cooldown_sec`·`daily_edit_limit`·`employ_vrf_ttl_days`·`session_hash_pepper`·`comp_email_hmac_pepper` 추가. **필드명에 `jwt`/`oauth`/`password_reset`/`social` 부분문자열 금지**. `comp_email_hmac_pepper` 미설정 시 재직 경로 기동 실패.
  - 테스트: AU-5 (pytest — `server/tests/test_config.py`) — 신규 필드 positive + 금지 substring={jwt,oauth,password_reset,social} 부재(smtp/session/pepper 허용) — RED 먼저
  - refs: SP-AUTH-2 · T10 · NFR30·NFR31

## T-13.5 세션·라우트 의존성  — (SP-AUTH-4, FR-100·101, INV-8·INV-9)

- [v] T-13.5.1 session.py 발급·검증·폐기·retention — **완료(2026-07-23)**: `services/session.py`(issue_session 해시만 저장·resolve_session 미만료/미폐기만·revoke·purge_expired·set/clear_session_cookie) + `database.execute/transaction`(표면 세그먼트 선착지). test_session.py AS-1/2/3.
  - 구현: `server/services/session.py` — `issue_session`(`secrets.token_urlsafe(48)`·DB엔 SHA-256 해시만·`loupit_sid` 쿠키 원문 반환)·`resolve_session`(해시 조회·미만료·미폐기)·`revoke_session`. `database.py`에 쓰기 헬퍼(`execute`)·`_retention_scheduler`(만료 세션·코드 퍼지) 추가.
  - 테스트: AS-1(쿠키 속성 HttpOnly·Secure·SameSite=Lax·Path=/api/v1)·AS-2(만료·폐기 401)·AS-3(DB 해시만) (pytest+httpx — `server/tests/test_session.py`) — RED 먼저
  - refs: SP-AUTH-4 · FR-101 · INV-8·INV-9 · NFR17·NFR30
- [v] T-13.5.2 deps.py require_member·require_employment — **완료(2026-07-23)**: `server/deps.py` require_member(Cookie→resolve_session→401)·**require_employment(comp_id → active_verification, 없으면 403 IDOR 방어)**. 미들웨어 아닌 Depends(INV-9). test_deps·test_employment.
  - 구현: `server/deps.py` — `require_member`(쿠키→`resolve_session`, 없으면 401)·`require_employment(comp_id)`(활성 재직 인증 요구, 없으면 403). **미들웨어 아님**(Depends 전용).
  - 테스트: AM-1(무세션 401)·재직 미보유 403 (pytest+httpx — `server/tests/test_deps.py`) — RED 먼저
  - refs: SP-AUTH-4 · FR-100 · INV-9
- [v] T-13.5.3 AU-7 test_database write_symbols 정확 열거 — **완료(2026-07-22)**: `database.execute`(rowcount 반환)·`transaction`(원자 CM) 추가 + fake-pool 유닛테스트 3종, 베이스 write_symbols 게이트를 `[execute, insert_compare_log, purge_compare_log]`로 재명세, 구 `test_AU7_sc14` 흡수·삭제. GREEN. ※ T-13.5.1 `session.py` 본체는 미착수 — execute/transaction **인프라만** 표면 세그먼트로 선착지.
  - 구현: `database.py` 쓰기 헬퍼 추가에 맞춰 `test_database`의 `write_symbols` 정확 열거를 참여 쓰기 헬퍼(`execute`·`transaction`) 포함으로 재명세(익명 읽기 헬퍼 집합 불변). SP-API가 소유한 기존 열거를 SC14로 확장(중복 정의 금지). **SPEC/13 릴리스 게이트 AU-1~7 중 AU-7**(Tier-0 핵심 AU-1~4엔 미포함).
  - 테스트: AU-7 (pytest — `server/tests/test_database.py`) — `write_symbols` 정확 집합({execute, transaction} 포함)·그 외 쓰기 심볼 0 — RED 먼저
  - refs: SP-AUTH-4·14 · INV-1 · NFR20

## T-13.6 무비밀번호 로그인  — (SP-AUTH-5, FR-102·103, UC-70·71)

- [v] T-13.6.1 auth_code.py 생성·해시·검증·소비 — **완료(2026-07-23)**: `services/auth_code.py`(_gen_code 6자리·_hash_code 대상스코프·_hash_target 조회키·issue_login_code 해시만+메일·verify_login_code 시도상한→만료→constant-time 대조→소비, CodeResult). test_auth_code.py 원문 미저장 게이트.
  - 구현: `server/services/auth_code.py` — `_gen_code`(6자리·`secrets.randbelow`)·`_hash_code`(SHA-256, target 스코프)·`issue_login_code`(해시만 저장·+10분)·검증(시도 상한·소비 `CONSUMED_DTM`). **코드·이메일 원문 무저장**.
  - 테스트: 해시 대조·만료·소비 단위 (pytest — `server/tests/test_auth_code.py`) — RED 먼저
  - refs: SP-AUTH-5 · FR-102·103·111 · INV-8 · NFR30
- [v] T-13.6.2 `POST /members/login-code` 균일 204·계정 열거 차단 (AL) — **완료(2026-07-23)**: 계정 유무 무관 균일 204·no-store·이메일 형식 422. test_member.py AL-1.
  - 구현: `member.py` — `{email}` 형식 검증(422)→`issue_login_code`→**균일 204**(계정 유무 무관). `Cache-Control: no-store`.
  - 테스트: AL-1 (pytest+httpx — `server/tests/test_member.py`) — 존재/부재 계정 모두 204·본문·로그에 코드/계정여부 부재 — RED 먼저
  - refs: SP-AUTH-5·12 · FR-102 · NFR31
- [v] T-13.6.3 `POST /members/login` 코드 검증·닉네임 자동·Set-Cookie (AL·AS) — **완료(2026-07-23)**: 검증→_get_or_create_member(닉네임 `직장인-######` 자동·UNIQUE 재시도)→issue_session→Set-Cookie(HttpOnly·Secure·Lax·/api/v1)→200 {nickname,is_new}. 실패 401/410/429. test_member.py AL-2/3/4·AS-1. ※CSRF `X-Loupit-Client`(T-13.13.1) 이월.
  - 구현: `member.py` — `{email,code}` 검증→성공 시 계정 조회/생성(닉네임 `직장인-{랜덤6}`)·`issue_session`·`Set-Cookie`·200 `{nickname,is_new}`. 실패 401/410/429.
  - 테스트: AL-2(200·Set-Cookie·is_new)·AL-3(불일치 401/만료 410/시도>5 429)·AL-4(신규 닉네임) (pytest+httpx — `test_member.py`) — RED 먼저
  - refs: SP-AUTH-5 · FR-103 · INV-8·INV-9 · NFR30
- [v] T-13.6.4 `_retention_scheduler` lifespan 배선 — **완료(2026-07-23)**: main.py 스케줄러에 `_purge_sessions_safe`(session.purge_expired — 만료·폐기 세션 + 만료·소비 코드) 추가, 예외 삼킴·빈 테이블 no-op 안전. test_session.py purge.
  - 구현: `main.py` lifespan에 만료 세션·코드 주기 퍼지 스케줄러 등록(백그라운드 태스크). 익명 표면 불변(미들웨어 0).
  - 테스트: 만료 레코드 퍼지 단위 (pytest — `test_session.py`) — RED 먼저
  - refs: SP-AUTH-4 · FR-101

## T-13.7 계정 관리·탈퇴  — (SP-AUTH-6, FR-104, UC-72)

- [v] T-13.7.1 `GET/PUT /members/me`·`POST /members/logout` (AM) — **완료(2026-07-23)**: GET me(닉네임·상태·활성 재직인증 목록·MBR_ID/회사이메일 미노출·no-store)·PUT me(닉네임 UNIQUE 원자검사 409·금칙어/형식 422)·logout(세션 폐기+쿠키 삭제 204). test_member AM-1/2/3.
  - 구현: `member.py`(`require_member`) — `GET`(닉네임·재직 인증 목록·no-store)·`PUT`(닉네임 고유성, 중복 409)·`logout`(세션 폐기·쿠키 삭제·204).
  - 테스트: AM-2(닉네임 중복 409)·AM-3(로그아웃 204·세션 폐기) (pytest+httpx — `test_member.py`) — RED 먼저
  - refs: SP-AUTH-6 · FR-104 · INV-8 · NFR16
- [v] T-13.7.2 `DELETE /members/me` 탈퇴 (AM) — **완료(2026-07-23)**: 원자 트랜잭션으로 이메일 NULL+STATUS=withdrawn·전 세션 폐기·재직인증 삭제(회사 이메일 HMAC 파기), 닉네임·`TBENEFIT_EDIT_LOG` 존치(약관 T5·개인정보 P7). test_member AM-4.
  - 구현: `member.py`(`require_member`) — `LOGIN_EMAIL_NM=NULL` + 전 세션 폐기 + **재직 인증(`TEMPLOY_VERIFICATION`) 폐기·회사 이메일 HMAC 파기**. **닉네임·`TBENEFIT_EDIT_LOG` 존치**(약관 T5·개인정보 P7 고지). 204.
  - 테스트: AM-4 (pytest+httpx — `test_member.py`) — 이메일 NULL·재직 인증 폐기·닉네임/이력 존치 — RED 먼저
  - refs: SP-AUTH-6 · FR-104 · INV-8 · SP-POL(T5·P7)

## T-13.8 재직 인증  — (SP-AUTH-7, FR-105·106, UC-73) · **DG-5 차단**

- [v] T-13.8.1 employment.py 도메인 매칭·HMAC·원문 파기 — **완료(2026-07-23)**: `services/employment.py` `_normalize_domain`·`domain_status`(ok/mismatch/no_domains)·`_hmac_email`(comp_email_hmac_pepper)·`active_verification`·`issue_employ_code`·`verify_employ_code`(로그인과 동일 원자·섀도잉 내성, purpose+comp_id 스코프)·`create_domain_verification`·`submit_manual_request`. 회사 이메일 원문 무저장(해시/HMAC만). test_employment 유닛.
  - 구현: `server/services/employment.py` — `_normalize_domain`·`domain_matches(comp_id,email)`(TCOMPANY_EMAIL_DOMAIN 화이트리스트)·`_hmac_email`(HMAC·`comp_email_hmac_pepper`)·`active_verification`. **회사 이메일 원문 무저장**(검증 성공 시 HMAC만).
  - 테스트: 도메인 매칭·HMAC 결정성·원문 미보관 단위 (pytest — `server/tests/test_employment.py`) — RED 먼저
  - refs: SP-AUTH-7 · FR-105·106 · INV-8 · NFR30
- [v] T-13.8.2 `POST /employment/verify-code` 도메인 매칭·발송 (AE) — **완료(2026-07-23)**: 등록 도메인 일치 시 코드 발송 204·미등록 회사 409 `manual_required`·불일치 422·무세션 401·no-store. test_employment AE-1/2.
  - 구현: `employment.py`(`require_member`) — `{comp_id,company_email}`→`domain_matches` 일치 시 코드 해시 저장·회사 이메일 발송·204. 미등록 도메인 409 `manual_required`, 불일치 422.
  - 테스트: AE-1(불일치 422)·AE-2(미등록 409 manual_required) (pytest+httpx — `test_employment.py`) — RED 먼저
  - refs: SP-AUTH-7 · FR-105 · NFR31
- [v] T-13.8.3 `POST /employment/verify` 인증 생성·HMAC 중복 (AE) — **완료(2026-07-23)**: 코드 검증 성공 시 TEMPLOY_VERIFICATION 생성(HMAC만·domain·+365일) 201·이미 인증됨/HMAC 중복 409·코드 실패 401/410/429. test_employment AE-3/4.
  - 구현: `employment.py`(`require_member`) — 코드 검증 성공 시 `TEMPLOY_VERIFICATION` 생성(원문 파기·HMAC만·`VRF_METHOD_CD='domain'`·+365일)·201. HMAC 중복 409·already_verified 409·코드 실패 401/410/429.
  - 테스트: AE-3(인증 후 원문 부재·HMAC만)·AE-4(HMAC 중복 409) (pytest+httpx — `test_employment.py`) — RED 먼저
  - refs: SP-AUTH-7 · FR-106 · INV-8 · NFR30
- [v] T-13.8.4 회사 도메인 화이트리스트 시드 (**DG-5**) — **완료(2026-07-23)**: `db/seed/company_email_domain.sql` 31매핑(회사 전용 21 + **그룹공용 samsung.com→삼성 7사·sk.com→SK 3사** — 사용자 그룹단위 인증 결정). `schema.sql uq_company_domain`을 `(COMP_ID, 도메인)` 복합으로 변경(그룹 도메인 다중 매핑 허용)·`load.py` 배선. **loupit_test 격리 검증 PASS**(31/7/3·삼성전자@samsung.com ok·복합키). 근거 조사 워크플로우(24개, **개인 이메일 PII 전량 폐기·도메인만 보존**)·나머지 회사는 수동 승인.
  - 구현: `db/seed/company_email_domain.sql` — 회사별 등록 도메인 시드(큐레이션 데이터). **DG-5 확정 전 착수 금지**(어느 회사=어느 도메인은 추측 불가). SP-SEED 관례 계승.
  - 테스트: 시드 도메인 ↔ 회사 매핑 정합·중복 0 (pytest+pymysql — `test_employment.py`) — 시드 검증(SP-SEED 계열, SPEC AE 대역 아님) — RED 먼저
  - refs: SP-AUTH-7 · SP-SEED · DG-5

## T-13.9 수동 승인·운영자 CLI  — (SP-AUTH-8, FR-107·115, UC-74)

- [v] T-13.9.1 `POST /employment/requests` 승인 큐 (AE) — **완료(2026-07-23)**: {comp_id,evidence}→TEMPLOY_VRF_REQUEST(pending) 202·동일 회사 pending 중복 409·evidence 파라미터 바인딩(표시 이스케이프는 SP-FE). test_employment AE-5.
  - 구현: `employment.py`(`require_member`) — `{comp_id,evidence}`→`TEMPLOY_VRF_REQUEST(pending)`·202. 동일 회사 pending 중복 409. `evidence` 이스케이프.
  - 테스트: AE-5(202·중복 409·이스케이프) (pytest+httpx — `test_employment.py`) — RED 먼저
  - refs: SP-AUTH-8 · FR-107 · NFR21
- [ ] T-13.9.2 ops.py 운영자 CLI (AO)
  - 구현: `server/ops.py`(`python -m server.ops`, 동기 pymysql) — `list-pending`·`approve`(manual 인증 생성)·`reject`·`revoke-verification`·`delete-benefit`(+EDIT_TYPE=delete 이력). **사용자 대면 DELETE 라우트 없음**. 감사 흔적(`DECIDED_BY_ID`·이력).
  - 테스트: AO-1(approve→manual 인증·감사)·AO-2(delete-benefit→delete 이력)·AO-3(사용자 DELETE 라우트 부재) (pytest+pymysql — `server/tests/test_ops.py`) — RED 먼저
  - refs: SP-AUTH-8 · FR-107·115 · INV-8

## T-13.10 복지 편집  — (SP-AUTH-9, FR-108·109, UC-75·76, INV-1·INV-5·INV-8)

- [ ] T-13.10.1 benefit_edit.py 배지 강제·동시성·원자 트랜잭션
  - 구현: `server/services/benefit_edit.py` — `create_benefit`(배지 서버 강제 `BADGE_CD='verified'`·`AMT_SOURCE_CD`=`QUAL_YN` 분기(estimated|none)·`BADGE_SRC_CD='user_report'`·+18개월)·`update_benefit`(`base_dtm` 낙관적 동시성·`FOR UPDATE`). 본체+`TBENEFIT_EDIT_LOG` **원자 트랜잭션**(`db.transaction()`). **calc.js 무변경**(INV-5).
  - 테스트: 배지 강제·`base_dtm` 불일치·트랜잭션 원자성 단위 (pytest — `server/tests/test_benefit_edit.py`) — RED 먼저
  - refs: SP-AUTH-9 · FR-108·109 · INV-1·INV-5·INV-8 · SP-DB·SP-ENGINE
- [ ] T-13.10.2 `POST /companies/{comp_id}/benefits` 등록 (AB)
  - 구현: `benefit_edit.py`(`require_employment(comp_id)`+CSRF) — 삽입·배지 강제·이력 append·201·`{benefit,benefits[]}`·캐시 무효화. 401/403·중복 409·일일 상한 429. `QUAL_YN` 불변식 위반 422.
  - 테스트: AB-1(재직 미보유 403)·AB-2(배지 강제)·AB-4(원자 트랜잭션)·AB-5(일일 상한 429)·AB-6(QUAL_YN 불변식 422) (pytest+httpx — `test_benefit_edit.py`) — RED 먼저
  - refs: SP-AUTH-9 · FR-108 · INV-1·INV-8 · NFR21
- [ ] T-13.10.3 `PUT /companies/{comp_id}/benefits/{benefit_id}` 수정·409 (AB)
  - 구현: `benefit_edit.py` — `base_dtm` 일치 시 갱신·이력(update)·200; 불일치 **409+현재 행 동봉**. official→verified 강등. v1 사용자 DELETE 없음.
  - 테스트: AB-3(`base_dtm` 불일치 409+현재행) (pytest+httpx — `test_benefit_edit.py`) — RED 먼저
  - refs: SP-AUTH-9 · FR-109 · INV-8

## T-13.11 편집 이력 공개 조회  — (SP-AUTH-10, FR-110, UC-77)

- [ ] T-13.11.1 `GET /companies/{comp_id}/edits` 공개 (AH)
  - 구현: `benefit_edit.py`(세션 불필요) — 최신순·닉네임 조인·`limit`(기본50·최대200)·`before` 커서·`no-store`·200. 미존재 comp_id 404. **닉네임만·MBR_ID/이메일 미노출**·전 필드 이스케이프.
  - 테스트: AH-1(익명 200)·AH-2(닉네임만·MBR_ID 미노출)·AH-3(이스케이프)·AH-4(탈퇴자 닉네임 존치)·AH-5(no-store) (pytest+httpx — `test_benefit_edit.py`) — RED 먼저
  - refs: SP-AUTH-10 · FR-110 · INV-8 · NFR21

## T-13.12 메일러  — (SP-AUTH-11, FR-102·105)

- [v] T-13.12.1 mailer.py Console/SMTP 추상화 — **완료(2026-07-23)**: `server/mailer.py`(ConsoleMailer 개발·SmtpMailer stdlib smtplib+asyncio.to_thread·get_mailer 폴백). 신규 의존성 0·코드 원문 로그 미노출(NFR31). test_mailer.py.
  - 구현: `server/mailer.py` — `ConsoleMailer`(개발·코드 stdout·게이트 대상 외)·`SmtpMailer`(stdlib `smtplib`+`asyncio.to_thread`)·`get_mailer`(`mailer_mode`+`smtp_user` 미설정 시 Console 폴백). **신규 의존성 0**. 운영 로그·응답에 코드/이메일 원문 미출력.
  - 테스트: 모드 선택·폴백·원문 미노출 단위 (pytest — `server/tests/test_mailer.py`) — RED 먼저
  - refs: SP-AUTH-11 · FR-102·105 · NFR31 · SP-INFRA

## T-13.13 CSRF·전송 헤더·리밋·오류  — (SP-AUTH-12·13, FR-112·113·114)

- [ ] T-13.13.1 CSRF `X-Loupit-Client`·쿠키 속성 (AC)
  - 구현: 상태변경 요청 `X-Loupit-Client` 필수(부재 403)·`SameSite=Lax`·CORS `allow_credentials=false` 유지. 앱 레벨 이중 검사(nginx 게이트는 SP-INFRA 재명세).
  - 테스트: AC-1(헤더 부재 쓰기 403)·AC-2(익명 GET 무쿠키) (pytest+httpx — `server/tests/test_csrf.py`) — RED 먼저
  - refs: SP-AUTH-12 · FR-113 · INV-9 · NFR16
- [ ] T-13.13.2 발송·시도 리밋·계정 열거 (AL·AE)
  - 구현: 앱 레벨 재전송 쿨다운(`mail_resend_cooldown_sec`)·시도 상한(`code_max_attempts`)·일일 편집 상한(`daily_edit_limit`)·계정 열거 균일 응답. nginx `loupit_mail` 존은 SP-INFRA 재명세(재명세 대기).
  - 테스트: 쿨다운·시도 상한 429·균일 응답 (pytest+httpx — `test_member.py`,`test_employment.py`) — RED 먼저
  - refs: SP-AUTH-12 · FR-112 · NFR31 · SP-INFRA
- [ ] T-13.13.3 공통 상태코드·오류 envelope (AX)
  - 구현: 401/403/404/405/409/410/422/429/500 매트릭스·`{"detail":...}` envelope·`no-store`·**PII/스택/SQL/원문 미노출**. 409는 현재 상태 동봉(base_dtm).
  - 테스트: AX-1(상태 매트릭스)·AX-2(envelope PII 미노출)·AX-3(405 Allow) (pytest+httpx — `server/tests/test_participation_errors.py`) — RED 먼저
  - refs: SP-AUTH-13 · FR-114 · NFR16·NFR26

## T-13.14 프론트 전송 헬퍼  — (SP-AUTH-12, SP-FE 확장)

- [ ] T-13.14.1 apiSend credentialed·X-Loupit-Client (AC)
  - 구현: `web/assets/js/api.js`에 `apiSend`(credentialed·`X-Loupit-Client` 헤더·no-store) 추가. 익명 `apiFetch`는 `credentials:'omit'` 불변(무쿠키). **편집 폼·마이페이지 UI 전체는 ML-F/SP-FE 확장 — 본 M9 범위 밖**(본 리프는 전송 헬퍼만).
  - 테스트: apiSend 헤더·credentialed / apiFetch 무쿠키 검증(프론트 대역 — 백엔드 AC-1·2와 구분) (node --test — `web/test/api.test.js`) — RED 먼저
  - refs: SP-AUTH-12 · FR-113 · SP-FE(위임)

---

## 위임 (중복 구현 금지)

| 위임 산출물 | 소유 대역·리프 | 본 문서 접점 |
| --- | --- | --- |
| 참여 7테이블 DDL 정본·`BADGE_CD='verified'`·`_CNT` 비준·`TMEMBER` 제거 해제·SP-ARCH-8 5→7 | **SP-DB(SPEC 02)** T-13.3.* 확장 | T-13.3.8 선행·AU-3·4 대상 |
| nginx `loupit_mail` rate-limit 존·`X-Loupit-Client` 게이트·`SMTP_*` 허용 | **SP-INFRA(SPEC 11)** 재명세 대기 | T-13.12·T-13.13.2 접점 |
| 약관 **T5·개인정보 P7 신설 + 기존 약관 T2·개인정보 P1·privacy meta의 "로그인 없음" 절대문구 SC14 개정** → SP-GEN 정책 페이지 재생성 | **SP-POL(SPEC 09)** SP-POL-3·4(Step 7 완료) · **SP-GEN**(재생성, M9) | T-13.7.2(탈퇴 고지) · M9 승인 게이트 |
| `run_tests.sh`·`release.sh` 게이트 배선 | SP-INFRA(SP-ARCH-9)·SP-TEST | T-13.2.1 확장 대상 |
| 편집 폼·마이페이지·재직 인증 UI | SP-FE(ML-F 후속) | T-13.14.1(전송 헬퍼만) |
| calc.js 밴드·판정(무변경) | SP-ENGINE(SPEC 05) | T-13.10.1 표시 계층만 확장(INV-5) |

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)

| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-13.1.1 | SP-AUTH-1 | import·package 스모크 | FR-100 · INV-1·9 |
| T-13.1.2 | SP-AUTH-1·14 | AU-1 (Tier0) | INV-1 · FR-100 · NFR20 |
| T-13.1.3 | SP-AUTH-1·4·14 | AU-2 (Tier0) | INV-9 · T2·T10 |
| T-13.1.4 | SP-AUTH-14 | AU-6 | T10 |
| T-13.2.1 | SP-AUTH-14 | 백업 대상 grep·dry-run | INV-8 · SP-INFRA·SP-TEST |
| T-13.2.2 | SP-AUTH-14 | conftest 스모크 | (SP-DB T-02.1.1 위임) |
| T-13.3.1 | SP-AUTH-3 | SC·CN(TMEMBER) | FR-111 · INV-8 · NFR30 |
| T-13.3.2 | SP-AUTH-3·4 | SC·CN(TSESSION) | INV-8 · T9 · NFR30 |
| T-13.3.3 | SP-AUTH-3·5 | SC·CN(TAUTH_CODE) | INV-8 · T9 · NFR30 |
| T-13.3.4 | SP-AUTH-3·7 | SC·CN(TCOMPANY_EMAIL_DOMAIN) | (SP-DB) |
| T-13.3.5 | SP-AUTH-3·7 | SC·CN(TEMPLOY_VERIFICATION) | INV-8 · T9 · NFR30 |
| T-13.3.6 | SP-AUTH-3·8 | SC·CN(TEMPLOY_VRF_REQUEST) | INV-8 · NFR21 |
| T-13.3.7 | SP-AUTH-3·9·10 | SC·CN(TBENEFIT_EDIT_LOG) | INV-8 · NFR21 |
| T-13.3.8 | SP-AUTH-3 / SP-DB-5·9·11 | DC-6 확장·`_CNT`·TMEMBER 부재 해제 | INV-5 |
| T-13.3.9 | SP-AUTH-3·14 | AU-3 (Tier0) | INV-8 · T9 |
| T-13.3.10 | SP-AUTH-3·14 | AU-4 (Tier0) | INV-8 · T9 · NFR30 |
| T-13.4.1 | SP-AUTH-2 | AU-5 | T10 · NFR30·31 |
| T-13.5.1 | SP-AUTH-4 | AS-1·2·3 | INV-8·9 · NFR17·30 |
| T-13.5.2 | SP-AUTH-4 | AM-1·재직 403 | INV-9 |
| T-13.5.3 | SP-AUTH-4·14 | AU-7 | INV-1 · NFR20 |
| T-13.6.1 | SP-AUTH-5 | 코드 해시·소비 | FR-102·103·111 · INV-8 |
| T-13.6.2 | SP-AUTH-5·12 | AL-1 | FR-102 · NFR31 |
| T-13.6.3 | SP-AUTH-5 | AL-2·3·4 | FR-103 · INV-8·9 · NFR30 |
| T-13.6.4 | SP-AUTH-4 | 퍼지 단위 | FR-101 |
| T-13.7.1 | SP-AUTH-6 | AM-2·3 | FR-104 · INV-8 · NFR16 |
| T-13.7.2 | SP-AUTH-6 | AM-4 | FR-104 · INV-8 · SP-POL(T5·P7) |
| T-13.8.1 | SP-AUTH-7 | 도메인·HMAC 단위 | FR-105·106 · INV-8 · NFR30 |
| T-13.8.2 | SP-AUTH-7 | AE-1·2 | FR-105 · NFR31 |
| T-13.8.3 | SP-AUTH-7 | AE-3·4 | FR-106 · INV-8 · NFR30 |
| T-13.8.4 | SP-AUTH-7 | 시드 매핑 정합(SP-SEED 계열) | SP-SEED · DG-5 |
| T-13.9.1 | SP-AUTH-8 | AE-5 | FR-107 · NFR21 |
| T-13.9.2 | SP-AUTH-8 | AO-1·2·3 | FR-107·115 · INV-8 |
| T-13.10.1 | SP-AUTH-9 | 배지·동시성·트랜잭션 | FR-108·109 · INV-1·5·8 |
| T-13.10.2 | SP-AUTH-9 | AB-1·2·4·5·6 | FR-108 · INV-1·8 · NFR21 |
| T-13.10.3 | SP-AUTH-9 | AB-3 | FR-109 · INV-8 |
| T-13.11.1 | SP-AUTH-10 | AH-1~5 | FR-110 · INV-8 · NFR21 |
| T-13.12.1 | SP-AUTH-11 | mailer 단위 | FR-102·105 · NFR31 |
| T-13.13.1 | SP-AUTH-12 | AC-1·2 | FR-113 · INV-9 · NFR16 |
| T-13.13.2 | SP-AUTH-12 | 리밋 429·균일 | FR-112 · NFR31 |
| T-13.13.3 | SP-AUTH-13 | AX-1·2·3 | FR-114 · NFR16·26 |
| T-13.14.1 | SP-AUTH-12 | apiSend(프론트) | FR-113 · (SP-FE 위임) |
| (위임) | SP-DB / 7테이블 DDL·verified | T-13.3.* · DC-6 확장 | INV-5·8 |
| (위임) | SP-INFRA / loupit_mail·SMTP_* | 재명세 대기 | NFR31 |
| (위임) | SP-POL / 약관 T5·개인정보 P7 | SP-POL-3·4 | SC14 |
