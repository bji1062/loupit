# TASK 12 — 테스트 전략·품질 게이트(횡단) (SP-TEST)

> SPEC 계약: [SPEC/12-테스트-전략.md](../SPEC/12-테스트-전략.md) (SP-TEST 횡단). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M0(집계 러너 `run_tests.sh`·`harness.test.js` 골격·MT-1) + M7(MT-2 집계 완성 — 전 스위트 파일 랜딩) + M8(MT-3 릴리스 게이트 배선). MT-4는 SP-ENGINE(`calc.js`/`calc.test.js`) 랜딩 시 green. 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: SP-ARCH-6(디렉토리 트리 `server/tests/`·`generator/tests/`·`web/`·`infra/deploy/`) + `web/package.json`에 `{"type":"module"}`(node:test ESM 해석, SP-TEST-2 — **위임 SP-ARCH T-01.2.2**; 부재 시 `harness.test.mjs` 확장자 사용). MT-2/3/4 **green**은 각 도메인 스위트(전 SP-*, 횡단)·`release.sh`(SP-INFRA)·`calc.js`+`calc.test.js`(SP-ENGINE) 랜딩에 의존(RED는 M0에 본 문서가 소유). 확정 순서는 00이 소유.
> 테스트: 러너=`node --test web/`(node:test, ES module — 부수효과 없는 소스·파일시스템 정적 파싱, 러너·DB·서버 불요) + 집계 `bash infra/deploy/run_tests.sh` · 위치=`web/test/harness.test.js`(메타 스위트)·`infra/deploy/run_tests.sh`(집계 러너) · 케이스 대역=MT-1~4(전 4 Tier-0/§8 G3).

## 진행 요약
- 리프 총 4 / 완료 1 / 진행 0 / 미착수 3
- Tier-0 게이트 리프: **T-12.1.1(MT-1)·T-12.2.1(MT-2)·T-12.2.2(MT-3)·T-12.2.3(MT-4)** — MT-1~4 green이 §8 G3(`node --test web/`)에 포함되어 실패 시 배포 차단(SP-TEST-10 "릴리스 게이트", 프롬프트 "MT-1~4는 Tier-0 포함(G3)")
- 선결정 게이트: **없음** (SP-TEST-4.2 `run_tests.sh` 소스·SP-TEST-10 MT-1~4가 SPEC에 확정 — 개방 결정/DG 없음. 개별 케이스·픽스처·`release.sh`·`web/package.json`·`conftest.py`는 **각 도메인 위임**이라 본 문서 결정 대상 아님)

## T-12.1 집계 러너·실행 매트릭스 (run_tests.sh)  — (SP-TEST-2·SP-TEST-4, SP-TEST-8 G1~G3, SP-ARCH-9)
- [v] T-12.1.1 `run_tests.sh` 5단계 집계 러너 + `harness.test.js` 부트스트랩·MT-1 완전성 린트 (Tier0)
  - 구현: `infra/deploy/run_tests.sh`(bash, `set -euo pipefail`·`ROOT="$(cd "$(dirname "$0")/../.." && pwd)"; cd "$ROOT"`) — 5단계 순차 호출: `[1/5] python -m pytest server/tests/ -q`(백엔드 API·스키마·시드, +`loupit_test` MySQL) · `[2/5] python -m pytest generator/tests/ -q`(정적 생성물·정책, fake 번들) · `[3/5] node --test web/`(프론트 순수모듈·계산엔진·광고·디자인토큰·**본 메타 스위트**) · `[4/5] 메타=이미 [3] 포함(web/test/harness.test.js)` · `[5/5] command -v nginx && nginx -t -c "$ROOT/infra/nginx/loupit.conf"` else 스킵. 어느 단계 실패 시 non-zero exit로 배포 차단(SP-TEST-4.2 정본 스니펫 1:1). 개별 실행 명령 매트릭스(SP-TEST-4.1: `-k "surface or health or …"` DB 없이·`--bundle-json` 스모크)는 러너 상단 주석에 문서화. 동시에 `web/test/harness.test.js` 파일 스캐폴드(node:test `import { test } from 'node:test'; import assert from 'node:assert/strict'`, ESM — `web/package.json` `type:module` 소비, 위임 SP-ARCH)에 **MT-1** 케이스 배치. `smoke.sh`(T7·T8, 릴리스 6~7단계)는 러너 미포함(위임 SP-ARCH/SP-INFRA).
  - 테스트: **MT-1**(`node --test` — `web/test/harness.test.js`) — `run_tests.sh` 소스 텍스트 파싱(부수효과 없이 문자열 검사)으로 `pytest server/tests`·`pytest generator/tests`·`node --test web`·`nginx -t` 4개 서브스위트 호출 문자열 전부 존재 + `set -euo pipefail`(실패 전파) 존재 assert — RED 먼저(`run_tests.sh` 부재 → 파일 read 실패). MT-1은 소스 파싱이므로 러너 실제 통과(전 스위트 green, M7)와 무관하게 M0에 green
  - refs: SP-TEST-4.2·SP-TEST-2·SP-TEST-10(MT-1) · SP-ARCH-9(릴리스 4단계) · SP-TEST-8 G1~G3 · (Tier0/§8 G3)

## T-12.2 메타 검증 하네스 harness.test.js (MT-2~4)  — (SP-TEST-3·SP-TEST-7·SP-TEST-8·SP-TEST-9·SP-TEST-10)
- [ ] T-12.2.1 MT-2 필수 테스트 파일 존재·명명 규약 린트 (Tier0)
  - 구현: `web/test/harness.test.js`에 **MT-2** 추가 — 파일시스템 존재 대조(SP-TEST-3 규약 목록): `server/tests/test_{surface,health,reference,search,company_detail,schema_load,constraints,data_contract,seed_counts,seed_integrity,seed_badge_backfill,seed_idempotency}.py`(12) + `generator/tests/test_policy_{content,pages}.py`·`generator/tests/test_footer_links.py`(3) + `web/assets/js/calc.test.js`(1) 전부 존재 assert(`fs.existsSync`). 누락 시 실패 = 미구현 영역 노출(SP-TEST-3 명명 규약 `test_*.py`·co-located `*.test.js`을 파일 존재로 간접 강제). 신규 코드 없음(개별 케이스·스위트 본문은 각 도메인 위임 — 본 리프는 **존재 린트 배선만** 소유, 중복 정의 금지).
  - 테스트: **MT-2**(`node --test`) — 필수 테스트 파일 집합 `fs.existsSync` 전부 true assert — RED 먼저(파일 미랜딩 시 실패). **green = M7**(전 도메인 스위트 랜딩 후 집계 완성; 각 파일이 랜딩될수록 점진 green)
  - refs: SP-TEST-3·SP-TEST-6·SP-TEST-9·SP-TEST-10(MT-2) · INV-6(회사 스위트 존재 간접) · (Tier0/§8 G3)
- [ ] T-12.2.2 MT-3 `release.sh` 게이트 차단 배선·early-exit 린트 (Tier0)
  - 구현: `web/test/harness.test.js`에 **MT-3** 추가 — `infra/deploy/release.sh` 소스 텍스트 파싱(부수효과 없이): 4단계에서 `run_tests.sh`(또는 동등 `pytest`/`node --test` 게이트) 호출 문자열 존재 + non-zero exit 시 후속 단계(서비스 재시작·Nginx reload) **미실행**(`set -e`/명시 `exit` early-exit 패턴) 검증. `release.sh` 실구현·4~7단계 순서는 **위임 SP-INFRA**(SP-ARCH-9); 본 리프는 게이트 배선 **회귀 린트만** 소유.
  - 테스트: **MT-3**(`node --test`) — `release.sh` 소스에 게이트 호출 + early-exit(테스트 실패→배포 차단) 배선 assert — RED 먼저(`release.sh` 부재). **green = M8**(SP-INFRA `release.sh` 랜딩 후)
  - refs: SP-TEST-8·SP-TEST-10(MT-3) · SP-ARCH-9(4·7단계) · INV(게이트 차단 강제) · (Tier0/§8 G3)
- [ ] T-12.2.3 MT-4 계산 export 커버리지 하한 린트 (Tier0)
  - 구현: `web/test/harness.test.js`에 **MT-4** 추가 — `web/assets/js/calc.js` 정적 파싱으로 `export function <name>` 집합 추출 → `web/assets/js/calc.test.js` 소스에서 각 함수명 참조(등장) 여부 대조, **미커버 export = 0** assert(모든 공개 계산 함수가 ≥1 케이스에 등장 — SP-TEST-7 Tier 1 "함수 100% 도달" 강제). `calc.js`/`calc.test.js` 실구현·T-ENGINE-1~48은 **위임 SP-ENGINE**; 본 리프는 export 커버리지 **하한 게이트만** 소유(개별 계산 케이스 재정의 금지).
  - 테스트: **MT-4**(`node --test`) — `calc.js` `export function` 집합 − `calc.test.js` 참조 집합 = ∅ assert(미커버 함수 목록 리포트) — RED 먼저(`calc.js` 부재 또는 미참조 export 존재). **green = SP-ENGINE 스위트 랜딩 시**
  - refs: SP-TEST-7(Tier 1 함수 100%)·SP-TEST-10(MT-4) · FR-30~39·FR-42(calc export 간접) · (Tier0/§8 G3)

## 위임·문서 계약 (다른 SP 스위트/문서가 소유 — 본 도메인 이중 구현 없음)

SP-TEST는 **테스트 전략·규약·집계·게이트·커버리지 매핑**만 소유하고, 개별 케이스 정의·픽스처·per-domain 인프라는 각 SP-* 문서가 소유한다(SPEC §0·§5·§6 "중복 정의 없이 종합·매핑·게이트만"). 아래는 본 문서가 **인용·린트만** 하고 이중 구현하지 않는 산출물이다.

| 위임 산출물 | 소유 대역·리프 | 본 문서 접점 |
| --- | --- | --- |
| `server/tests/conftest.py`(pymysql·`loupit_test` drop/create·`SET NAMES utf8mb4` 픽스처) | SP-DB T-02.1.1 | MT-2 파일 존재 대상(간접) |
| `server/tests/conftest.py`(ASGITransport `client`·`fake_data` monkeypatch·`build_reference_bundle` 캔드) | SP-API T-04.1.2 | MT-2·G1 |
| `server/tests/conftest.py`(`seeded_db`=`load.main(fresh=True)` 오케스트레이션) | SP-SEED T-03.1.1 | MT-2·G1 |
| `generator/tests/conftest.py`·`fixtures.py`(`FAKE_BUNDLE`·XSS 변형) | SP-GEN T-07.11.1 | MT-2·G2 |
| `web/assets/js/package.json` `{"type":"module","private":true}` | SP-ENGINE T-05.1.1 | node:test ESM 해석 |
| `web/package.json`(`"scripts":{"test":"node --test"}`·무빌드) | SP-ARCH T-01.2.2 | `harness.test.js` ESM 전제(SP-TEST-2) |
| `web/test/util/contrast.mjs`(WCAG 대비 유틸) | SP-DS T-10.1.1 | `node --test web/` 발견 대상(테스트 아님) |
| `infra/deploy/release.sh`(릴리스 4·7단계) | SP-INFRA(SP-ARCH-9) | MT-3 소스 파싱 대상 |
| `infra/deploy/smoke.sh`(통합 스모크 T7·T8, G4) | SP-ARCH T-01.4.1·T-01.5.1 / SP-INFRA | 러너 6~7단계(집계 미포함) |
| 개별 케이스 전량(SC·CN·DC·SD·SI·SB·SM·TS·TH·TR·TSE·TC·TM·TN·TCORS·TE·T-ENGINE·UT-*·GC·PC·UT-ADS·UT-TOKEN 등) | 각 도메인 TASK 02~11 | §6 매핑·§8 게이트로 종합만 |

**문서 전용 SP-TEST 절(코드 산출 없음 — 규약·매핑·역추적, 위 리프가 배선·린트로 강제):**
- **SP-TEST-1**(TDD DoD): 전 도메인 TASK의 **완료 기준**(구현+대응 테스트 green). 본 문서 헤더·전 리프의 "리프 완료(- [v])" 규약으로 실체화, MT-4(코드↔테스트 존재)가 부분 강제.
- **SP-TEST-2**(계층·러너 매트릭스): `run_tests.sh`(T-12.1.1)의 3러너(pytest·node·nginx) 구성으로 실체화.
- **SP-TEST-5**(픽스처·격리 전략): 위 위임 표(conftest·fake 번들·순수 함수 고정입력·`now` 주입). 본 문서는 전략 서술만.
- **SP-TEST-6**(스위트 종합 매핑 표): SPEC 정본; MT-2 파일 존재 린트가 대역↔파일 1:1을 간접 강제.
- **SP-TEST-9**(FR/UC·INV → 스위트 역추적): SPEC 정본 + "커버리지 완전성 규약"(새 FR 시 §9 갱신). MT-2가 각 영역 파일 존재를 린트.
- **SP-TEST-11**(추적 요약): 아래 추적표가 대응(SP-TEST-N ↔ 산출 ↔ 근거).

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-12.1.1 | SP-TEST-4.2·2·8(G1~G3)·10 | MT-1 (node:test) — 러너 소스 파싱 (Tier0) | SP-ARCH-9 · 전 FR·NFR(횡단 게이트) |
| T-12.2.1 | SP-TEST-3·6·9·10 | MT-2 (node:test) — 파일 존재·명명 린트 (Tier0) | INV-6 · FR-08(추적성) |
| T-12.2.2 | SP-TEST-8·10 | MT-3 (node:test) — release.sh 게이트·early-exit (Tier0) | SP-ARCH-9 · INV(차단) |
| T-12.2.3 | SP-TEST-7·10 | MT-4 (node:test) — calc export 커버리지 하한 (Tier0) | FR-30~39·FR-42 |
| (문서/규약) | SP-TEST-1(DoD) | 전 리프 완료 기준 · MT-4 부분 강제 | FR-08 |
| (문서/규약) | SP-TEST-2(러너 매트릭스) | T-12.1.1 러너 3계층 | 전 NFR(횡단) |
| (문서/규약) | SP-TEST-5(픽스처 전략) | 위임(conftest·fake 번들·`now` 주입) | INV-4·5 |
| (문서/규약) | SP-TEST-6(스위트 매핑) | MT-2(파일 존재)로 간접 강제 | 전 FR |
| (문서/규약) | SP-TEST-9(FR/INV 역추적) | MT-2 + 커버리지 완전성 규약 | 전 FR · INV-1~7 |
| (문서/규약) | SP-TEST-11(추적 요약) | 본 추적표 | 전 FR·NFR |
| (위임) | SP-TEST-5 / conftest·fixtures | SP-DB T-02.1.1·SP-API T-04.1.2·SP-SEED T-03.1.1·SP-GEN T-07.11.1 | INV-1·4 |
| (위임) | SP-INFRA / release.sh·smoke.sh | SP-ARCH T-01.4.1·T-01.5.1(T7·T8) · MT-3 린트 | INV-3·7 · NFR22·23 |
| (위임) | SP-ENGINE / calc.js·calc.test.js | T-ENGINE-1~48 · MT-4 커버리지 하한 | FR-30~39·42 · INV-4·5 |
