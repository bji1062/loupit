# TASK 07 — 정적 생성기·SEO 산출물 (SP-GEN)

> SPEC 계약: [SPEC/07-정적-생성기.md](../SPEC/07-정적-생성기.md) (SP-GEN). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M5(정적 생성) — 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: **SP-API**(`server/services/reference.py::build_reference_bundle` 단일 소스, SP-ARCH-4 — 생성기는 PYTHONPATH import로 소비, 러닝 API 불필요) · **SP-POL**(정책 4종 본문 문안 — 렌더·SEO만 본 도메인) · **SP-ADS**(광고 슬롯 컴포넌트·주입 로직 — 본 도메인은 슬롯 자리·크기 예약·"광고" 표기·placeholder client id만) · **SP-DS**(`styles.css` 토큰·클래스명). 테스트는 실 DB·러닝 API 없이 **소형 fake 번들 dict 직접 주입**으로 전 렌더 경로 구동(무 DB). 확정 순서는 00이 소유.
> 테스트: 러너=**pytest** 8.3.x(SP-ARCH-7) · 위치=`generator/tests/` · 케이스 대역=GC-1~GC-26. 실행 `pytest generator/tests/ -v`. `run_generated_checks(dir, pages)`는 릴리스 게이트(SP-GEN-11 4단계)에서도 동일 호출.

## 진행 요약
- 리프 총 36 / 완료 36 / 진행 0 / 미착수 0 (2026-07-11 완료)
- Tier-0 게이트 리프: T-07.11.2 (GC-2 회사 개수=번들 수·200 아님, INV-6) **green**, T-07.11.3 (GC-10 비-JS 본문, INV-3) **green**, T-07.11.4 (GC-21 XSS 이스케이프, NFR21) **green** — `generator/tests/test_pipeline.py`·`test_escape.py`
- 선결정 게이트: 없음 (SPEC 자족적 — `site_origin`·`adsense_client_id`·`lastmod`은 env/CLI 인자 기본값 확정, 정책 문안·광고 컴포넌트·OG 이미지 자산은 각각 SP-POL/SP-ADS/SP-DSN 위임)
- **구현 요약**: `generator/{config,bundle,context,slug,format,render,build,release,checks}.py` + `generator/pages/{company,combo,policy,sitemap}.py` + `generator/templates/`(base·company·combo·policy·404·sitemap.xml·partials 7종) + `generator/data/combinations.json`(프로덕션 큐레이션) + `generator/tests/`(fixtures·conftest·GC-1~26 전 스위트). `pytest generator/tests/ -q` = **151 tests, 149 passed, 2 known-fail**(SP-FE M6 핸드오프 블로커, 아래 참고). GC-1~26 전부 green(회사 3·조합 2·정책 4·404·sitemap·robots). SP-POL(09) 생성물 테스트 해소분(PC-3·4·7·8·9·10·12)도 본 세션에서 green — 09 문서 참고.
- **알려진 블로커(본 도메인 밖, web/ 코드 금지로 미수정)**: `generator/tests/test_footer_links.py`의 수기 셸 검증 2건이 의도적으로 RED — `web/compare/index.html`(M0 스캐폴드, 다른 세션 산출물로 추정)의 푸터가 `POLICY_FOOTER_LINKS`(`/privacy`·`/terms`·`/disclaimer`·`/ads`)와 다른 라우트(`/policy/privacy` 등)를 쓰고 `/terms` 링크가 없다. `web/index.html`(랜딩)은 아직 미생성(SP-FE M6 본체 미착수). SP-FE(06) 담당 세션에서 두 파일의 푸터를 `POLICY_FOOTER_LINKS`와 일치시켜야 해당 2건이 green된다.

## T-07.1 모듈 골격·데이터 소스·CLI  — (SP-GEN-1, FR-50, NFR1·NFR22, SP-ARCH-3·4·6)
- [v] T-07.1.1 config.py 사이트 상수 (GenConfig·CFG)
  - 구현: `generator/config.py` `@dataclass(frozen=True) class GenConfig` + `CFG=GenConfig()` — `site_origin`(env `SITE_ORIGIN`, 기본 `https://jobcho.wiki`)·`out_dir`(`GEN_OUT_DIR`, `web/dist`)·`default_og_image`(`/assets/og-default.png`)·`adsense_client_id`(env `ADSENSE_CLIENT_ID`, placeholder `ca-pub-XXXX…`, NFR22)·`compare_path`(`/compare`)·`site_name`(`loupit`)·`lang`(`ko`)·`desc_max`(155)·`extra_sitemap_paths`(`("/",)`)·`policy_pages`(4종 `(slug,label)`: privacy/terms/disclaimer/ads). DB 자격증명 미포함(시크릿 부재, NFR22).
  - 테스트: 상수 직접 케이스 없음 — 소비 케이스로 강제(`compare_path`→GC-16 · `adsense_client_id`→GC-17 · `site_origin`/https→GC-5·GC-8·GC-18 · `desc_max`→GC-5 · `policy_pages`→GC-1). 시크릿 스캔은 릴리스 GC-24 (pytest) — RED 먼저(파일 부재)
  - refs: SP-GEN-1.3 · FR-50 · NFR22 · SP-ARCH-6
- [v] T-07.1.2 bundle.py 번들 로더 (load_bundle·load_bundle_json)
  - 구현: `generator/bundle.py` `_load_async()`(aiomysql.connect + `build_reference_bundle(conn)` 소비 — SP-API-7 단일 소스, 재정의 금지)·`load_bundle()`=`asyncio.run(_load_async())`·`load_bundle_json(path)`(사전 덤프 JSON 로드, DB 없이 렌더). 반환 = 최상위 3키 dict `{company_types[], benefit_presets{}, companies[…]}`(INV-2). 필드명 SP-API-6/7 계약 1:1 소비.
  - 테스트: GC-1(fake 번들 파이프라인 소비 — 테스트는 dict 직접 주입, DB 무경유). `load_bundle` DB 경로는 실 시드 `--bundle-json` 스모크(SP-API 위임) (pytest) — RED 먼저
  - refs: SP-GEN-1.2 · FR-50 · NFR1 · INV-2 · SP-API-7(위임)
- [v] T-07.1.3 build.py 엔트리 CLI (run·main·argparse)
  - 구현: `generator/build.py` `run(out_dir, bundle, *, incremental, only, lastmod, gzip)->int`(make_env→build_context→company/combo/policy `render_all`→`--only` 접두 필터→`site_urls`(in_sitemap Page + `extra_sitemap_paths`)→sitemap/robots append→`stage_and_swap`→`write_manifest`, return 0) · `main(argv)`(argparse `--out`·`--bundle-json`·`--incremental`·`--only`·`--lastmod`·`--no-gzip`; `_today_iso()` 기본 lastmod) · `__main__`=`sys.exit(main())`. 종료코드 0=성공, 비0=렌더/검증/스왑 실패(이전 산출물 유지, SP-ARCH-9). 검증 실패=`BuildError`.
  - 테스트: GC-1(fake 번들 `run()` → 예외 없이 회사 3·조합 2·정책 4·404·sitemap·robots Page 생성) (pytest) — RED 먼저
  - refs: SP-GEN-1.4 · FR-50 · SP-ARCH-3·9 · NFR1

## T-07.2 출력 경로·Page 모델·컨텍스트 인덱스  — (SP-GEN-2, FR-50·51·60, SP-ARCH-6)
- [v] T-07.2.1 Page 모델 (context.py @dataclass Page)
  - 구현: `generator/context.py` `@dataclass(frozen=True) class Page`(`path`·`url`·`html`·`title`·`description`·`in_sitemap=True`·`content_type="text/html; charset=utf-8"`). 전 렌더 함수 공통 반환형(릴리스·sitemap·검증 공통 소비). 경로↔URL 매핑(SP-ARCH-6 평면 `.html`): `company/{slug}.html`→`/company/{slug}`, `vs/{first}-{second}.html`→`/vs/{first}-{second}`.
  - 테스트: 필드 직접 케이스 없음 — 소비 케이스로 강제(Page 생성→GC-1 · `url`→GC-8 · `in_sitemap`→GC-18 · `content_type`→GC-19 robots/GC-18 sitemap) (pytest) — RED 먼저
  - refs: SP-GEN-2.1·2.2 · FR-50 · SP-ARCH-6
- [v] T-07.2.2 build_context — 인덱스·유형 조인 (Ctx)
  - 구현: `generator/context.py` `build_context(bundle)->Ctx` — `by_eng{comp_eng_nm:c}`·`by_id{comp_id:c}`·`types_by_cd{comp_tp_cd:t}`·`slugs`(`validate_slugs` 호출, 충돌→BuildError)·`companies`·`build_now`. `types_by_cd`로 `comp_tp_cd`→`comp_tp_nm` 조인(회사 인라인은 `comp_tp_cd`만, FR-52). `@dataclass Ctx`.
  - 테스트: GC-1(파이프라인 `build_context` 무예외·인덱스 구성). 유형 조인은 회사뷰 GC-12/GC-13 소비, slug 충돌은 T-07.3.2(GC-3) 위임 (pytest) — RED 먼저
  - refs: SP-GEN-2.3 · FR-50·52·60 · SP-ARCH-6

## T-07.3 slug 규칙·조합 경로·충돌 검증  — (SP-GEN-3, FR-51·59·60, NFR11)
- [v] T-07.3.1 slug_of — 회사 slug 정규화 (FR-51)
  - 구현: `generator/slug.py` `class BuildError(Exception)` + `slug_of(comp_eng_nm)->str` — `strip().lower()`, `re.sub(r"[^a-z0-9]+","-")`, 연속 하이픈 축약(`-{2,}`→`-`), 양끝 하이픈 제거, 빈 문자열→BuildError, `re.fullmatch(r"[a-z0-9]+(-[a-z0-9]+)*")` 불일치→BuildError. 소스=`COMP_ENG_NM`(안정 URL, 한글명 URL 미포함).
  - 테스트: GC-3(전 회사 slug 정규식 `^[a-z0-9]+(-[a-z0-9]+)*$` 준수 — `samsung_elec`→`samsung-elec`·`sk_hynix`→`sk-hynix`·`cj`→`cj`) (pytest) — RED 먼저
  - refs: SP-GEN-3.1 · FR-51 · NFR11
- [v] T-07.3.2 validate_slugs — slug 유일성·충돌=빌드 실패 (FR-59)
  - 구현: `generator/slug.py` `validate_slugs(companies)->dict[str,str]` — 전 회사 `slug_of`, 동일 slug 다른 `comp_eng_nm`→BuildError(`slug collision`), `assert len({slugs})==len(companies)`. 반환 `{comp_eng_nm: slug}`.
  - 테스트: GC-3(정상 시 유일 집합 반환 · slug 충돌 데이터 주입 시 `BuildError`) (pytest) — RED 먼저
  - refs: SP-GEN-3.2 · FR-59 · NFR11
- [v] T-07.3.3 combo_slug — 사전식 정규화 canonical (FR-60 R2)
  - 구현: `generator/slug.py` `combo_slug(eng_a, eng_b, slugs)->(path, first, second)` — `sa,sb=slugs[eng_a],slugs[eng_b]`, `first,second=sorted([sa,sb])`, 반환 `f"{first}-{second}"`. 역순 쌍(B vs A)은 동일 canonical로 귀속(별도 파일 없음, NFR11).
  - 테스트: GC-4(`first ≤ second` 사전식 정렬) · GC-22((A,B)·(B,A) 두 입력 → 동일 canonical `/vs/{first}-{second}`) (pytest) — RED 먼저
  - refs: SP-GEN-3.3 · FR-60 R2 · NFR11
- [v] T-07.3.4 validate_combo_paths — 조합 경로 유일성·모호성 방어 (FR-60 R3)
  - 구현: `generator/slug.py` `validate_combo_paths(pairs, slugs)->dict[str,tuple]` — 각 pair `combo_slug`, 동일 `first-second` 문자열 다른 쌍(예 `(a-b,c)` vs `(a,b-c)`→`a-b-c`)→BuildError(`combo path collision`). 전 조합 경로 유일성 빌드타임 강제.
  - 테스트: GC-4(충돌 조합 주입 시 `BuildError`) (pytest) — RED 먼저
  - refs: SP-GEN-3.3 · FR-60 R3 · NFR11

## T-07.4 Jinja2 환경·포맷 헬퍼·base 템플릿  — (SP-GEN-4, FR-52~55·61·64·04·05·54, NFR6·8·12·13·21·24·29, SP-ARCH-7)
- [v] T-07.4.1 make_env — Jinja Environment (autoescape·StrictUndefined·필터)
  - 구현: `generator/render.py` `make_env(templates_dir="generator/templates")->Environment` — `FileSystemLoader`, `select_autoescape(["html","xml"], default_for_string=True)`(NFR21), `trim_blocks=True`·`lstrip_blocks=True`, `undefined=StrictUndefined`(누락 변수=렌더 오류). 필터 등록: `krw`·`badge`·`jsonld`·`isodate`·`ws_label`.
  - 테스트: GC-1(env 렌더 스모크) · autoescape/StrictUndefined 상세는 T-07.11.4(GC-21)·SEO 케이스 위임 (pytest) — RED 먼저
  - refs: SP-GEN-4.1 · NFR21 · SP-ARCH-7
- [v] T-07.4.2 표시 포맷 헬퍼 krw_manwon·iso_date·work_style_label
  - 구현: `generator/format.py` `krw_manwon(amt)`(None→""·`divmod(amt,10000)`→"N억 M,MMM만원"/"N억원"/"M,MMM만원", 천단위 콤마, FR-04) · `iso_date(v)`(None→""·문자열/date→`YYYY-MM-DD` 10자) · `work_style_label(key)`(`WS_LABELS` remote→재택근무·flex→유연근무·unlimitedPTO→무제한 휴가·refreshLeave→리프레시 휴가·overtime→야근 있음(고지), 미상→key).
  - 테스트: `test_format` 직접 단위(krw `1200`→`"1,200만원"`·iso `"2026-04-15"`→`"2026-04-15"`·ws `remote`→`재택근무`) + 소비 GC-11(복지 금액)·GC-12(근무형태 라벨) (pytest) — RED 먼저
  - refs: SP-GEN-4.3 · FR-04·FR-52 · NFR15
- [v] T-07.4.3 badge_state — 공식/추정/만료 3파생 (FR-54)
  - 구현: `generator/format.py` `badge_state(benefit, now)->{code,label}` — `expires_dtm < now`→`{stale,"만료·재확인 필요"}`, `badge_cd=="official"`→`{official,"공식 확인"}`, else `{est,"추정"}`. **밴드 계수(DEC-2) 미산출**(SP-CALC 소유, INV-5). `now` 인자 주입(결정성).
  - 테스트: GC-13(만료 지난/유효 official/est 표본 → 라벨 정확·색상 아닌 텍스트 라벨 존재, NFR15) (pytest) — RED 먼저
  - refs: SP-GEN-4.3 · FR-54·FR-05 · INV-5 · NFR15
- [v] T-07.4.4 jsonld_dumps — script-safe JSON (NFR21)
  - 구현: `generator/format.py` `jsonld_dumps(obj)->str` — `json.dumps(ensure_ascii=False, separators=(",",":"))` 후 `<`→`<`·`>`→`>`·`&`→`&` 치환(script breakout·HTML 파싱 오염 차단). 템플릿 `{{ jsonld | jsonld | safe }}` 유일 `| safe` 예외 경로.
  - 테스트: GC-9(JSON-LD `json.loads` 파싱 오류 0) · GC-21(JSON-LD도 `<` 안전) (pytest) — RED 먼저
  - refs: SP-GEN-4.3 · NFR21·NFR8
- [v] T-07.4.5 base.html 상속 골격·head 필수요소·랜드마크·비-JS 본문
  - 구현: `generator/templates/base.html` `<html lang="ko">`·head(`{% block head %}{% include "partials/_head_meta.html" %}` `{% block jsonld %}`) — charset utf-8·viewport·title·description·canonical·OG 4~5종·폰트 preload(`font-display: swap`, NFR5)·랜드마크 header/nav/main/footer(NFR13)·`{% block main %}`. partials 골격: `_head_meta.html`·`_jsonld_org.html`·`_header.html`·`_footer.html`·`_benefit_table.html`·`_badge.html`·`_disclaimer.html`. 본문 JS 없이 완성(`<script>`=광고/enhancement 전용, 본문 표시·CTA 무차단, NFR12·24).
  - 테스트: GC-5(head 필수태그 각 1)·GC-6(`<h1>` 정확히 1)·GC-26(`<html lang="ko">`·`<meta charset="utf-8">`·한글 미깨짐). 비-JS 본문 GC-10은 Tier0 T-07.11.3 위임 (pytest) — RED 먼저
  - refs: SP-GEN-4.2 · FR-55 · NFR5·6·12·13·24·29

## T-07.5 회사 상세 페이지 본문 렌더  — (SP-GEN-5, FR-52·53·54·57·58, NFR13·15·16·21·24)
- [v] T-07.5.1 _company_view — 뷰모델 파생 (기업정보·근무형태)
  - 구현: `generator/pages/company.py` `_company_view(c, ctx, now)->dict` — `comp_nm`(→`<h1>`)·`industry_nm`·`comp_tp_nm`(`types_by_cd` 조인)·`logo_nm`(텍스트 모노그램, 이미지 URL 창작 금지 FR-52)·`work_style`(`("remote","flex","unlimitedPTO","refreshLeave","overtime")` 중 `ws.get(k)` true 키만 `(k,v)`)·`benefit_groups`·`compare_href`=`/compare?a={comp_eng_nm}`.
  - 테스트: GC-12(`work_style_val` false/누락 키 "제공" 미노출·true만) · GC-6(`<h1>`=comp_nm) (pytest) — RED 먼저
  - refs: SP-GEN-5.2 · FR-52 · NFR13
- [v] T-07.5.2 _group_benefits — 9카테고리 그룹·정렬·정성/금액·출처 스킴 (FR-53·54)
  - 구현: `generator/pages/company.py` `_group_benefits(benefits, now)` — `CATEGORY_ORDER`(9종 compensation…perks)·`CATEGORY_LABEL`, 버킷 item `{name·amount(krw_manwon, qual_yn면 "")·qual·qual_desc·note·badge(badge_state)·src_cd·src_url(_safe_http)·verified(iso_date)·expires(iso_date)·sort}`, `sort_order_no` 정렬, 비지 않은 카테고리만 `(k,label,items)` 반환. `_safe_http(url)`(`http://`/`https://`만, 아니면 None → 링크 차단 배지만).
  - 테스트: GC-11(비지 않은 카테고리 ≥1·프리셋 폴백 흔적 없음, INV-6) · GC-14(`badge_src_url_ctnt="javascript:…"` → 링크 미생성·http/https만 `<a href>`) (pytest) — RED 먼저
  - refs: SP-GEN-5.3 · FR-53·54 R3 · INV-6 · NFR21
- [v] T-07.5.3 company.html + render_all (복지표·CTA·면책·광고 자리 조립)
  - 구현: `generator/pages/company.py` `render_all(env, ctx)->list[Page]`(회사 루프: slug·url·`_company_view`·`_company_seo`·`tpl.render(**vm,**seo,cfg=CFG)`→Page) + `generator/templates/company.html`(extends base, main=기업정보 G2+근무형태 G3+복지표 G4/`_benefit_table`+CTA C1+면책 B6/`_disclaimer`+광고 슬롯 `content_mid`/`content_bottom`). CTA=순수 `<a href="/compare?a={COMP_ENG_NM}">이 회사로 비교하기</a>`(JS 없이, `slot`/`prefill` 미방출, SP-FE-11 계약). 면책 블록+`/disclaimer` 링크 전 회사 노출(D4.1).
  - 테스트: GC-16(회사 CTA `href="/compare?a={comp_eng_nm}"` 순수 `<a>`·slot/prefill 미방출) · GC-15(데이터 정확성 면책 블록+`/disclaimer` 링크) (pytest) — RED 먼저
  - refs: SP-GEN-5.1·5.4·5.5 · FR-53·54·57·58 · NFR16·24

## T-07.6 회사 상세 SEO `<head>`·구조화 데이터  — (SP-GEN-6, FR-55·59, NFR6·7·8·11)
- [v] T-07.6.1 _company_seo — title·description·OG·canonical·JSON-LD 뷰모델
  - 구현: `generator/pages/company.py` `_company_seo(c, ctx, url)->dict` — `title`=`"{comp_nm} 복지·연봉·근무조건 | jobcho.wiki"`(고유), `desc`(유형·산업·대표복지 3개·항목 수, `_truncate(desc, CFG.desc_max=155)` 회사명 보존), `jsonld`={`@context`·`@type=Organization`·`name`·`url`·`alternateName`=`[comp_eng_nm,*aliases]`·`industry`}(빈값 제거, **로고이미지/주소/전화 미포함** FR-55), 반환 `meta_title`·`meta_desc`·`canonical`(=url)·`og`{title·description·`type=website`·url·`image`=`site_origin+default_og_image`}·`jsonld`.
  - 테스트: GC-5(title·meta description·canonical·og:title/description/type/url/image 각 1) · GC-9(JSON-LD `@type=Organization`·name=comp_nm·alternateName에 comp_eng_nm·로고/주소/전화 키 부재) · GC-8(canonical==og:url==`/company/{slug}`) (pytest) — RED 먼저
  - refs: SP-GEN-6.1·6.2 · FR-55 · NFR6·7·8·11
- [v] T-07.6.2 _head_meta·_jsonld_org 파셜 — OG/canonical/JSON-LD 삽입 마크업
  - 구현: `generator/templates/partials/_head_meta.html`(title·`<meta name="description">`·`<link rel="canonical">`·og:title/description/type/url/image) + `_jsonld_org.html`(`<script type="application/ld+json">{{ jsonld | jsonld | safe }}</script>`). company.html `{% block jsonld %}`=`_jsonld_org`. 자기참조 canonical=og:url(NFR11).
  - 테스트: GC-5(회사 head 태그 각 1)·GC-8(canonical==og:url) — 파셜은 `_company_seo` 값(T-07.6.1)을 소비해 마크업화 (pytest) — RED 먼저
  - refs: SP-GEN-6.2 · FR-55 · NFR7·11

## T-07.7 인기 조합 선정·조합 페이지 렌더  — (SP-GEN-7, FR-60·61·62·63·64, NFR6·9·11·12·24)
- [v] T-07.7.1 combinations.json — 인기 조합 큐레이션 목록 (FR-60)
  - 구현: `generator/data/combinations.json` `{"combinations":[{"a","b","note"}]}` — `a`·`b`=`COMP_ENG_NM`(정본 SP-SEED-2), `note`(선택 내부용). 대표성 있는 인기 회사쌍만(전수 조합 아님, 목록 외 경로=404 FR-60 R1). 코드 무수정 교체 데이터 파일.
  - 테스트: GC-1(조합 2 생성 소비)·GC-23(무효 조합 스킵 소비) — 목록 로드·검증은 T-07.7.2 (pytest) — RED 먼저
  - refs: SP-GEN-7.1 · FR-60 R1
- [v] T-07.7.2 combo render_all — 로드·무효 스킵·경로 검증·렌더
  - 구현: `generator/pages/combo.py` `render_all(env, ctx, cfg)->list[Page]` — `combinations.json` 로드, `a not in by_eng`/`b not in by_eng`/`a==b` 무효 조합 `log.warning` 스킵(빌드 계속), `validate_combo_paths(pairs, slugs)`(경로 충돌→BuildError 위임 T-07.3.4), 각 pair `combo_slug`·정렬 첫 회사→슬롯 a·`url=/vs/{path}`·`_combo_view`·`_combo_seo`·`tpl.render`→Page.
  - 테스트: GC-23(combinations에 미등록 eng 포함 → 해당 조합 미생성·경고 로그·빌드 성공) (pytest) — RED 먼저
  - refs: SP-GEN-7.2 · FR-60
- [v] T-07.7.3 _combo_view — 사전 계산 비교 요약 (FR-61)
  - 구현: `generator/pages/combo.py` `_combo_view(a_first, a_second, ctx, pairs)` — M1 기업정보(정식명·`comp_tp_nm`·`industry_nm`·`logo_nm` 양사, 유형 미보유 생략)·M2 근무형태 5축 대조(true만 "제공")·M3 9카테고리 항목수·대표복지·정량금액(만원)+배지(공식/추정/만료)·M4 "개인 입력 없는 사전값 기준" 고지+비교 툴 진입 안내·M5 면책+`/disclaimer`. 개인화 판정(vdCard)·실효연봉·시간가치 **미렌더**(FR-61 R1), 정성(`qual_yn`)은 금액 제외.
  - 테스트: GC-13(배지 3파생 조합) · M4 사전값 고지·비-JS 본문은 GC-10(T-07.11.3)·GC-1 스모크 소비 (pytest) — RED 먼저
  - refs: SP-GEN-7.3 · FR-61 R1·R4 · INV-6
- [v] T-07.7.4 combo.html + 양사 프리필 CTA (FR-62)
  - 구현: `generator/templates/combo.html`(extends base, main=사전요약 M1~M5+양사 CTA V+내부링크 L+광고 `content_mid`/`content_bottom`, `<h1>`="A vs B") + CTA=순수 `<a href="/compare?a={ENG_FIRST}&b={ENG_SECOND}">비교 툴에서 열기</a>`(정렬 첫→슬롯 a(현직)·둘째→슬롯 b(이직처), 명시 파라미터·경로 파싱 비의존 FR-62 R2, `slot`/`prefill` 미방출).
  - 테스트: GC-16(조합 CTA `href="/compare?a={engA}&b={engB}"`·slot/prefill 미방출) · GC-6(`<h1>`="A vs B" 1개) (pytest) — RED 먼저
  - refs: SP-GEN-7.4 · FR-62 R2 · NFR16·24
- [v] T-07.7.5 _related — 내부 링크(회사 상세·관련 조합) (FR-63)
  - 구현: `generator/pages/combo.py` `_related(eng_first, eng_second, ctx, all_pairs)` — L1 `company_links`(양사 `/company/{slug}` 항상 2개) + L2 `related`(생성된 조합 중 공유 회사 or 동일 `industry_nm`, `[:6]`, 없으면 미표시 FR-63 R1). **실제 생성 페이지로만** 한정(404 유입 방지).
  - 테스트: GC-20(전 생성 HTML 내부 `href`(`/company/…`·`/vs/…`)가 실제 생성 파일 또는 허용 라우트(`/`·`/compare`·정책)에 매핑·미존재 링크 0) (pytest) — RED 먼저
  - refs: SP-GEN-7.5 · FR-63 R1
- [v] T-07.7.6 _combo_seo — 조합 SEO head·canonical 정규화 (FR-64)
  - 구현: `generator/pages/combo.py` `_combo_seo(a_first, a_second, url)` — `title`=`"{A} vs {B} 복지·연봉 비교 | jobcho.wiki"`(두 회사명·중복 0), `desc`(두 회사 처우·복지·근무형태 요지, 155 절단), `og`(title·description·`type=website`·url·image 기본), `canonical`=자기참조 `/vs/{first}-{second}`(역순·변형 동일 정규화 FR-60 R2). JSON-LD 필수 아님.
  - 테스트: GC-5(조합 head 태그 각 1) · GC-8(canonical==og:url==`/vs/{first}-{second}`) · GC-22(역순 동일 canonical·파일 1개) (pytest) — RED 먼저
  - refs: SP-GEN-7.6 · FR-64·FR-60 R2 · NFR6·11

## T-07.8 광고 슬롯 자리·정책/404 렌더  — (SP-GEN-8 + 정책 렌더, FR-58·65·80, NFR5·19·22·24)
- [v] T-07.8.1 _ad_slot.html — 광고 슬롯 자리 (FR-58·65) **(2026-07-19 개정 — SP-ADS-9 계약 정렬, 커밋 557ef02)**
  - 구현(개정): `generator/templates/partials/_ad_slot.html`은 셸과 동일한 빈 호스트 `<div data-ad-position="{{ slot_name }}"></div>`만 방출한다 — 구판 aside.ad-slot(+data-ad-client+라벨+min-height)은 ads.js `mountManualSlot`이 못 찾는 별개 계약이었고 승인 전에도 빈 '광고' 박스를 상시 렌더했다(감사 #12). 라벨·예약·<ins>·placeholder 게이팅은 전부 SP-ADS 런타임 소유. 배치(WIREFRAME §5.1): `content_mid`(CTA 아래)·`content_bottom`(내부링크 아래) 각 1 + `[data-affiliate-host]` 인접, **복지표·요약·내부링크 구획 내부 호스트 0**. base.html이 `<body data-page-type>`·`_consent_banner.html`·`static-ads.js`를 전 페이지 방출(SP-GEN-8.2 개정판).
  - 테스트: GC-17 개정(빈 호스트 2·구 마크업 0·구획 내부 0) + GC-24(page-type·배너·스크립트·제휴호스트) (pytest) — RED 먼저
  - refs: SP-GEN-8.1·8.2 · FR-58·65 · NFR5·19·22·24
- [v] T-07.8.2 policy.py render_all — 정책 4종·404 렌더·병합 (SP-POL 문안 소비)
  - 구현: `generator/pages/policy.py` `render_all(env, ctx)->list[Page]` — `CFG.policy_pages` 4종(privacy·terms·disclaimer·ads) 본문 문안(SP-POL 소비, 재작성 금지) → `policy.html`(extends base, main=`{{ policy_body | safe }}` 사전 신뢰)로 감싸 SEO·canonical·sitemap 엔트리 부여, + `404.html`(extends base, main=안내, `in_sitemap=False`). 정책 4종 `in_sitemap=True`.
  - 테스트: GC-1(정책 4·404 Page 생성) · 정책 sitemap 포함·404 부재는 GC-18(T-07.9.1) 위임. 문안 정합은 SP-POL(PC) 위임 (pytest) — RED 먼저
  - refs: SP-GEN-1.1·4.2 · FR-80(렌더) · SP-POL(문안 위임)

## T-07.9 sitemap.xml·robots.txt (사이트 전역)  — (SP-GEN-9, FR-56·64, NFR9·10·12)
- [v] T-07.9.1 render_sitemap + render_robots + sitemap.xml 템플릿
  - 구현: `generator/pages/sitemap.py` `render_sitemap(env, urls, lastmod)->Page`(`sorted(set(urls))` 중복 0, `sitemap.xml` 템플릿 render, `content_type="application/xml; charset=utf-8"`, `in_sitemap=False`) + `render_robots()->Page`(`User-agent: *`·`Allow: /`·`Disallow: /api/`·`Sitemap: {site_origin}/sitemap.xml`, `text/plain`). `generator/templates/sitemap.xml`(`urlset`·`<url><loc>{{u}}</loc><lastmod>{{lastmod}}</lastmod></url>`). 포함 URL=회사~95+조합 N+정책 4+랜딩`/`; `/compare`·404 제외.
  - 테스트: GC-18(sitemap `<loc>`=회사+조합+정책 4+`/` 전부·누락 0·`/compare`·404 부재·각 loc==canonical·https) · GC-19(robots `Sitemap: https://jobcho.wiki/sitemap.xml` 라인 존재) (pytest) — RED 먼저
  - refs: SP-GEN-9.1·9.2 · FR-56·64 · NFR9·10·12

## T-07.10 릴리스 플로우·증분·매니페스트  — (SP-GEN-10·11, FR-50, NFR2·5·22, SP-ARCH-9)
- [v] T-07.10.1 stage_and_swap — 스테이징·gzip·해시·검증 게이트·원자 스왑
  - 구현: `generator/release.py` `stage_and_swap(out, pages, *, incremental, gzip)->dict` — `{out}.next` reset, pages 루프 `write_bytes`(sha256·bytes 기록), `_gzip_if_smaller`(level 9, `_is_compressible` `.html`/`.xml`/`.txt`, 원본보다 크면 `.gz` 생략), `run_generated_checks(nxt, pages)` 게이트, `_atomic_swap`(`os.replace` 2단: `{out}`→`{out}.prev` 백업 → `{out}.next`→`{out}` 승격). `write_manifest(out, manifest)`.
  - 테스트: GC-24(`.html.gz` 존재·decompress 시 원본 동일·`.manifest.json` sha256 일치) (pytest) — RED 먼저
  - refs: SP-GEN-11.1·11.2 · FR-50 · NFR2·5·22 · SP-ARCH-9
- [v] T-07.10.2 원자적 스왑 실패 시 산출물 유지 (SP-ARCH-9)
  - 구현: `generator/release.py` — `run_generated_checks` 검증 실패 시 예외 전파·스왑 중단·`{out}` 불변·`{out}.prev` 롤백 보존(무중단 지향). 부분 노출 없음.
  - 테스트: GC-25(검증 강제 실패 주입 → 스왑 중단·기존 `web/dist` 불변·예외 전파) (pytest) — RED 먼저
  - refs: SP-GEN-11.1 · SP-ARCH-9
- [v] T-07.10.3 증분·전체·필터 모드·매니페스트 (SP-GEN-10)
  - 구현: `generator/release.py` 증분 경로(각 Page `sha256(html)` vs 이전 `.manifest.json`, 일치+파일 존재 시 이전본 복사·불일치만 기록; sitemap·robots·매니페스트 항상 갱신) + `build.py` `--incremental`/`--only` 접두 필터. `.manifest.json`={`generated_at`·`files{path:{sha256·bytes·gz}}`}(gitignore). 증분도 검증 통과해야 스왑.
  - 테스트: GC-24(매니페스트 sha256 일치·증분 재빌드 변경분만 기록·sitemap 전 URL 재계산) (pytest) — RED 먼저
  - refs: SP-GEN-10.1·10.2 · FR-50 · SP-ARCH-9

## T-07.11 생성물 검증 게이트·픽스처 (Tier-0)  — (SP-GEN-12, FR-59, NFR6·12·21·24·29, INV-3·6)
- [v] T-07.11.1 fake 번들 픽스처·conftest
  - 구현: `generator/tests/fixtures.py` `FAKE_BUNDLE`(`company_types`·`benefit_presets`·`companies`[회사 3: samsung_elec·sk_hynix·naver, 각 `comp_eng_nm`·`comp_nm`·`comp_tp_cd`·`industry_nm`·`logo_nm`·`work_style_val`·`aliases[]`·`benefits[]`]) + XSS 변형 번들(`comp_nm="<script>alert(1)</script>삼성"`) + `generator/tests/conftest.py`(fixture 노출). 조합 2개용 소스. SP-API-6/7 계약 필드 1:1.
  - 테스트: 픽스처 자체 — GC-1~26 전 스위트가 소비(무 DB). 픽스처 부재 시 전 테스트 수집 실패(RED) (pytest) — RED 먼저
  - refs: SP-GEN-12.1 · FR-50
- [v] T-07.11.2 회사 페이지 개수 검증 (GC-2) (Tier0)
  - 구현: `generator/tests/test_pipeline.py` — 회사 HTML 파일 수 == 번들 회사 수(fake=3; 실 시드 스모크 ≥90, **200 아님** INV-6). 구현측 담보=`company.render_all`(회사당 정확히 1 페이지·프리셋 폴백 없음). **Tier-0 INV-6 회귀 게이트**.
  - 테스트: GC-2(회사 HTML 파일 수=번들 회사 수, 200 아님) (pytest) — RED 먼저
  - refs: SP-GEN-12(GC-2)·5.1 · FR-59 · **INV-6**
- [v] T-07.11.3 비-JS 본문 검증 (GC-10) (Tier0)
  - 구현: `generator/tests/test_pipeline.py` — 각 회사(및 조합) HTML에서 `<script>…</script>` 제거 후 텍스트에 회사명 + 복지 항목명 ≥1 존재(광고/JS 없이 본문 가독·색인 유지). 구현측 담보=base/company 템플릿(script는 enhancement 전용). **Tier-0 INV-3 회귀 게이트**.
  - 테스트: GC-10(script 제거 후 회사명·복지 항목명 잔존) (pytest) — RED 먼저
  - refs: SP-GEN-12(GC-10)·4.2·5 · FR-E5 · NFR12·24 · **INV-3**
- [v] T-07.11.4 XSS 이스케이프 검증 (GC-21) (Tier0)
  - 구현: `generator/tests/test_escape.py` — `comp_nm="<script>alert(1)</script>삼성"` 주입 번들 렌더 시 출력에 실행 가능한 `<script>alert` 부재(autoescape), JSON-LD도 `<`로 안전(`jsonld_dumps`). 구현측 담보=`make_env` autoescape(T-07.4.1)+`jsonld_dumps`(T-07.4.4). **Tier-0 NFR21 회귀 게이트**.
  - 테스트: GC-21(XSS 표본 → 이스케이프됨·JSON-LD `<` 안전) (pytest) — RED 먼저
  - refs: SP-GEN-12(GC-21)·4.1·4.3 · **NFR21**
- [v] T-07.11.5 title·description 페이지 간 중복 0 (GC-7)
  - 구현: `generator/tests/test_seo_meta.py` — 전 페이지(회사+조합) `Page.title`·`Page.description` 집합 수집·중복 0(회사명/조합명 반영으로 유일). 구현측 담보=`_company_seo`·`_combo_seo` 파생 규칙(별도 로직 없음).
  - 테스트: GC-7(전 페이지 title·description 집합 중복 0, NFR6·FR-59) (pytest) — RED 먼저
  - refs: SP-GEN-12(GC-7)·6.2·7.6 · NFR6 · FR-59

## 위임 케이스 (다른 SP 스위트에 실구현/검증 — 본 도메인 이중 구현 없음)
- **SP-API(04) 위임**: `build_reference_bundle`(참조 번들 원시 SQL·조립 계약, 단일 소스 SP-ARCH-4). 생성기는 PYTHONPATH import로 **소비만**(재정의 금지). 필드맵 SP-API-6/7 정본. 실 시드 `--bundle-json` 스모크는 SP-API/SP-SEED 산출물 소비.
- **SP-POL(09) 위임**: 정책 4종(개인정보/약관/면책/광고고지) **본문 문안**·필수 섹션(PC-1~13). 본 도메인은 문안을 base 템플릿에 감싸 렌더·SEO·canonical·sitemap 엔트리 부여만(T-07.8.2).
- **SP-ADS(08) 위임**: 광고/제휴 슬롯 **컴포넌트·게이팅·클라이언트 주입 로직**(`ads.js`·`adsConfig`·`adPolicy`·`affiliate.json`, UT-ADS). 본 도메인은 정적 슬롯 자리·크기 예약·"광고" 표기·placeholder client id만(T-07.8.1).
- **SP-FE(06) 위임**: 비교 툴 SPA 셸·프리필 **소비**(REF 해석·`restoreFromPrefill`이 `a`/`b` 파라미터→슬롯 a/b). 본 도메인은 CTA가 방출하는 프리필 URL 파라미터 **형태**만 규정(FR-57·62).
- **SP-CALC≡SP-ENGINE(05) 위임**: 밴드 계수(DEC-2)·실효연봉·시간가치·vdCard 판정. 정적 페이지는 배지(공식/추정/만료) **표시만**(FR-54, `badge_state`는 밴드 미산출).
- **SP-INFRA(11) 위임**: Nginx location(`try_files`·`gzip_static`)·TLS·`release.sh` 오케스트레이션. 본 도메인은 생성기 CLI(`build.py`)·출력 계약·릴리스 산출물(gzip·해시·매니페스트) 규격만.
- **SP-DS(10) 위임**: `styles.css` `:root` 토큰·배지/밴드/광고 라벨 색·클래스. 템플릿은 클래스명만 참조.
- **SP-TEST(12) 위임**: 집계 러너(`run_tests.sh`)·릴리스 게이트(SP-ARCH-9 4단계). 본 도메인은 GC-1~26 전량 green 제공.

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-07.1.1 | SP-GEN-1.3 | 소비 GC-5·8·16·17·18·24 | FR-50 · NFR22 |
| T-07.1.2 | SP-GEN-1.2 | GC-1 | FR-50 · NFR1 · INV-2 |
| T-07.1.3 | SP-GEN-1.4 | GC-1 | FR-50 · SP-ARCH-3·9 |
| T-07.2.1 | SP-GEN-2.1·2.2 | 소비 GC-1·8·18·19 | FR-50 · SP-ARCH-6 |
| T-07.2.2 | SP-GEN-2.3 | GC-1 (조인→GC-12·13) | FR-50·52·60 |
| T-07.3.1 | SP-GEN-3.1 | GC-3 | FR-51 · NFR11 |
| T-07.3.2 | SP-GEN-3.2 | GC-3 | FR-59 · NFR11 |
| T-07.3.3 | SP-GEN-3.3 | GC-4·22 | FR-60 R2 · NFR11 |
| T-07.3.4 | SP-GEN-3.3 | GC-4 | FR-60 R3 · NFR11 |
| T-07.4.1 | SP-GEN-4.1 | GC-1 (XSS→GC-21) | NFR21 · SP-ARCH-7 |
| T-07.4.2 | SP-GEN-4.3 | test_format·GC-11·12 | FR-04·52 · NFR15 |
| T-07.4.3 | SP-GEN-4.3 | GC-13 | FR-54·05 · INV-5 · NFR15 |
| T-07.4.4 | SP-GEN-4.3 | GC-9·21 | NFR21·8 |
| T-07.4.5 | SP-GEN-4.2 | GC-5·6·26 | FR-55 · NFR5·6·12·13·24·29 |
| T-07.5.1 | SP-GEN-5.2 | GC-12·6 | FR-52 · NFR13 |
| T-07.5.2 | SP-GEN-5.3 | GC-11·14 | FR-53·54 R3 · INV-6 · NFR21 |
| T-07.5.3 | SP-GEN-5.1·5.4·5.5 | GC-16·15 | FR-53·54·57·58 · NFR16·24 |
| T-07.6.1 | SP-GEN-6.1·6.2 | GC-5·9·8 | FR-55 · NFR6·7·8·11 |
| T-07.6.2 | SP-GEN-6.2 | GC-5·8 | FR-55 · NFR7·11 |
| T-07.7.1 | SP-GEN-7.1 | 소비 GC-1·23 | FR-60 R1 |
| T-07.7.2 | SP-GEN-7.2 | GC-23 | FR-60 |
| T-07.7.3 | SP-GEN-7.3 | GC-13 (본문→GC-10·1) | FR-61 R1·R4 · INV-6 |
| T-07.7.4 | SP-GEN-7.4 | GC-16·6 | FR-62 R2 · NFR16·24 |
| T-07.7.5 | SP-GEN-7.5 | GC-20 | FR-63 R1 |
| T-07.7.6 | SP-GEN-7.6 | GC-5·8·22 | FR-64·60 R2 · NFR6·11 |
| T-07.8.1 | SP-GEN-8.1·8.2 | GC-17 | FR-58·65 · NFR5·19·22·24 |
| T-07.8.2 | SP-GEN-1.1·4.2 | GC-1 (sitemap→GC-18) | FR-80 · SP-POL(위임) |
| T-07.9.1 | SP-GEN-9.1·9.2 | GC-18·19 | FR-56·64 · NFR9·10·12 |
| T-07.10.1 | SP-GEN-11.1·11.2 | GC-24 | FR-50 · NFR2·5·22 · SP-ARCH-9 |
| T-07.10.2 | SP-GEN-11.1 | GC-25 | SP-ARCH-9 |
| T-07.10.3 | SP-GEN-10.1·10.2 | GC-24 | FR-50 · SP-ARCH-9 |
| T-07.11.1 | SP-GEN-12.1 | GC-1~26 픽스처 | FR-50 |
| T-07.11.2 | SP-GEN-12(GC-2)·5.1 | GC-2 (Tier0) | FR-59 · **INV-6** |
| T-07.11.3 | SP-GEN-12(GC-10)·4.2·5 | GC-10 (Tier0) | FR-E5 · NFR12·24 · **INV-3** |
| T-07.11.4 | SP-GEN-12(GC-21)·4.1·4.3 | GC-21 (Tier0) | **NFR21** |
| T-07.11.5 | SP-GEN-12(GC-7)·6.2·7.6 | GC-7 | NFR6 · FR-59 |
