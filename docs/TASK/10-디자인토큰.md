# TASK 10 — 디자인 토큰·CSS 시스템 (SP-DS)

> SPEC 계약: [SPEC/10-디자인-토큰.md](../SPEC/10-디자인-토큰.md) (SP-DS ≡ SP-DSN). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M0(스캐폴드) — 독립. 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: SP-ARCH-6(디렉토리 트리 `web/assets/css/styles.css`·`web/assets/fonts/` + **Pretendard 가변 woff2 self-host 바이너리 배치** — 본 문서는 `@font-face` 선언·`src` 경로 계약만 소유, 바이너리 획득·배치는 SP-ARCH 위임). 소비처=SP-FE(`styles.css` `<head>` 우선 로드·SPA 셸 클래스가 `var(--*)` 소비)·SP-GEN(`base.html` `<head>` preload·`font-display: swap`·정적 본문)·SP-ADS(`.ad-label`/`.ad-slot`/`.affiliate-card` 색·라벨 폰트 토큰 소비)·SP-RPT(흡수: 배지·밴드·판정 색 소비). 확정 순서는 00이 소유.
> 테스트: 러너=`node --test`(node:test, ES module — 정적 CSS 파싱 + WCAG 대비 유틸 `test/util/contrast.mjs`, 브라우저 렌더 없이 텍스트 수준 검증) + 수동(MD-*, axe/Lighthouse·육안) · 위치=`web/assets/css/styles.test.js`(파싱·린트 스위트)·`test/util/contrast.mjs`(대비 유틸) · 케이스 대역=UT-TOKEN-EXIST/CONTRAST-BODY/CONTRAST-T4/CONTRAST-ACCENT-TXT/CONTRAST-FILL/BADGE-CONTRAST/AD-LABEL/TYPE-SCALE/FONT-DISPLAY/SPACING/BP/REDUCED-MOTION/NO-LITERAL/DARK-DEFAULT(14 자동) + MD-1~6(6 수동).

## 진행 요약
- 리프 총 25 / 완료 19 / 진행 0 / 미착수 6(T-10.11.1~6 MD-* 수동 QA)
- **비주얼 폴리시 구현(2026-07-13)**: 옵션1대로 토큰만 있던 `styles.css`(89줄)에 **컴포넌트/레이아웃 스타일을 실제 작성**. **사용자 결정: 시안 A "신뢰형·라이트" 채택**(3개 시안 gstack 비교 후) → 원시 팔레트를 **라이트 기본**으로 재정의(다크는 후속 `[data-theme]` 훅), 컴포넌트는 `var(--*)`만 소비(UT-NO-LITERAL 유지). 회사페이지·랜딩·비교툴 3뷰·리포트·정책·헤더/푸터·광고·배지 전 표면 적용. `tokens.test.js` **UT-DARK-DEFAULT→UT-LIGHT-DEFAULT** 갱신, WCAG AA 대비 62/62 green. beta 라이브 gstack 검증(회사·랜딩·검색·입력·리포트 전 표면). 남은 MD-2~6(포커스/axe/200%/reduced-motion/광고라벨 육안)은 이 스타일 위에서 릴리스 QA로 확인.
- Tier-0 게이트 리프: **없음** (SP-DS 대비·토큰 린트 green은 **Tier-2**이며 SPEC.md §5 Tier-0 회귀 게이트 목록에 SP-DS 케이스가 없음 — 배포 차단 게이트는 SP-ENGINE·SP-API·SP-GEN·SP-ADS 소유)
- 릴리스 게이트 리프(비-Tier0, SP-DS-11 릴리스 게이트): 자동 = T-10.2.1·T-10.2.2·T-10.2.3(UT-CONTRAST-*)·T-10.3.2(UT-AD-LABEL)·T-10.4.1(UT-FONT-DISPLAY)·T-10.10.2(UT-NO-LITERAL) / 수동 = T-10.11.2(MD-2 포커스)·T-10.11.3(MD-3 axe)·T-10.11.6(MD-6 광고 라벨)
- 선결정 게이트: **없음** (SP-DS는 토큰값·이름·`:root` 구조·`@font-face`·테스트 케이스까지 SPEC이 확정 — 개방 결정/DG 없음. 값 polish는 게이트가 아니라 개발 후 실제 앱을 보며 반복(SP-DS-1 R3)하되 **토큰 이름·구조·대비 하한은 계약으로 고정**, SP-DS-11이 회귀 방지)

## T-10.1 토큰 시스템 스캐폴드·대비 유틸  — (SP-DS-1·SP-DS-10·SP-DS-11.1, NFR2·5·13·15·19, SP-ARCH-6, SP-FE-1)
- [v] T-10.1.1 `contrast.mjs` WCAG 대비·상대휘도 유틸(styles.css 파싱 주입)
  - 구현: `test/util/contrast.mjs` — `relLuminance(rgb)`(sRGB→선형화→`L`), `contrastRatio(fg,bg)`(`(L1+0.05)/(L2+0.05)`), `parseColor(str)`(`#hex`·`rgb(r g b / a)` 지원), `composite(fgWithAlpha, bg)`(반투명 `--x-dim` 배경 위 알파 합성), `parseTokens(cssText)`(`styles.css` `:root`에서 `--name:value` 추출·별칭 `var(--x)` 재귀 해석). 팔레트 값은 하드코딩 금지 — `styles.css` `:root`에서 파싱해 주입(SP-DS-11.1).
  - 테스트: 유틸 자체 단위 테스트(`node --test` — 알려진 대비값 sanity: `#000000`/`#ffffff`==21:1(±0.01), `#0d0f12`/`#f2f4f7`≈17.4, `-dim` 합성 후 대비 산출, `parseTokens`가 별칭 `var()` 해석) — RED 먼저
  - refs: SP-DS-11.1 · NFR15 · SP-ARCH §9(node:test 도구 정합)
- [v] T-10.1.2 `styles.css` 파일 스캐폴드·`@font-face` 블록·`:root` 골격·리셋
  - 구현: `web/assets/css/styles.css` 생성 — 상단 주석 배너, `@font-face`(SP-DS-4.1 자리, T-10.4.1이 값 채움), `:root { … }` 8블록 골격(원시 배경/텍스트·액센트·시맨틱·배지/밴드/광고·타이포·간격·반경·모션·브레이크포인트 — SP-DS-10 정본 순서), `*, *::before, *::after { box-sizing:border-box; }` 최소 리셋, `@media (prefers-reduced-motion: reduce)` 블록 자리(T-10.8.2). 컴포넌트 선택자 규칙은 미포함(각 SPEC 위임). 무빌드·단일 파일·외부 CSS CDN 0(R4·R5).
  - 테스트: 구조 스모크(`node --test` — 파일 존재·`:root` 파싱 성립·`@font-face` 블록 1개·`box-sizing:border-box` 리셋 존재·`parseTokens` 무크래시) — RED 먼저. SP-DS-11 전용 UT 없음(구조 스모크); 토큰 존재는 T-10.10.1(UT-TOKEN-EXIST)이 종합 검증
  - refs: SP-DS-1.1(R1·R4·R5)·1.2 · SP-DS-10 · NFR2·NFR5 · SP-ARCH-6 · SP-FE-1

## T-10.2 색 팔레트(다크 기본)  — (SP-DS-2, NFR13·15, WIREFRAME §7·§8)
- [v] T-10.2.1 배경·텍스트 계층 `--bg-0..4`·`--t1..4` + 본문 대비·다크 기본 (UT-CONTRAST-BODY·T4·DARK-DEFAULT)
  - 구현: `styles.css` `:root` — `--bg-0:#0d0f12`~`--bg-4:#39424f`(SP-DS-2.1 깊은→얕은 층), `--t1:#f2f4f7`·`--t2:#c3cad4`·`--t3:#8b94a3`·`--t4:#646d7c`(SP-DS-2.2). 규칙 T1: 본문은 `--t1`~`--t3`만(≥4.5:1), `--t4`는 비활성·장식·placeholder 한정. SP-DS-2.5 라이트 훅은 미부착=다크(코드는 주석 훅만, `data-theme` 재선언 자리).
  - 테스트: UT-CONTRAST-BODY(`--t1/t2/t3` × `--bg-0/1/2` 전 조합 ≥4.5:1)·UT-CONTRAST-T4(`--t4` on `--bg-0` = 3:1≤대비<4.5:1)·UT-DARK-DEFAULT(`--bg-0` 밝기 < `--t1` 밝기, `data-theme` 미부착=다크) (`node --test`, contrast.mjs) — RED 먼저
  - refs: SP-DS-2.1·2.2·2.5 (T1·ACC-1) · NFR15·NFR13 · FR-01(로그인·아바타 토큰 부재)
- [v] T-10.2.2 액센트 팔레트 `--blue/amber/green/red/purple/gold` + `-dim` (UT-CONTRAST-ACCENT-TXT)
  - 구현: `styles.css` `:root` — `--blue:#4c8dff`·`--amber:#f0a83c`·`--green:#43c489`·`--red:#ff6161`·`--purple:#a98cfb`·`--gold:#d9b45a`(SP-DS-2.3 원색, 텍스트/아이콘/테두리용) + `--*-dim: rgb(<원색> / 0.16)` 6종(반투명 배경/후광). 규칙 A2(색 비의존은 SP-RPT 아이콘·텍스트 병기)·A3(승/패 매핑 정본은 SP-DS-2.4)은 소비처 계약.
  - 테스트: UT-CONTRAST-ACCENT-TXT(6 액센트 원색 on `--bg-0` 전부 ≥4.5:1 — 텍스트/아이콘 사용 가능) (`node --test`, contrast.mjs) — RED 먼저
  - refs: SP-DS-2.3 (A2·A3) · NFR13·NFR15 · WIREFRAME §7·§8
- [v] T-10.2.3 시맨틱 색 별칭 `--accent/--accent-ink/--link/--positive/…/--focus` (UT-CONTRAST-FILL)
  - 구현: `styles.css` `:root` — `--accent:var(--blue)`·`--accent-ink:var(--bg-0)`·`--link:var(--blue)`·`--positive:var(--green)`·`--negative:var(--red)`·`--warning:var(--amber)`·`--brand-axis:var(--purple)`·`--text:var(--t1)`·`--text-muted:var(--t3)`·`--surface:var(--bg-1)`·`--surface-raised:var(--bg-2)`·`--border:var(--bg-3)`·`--border-strong:var(--bg-4)`·`--focus:var(--blue)`(SP-DS-2.4). 규칙 A1: 액센트 채움 버튼 텍스트=`--accent-ink`(어두운). polish 시 원시→별칭 매핑만 변경.
  - 테스트: UT-CONTRAST-FILL(각 액센트 배경 + `--accent-ink`(=`--bg-0`) 전부 ≥4.5:1 — 흰 텍스트 대비 미달 회귀 방지, 특히 `--amber`/`--gold`) (`node --test`, contrast.mjs) — RED 먼저. **규칙 회귀**: `--text`/`--text-muted` 별칭이 `--t4`로 매핑되면 실패(UT-CONTRAST-T4, T-10.2.1)가 가드
  - refs: SP-DS-2.4 (A1) · NFR15 · WIREFRAME §8(BTN-1)

## T-10.3 배지·밴드·광고 라벨 색  — (SP-DS-3, WIREFRAME §7, FR-38, FR-76, NFR19, SP-ADS-5.2)
- [v] T-10.3.1 배지·밴드 상태 색 `--badge-*-fg/-bg`·`--band-*-fg` (UT-BADGE-CONTRAST)
  - 구현: `styles.css` `:root` — 배지 4쌍(SP-DS-3.1): `--badge-official-fg:var(--green)`/`-bg:var(--green-dim)`·`--badge-est-fg:var(--t3)`/`-bg:var(--bg-3)`·`--badge-expired-fg:var(--amber)`/`-bg:var(--amber-dim)`·`--badge-qual-fg:var(--t2)`/`-bg:var(--bg-3)`; 밴드(SP-DS-3.2): `--band-label-fg:var(--t3)`·`--band-expired-fg:var(--amber)`. 배지 표시 마크업은 SP-RPT, 밴드 계수(±5/±20/+15)는 SP-CALC(DEC-2) 소유 — 본 리프는 색 토큰만.
  - 테스트: UT-BADGE-CONTRAST(`--badge-*-fg` on 대응 `--*-bg`, `-dim`은 `--bg-0` 위 합성 후 배경 — official/expired ≥4.5:1, est/qual 뮤트 ≥4.5:1) (`node --test`, contrast.mjs) — RED 먼저
  - refs: SP-DS-3.1·3.2 · WIREFRAME §7(BG-1·BG-4·BG-6) · FR-38 · SP-CALC(DEC-2)
- [v] T-10.3.2 광고·제휴 라벨 색·크기 `--ad-label-fg/-fs`·`--ad-slot-bg/-border` (UT-AD-LABEL, 릴리스 게이트)
  - 구현: `styles.css` `:root` — `--ad-label-fg:var(--t3)`(대비 6.3:1 on `--bg-0`, 은폐 아님)·`--ad-label-fs:12px`(WIREFRAME §0 11~12·SP-ADS-5.2 하한 11 준수)·`--ad-slot-bg:var(--bg-1)`·`--ad-slot-border:var(--bg-3)`(SP-DS-3.3). 규칙 AD1: `--ad-label-fg`에 `--t4`(<4.5:1) 금지(은폐 해석 방지). 값은 SP-ADS `.ad-label`/`.ad-slot` 규칙이 소비(클래스·`min-height`는 SP-ADS 계약).
  - 테스트: UT-AD-LABEL(`--ad-label-fg` on `--ad-slot-bg` 대비 ≥4.5:1(은폐 아님) & `--ad-label-fs` ≥11px(SP-ADS 하한)) (`node --test`, contrast.mjs) — RED 먼저
  - refs: SP-DS-3.3 (AD1) · FR-76 · NFR19 · SP-ADS-5.2

## T-10.4 타입 스케일(Pretendard)  — (SP-DS-4, WIREFRAME §0, NFR5, SP-ARCH-6, SP-GEN)
- [v] T-10.4.1 `@font-face` Pretendard self-host·`font-display: swap`·`--font-sans` (UT-FONT-DISPLAY, 릴리스 게이트)
  - 구현: `styles.css` `@font-face` — `font-family:'Pretendard'`·`src:url('/assets/fonts/PretendardVariable.woff2') format('woff2-variations')`·`font-weight:45 920`(가변축)·`font-display:swap`(NFR5 FOIT 방지·폰트 CLS 억제)·`font-style:normal`; `:root --font-sans:'Pretendard',system-ui,-apple-system,'Apple SD Gothic Neo','Malgun Gothic','Noto Sans KR',sans-serif`(시스템 한글 폴백). woff2 바이너리 배치·`<head>` preload는 SP-ARCH-6·SP-GEN/SP-FE 위임(본 리프는 선언·경로 계약만).
  - 테스트: UT-FONT-DISPLAY(`@font-face`에 `font-display:swap` 존재·`src`가 self-host `/assets/fonts/…woff2`·외부 CDN URL 0) (`node --test`, CSS 소스 스캔) — RED 먼저
  - refs: SP-DS-4.1 · NFR5 · SP-ARCH-6 · SP-GEN(preload)
- [v] T-10.4.2 사이즈 스케일 `--fs-ad/xs/sm/base/lg/h3/h2/h1/display` (UT-TYPE-SCALE)
  - 구현: `styles.css` `:root` — `--fs-ad:12px`·`--fs-xs:13px`·`--fs-sm:14px`·`--fs-base:16px`·`--fs-lg:18px`·`--fs-h3:20px`·`--fs-h2:24px`·`--fs-h1:30px`·`--fs-display:38px`(SP-DS-4.2, 기준 본문 16·모듈러≈1.2~1.25). 반응형 `clamp()`는 polish 시 도입(토큰 계약 유지).
  - 테스트: UT-TYPE-SCALE(`--fs-ad<xs<sm<base<lg<h3<h2<h1` 엄격 증가, `--fs-base===16px`, `--fs-xs===13px`) (`node --test`, contrast.mjs `parseTokens`) — RED 먼저
  - refs: SP-DS-4.2 · WIREFRAME §0(본문 16·라벨 13·광고 11~12)
- [v] T-10.4.3 라인하이트·자간·굵기 `--lh-*`·`--ls-tight`·`--fw-*`
  - 구현: `styles.css` `:root` — `--lh-tight:1.25`·`--lh-base:1.6`·`--lh-snug:1.4`·`--ls-tight:-0.01em`·`--fw-regular:450`·`--fw-medium:550`·`--fw-semibold:640`·`--fw-bold:720`(SP-DS-4.3, 가변축값 45~920 범위 내). 규칙 F1(`body` 기본 타이포·색)·F2(제목 크기)·F3(200% 확대 무붕괴)는 소비처(SP-FE `body`) 계약.
  - 테스트: 존재·정합 린트(`node --test` — `--lh-base===1.6`(WIREFRAME §0)·`--fw-*` 값 전부 45~920 범위(=`@font-face font-weight` 축과 정합)) (`node --test`) — RED 먼저. 종합 존재는 T-10.10.1(UT-TOKEN-EXIST)
  - refs: SP-DS-4.3 (F1·F2·F3) · WIREFRAME §0 · NFR13

## T-10.5 간격 스케일·컨테이너  — (SP-DS-5, WIREFRAME §0·§2·§8, NFR2·13)
- [v] T-10.5.1 간격 토큰 `--sp-1..8`(4px 베이스) (UT-SPACING)
  - 구현: `styles.css` `:root` — `--sp-1:4px`·`--sp-2:8px`·`--sp-3:12px`·`--sp-4:16px`·`--sp-5:24px`·`--sp-6:32px`·`--sp-7:48px`·`--sp-8:64px`(SP-DS-5.1). `--sp-4`=기본 간격·모바일 거터, `--sp-5`=데스크톱 거터.
  - 테스트: UT-SPACING(`--sp-1..8` 4px 배수·단조 증가·`--sp-4===16px`) (`node --test`, contrast.mjs `parseTokens`) — RED 먼저
  - refs: SP-DS-5.1 · WIREFRAME §0 · NFR2
- [v] T-10.5.2 컨테이너·거터·터치 `--container-max`·`--gutter-*`·`--tap-min` (UT-SPACING)
  - 구현: `styles.css` `:root` — `--container-max:960px`(WIREFRAME §0 본문 max-width)·`--gutter-mobile:16px`(=`--sp-4`)·`--gutter-desktop:24px`(=`--sp-5`)·`--tap-min:44px`(터치 타깃 최소)(SP-DS-5.2). 규칙 SP1(컨테이너 `max-width`·`margin-inline:auto`·`padding-inline`)·SP2(터치 `min-height`)는 SP-FE/SP-GEN 레이아웃이 소비.
  - 테스트: UT-SPACING(`--gutter-desktop===24px`) + 존재 린트(`--container-max===960px`·`--tap-min===44px`) (`node --test`) — RED 먼저
  - refs: SP-DS-5.2 (SP1·SP2) · WIREFRAME §0·§2·§8 · NFR13

## T-10.6 반경·테두리·엘리베이션  — (SP-DS-6, WIREFRAME §5·§9, NFR2)
- [v] T-10.6.1 반경·테두리·그림자 `--r-*`·`--bw`·`--border-color`·`--shadow-1·2`
  - 구현: `styles.css` `:root` — `--r-xs:4px`·`--r-sm:8px`·`--r-md:12px`·`--r-lg:16px`·`--r-pill:999px`·`--bw:1px`·`--border-color:var(--bg-3)`·`--shadow-1:0 1px 2px rgb(0 0 0 / .35)`·`--shadow-2:0 6px 20px rgb(0 0 0 / .45)`(SP-DS-6). 규칙 E1(다크는 배경 층+테두리로 깊이, `--shadow-2`는 오버레이 한정)·E2(그림자·반경은 CLS 무관·polish 자유)는 소비처 계약.
  - 테스트: 존재·단조 린트(`node --test` — `--r-xs<--r-sm<--r-md<--r-lg`·`--r-pill===999px`·`--bw===1px`·`--shadow-1`/`--shadow-2` 존재) (`node --test`) — RED 먼저. 종합 존재는 T-10.10.1(UT-TOKEN-EXIST)
  - refs: SP-DS-6 (E1·E2) · WIREFRAME §5·§9 · NFR2

## T-10.7 브레이크포인트  — (SP-DS-7, WIREFRAME §0, 브리프 §8)
- [v] T-10.7.1 `--bp-desktop` 참고 토큰·768 단일 분기 계약 (UT-BP)
  - 구현: `styles.css` `:root` — `--bp-desktop:768px`(참고 토큰·문서 정본; 미디어쿼리엔 리터럴 `768px` 사용)(SP-DS-7). 규칙 BP1(768 단일 분기·중간 단계 없음)·BP2(모바일 우선, `min-width:768px`로 데스크톱 오버라이드)는 소비처 반응형 규칙 계약. 토큰 파일 자체의 min-width 미디어쿼리는 0(레이아웃은 위임).
  - 테스트: UT-BP(`--bp-desktop===768px` & `styles.css` 사용 `min-width` 미디어쿼리 = `768px` 단일 분기, 768 아닌 min-width 중간 분기 0) (`node --test`, CSS 소스 스캔) — RED 먼저
  - refs: SP-DS-7 (BP1·BP2) · WIREFRAME §0 · 브리프 §8

## T-10.8 애니메이션·모션 토큰  — (SP-DS-8, NFR2·13)
- [v] T-10.8.1 모션 토큰 `--dur-*`·`--ease-*`
  - 구현: `styles.css` `:root` — `--dur-fast:120ms`·`--dur-base:200ms`·`--dur-slow:320ms`·`--ease-out:cubic-bezier(.2,0,0,1)`·`--ease-inout:cubic-bezier(.4,0,.2,1)`·`--ease-standard:ease`(SP-DS-8). 규칙 MO1(`transform`/`opacity` 위주·레이아웃 속성 애니메이션 지양)·MO3(무한 반복·자동재생 금지)는 소비처 전이 계약.
  - 테스트: 존재·단조 린트(`node --test` — `--dur-fast<--dur-base<--dur-slow`·`--ease-out`/`--ease-inout` 존재) (`node --test`) — RED 먼저. 종합 존재는 T-10.10.1(UT-TOKEN-EXIST, `--dur-base`)
  - refs: SP-DS-8 (MO1·MO3) · NFR2
- [v] T-10.8.2 `prefers-reduced-motion: reduce` 블록 (UT-REDUCED-MOTION)
  - 구현: `styles.css` 하단 — `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration:.001ms !important; animation-iteration-count:1 !important; transition-duration:.001ms !important; scroll-behavior:auto !important; } }`(SP-DS-8 MO2, 모든 모션 사실상 제거). 파일 하단 고정.
  - 테스트: UT-REDUCED-MOTION(`@media (prefers-reduced-motion: reduce)` 블록 존재) (`node --test`, CSS 소스 스캔) — RED 먼저. 실효 검증은 MD-5(T-10.11.5, 수동)
  - refs: SP-DS-8 (MO2·ACC-5) · NFR13

## T-10.9 접근성 토큰·계약(대비 AA)  — (SP-DS-9, NFR13·15·2·5)
- [v] T-10.9.1 포커스 링 토큰 `--focus-width/--focus-offset`·접근성 계약표(ACC-1~6) 배선
  - 구현: `styles.css` `:root` — `--focus-width:2px`·`--focus-offset:2px`(`--focus`=`var(--blue)`는 T-10.2.3에서 정의)(SP-DS-9.2). 표준 규칙 주석(`:focus-visible { outline: var(--focus-width) solid var(--focus); outline-offset: var(--focus-offset); }`)은 소비처(SP-FE/컴포넌트)가 적용(규칙 ACC-R1: `outline:none` 단독 금지). 계약표 ACC-1(본문≥4.5)·ACC-2(UI/대형≥3)는 UT-CONTRAST-* 위임, ACC-3(포커스)·4(색 비의존)·5(모션)·6(터치)는 수동/소비처 위임.
  - 테스트: 존재 린트(`node --test` — `--focus`/`--focus-width===2px`/`--focus-offset===2px`) (`node --test`) — RED 먼저. ACC-1·2 회귀는 UT-CONTRAST-*(T-10.2.*·T-10.3.*), ACC-3 가시성은 MD-2(T-10.11.2, 수동) 위임
  - refs: SP-DS-9.1·9.2 (ACC-1~6·ACC-R1) · NFR13·NFR15·NFR2·NFR5

## T-10.10 토큰 존재·리터럴 산발 종합 린트  — (SP-DS-11.2 종합, SP-DS-1 R1·R5)
- [v] T-10.10.1 필수 토큰 집합 존재 종합 린트 (UT-TOKEN-EXIST)
  - 구현: (린트 — 신규 CSS 없음, T-10.2~10.9 토큰이 모두 정의된 뒤 종합 검증) `styles.test.js` — `parseTokens(styles.css)` → 필수 토큰 집합 존재 확인. 이 리프는 전 토큰 그룹이 랜딩된 뒤 독립 완료(누락 0 게이트).
  - 테스트: UT-TOKEN-EXIST(`--bg-0..4`·`--t1..4`·`--blue/amber/green/red/purple/gold`(+`-dim`)·`--accent`·`--accent-ink`·`--focus`·`--badge-official-fg`·`--band-label-fg`·`--ad-label-fg`·`--font-sans`·`--fs-base`·`--sp-4`·`--container-max`·`--tap-min`·`--r-sm`·`--dur-base`·`--bp-desktop` 등 전 계약 토큰 **누락 0**) (`node --test`, contrast.mjs `parseTokens`) — RED 먼저
  - refs: SP-DS-11.2 · SP-DS-1(R1) · SP-DS-10(정본 스니펫)
- [v] T-10.10.2 리터럴 산발 린트 (UT-NO-LITERAL, 릴리스 게이트)
  - 구현: (린트 — 신규 CSS 없음) `styles.test.js` — `styles.css`에서 `:root`·`@font-face`·`@media` 블록을 제외한 컴포넌트 규칙을 스캔해 색 리터럴(`#hex`·`rgb(`) 직접 등장을 탐지·위반 목록 리포트(R1: 컴포넌트는 `var(--*)`만). 토큰 단계엔 컴포넌트 규칙이 ~0이라 통과하되, SP-FE/SP-RPT/SP-ADS/SP-GEN이 `styles.css`에 규칙을 추가할 때 회귀 가드로 상주.
  - 테스트: UT-NO-LITERAL(`:root`/`@font-face`/`@media` 외 규칙에 색 리터럴 직접 등장 0 — 전부 `var(--*)`, 위반 목록 리포트) (`node --test`, CSS 소스 스캔) — RED 먼저
  - refs: SP-DS-11.2 · SP-DS-1(R1·R5)

## T-10.11 수동 QA 체크리스트(SP-DS-11.3 — polish 단계, 자동 아님)  — (SP-DS-9·SP-DS-11.3, NFR2·13·19)
- [ ] T-10.11.1 MD-1 실제 앱 다크 화면 육안
  - 구현: (수동 검증 — 신규 코드 없음) 개발 후 실제 앱 다크 화면 육안 관찰(polish 대상 발견 시 값만 조정, 이름·구조·대비 하한 고정 R3).
  - 테스트: MD-1 (수동, 육안) — 배경 층 구분·본문 가독·액센트 톤 자연. 대비 하한 회귀는 UT-CONTRAST-*(T-10.2·10.3)가 자동 가드
  - refs: SP-DS-11.3 · SP-DS-1(R3) · NFR15
- [ ] T-10.11.2 MD-2 키보드 Tab 포커스 링 가시 (릴리스 게이트)
  - 구현: (수동 검증) 키보드 Tab 순회로 전 포커스 대상 관찰.
  - 테스트: MD-2 (수동, 키보드) — 모든 포커스 대상에 `--focus` 링 가시(ACC-3). **릴리스 게이트**(NFR13). 토큰 존재는 T-10.9.1
  - refs: SP-DS-9.1(ACC-3) · SP-DS-11.3 · NFR13
- [ ] T-10.11.3 MD-3 axe/Lighthouse 접근성 (릴리스 게이트)
  - 구현: (수동 검증) axe/Lighthouse 접근성 스캔 실행.
  - 테스트: MD-3 (수동, axe/Lighthouse) — critical 위반 0·대비 경고 0. **릴리스 게이트**(NFR13). 자동 대비 회귀는 UT-CONTRAST-*
  - refs: SP-DS-9.1(ACC-1·2) · SP-DS-11.3 · NFR13·NFR15
- [ ] T-10.11.4 MD-4 200% 확대 레이아웃 무붕괴
  - 구현: (수동 검증) 브라우저 200% 확대 상태로 주요 화면 관찰.
  - 테스트: MD-4 (수동, 확대) — 레이아웃 붕괴·가로 스크롤 없음(F3)
  - refs: SP-DS-4.3(F3) · SP-DS-11.3 · NFR13
- [ ] T-10.11.5 MD-5 `prefers-reduced-motion` ON 모션 제거
  - 구현: (수동 검증) OS/브라우저 `prefers-reduced-motion` ON 상태로 전환·애니메이션 관찰.
  - 테스트: MD-5 (수동) — 전환·애니메이션 사실상 제거(ACC-5). 블록 존재 회귀는 UT-REDUCED-MOTION(T-10.8.2)
  - refs: SP-DS-8(MO2·ACC-5) · SP-DS-11.3 · NFR13
- [ ] T-10.11.6 MD-6 광고 슬롯 "광고" 라벨 인지·콘텐츠 구분 (릴리스 게이트)
  - 구현: (수동 검증) 실제 광고 슬롯 렌더 후 육안 관찰(슬롯 마크업은 SP-ADS, 색·라벨 토큰은 본 도메인).
  - 테스트: MD-6 (수동, 육안) — "광고" 라벨 인지 가능(은폐 아님)·콘텐츠와 시각 구분. **릴리스 게이트**(NFR19·SP-ADS M-5). 자동 대비·크기 회귀는 UT-AD-LABEL(T-10.3.2)
  - refs: SP-DS-3.3(AD1) · SP-DS-11.3 · NFR19 · SP-ADS-5.2

## 위임 케이스 (다른 SP 스위트에 실구현/검증 — 본 도메인 이중 구현 없음)
- **SP-ARCH-6 위임**: `web/assets/fonts/PretendardVariable.woff2` 가변폰트 **바이너리 획득·self-host 배치** + 디렉토리 트리(`web/assets/css/`·`web/assets/fonts/`). 본 도메인은 `@font-face` 선언·`src` 경로 계약만(T-10.4.1).
- **SP-FE 위임**: `styles.css` `<head>` 우선 로드 순서·SPA 셸 클래스·`body { font-family/font-size/line-height/color/background }` 적용(F1)·`:focus-visible` 규칙 적용(ACC-R1)·`.container` `max-width`/`padding-inline` 레이아웃(SP1)·터치 `min-height`(SP2). 본 도메인은 토큰값·계약만 제공, 컴포넌트 선택자 규칙 미소유.
- **SP-GEN 위임**: `base.html` `<head>` 폰트 preload·`font-display` 정합·정적 본문 마크업·landmark. 본 도메인은 `@font-face`·`--font-sans`만.
- **SP-ADS 위임**: `.ad-label`(`--ad-label-fg`/`--ad-label-fs` 소비)·`.ad-slot`/`.ad-slot--content-mid`(`min-height:280px`)/`--content-bottom`/`--report-bottom`(250px)·`.affiliate-card`/`.affiliate-cta`(`--ad-slot-bg`/`--ad-slot-border` 소비) 선택자 규칙·예약 높이·라벨 11px 하한 배선. 본 도메인은 색·폰트 토큰만 제공(SP-DS-3.3).
- **SP-RPT(SP-FE/SP-ENGINE 흡수) 위임**: 배지 표시 마크업(`--badge-*` 소비)·밴드 문구·색 적용(`--band-*` 소비)·vdCard winner→색 매핑(A3: 이득/승 `--green`·손실/열위 `--red`·동점 `--t3`·브랜드 `--purple`). 밴드 계수(±5/±20/+15)는 SP-CALC(DEC-2) 정본. 본 도메인은 색 토큰만.
- **SP-DS-2.5 라이트 테마**: MVP 밖(브리프 §8 다크 기본). 확장 훅(`:root[data-theme="light"] { 원시 팔레트 재선언 }`)만 남기고 코드 없음 — `data-theme` 미부착=다크(UT-DARK-DEFAULT, T-10.2.1가 가드).
- **SP-DS-12(추적 요약)**: 메타 문서(SP-DS↔상위 근거↔소비 SPEC 매핑) — 코드 산출 없음, 아래 추적표가 대응.

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-10.1.1 | SP-DS-11.1 | contrast.mjs 유틸 자체 테스트 | NFR15 |
| T-10.1.2 | SP-DS-1.1·1.2·10 | 구조 스모크 | NFR2·5 · SP-ARCH-6 |
| T-10.2.1 | SP-DS-2.1·2.2·2.5 | UT-CONTRAST-BODY·UT-CONTRAST-T4·UT-DARK-DEFAULT | NFR15·NFR13 · FR-01 |
| T-10.2.2 | SP-DS-2.3 | UT-CONTRAST-ACCENT-TXT | NFR13·NFR15 |
| T-10.2.3 | SP-DS-2.4 | UT-CONTRAST-FILL | NFR15 |
| T-10.3.1 | SP-DS-3.1·3.2 | UT-BADGE-CONTRAST | FR-38 · WIREFRAME §7 |
| T-10.3.2 | SP-DS-3.3 | UT-AD-LABEL (릴리스 게이트) | FR-76 · NFR19 · SP-ADS-5.2 |
| T-10.4.1 | SP-DS-4.1 | UT-FONT-DISPLAY (릴리스 게이트) | NFR5 · SP-ARCH-6 |
| T-10.4.2 | SP-DS-4.2 | UT-TYPE-SCALE | WIREFRAME §0 |
| T-10.4.3 | SP-DS-4.3 | 존재·정합 린트 / UT-TOKEN-EXIST | NFR13 · WIREFRAME §0 |
| T-10.5.1 | SP-DS-5.1 | UT-SPACING | NFR2 |
| T-10.5.2 | SP-DS-5.2 | UT-SPACING / 존재 린트 | NFR13 · WIREFRAME §0 |
| T-10.6.1 | SP-DS-6 | 존재·단조 린트 / UT-TOKEN-EXIST | NFR2 |
| T-10.7.1 | SP-DS-7 | UT-BP | WIREFRAME §0 · 브리프 §8 |
| T-10.8.1 | SP-DS-8 | 존재·단조 린트 / UT-TOKEN-EXIST | NFR2 |
| T-10.8.2 | SP-DS-8(MO2) | UT-REDUCED-MOTION | NFR13 |
| T-10.9.1 | SP-DS-9.1·9.2 | 존재 린트 / UT-CONTRAST-*·MD-2 위임 | NFR13·NFR15·NFR2·5 |
| T-10.10.1 | SP-DS-11.2 | UT-TOKEN-EXIST | R1 · SP-DS-10 |
| T-10.10.2 | SP-DS-11.2 | UT-NO-LITERAL (릴리스 게이트) | R1·R5 |
| T-10.11.1 | SP-DS-11.3 | MD-1 (수동) | NFR15 |
| T-10.11.2 | SP-DS-9.1·11.3 | MD-2 (수동·게이트) | NFR13(ACC-3) |
| T-10.11.3 | SP-DS-9.1·11.3 | MD-3 (수동·게이트) | NFR13·NFR15 |
| T-10.11.4 | SP-DS-4.3·11.3 | MD-4 (수동) | NFR13(F3) |
| T-10.11.5 | SP-DS-8·11.3 | MD-5 (수동) | NFR13(ACC-5) |
| T-10.11.6 | SP-DS-3.3·11.3 | MD-6 (수동·게이트) | NFR19 · SP-ADS-5.2 |
| (위임) | SP-ARCH-6 | 폰트 바이너리 self-host | — |
| (위임) | SP-FE | `body`·`:focus-visible`·컨테이너 규칙 | NFR13 |
| (위임) | SP-ADS | `.ad-label`/`.ad-slot`/`.affiliate-card` 규칙 | NFR19·NFR2 |
| (위임) | SP-RPT(흡수) | 배지·밴드·vdCard 색 적용 | FR-38·FR-40~45 |
| (위임) | SP-CALC | 밴드 계수(±5/±20/+15, DEC-2) | FR-38 |
