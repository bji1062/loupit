# TASK 05 — 비교 계산엔진(calc.js) (SP-ENGINE)

> SPEC 계약: [SPEC/05-비교-계산엔진.md](../SPEC/05-비교-계산엔진.md) (SP-ENGINE). 이 문서는 해당 도메인을 최소 독립 기능 리프로 분해한다. 리프 완료(- [v]) = 구현 + 대응 테스트 green(SP-TEST-1 DoD). 진행중 - [-], 미착수 - [ ].
> 마일스톤: M3(계산엔진) — 전체 빌드순서·의존은 [00-빌드순서-마일스톤.md](00-빌드순서-마일스톤.md).
> 선행 의존: **데이터 계약 필드명만 참조** — SP-DB(`BENEFIT_AMT`·`AMT_SOURCE_CD`↔`amt_source`·`BENEFIT_CTGR_CD`·`QUAL_YN`·`EXPIRES_DTM`·`WORK_STYLE_VAL`·`COMP_TP_CD`·`GROWTH_RATE_VAL`·`STABILITY_SCORE_NO`)와 SP-API(`reference/all` 번들 형태: `company_types`·`companies`·`benefits[]`)의 필드 계약. **러닝 코드 의존 0** — `calc.js`는 인자 주입 순수 ES모듈이므로 SP-DB/SP-API 산출물 없이도 단독 구현·테스트 가능(참조 데이터는 테스트가 캔드 객체 주입). 실 데이터·번들 구현은 SP-DB/SP-API 소유(위임). 확정 순서는 00이 소유.
> 테스트: 러너=node --test (node:test + node:assert/strict, 브라우저와 동일 ES module import) · 위치=web/assets/js/calc.test.js · 케이스 대역=T-ENGINE-1~48.

## 진행 요약
- 리프 총 29 / 완료 0 / 진행 0 / 미착수 29
- Tier-0 게이트 리프: T-05.6.3 (T-ENGINE-26 official×estimated=±20%, INV-5), T-05.8.3 (T-ENGINE-41 필수결측 무크래시), T-05.9.1 (T-ENGINE-45 순수성 INV-4)
- 선결정 게이트: DG-1 (T-05.6.2 위 — OI-3 만료 판정 TTL/유예 정책)

## T-05.1 모듈 골격·타입·상수  — (SP-ENGINE-1·2·3, FR-30, INV-4·5, NFR16·20·26)
- [ ] T-05.1.1 ESM 모듈 부트스트랩 — package.json·JSDoc typedef 헤더·export const 상수 전량
  - 구현: `web/assets/js/package.json`=`{"type":"module","private":true}`(node 테스트 전용 ESM 해석, 브라우저 무영향·무빌드). `web/assets/js/calc.js` 상단 JSDoc `@typedef` 블록(`Range`·`CtgrCode`·`BenefitItem`·`WorkStyle`·`SlotMeta`·`CompanyType`·`PriKey`·`CompareState`·`SlotResult`·`Report`·`LightSummary`·`VdPersp`·`VdCard`·`SacrificeCost`·`BrandProjection`) + PriKey 정규화 표(§2.1: 연봉→salary·워라밸→wlb·복지→benefits·브랜드→brand, 엔진은 영어 키만 소비). `export const` 상수: `OT_HRS`·`LEGAL_WEEK_HRS`·`REMOTE_SAVE`·`FLEX_BONUS`·`PTO_BONUS`·`MONTHLY_STD_HRS`·`OT_MULT`·`WEEKS_PER_MONTH`·`WEEKS_PER_YEAR`·`WON_PER_MANWON`·`COMMUTE_ROUND_TRIP`·`COMMUTE_WORKDAYS`·`WORKDAY_HRS`·`PROJECTION_YEARS`·`BENEFIT_SAT_THRESHOLD`·`GROWTH_RATE_FALLBACK`·`BAND_BASE={stated:0.05,estimated:0.20,none:0}`·`BAND_EXPIRE=0.15`·`BENEFIT_CATEGORIES`(9종 고정순서). `calc.test.js` 골격(import 스모크). 부수효과 0(document/window/fetch/localStorage/전역 미참조).
  - 테스트: import 스모크(`node --test web/assets/js/` 수집이 ESM 해석·전 export 심볼 존재 강제). 상수값은 **직접 케이스 없음** — 소비 함수 케이스로 값 강제(OT_HRS→T-ENGINE-11 · REMOTE_SAVE/FLEX_BONUS/PTO_BONUS→T-ENGINE-18 · MONTHLY_STD_HRS/OT_MULT/WEEKS_PER_MONTH→T-ENGINE-14 · WEEKS_PER_YEAR→T-ENGINE-16 · BAND_BASE→T-ENGINE-21·22 · BAND_EXPIRE→T-ENGINE-24·25 · PROJECTION_YEARS→T-ENGINE-29 · BENEFIT_SAT_THRESHOLD→T-ENGINE-35 · COMMUTE_*→T-ENGINE-20). RED 먼저(파일 부재). 순수성 게이트는 T-05.9.1(T-ENGINE-45) 위임
  - refs: SP-ENGINE-1·2·3 · FR-30 · INV-4·INV-5 · NFR16·NFR20·NFR26 · SP-ARCH-6·T5

## T-05.2 연봉 Range  — (SP-ENGINE-4, FR-30, UC-30)
- [ ] T-05.2.1 parseSalRange(salStr) — 슬롯 a 문자열 파싱
  - 구현: `web/assets/js/calc.js` `export function parseSalRange(salStr)` — `"lo-hi"`→`{min,max,mid}`(mid=round((lo+hi)/2)), 미입력/파싱불가/NaN→`{0,0,0}`(0나눗셈·NaN 방어). DOM 미접근.
  - 테스트: T-ENGINE-1(`"5000-7000"`→`{5000,7000,6000}`)·T-ENGINE-2(`""`/`null`/`"abc"`→`{0,0,0}`) (node --test) — RED 먼저
  - refs: SP-ENGINE-4 · FR-30 · UC-30
- [ ] T-05.2.2 deriveOfferRange(baseRange, selectedRate) — 슬롯 b 상승률 배율
  - 구현: `calc.js` `export function deriveOfferRange(baseRange, selectedRate)` — b=base×(1+rate/100), 세 값 각각 배율 후 `Math.round`. base 미입력(min=0)·rate null→`{0,0,0}`. rate=0(동결)=항등 배율(유효). 부동소수 반올림 경계 `Math.round` 일치.
  - 테스트: T-ENGINE-3(`({5000,7000,6000},10)`→`{5500,7700,6600}`)·T-ENGINE-4(rate=0→base 동일)·T-ENGINE-5(`{0,0,0}`/rate null→`{0,0,0}`)·T-ENGINE-48(`({5001..},33)` 각 값 Math.round 일치) (node --test) — RED 먼저
  - refs: SP-ENGINE-4 · FR-30 · UC-30

## T-05.3 복지 합산·카테고리·정성  — (SP-ENGINE-5, FR-31, UC-31·32, NFR21)
- [ ] T-05.3.1 benTotal(list) — 체크·순복지 합산
  - 구현: `calc.js` `export function benTotal(list)` — 체크 항목만; `ben`=체크 전 합(정성 amt null→0), `net`=체크·비정성 합(순복지). 미체크·정성 규칙 준수, 단위 만원.
  - 테스트: T-ENGINE-6(체크3(정성1 amt=null)+미체크1 → ben=체크 합·net=비정성 체크 합, 정성·미체크 제외) (node --test) — RED 먼저
  - refs: SP-ENGINE-5 · FR-31 · UC-31 · §4(QUAL_YN)
- [ ] T-05.3.2 benByCat(list)·benCatCompare(listA,listB) — 9카테고리 델타
  - 구현: `calc.js` `export function benByCat(list)`(9종 전키 존재·미보유=0, 체크·비정성만, 미상 코드→`perks` 폴백 FR-D8) + `export function benCatCompare(listA,listB)`(9행 `{ctgr,sumA,sumB,delta:sumB−sumA}`, 고정순서 `BENEFIT_CATEGORIES`). benByCat은 benCatCompare 케이스로 커버(직접 케이스 없음).
  - 테스트: T-ENGINE-7(a perks 200·b perks 300 → perks 행 `{200,300,100}`·9행)·T-ENGINE-8(미상 ctgr `'xxx'` amt 50 → perks 50 폴백) (node --test) — RED 먼저
  - refs: SP-ENGINE-5 · FR-31·FR-D8 · UC-32 · §4(BENEFIT_CTGR_CD)
- [ ] T-05.3.3 qualCompare(listA,listB) — 정성 항목 목록
  - 구현: `calc.js` `export function qualCompare(listA,listB)` — 체크·`qual_yn` 항목만 `{benefit_nm,benefit_ctgr_cd,qual_desc}` a/b 목록(금액 없음, 원문 이스케이프는 렌더 계층 NFR21). 금액·카테고리·밴드에 절대 미포함.
  - 테스트: T-ENGINE-9(정성 a1/b0 → `{a:[{benefit_nm,...}],b:[]}`) (node --test) — RED 먼저
  - refs: SP-ENGINE-5 · FR-31 · UC-31 · NFR21

## T-05.4 실효연봉·근무시간·야근·시간가치  — (SP-ENGINE-6·7·8·9, FR-32~35, NFR26)
- [ ] T-05.4.1 effSalary(salRange, net) — 실효연봉
  - 구현: `calc.js` `export function effSalary(salRange, net)` — `{min:min+net,max:max+net,mid:mid+net}`(세 값 동일 net 가산). 순복지만 가산, 정성·미체크 미가산. `eff_shrink` 경고 판정은 상위 compare 소유(T-05.8.4).
  - 테스트: T-ENGINE-10(`({5000,7000,6000},600)`→`{5600,7600,6600}`) (node --test) — RED 먼저
  - refs: SP-ENGINE-6 · FR-32 · UC-31 · NFR26
- [ ] T-05.4.2 getWSHours(ws) — 주간근무시간 매핑
  - 구현: `calc.js` `export function getWSHours(ws)` — `OT_HRS[ws.ot]`; ot null/미선택→0(미입력). 매핑은 `OT_HRS` 고정값만(low40/mid45/high54).
  - 테스트: T-ENGINE-11(`{ot:'low'}`/`{ot:'mid'}`/`{ot:'high'}`/`{ot:null}`→`40`/`45`/`54`/`0`) (node --test) — RED 먼저
  - refs: SP-ENGINE-7 · FR-33 · UC-31·33
- [ ] T-05.4.3 getOTPay(ws, salRange) — 포괄/비포괄 분기 야근수당
  - 구현: `calc.js` `export function getOTPay(ws, salRange)` — 포괄(`wage='inclusive'`)·`wage=null`·`ot∈{low,null}`·`salRange.mid=0`→**0**. 비포괄+야근(mid/high)+mid>0 → 통상시급(`mid×1e4/12/MONTHLY_STD_HRS`)×연장근로시간(`OT_HRS[ot]−LEGAL_WEEK_HRS`)×`OT_MULT`×`WEEKS_PER_MONTH`×12/`WON_PER_MANWON`, `Math.round`, 음수 불가. 포괄은 결코 미가산.
  - 테스트: T-ENGINE-12(inclusive,high,mid6000→0)·T-ENGINE-13(separate,low→0)·T-ENGINE-14(separate,mid,mid6000→932)·T-ENGINE-15(separate,high,mid0→0) (node --test) — RED 먼저
  - refs: SP-ENGINE-8 · FR-34 · UC-31
- [ ] T-05.4.4 hourlyValue(effMid, otPay, wsHours) — 시간당 가치(0나눗셈 방지)
  - 구현: `calc.js` `export function hourlyValue(effMid, otPay, wsHours)` — 연간근무시간=`wsHours×WEEKS_PER_YEAR`; ≤0이면 `null`(0나눗셈 방지, throw 금지); 아니면 `round((effMid+otPay)×WON_PER_MANWON/annHrs)`(원). 승자·차이%는 상위 조립.
  - 테스트: T-ENGINE-16(`(6600,0,45)`→`28205`)·T-ENGINE-17(`(6600,0,0)`→`null` ★0나눗셈) (node --test) — RED 먼저
  - refs: SP-ENGINE-9 · FR-35 · UC-31 · NFR26

## T-05.5 워라밸·통근  — (SP-ENGINE-10·11, FR-36·37)
- [ ] T-05.5.1 autonomyScore(ws, slotMeta) — 자율성 점수
  - 구현: `calc.js` `export function autonomyScore(ws, slotMeta)` — `REMOTE_SAVE[ws.remote]` + (flex≠none?`FLEX_BONUS`) + (slotMeta.work_style_val.unlimitedPTO?`PTO_BONUS`). 회사 미선택(slotMeta=null)→PTO 가산 0. 근무시간(FR-33)과 별개 축.
  - 테스트: T-ENGINE-18(`({remote:'hybrid',flex:'flexible'},{work_style_val:{unlimitedPTO:true}})`→`250`)·T-ENGINE-19(`({remote:'none',flex:'none'},null)`→`0`) (node --test) — RED 먼저
  - refs: SP-ENGINE-10 · FR-36 · UC-31·33 · §4(WORK_STYLE_VAL)
- [ ] T-05.5.2 commuteCompare(comA, comB) — 편도 통근 비교·연환산
  - 구현: `calc.js` `export function commuteCompare(comA, comB)` — `{a,b,annA,annB,winner}`; annX=`round(min×COMMUTE_ROUND_TRIP×COMMUTE_WORKDAYS/60)`; winner=짧은 쪽('a'/'b'/'tie'). 금액 합산 미포함(통근 금전화는 REMOTE_SAVE로만).
  - 테스트: T-ENGINE-20(`(30,60)`→`{a:30,b:60,annA:240,annB:480,winner:'a'}`) (node --test) — RED 먼저
  - refs: SP-ENGINE-11 · FR-37 · UC-31 · FR-36

## T-05.6 복지 불확실성 밴드 — DEC-2  — (SP-ENGINE-12, FR-38, INV-5)
- [ ] T-05.6.1 bandCoeff 기본 계수 — amt_source 기준·미상 폴백
  - 구현: `calc.js` `export function bandCoeff(item, now)` 기본 경로 — `BAND_BASE[amt_source]`(stated 0.05·estimated 0.20·none 0), 정성(none)→0, 미상 amt_source→estimated(0.20) 폴백. item null→0. (만료 가산·디커플링은 후속 리프에서 추가.)
  - 테스트: T-ENGINE-21(`{stated,expires null}`→0.05)·T-ENGINE-22(`{estimated,expires null}`→0.20)·T-ENGINE-23(`{none}`→0) (node --test) — RED 먼저
  - refs: SP-ENGINE-12 · FR-38 · INV-5 · RESEARCH §4.2·DEC-2
- 선결정 DG-1: OI-3 만료 판정 TTL/유예 정책 — SPEC 기본은 즉시 만료(`expires_dtm != null && Date.parse(expires_dtm) < now`); 재확인 유예 TTL(예: 만료 후 N일 grace) 도입 여부·기준. `now`는 인자 주입(Date.now 금지, 결정성). 구현 전 AskUserQuestion 확인. (미도입 시 SPEC 기본 즉시 만료 유지)
- [ ] T-05.6.2 bandCoeff 만료 가산 — +0.15(치환 아님)·now 주입  [선결정 DG-1]
  - 구현: `calc.js` `bandCoeff`에 만료 경로 추가 — `expired = item.expires_dtm != null && Date.parse(item.expires_dtm) < now`이면 base + `BAND_EXPIRE`(가산, 치환 아님); none은 만료여도 0. `now`는 인자로만 참조(Date.now 미호출 → 결정성). 진리표: stated→0.20·estimated→0.35.
  - 테스트: T-ENGINE-24(`{stated,expires '2020-01-01'}`, now=Date.now()→0.20=0.05+0.15)·T-ENGINE-25(`{estimated,expires '2020-01-01'}`→0.35) (node --test) — RED 먼저
  - refs: SP-ENGINE-12 · FR-38 · INV-5 · DG-1 · RESEARCH §4.2·DEC-2
- [ ] T-05.6.3 bandCoeff DEC-2 디커플링 — badge_cd 무시 (Tier0)
  - 구현: `calc.js` `bandCoeff`가 `badge_cd`를 **읽지 않음**을 확정 — `badge_cd='official'`이어도 `amt_source='estimated'`면 0.20 유지(금액 신뢰도만 기준, 출처 배지와 디커플링). 별도 로직 추가 없음(기본 경로가 amt_source만 참조), **Tier-0 INV-5 회귀 게이트** 소유.
  - 테스트: T-ENGINE-26(`{badge_cd:'official', amt_source:'estimated'}`→0.20, badge 무시) (node --test) — RED 먼저
  - refs: SP-ENGINE-12·SP-ENGINE-3 · FR-38 · **INV-5** · DEC-2
- [ ] T-05.6.4 sumBand(list, now) — 항목별 ± 절대 합
  - 구현: `calc.js` `export function sumBand(list, now)` — 체크·비정성 항목만 `Σ |benefit_amt| × bandCoeff(item, now)`(단일 % 일괄 금지, 항목별 산정 후 합). 정성·미체크 제외. 만원 단위.
  - 테스트: T-ENGINE-27(체크 estimated 200 + 체크 stated 100 + 미체크 estimated 500 + 정성 → 200×0.2+100×0.05=45) (node --test) — RED 먼저
  - refs: SP-ENGINE-12 · FR-38 · INV-5 · UC-34

## T-05.7 우선순위 판정 vdCard·브랜드·희생  — (SP-ENGINE-13·13b·14, FR-39, UC-33)
- [ ] T-05.7.1 getCompanyType(companyTypes, compTpCd) — REF 유형 조회
  - 구현: `calc.js` `export function getCompanyType(companyTypes, compTpCd)` — `companyTypes[]`에서 `comp_tp_cd` 일치 항목 반환, 없으면 `null`(하드코딩 GROWTH_RATES 대체, REF 조회만).
  - 테스트: T-ENGINE-28(`(cts,'startup')`→해당 `{growth_rate_val,stability_score_no}` / 없으면 null) (node --test) — RED 먼저
  - refs: SP-ENGINE-13b · FR-39 · §6(REF)
- [ ] T-05.7.2 brandProjection(salMidA, salMidB, typeA, typeB) — 3년 투영·안정성·폴백
  - 구현: `calc.js` `export function brandProjection(...)` — grX=typeX?growth_rate_val:`GROWTH_RATE_FALLBACK`(0.04); `proj[0]=salMid`·`proj[y]=round(proj[y-1]×(1+gr))` `PROJECTION_YEARS`(3)회; `cumDiff=Σ(projB[y]−projA[y])`; `stabX=typeX?stability_score_no:0`; `limited=!typeA||!typeB`(한쪽 미선택 시 브랜드 제한).
  - 테스트: T-ENGINE-29(`(5000,5500,{gr:0.04},{gr:0.10})`→projA=[5000,5200,5408,5624]·projB=[5500,6050,6655,7321]·cumDiff=3794)·T-ENGINE-30(typeA=null→grA=0.04 폴백·limited:true) (node --test) — RED 먼저
  - refs: SP-ENGINE-13b · FR-39 · §2-3·§4(GROWTH_RATE_VAL·STABILITY_SCORE_NO)·§6
- [ ] T-05.7.3 buildVdCard('salary') — 총액·시간당 관점(vdSalary)
  - 구현: `calc.js` `export function buildVdCard(axis, m)` 디스패처 + 내부 `vdSalary(m)` — p1 총액(`totalB−totalA` 부호 승자·diff), p2 시간당(양쪽 hourly non-null일 때 승자·pct=`round(|hd|/min(hvA,hvB)×100)`, 한쪽 null→winner 'tie'·detail.missing:true). tie=p1·p2 모두 tie. 데이터만 반환(문구·색 SP-RPT).
  - 테스트: T-ENGINE-31(total A6000/B7000·hourly 양쪽→p1.winner 'b'·p2 시간당 승자·pct)·T-ENGINE-32(hourlyA=null→p2.winner 'tie'·detail.missing:true) (node --test) — RED 먼저
  - refs: SP-ENGINE-13 · FR-39 · UC-33 · §2-3(프로파일러 없음)
- [ ] T-05.7.4 buildVdCard('wlb') — 적게 일하기·자율성 관점(vdWlb)
  - 구현: `calc.js` 내부 `vdWlb(m)` — p1 적게 일하기(양쪽 wsHours>0일 때만 적은 쪽 승자·detail{wsA,wsB,weekDiff,annDiff=wd×WEEKS_PER_YEAR,annDays=round(annDiff/WORKDAY_HRS)}, 미입력→winner 'tie'·missing:true), p2 자율성(autonomy 큰 쪽). default 폴백=vdWlb.
  - 테스트: T-ENGINE-33(wsA40/wsB45·autoA<autoB→p1.winner 'a'·p2.winner 'b')·T-ENGINE-34(wsA=0→p1.winner 'tie'·p1.detail.missing:true) (node --test) — RED 먼저
  - refs: SP-ENGINE-13 · FR-39 · UC-33 · §7
- [ ] T-05.7.5 buildVdCard('benefits') — 복지항목·총보상 관점(vdBenefits)
  - 구현: `calc.js` 내부 `vdBenefits(m)` — p1 복지항목(`ben` 큰 쪽), p2 총보상(`total` 큰 쪽·detail.satisfy=`|totalDiff|<BENEFIT_SAT_THRESHOLD`(1200)). tie=false 고정.
  - 테스트: T-ENGINE-35(benA>benB·|totalDiff|<1200→p1.winner 'a'·p2.detail.satisfy:true) (node --test) — RED 먼저
  - refs: SP-ENGINE-13 · FR-39 · UC-33 · §7
- [ ] T-05.7.6 buildVdCard('brand') — 성장성·안정성 관점(vdBrand)
  - 구현: `calc.js` 내부 `vdBrand(m)` — p1 성장성(`brand.cumDiff` 부호 승자·detail{cumDiff,grA,grB,projA,projB}), p2 안정성(`stabA/stabB` 큰 쪽), `limited=brand.limited`(한 슬롯 회사 미선택 시 제한, UC-33 2a). brand=BrandProjection(§13.4) 소비.
  - 테스트: T-ENGINE-36(cumDiff>0·stabA>stabB→p1.winner 'b'·p2.winner 'a') (node --test) — RED 먼저
  - refs: SP-ENGINE-13 · FR-39 · UC-33 · §6
- [ ] T-05.7.7 sacrificeCost(sacrifice, m) — 희생요소 포기비용 4축
  - 구현: `calc.js` `export function sacrificeCost(sacrifice, m)` — salary(`|totalDiff|` annual/monthly/daily(원)·better), wlb(양쪽 wsHours>0일 때만 ok:true·weekDiff/annHours/annDays/more, 아니면 ok:false·missing:true), benefits(`|benA−benB|`·better), brand(ok=!limited·cumDiff/stabDiff/grA/grB), default→null. 데이터만 반환. brand 분기는 직접 케이스 없음 — compare 통합(T-ENGINE-40)에서 소비.
  - 테스트: T-ENGINE-37(salary totalA6000/B7200→`{annual:1200,monthly:100,daily:round(1200×1e4/365),better:'b'}`)·T-ENGINE-38(wlb wsA=0→ok:false·missing:true)·T-ENGINE-39(benefits benA800/B500→`{diff:300,better:'a'}`) (node --test) — RED 먼저
  - refs: SP-ENGINE-14 · FR-39 · UC-33 · §7

## T-05.8 오케스트레이터 — compare·calc·restSummary  — (SP-ENGINE-15, FR-30·42, NFR2·16·20·26·27)
- [ ] T-05.8.1 calc(state) — 경량 실시간 요약
  - 구현: `calc.js` `export function calc(state)` — 편집 중 `{salRange:{a,b},net:{a,b},eff:{a,b}}`만(parseSalRange·deriveOfferRange·benTotal.net·effSalary 조립). vdCard·밴드·투영 미산출. INP≤200ms(순수·경량).
  - 테스트: T-ENGINE-44(정상 state → `{salRange,net,eff}`만, vdCard 키 부재) (node --test) — RED 먼저
  - refs: SP-ENGINE-15 · FR-30·FR-42 · NFR2·NFR27
- [ ] T-05.8.2 compare(state, now) — 전체 리포트 조립 + restSummary
  - 구현: `calc.js` `export function compare(state, now=Date.now())` — 슬롯 a/b 루프로 salRange·benTotal·effSalary·getWSHours·getOTPay·total(eff.mid+otPay)·hourlyValue·autonomyScore·sumBand·`totalRange:[total−band,total+band]`·commuteMin 조립 → `R.a/R.b`(SlotResult). 브랜드축(getCompanyType×2 + brandProjection). `m` 지표 벡터 구성. `deltas`(effMid/effMin/effMax/salMid/totalDiff/hourlyDiff(양쪽 non-null일 때만)/benDiff), `catDelta`(benCatCompare, 9행), `qual`(qualCompare), `commute`(commuteCompare), `vdCard`(buildVdCard(curPri,m)), `sacrifice`(curSacrifice?sacrificeCost:null), `brand`, `rest`(**`export function restSummary(pri,sac,m,R)`** — curPri·curSacrifice 아닌 축 승자 요약, 4축 분기; restSummary는 직접 케이스 없음 — compare().rest로 검증). throw·서버 전송·저장 0. `pfResult` 미참조.
  - 테스트: T-ENGINE-40(정상 state → ok:true·a/b SlotResult·deltas·vdCard·catDelta.length===9·rest 배열 검증)·T-ENGINE-47(total=6600·sumBand=45 → totalRange `[6555,6645]`) (node --test) — RED 먼저
  - refs: SP-ENGINE-15 · FR-30·FR-42 · UC-30·35 · NFR16·NFR20·NFR26
- [ ] T-05.8.3 compare 필수 결측 무크래시 — missing 부분 미산출 (Tier0)
  - 구현: `calc.js` `compare`가 연봉 결측(`!salA.min && !salA.max`) 시 `missing.push('salary')`·`ok:false`, **throw 없이** 나머지 지표는 산출(부분 미산출, 리포트 전체 무효화 금지). **Tier-0 무크래시 게이트** 소유(FR-40 2a).
  - 테스트: T-ENGINE-41(`salStr:null` → ok:false·missing:['salary']·throw 없음) (node --test) — RED 먼저
  - refs: SP-ENGINE-15 · FR-30·FR-40 · UC-30 · NFR26
- [ ] T-05.8.4 compare warnings — inclusive·eff_shrink 규칙 코드
  - 구현: `calc.js` `compare`의 `warnings` 조립 — 양쪽 포괄+야근→`'both_inclusive'`, 한쪽만→`'inclusive_a'`/`'inclusive_b'`(단일측은 both_inclusive와 동일 코드 경로, 직접 케이스는 both_inclusive만), `salDiffMid>0 && effDiffMid<salDiffMid && benA>benB`→`'eff_shrink'`. 코드만(문구 SP-RPT).
  - 테스트: T-ENGINE-42(양쪽 포괄+야근 → warnings에 'both_inclusive')·T-ENGINE-43(b연봉↑·a복지↑ 실질 축소 → warnings에 'eff_shrink') (node --test) — RED 먼저
  - refs: SP-ENGINE-15·SP-ENGINE-6 · FR-30·FR-32·FR-34 · UC-35

## T-05.9 순수성·프로파일러 제거 게이트  — (SP-ENGINE-1·16, INV-4, §2-3)
- [ ] T-05.9.1 순수성 계약 — 부수효과 0·불변 입력·결정성 (Tier0)
  - 구현: `calc.js` 전 export가 INV-4 계약 준수 확정 — document/window/fetch/localStorage/XMLHttpRequest/전역 미접근, 인자 객체 미변형(불변 입력·새 객체 반환), 동일 `(state, now)` 반복 호출 deep-equal(결정성). 별도 로직 없음(설계 강제), **Tier-0 순수성 게이트** 소유.
  - 테스트: T-ENGINE-45(import 후 전역 미주입 호출 → document/fetch/localStorage 미접근·입력 객체 `Object.freeze` 후 호출 시 예외 없음·미변형; 반복 호출 deep-equal 보강) (node --test) — RED 먼저
  - refs: SP-ENGINE-1 · **INV-4** · NFR16·NFR20 · T5
- [ ] T-05.9.2 프로파일러 벡터 제거 — 소스 토큰 0·주입 무영향
  - 구현: `calc.js` 소스에 `pfResult`·`profiler`·`fetch`·`localStorage`·`document`·`openAuth` 토큰 **0회** 등장(정규식 grep), `state`에 프로파일러 키 주입해도 결과 불변. 판정 입력=`curPri`/`curSacrifice`+지표값뿐(레거시 프로파일러 콜아웃·가치관 CTA·computeDataTrust·saveDraft 부재).
  - 테스트: T-ENGINE-46(state에 `pfResult` 주입 → 결과 T-ENGINE-40과 동일(무영향)·소스에 `pfResult` 토큰 0) (node --test) — RED 먼저
  - refs: SP-ENGINE-16 · §2-3·01 §5.1 · FR-30·FR-39 · INV-4

## 위임 케이스 (다른 SP 스위트에 실구현/검증 — 본 도메인 이중 구현 없음)
- **SP-FE(06) 위임**: 입력 수집·검증·초기값·프리셋 폴백 UI, PriKey 라벨→키 매핑 실행(§2.1), 엔진 어댑터(`assembleCompareState`), 우선순위 미선택 게이트(UC-33 1a), 재계산 트리거(FR-42 재호출). 엔진은 유효 `CompareState`·유효 `curPri` 전제.
- **SP-RPT≡SP-FE(06/08) 위임**: 반환 구조체(SlotResult·Report·VdCard) → DOM 렌더·배지/밴드 표시·경고 코드→문구 생성·XSS 이스케이프·"최근 비교" localStorage. 엔진은 HTML 미반환(순수 데이터만, NFR21).
- **SP-DB(02) 위임**: `AMT_SOURCE_CD`(DEC-2)·`BENEFIT_CTGR_CD`·`QUAL_YN`·`EXPIRES_DTM`·`WORK_STYLE_VAL`·`GROWTH_RATE_VAL`·`STABILITY_SCORE_NO` 컬럼·제약. 엔진은 필드 계약만 소비.
- **SP-API(04) 위임**: `reference/all` 번들·`companies/{id}` 형태(`company_types`·`companies.benefits[]`·`work_style_val`). 엔진 `companyTypes`는 인자 주입(REF에서 UI 추출).
- **SP-TEST(12) 위임**: 집계 러너(`run_tests.sh`)·export 커버리지 메타(MT-4)·릴리스 게이트(SP-ARCH-9 4단계). 본 도메인은 T-ENGINE-1~48 전량 green 제공.

## 추적 (리프 → SPEC 항목 → 테스트 → 상위)
| 리프 | SP 항목 | 테스트 케이스 | 상위 FR/INV/NFR |
| --- | --- | --- | --- |
| T-05.1.1 | SP-ENGINE-1·2·3 | import 스모크(상수는 소비함수 케이스로 강제) | FR-30 · INV-4·5 · NFR16·20·26 |
| T-05.2.1 | SP-ENGINE-4 | T-ENGINE-1·2 | FR-30 |
| T-05.2.2 | SP-ENGINE-4 | T-ENGINE-3·4·5·48 | FR-30 |
| T-05.3.1 | SP-ENGINE-5 | T-ENGINE-6 | FR-31 |
| T-05.3.2 | SP-ENGINE-5 | T-ENGINE-7·8 | FR-31·FR-D8 |
| T-05.3.3 | SP-ENGINE-5 | T-ENGINE-9 | FR-31 · NFR21 |
| T-05.4.1 | SP-ENGINE-6 | T-ENGINE-10 | FR-32 · NFR26 |
| T-05.4.2 | SP-ENGINE-7 | T-ENGINE-11 | FR-33 |
| T-05.4.3 | SP-ENGINE-8 | T-ENGINE-12·13·14·15 | FR-34 |
| T-05.4.4 | SP-ENGINE-9 | T-ENGINE-16·17 | FR-35 · NFR26 |
| T-05.5.1 | SP-ENGINE-10 | T-ENGINE-18·19 | FR-36 |
| T-05.5.2 | SP-ENGINE-11 | T-ENGINE-20 | FR-37 |
| T-05.6.1 | SP-ENGINE-12 | T-ENGINE-21·22·23 | FR-38 · INV-5 |
| T-05.6.2 | SP-ENGINE-12 | T-ENGINE-24·25 | FR-38 · INV-5 · DG-1 |
| T-05.6.3 | SP-ENGINE-12·3 | T-ENGINE-26 (Tier0) | FR-38 · **INV-5** |
| T-05.6.4 | SP-ENGINE-12 | T-ENGINE-27 | FR-38 · INV-5 |
| T-05.7.1 | SP-ENGINE-13b | T-ENGINE-28 | FR-39 |
| T-05.7.2 | SP-ENGINE-13b | T-ENGINE-29·30 | FR-39 |
| T-05.7.3 | SP-ENGINE-13 | T-ENGINE-31·32 | FR-39 |
| T-05.7.4 | SP-ENGINE-13 | T-ENGINE-33·34 | FR-39 |
| T-05.7.5 | SP-ENGINE-13 | T-ENGINE-35 | FR-39 |
| T-05.7.6 | SP-ENGINE-13 | T-ENGINE-36 | FR-39 |
| T-05.7.7 | SP-ENGINE-14 | T-ENGINE-37·38·39 (brand 분기→T-40) | FR-39 |
| T-05.8.1 | SP-ENGINE-15 | T-ENGINE-44 | FR-30·42 · NFR2·27 |
| T-05.8.2 | SP-ENGINE-15 | T-ENGINE-40·47 (restSummary→rest 키) | FR-30·42 · NFR16·20·26 |
| T-05.8.3 | SP-ENGINE-15 | T-ENGINE-41 (Tier0) | FR-30·40 · NFR26 |
| T-05.8.4 | SP-ENGINE-15·6 | T-ENGINE-42·43 | FR-30·32·34 |
| T-05.9.1 | SP-ENGINE-1 | T-ENGINE-45 (Tier0) | **INV-4** · NFR16·20 · T5 |
| T-05.9.2 | SP-ENGINE-16 | T-ENGINE-46 | INV-4 · §2-3·01 §5.1 |
