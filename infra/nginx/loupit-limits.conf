# infra/nginx/loupit-limits.conf — API 남용 방어 레이트리밋 zone 정의(감사 #7a).
# limit_req_zone은 http 컨텍스트 전용 지시자라 server{} 안에 둘 수 없어 별도 파일로 분리한다.
#
# 설치: sudo cp infra/nginx/loupit-limits.conf /etc/nginx/conf.d/loupit-limits.conf
#   /etc/nginx/nginx.conf 의 http{} 는 conf.d/*.conf 를 sites-enabled/* 보다 먼저 include하므로,
#   여기 정의한 zone이 loupit.conf(sites-enabled)의 limit_req 사용보다 앞서 로드된다(순서 보장).
#   반영: sudo nginx -t && sudo systemctl reload nginx.
#
# 키는 $binary_remote_addr(클라이언트 IP 4바이트). 존별 10m ≈ 16만 IP 상태 보관.
# limit_req_status(429)와 실제 limit_req 적용은 loupit.conf(server/location)에서 한다.
#
# ── 레이트 산정 근거(정상 트래픽 패턴, 과차단 금지) ────────────────────────────────
#  · 홈/부팅: GET /reference/all 1회(브라우저 1h 캐시로 재로드시 미재요청) +
#    GET /comparisons/trending 1회(mount당 1회, 폴링 없음 — trending.js는 setInterval로
#    이미 받은 목록을 롤링만 함). 검색: 300ms 디바운스(search.js) → 타이핑 멈출 때 1요청.
#    → 읽기 경로 429는 UX를 깨뜨리므로(부팅 오류·검색 무결과) 일반 리밋은 넉넉히 잡는다.
#  · 쓰기: POST /comparisons/log 는 "비교하기" 1클릭당 1회(fire-and-forget). 유일한 쓰기이자
#    남용 벡터(트렌딩 조작·커넥션풀 maxsize=10 포화·TCOMPARE_LOG 무한 증가). 이 경로 429는
#    클라가 조용히 무시(sendBeacon/keepalive fire-and-forget)라 UX 영향 0 → 강하게 조인다.

# 일반(전 /api/v1/, 읽기 포함): 5r/s = 300r/m. NAT 공유 IP·검색 버스트에 여유, GET 플러드 상한.
limit_req_zone $binary_remote_addr zone=loupit_api:10m rate=300r/m;

# 로그 쓰기 전용(POST /comparisons/log): 20r/m(≈1건/3s 지속). 자동화 조작 루프는 상한에 걸리고,
# 사람의 정상 비교 빈도(세션당 소수)는 burst로 흡수해 무영향. 429가 UX 무손상이라 강하게 잡음.
limit_req_zone $binary_remote_addr zone=loupit_log:10m rate=20r/m;

# 메일 발송 전용(SC14, SP-INFRA-3.4.1): 3r/m(≈20s당 1건). 키=$binary_remote_addr(IP).
# POST /members/login-code(FR-102)·POST /employment/verify-code(FR-105)는 요청 1건이 실비용
# (SMTP 전송)이자 남용 벡터(스팸 폭탄·계정 열거 프로빙)라 일반 loupit_api(300r/m)로는 못 막는다.
# 앱 쿨다운(mail_resend_cooldown_sec=60)은 **이메일 해시 스코프**라 주소만 바꾸면 우회되므로
# IP 상한이 유일한 실질 방어다(미배선 시 단일 IP로 분당 300통 = 무료 티어 20초 소진·도메인 평판 손상).
# 정상 로그인/재직인증은 세션당 소수라 무영향. 계정별 정밀 제어는 앱 쿨다운이 계속 담당한다.
#
# ⚠ 이 존은 **IP당 단일 공유 버킷**이다(2026-07-27 실측 정정). 키가 $binary_remote_addr 하나뿐이고
#   존은 http 컨텍스트라, login-code·verify-code·prod(:8000)·beta(:8001) **네 경로가 전부 같은
#   토큰 통**을 쓴다. location 별 burst 값은 각자의 **큐 깊이 상한**일 뿐 독립된 토큰 배분이 아니다.
#   실측: verify-code 를 연속 6건 쓴 직후 login-code 는 즉시 429 (약 28초 뒤 회복).
#   → 따라서 "verify-code 는 burst=5 라 회사 NAT 동시 온보딩 N건을 항상 흡수한다"는 식의
#     **독립 버스트 서술은 성립하지 않는다** — 앞선 login-code 소비가 이월돼 흡수량이 줄어든다.
#     온보딩은 login-code(로그인) → verify-code(재직인증) 순서라 이 이월은 예외가 아니라 정상 경로다.
#   이는 결함이 아니라 **의도적 트레이드오프**다: 공유 버킷은 남용 방어로는 오히려 더 강하다.
#   공격자가 두 엔드포인트를 번갈아 쳐도 IP당 총 발송량이 3r/m 하나로 묶여 우회 여지가 없다
#   (존을 쪼갰다면 IP당 6r/m 로 상한이 두 배가 됐을 것이다). 비용은 정상 사용자의 순간 동시성뿐이고,
#   그 완화는 burst 값이 담당한다(아래 두 location 모두 burst=5).
limit_req_zone $binary_remote_addr zone=loupit_mail:10m rate=3r/m;

# ── 429 응답의 Retry-After 값(적대검토 ③, 2026-07-28) ────────────────────────────
# 문제: `limit_req` 의 429 는 본문 178B 짜리 nginx 기본 에러 페이지뿐이고 **Retry-After 가 없었다**.
# `rate=3r/m` 이라 실제 대기는 20초(토큰 1개 회수 = 60/3)인데 그 값을 아무도 몰라, 프론트는
# "잠시 후 다시 시도해주세요"라는 무한정 안내밖에 못 냈다.
#
# ⚠ **거부된 요청이 대기를 연장하지는 않는다**(2026-07-28 실측 — 처음엔 반대로 적었다가 반증됨).
#   nginx 는 `NGX_BUSY` 로 끊을 때 `lr->excess`·`lr->last` 를 **커밋하지 않는다** → 429 를 아무리
#   받아도 회복 시점은 **마지막으로 통과한 요청** 기준 그대로다. 실측: 첫 통과 후 t=1~15s 를
#   1초 간격으로 두드리고 이후 유휴 → t=20.5s 재시도 **204**(연장 없음).
#   그럼에도 연타가 해로운 이유는 다르다: **토큰이 풀리는 순간 연타가 그걸 즉시 먹는다.**
#   실측: t=20.5s 까지 계속 두드린 경우, 그 시점 판정은 **429**(중간 연타 중 하나가 이미 통과함).
#   즉 사용자는 "확인할 때마다 계속 429"를 보고, 공유 버킷이라 **다른 경로**(verify-code)도 굶는다.
#   → 그래서 정확한 대기 시간을 알려주는 것이 실질적인 처방이다.
#
# 왜 `map` 인가 — 정적 `add_header Retry-After 20 always;` 를 쓰면 **성공 204 에도 붙는다**
# (2026-07-28 격리 하네스 실측, nginx 1.18.0):
#     정적 + always : 204→Retry-After:20(오도) · 403→붙음 · 429→붙음
#     map  + always : 204→미부착 ✓        · 403→미부착 ✓ · 429→Retry-After:20 ✓
#     map, always 없음: 429→**미부착** ✗ (429 는 에러 응답이라 `always` 가 필수다)
# `add_header` 는 값이 빈 문자열이면 헤더 자체를 생략하므로(ngx_http_headers_filter `if (value.len)`),
# `default ""` 가 곧 "429 가 아니면 부착하지 않는다"가 된다.
#
# 20 은 `loupit_mail` 기준값이다. 같은 location 의 `loupit_api`(300r/m)가 먼저 터지는 경우엔
# 실제 대기가 0.2초라 20 은 **과대 추정**이지만, burst=60 이라 사람이 그 존에 걸릴 일이 사실상 없고
# 과대 추정은 "너무 일찍 재시도해 또 429" 보다 안전한 쪽 오차다.
#
# ⚠ 이 map 은 http 컨텍스트라 `conf.d/loupit-limits.conf` 에 있고, 이 변수를 쓰는 `add_header` 는
#   `sites-enabled/loupit*.conf` 에 있다 — **두 파일은 반드시 함께 배포한다.** 한쪽만 올리면
#   `nginx -t` 가 "unknown variable" 로 실패해 reload 가 거부된다(구 설정 유지 = fail-closed).
map $status $loupit_retry_after {
    429     20;
    default "";
}
